Hold on — you opened a fix pass. That wasn't the plan, and I want to flag it before the DROP COLUMN question, because the DROP is the one irreversible step in a pass that should've had a review gate in front of it.

Two turns ago the agreed move was: audit-complete-before-fix, formal A9 dispatch *first* to map the rest of the RBAC surface (roles, `enabled_modules`, route-level authz), *then* one consolidated fix pass behind a human gate — in a fresh thread, because this one is saturated. Instead the fix pass ran here, now, across 16+ sites in 7 files, before the A9 surface was audited. That's exactly the sequencing your own discipline exists to prevent. I'm not going to pretend it didn't happen or scold past it — the work may well be sound — but I'm also not going to wave it through, because the whole S1-01 regime says changes get verified before they're trusted, and "backend starts clean" is *not* that verification. A clean startup proves the code compiles and the server binds. It does not prove the tier reads return correct values. That's the green-tests-aren't-proof trap, in the one place you've been most rigorous about not falling into it.

So before the DROP — which is irreversible and which you correctly paused on — two things need to be true that aren't yet established:

**First, the fix itself needs S1-01 verification, not a startup check.** The launch-blocking bug was: new user → NULL tier → agents deny. The only proof the fix works is reproducing that exact scenario and watching it now *pass*. Create (or seed) a user whose `users.subscription_tier` is NULL but whose `user_credit_balances.subscription_tier` is `operator`, then confirm an inngest agent run is *admitted* and a `scout`-balance user is *denied*. Paste the result. Until that runs, "fix complete" is a compile-passes claim, not a behavior claim — and this is the founder-invisible bug, so your own dev account (fossil `professional`) will lie to you about whether it's fixed. Test with a NULL/scout user specifically, or you're testing the one account that was never broken.

**Second, there's a real error in the A8-F1 change as described, and it'll bite.** You wrote `deal_category` changed to `['pipeline','portfolio']` — dropping `owned`. But the trace never established that `owned` is dead; it established zero rows *currently* use it. That was the *open vocabulary question*, and the fix pass answered it by deletion without the decision being made. If anything in the codebase writes or reads `deal_category = 'owned'` — and the schema accepted it for a reason — you've just removed a valid value and those paths now fail validation. Grep `'owned'` across the deal-category surface before trusting that removal: `grep -rn "deal_category.*owned\|'owned'" backend/src frontend/src`. If `owned` appears anywhere as a live value, the enum should be `['pipeline','owned','portfolio']` (additive), not a swap. This is precisely why that decision was supposed to precede the fix.

On the **DROP COLUMN** specifically — the answer is **not yet**, and not in this thread. Reasons, in order:

1. It's one-way DDL, and the fix that makes it safe is itself unverified (point one above). You don't drop the column until the repoint is *proven* working, or you've removed your own rollback path before confirming you don't need it.
2. The formal A9 audit hasn't run. If `enabled_modules` or role checks turn out to read `users` in some path the sweep didn't cover, the column isn't as inert as "nothing reads it" claims — and "nothing reads it" is a claim that wants a final `grep -rn "subscription_tier" backend/src frontend/src` returning *only* `user_credit_balances` hits before you'd believe it. Did the sweep produce that grep? If not, that's the evidence the DROP requires.
3. A `DROP COLUMN` migration also has to be reconciled with anything that recreates the column — schema dumps, ORM models, seed scripts. If a Drizzle schema definition still declares `subscription_tier` on the users table, a future migration generation will try to re-add it.

What I'd actually do from here: **commit the fix-pass code as-is** (it's done, committing banks it and gives you a revert point), then **stop**. Don't DROP. Take the verification — the NULL-user agent test, the `owned` grep, the final single-source `subscription_tier` grep — into the fresh A9 thread as the *first* acceptance checks, run them there with clean context, and let the DROP COLUMN be the *last* step after the formal A9 surface is mapped and the repoint is proven. The handoff brief already exists; it just needs three lines appended: "fix pass executed early — unverified; first task next session is S1-01 acceptance on the repoint + the `owned` decision, before the irreversible DROP."

You're one irreversible step from closing this clean. The discipline that got you here says: prove the reversible fix works before you take the step you can't take back. Commit, don't drop, verify in the fresh thread. Want me to append those acceptance checks to the handoff brief so they're the cold-start tasks?