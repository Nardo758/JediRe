STRATEGY CANONICAL DECISION — A1 VS A2 INVESTIGATION

STATE VERIFICATION (per P8 corollary):
Before executing, confirm:
1. Verify docs/operations/A1_VS_A2_INVESTIGATION.md does not 
   already exist; if it does, read it and report state before 
   proceeding
2. Verify the verification verdict on 
   STRATEGY_AND_BEFORE_AFTER_INVESTIGATION.md is APPROVED (per 
   prior bot report, it is — but confirm)
3. Verify no prior closing note has already produced an A1/A2 
   recommendation

CONTEXT
Per STRATEGY_AND_BEFORE_AFTER_INVESTIGATION.md (verified APPROVED), 
investmentStrategy and deal_type are disconnected:
- PATCH /assumptions/strategy updates investment_strategy_lv only
- Pattern B routing in m09_line_item_patterns.ts reads deal_type
- The two fields use different vocabularies ('Rental' vs 'value_add')
- The strategy selector is decoratively non-functional

This investigation produces a cost/benefit analysis between two 
canonical models so Leon can decide:

A1 — investmentStrategy canonical:
- investmentStrategy becomes the source of truth
- deal_type is derived from investmentStrategy via a deterministic 
  mapping (or deprecated entirely)
- Pattern B routing migrates to read investmentStrategy

A2 — deal_type canonical:
- deal_type remains the source of truth
- investmentStrategy is derived from deal_type (or repurposed as 
  a UI display label)
- The strategy selector writes to deal_type directly

Each has migration cost, downstream impact, and consequences for 
how operators select strategy and how the agent reasons about it.

═══════════════════════════════════════════════════════════════════
SCOPE
═══════════════════════════════════════════════════════════════════

STEP 1 — Inventory consumers of each field
A. grep all read sites of investment_strategy_lv (frontend + 
   backend)
B. grep all read sites of deal_type (frontend + backend)
C. For each, document the consumer and its expectations
D. Identify overlapping consumers (read both) vs single-field 
   consumers

STEP 2 — Vocabulary mapping
A. List all values investmentStrategy currently takes
B. List all values deal_type currently takes
C. Document the conceptual mapping between them where it exists
D. Identify values that don't map cleanly (the gap between 
   vocabularies)

STEP 3 — Migration cost analysis per option
For A1 (investmentStrategy canonical):
- Files requiring changes (count + listing)
- Migration complexity (low / medium / high per consumer)
- Data migration: how do existing deals with deal_type set get 
  investmentStrategy backfilled?
- Risk: what breaks if migration is partial?

For A2 (deal_type canonical):
- Same analysis
- Specifically address: where does the UI strategy selector write? 
  Does it need its own UI changes?

STEP 4 — Downstream impact per option
For each option, identify impact on:
- Phase 2 derivation logic (state-conditional source selection)
- The Mandate lift design (Q2 — strategy-aware evidence 
  requirements)
- F9 module map per-strategy variation
- Agent prompts that reference strategy
- The "not yet supported" notice handling (currently checks 
  investmentStrategy)

STEP 5 — Recommendation
The investigation produces a recommendation with explicit 
reasoning, but Leon makes the final call. The recommendation 
section should:
- State which option (A1 or A2) the analysis favors
- Name the strongest argument for each option
- Identify any unresolved questions that should be answered 
  before Leon decides

═══════════════════════════════════════════════════════════════════
DELIVERABLE
═══════════════════════════════════════════════════════════════════

docs/operations/A1_VS_A2_INVESTIGATION.md

Structure:
1. Executive summary (the question, the recommendation, the 
   confidence)
2. Consumer inventory (Step 1)
3. Vocabulary mapping (Step 2)
4. Migration cost: A1 (Step 3a)
5. Migration cost: A2 (Step 3b)
6. Downstream impact: A1 (Step 4a)
7. Downstream impact: A2 (Step 4b)
8. Recommendation with reasoning (Step 5)
9. Open questions for Leon's decision

═══════════════════════════════════════════════════════════════════
DO NOT
═══════════════════════════════════════════════════════════════════

- Implement migration (this is investigation only)
- Modify any code
- Decide A1 vs A2 unilaterally — produce the analysis; Leon 
  decides
- Recommend a third option (A3) unless the investigation surfaces 
  a genuinely superior path; if so, flag explicitly and explain
- "While I'm here" cleanups