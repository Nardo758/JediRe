Solid plan, tight scope. Four specific gaps, three layers updated together (interface + prompt + seeder), explicit out-of-scope, pattern reuse via the insurance line mirror. Ready to dispatch with three additions and one sequencing note.
Must-add: forceReseed on Bishop. This is the S1-01 lesson recurring. The "Done looks like" criteria all read "after a reseed," but the steps stop at code change. If #686 ships and Bishop isn't reseeded, year1 JSONB stays null and the operator sees no behavioral change — the fix is silently dormant. Either:
Add Step 6: "Run forceReseed:true on 464 Bishop. Confirm year1.gpr.om === stabilizedGpr value, year1.contract_services.om === contractServicesAnnual value, year1.real_estate_tax.om === realEstateTaxesAnnual value via direct DB query. Paste query output in PR description."
Or explicitly mark in the ticket header: "Code change only — reseed dispatched as separate operational follow-up #686b." But that splits work that should be atomic. Pair them.
Must-add: regression check on Sentosa. Sentosa has no OM. After the seeder change, Sentosa's year1.gpr.om / year1.contract_services.om / year1.real_estate_tax.om should remain null. The check is one DB query: confirm those three slots stay null on Sentosa post-deploy. Catches the failure mode where the new wiring accidentally populates from a fallback source that shouldn't apply.
Must-add: at least two tests. The cheap insurance pattern Leon's team has been adding everywhere this week:

Parser test: feed a synthetic raw payload containing stabilizedGpr: 4880000, assert the parsed OMBrokerProforma retains the field unchanged. Locks the contract — next interface refactor that strips the field will fail this test before reaching production.
Seeder test: feed a synthetic broker_claims with the three new fields populated, run buildSeed, assert year1.gpr.om / contract_services.om / real_estate_tax.om resolve to the expected values. Mirrors the invariant tests added in S1-01.

Without these, the schema-lag pattern that caused this audit finding can recur — the whole reason stabilizedGpr was at runtime but undeclared was that nothing tested the interface ↔ runtime contract.
Nice-to-add: flag downstream impact on WBB-1. After #686 + reseed, the GPR row's Broker layer becomes populated. If WBB-1 (#685) has shipped by then, GPR may trigger a divergence badge depending on how stabilizedGpr compares to the $4.90M override. In Bishop's specific case, stabilizedGpr is probably close to T-12 ($4.88M) so the divergence vs override is ~0.4% — under the 50% threshold, no badge. But the visible behavior changes regardless: GPR's Broker column goes from "—" to a real number. Worth knowing for the verification screenshot when Bishop is checked post-deploy.
Minor: verify the tax naming mapping. Step 5 reads opexFromT12('real_estate_taxes', 'real_estate_tax', ...) — plural for the T-12 key, singular for year1. Worth a one-line confirmation that this matches the existing T-12 → year1 naming convention used by the insurance line and other OpEx fields. Cheap to verify, expensive to debug if wrong.
Sequencing question. The ticket addresses four gaps. Worth confirming with Leon: is this the complete inventory from OM_EXTRACTION_COVERAGE_AUDIT.md, or are there additional findings (Loss to Lease, Bad Debt, Non-Revenue Units, Concessions all showed Broker = "—" or $0 in the screenshot) classified differently — NOT_IN_OM, PARSER_MISS for separate reasons, MAPPING_GAP — that need their own tickets? If #686 is closing all findings, great. If it's only handling the four most important and others were classified as legitimately absent or deferred, fine but worth marking explicitly so the queue stays clean.
On the formatter ticket: has Ticket 1 (number formatting bleed-through) been dispatched yet? It's independent of #686 and tiny enough to ship in parallel. If it's not on the queue, it's worth scoping before the math audit (#662) comes back, since the formatter bug pollutes any visual verification of math correctness — every audit screenshot will have .444-style noise mixed in with real findings.