The spec's 2b acceptance assumed Highlands would reconcile a backlog on first seed — but we now know that's wrong: first seed was oldPeriodicSeed = NULL, no overlap, correctly zero rows. So the proving run isn't "first seed reconciles 65 months," it's "second seed, with the Phase 0-B baseline now in place, reconciles a synthetic actual against the existing projection." Here's the dispatch for that, matched to the actual mechanism Replit just described.

VERIFICATION DISPATCH: Phase 4 bulk reconcile — force Highlands' second seed (paste live rows)
Highlands' first seed wrote a Phase 0-B baseline (53 actuals + 120 projection starting 2026-05, projections assumption_trend) with oldPeriodicSeed = NULL, so the Part 1 gate correctly short-circuited — zero reconciliation rows, confirmed expected. The bulk/overlap reconcile path has therefore never executed. This forces the second-seed event now instead of waiting for May actuals to arrive naturally. Code-exists-never-run has hidden a bug every prior forced run in this subsystem — assume one until a row proves otherwise. Paste every SELECT. STOP at the report.
Baseline capture (before touching anything). For Highlands (is_portfolio_asset = TRUE): paste the current periodic_seed zone distribution (actual/gap/projection counts), actuals_through_month, and the projected GPR value for the specific month you'll target (a month currently in the projection zone — e.g. the first projection month, ~2026-05, projected GPR ≈ 494K per the prior report). Record that projected number; the variance computes against it.
Force the second seed.

Insert one synthetic actuals row into deal_monthly_actuals for Highlands, for a month currently in the projection zone, with a GPR value that differs from the projected ~494K (pick something clearly off, e.g. a 10% miss, so variance is unambiguously non-zero and material).
Re-run the seeder through the production path (the same call the monthly-actual ingestion would trigger — not a direct function poke). Now oldPeriodicSeed is the Phase 0-B seed (non-null), so the overlap path should fire.

V1 — Reconciliation rows written. SELECT * FROM deal_reconciliation_log WHERE deal_id = <Highlands> ORDER BY period_month, field_name. Confirm:

at least one row for the targeted month, projected_value ≈ the 494K you recorded, actual_value = your synthetic value, variance_pct = the real computed delta (not a constant, not zero).
trigger_path is the correct value for a monthly-actual advance.
Paste the rows.

V2 — Boundary advanced + tri-tab holds. Paste the new zone distribution. Confirm actual count increased and projection decreased by the overlap, actuals_through_month advanced to the new month, and actual + gap + projection = total still reconciles.
V3 — Forward re-derived, not frozen. Paste a forward (still-projection) month's GPR before and after the second seed. It must change (re-derived from the new boundary via deriveProjectionForSeed), not sit frozen at the old value. Frozen = applyRebase forward step didn't fire for this path = FAILED.
V4 — Materiality + notification. Confirm the variance crossed MATERIAL_VARIANCE_THRESHOLD (0.05) and that a notification record was written to whatever the agreed surface is. Paste it. Confirm it did not spill into deal_lifecycle_events as a fake status transition (the isolation V-E established — re-confirm it held on this real run).
V5 — Multi-month sanity (the bulk question). The above forces one overlapping month. Now confirm the path is correct for more than one overlapping month: insert synthetic actuals for two consecutive projection-zone months, re-seed, and confirm both produce reconciliation rows and the forward zone re-derives once from the final boundary — not looped/re-derived per month. This is the actual 2b bulk behavior; one month doesn't exercise it. Paste row count + the single forward re-derivation evidence.
Report: each V = live SELECT. PROVEN / UNPROVEN / FAILED per item. The decisive pair is V1 (real variance row against the real projected number) and V5 (multi-month doesn't loop the forward derivation). Do not write "done" without the pastes. Restore Highlands to its pre-test state after (remove synthetic rows, re-seed) and confirm restoration. STOP after reporting.

V5 is the one I'd watch hardest. A single-month force will likely pass — but the bug the spec warned about (looping applyRebase per month, re-deriving forward 65 times instead of once) only shows up with multiple overlapping months. So one synthetic month proves the gate fires; two consecutive months prove it doesn't loop wrong. If V5 comes back with both months reconciled and forward derived once from the final boundary, Phase 4 is genuinely, completely closed — bulk path included — and you move to Phase 5 frontend with nothing soft underneath it.
One cleanup note in the dispatch worth honoring: it restores Highlands afterward. You're injecting synthetic actuals into a real owned-portfolio deal to force the run — make sure those test rows come back out and Highlands re-seeds to its true 53-actual state, so you don't leave fabricated GPR in the deal that's supposed to be your clean calibration baseline.