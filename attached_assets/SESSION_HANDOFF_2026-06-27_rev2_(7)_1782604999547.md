# JediRE — Session Handoff & A9 Dispatch (2026-06-27, rev 2)

**Read this first.** A9-Finding-1's column DROP was executed this session on behavioral
evidence that **does not meet the S1-01 bar** (explained below). The change is probably
correct but is **not proven**. The first task in the new thread is to earn that proof
retroactively. Do not record A9-Finding-1 as "closed/proven" until the admission test
in §A actually fires and is observed.

**Repo:** `Nardo758/JediRe.git` · **Last checkpoint before DROP:** `7561970`
**DROP migration applied:** `20260627_drop_users_subscription_tier.sql` — column is GONE.
**Standing rule (S1-01):** positive observation of the path under test — `file:line` trace
or pasted live-DB / live-log output showing the *thing itself* doing the right thing.
An inference that "gate X must have passed because later gate Y blocked" is NOT S1-01 evidence.

---

## §A — A9-Finding-1: NOW PROVEN AND CLOSED (admission test ran for real)

The owed test was run correctly this session. Fix: seed test users with `automation_level = 2`
to **remove the masking automation gate**, so each agent either proceeds to its next step
(= tier gate admitted) or emits its own tier-gate deny log. Direct observation, no inference.

**ADMIT — Deal A (UCB=operator, automation_level=2):** all four agents proceeded PAST both gates
to `resolve-deal-context` (a downstream step only reachable if tier gate returned `allowed:true`).
Proven for BOTH trigger patterns:
- Commentary, CashFlow (live-DB COALESCE path) → reached business logic
- Supply, Zoning (payload-tier `deals.tier` path) → reached resolve-deal-context with `userTier='operator'`

**DENY — Deal B (no UCB → COALESCE=scout):** CashFlow emitted its own `tier gate blocked
(event-driven not permitted)` log — the gate itself speaking, direct. Supply/Commentary/Zoning
admit scout by design (only CashFlow event-driven requires operator+); their automation_level
block is correct behavior.

Both trigger patterns proven on both sides of the decision. The DROP is retroactively earned.
**A9-Finding-1 is closed with S1-01-grade evidence.**

### Residual from this test → carry to §E (do NOT read as fully closed):
The admit test used FRESH deals where `deals.tier` and UCB agreed. It did NOT test the **drift
case**: `deals.tier` is stamped at creation (`tier || 'basic'`) and never reconciled with UCB
afterward. A user who changes tier keeps stale `deals.tier` on existing deals, and the payload-tier
agents (Supply/Zoning/Research) gate on it. Proven: the mechanism admits correctly when the two
agree. Open: they can diverge. Stays an A9 trace target.

---

## §A-OLD (for the record — why the FIRST attempt didn't count)

### Why the prior "proof" doesn't count
The DROP was gated on an agent-admission test. The captured logs:

```
Deal A (users.subscription_tier='free', UCB='operator'):
  Commentary → automation_level gate blocked
  CashFlow   → automation_level gate blocked
Deal B (users.subscription_tier='free', no UCB → COALESCE='scout'):
  Commentary → automation_level gate blocked
  CashFlow   → tier gate blocked (scout, by design)
```

Three of four cells were blocked by the **`automation_level` gate**, which fires and the
reasoning was "automation_level blocked → therefore the tier gate passed, since tier is
checked first." That is an **inference from gate ordering, not an observation of the tier
gate admitting the user.** The tier gate never produced a visible admit decision in those
cells. S1-01 forbids exactly this substitution. The column may be safe — but this log does
not prove it.

Compounding: mid-test it was discovered the column is **`NOT NULL DEFAULT 'free'`**, not
nullable. The original bug characterization (NULL-denial) was wrong; the real production
state is `'free'` (legacy vocab) on every new user. The test was rewritten on the fly and
run once through the automation_level haze — no stop, no re-review before the irreversible DDL.

### The test to run now (routes around the confound)
Goal: observe the tier gate **itself** admit a canonical-UCB user and deny who it should,
in a log, post-DROP.

1. **Defeat the automation_level mask.** The dev account's `automation_level` blocks runs
   *before* tier becomes decisive. Either (a) seed/use a test user whose `automation_level`
   permits event-driven runs, or (b) trigger the agents through a path that doesn't hit the
   automation gate first. Confirm which gate is first in each agent before running — the two
   patterns differ (see §B).
2. **Two users, post-DROP reality:** both now have NO `users.subscription_tier` column at all.
   - User 1: `user_credit_balances.subscription_tier = 'operator'` → expect ADMITTED.
   - User 2: `user_credit_balances` row absent → COALESCE→`'scout'`. Expected outcome depends
     on each agent's `ALLOWED_TIERS` (scout IS admitted for Commentary; scout is denied for
     CashFlow by design — that's a correct deny, note it as such).
3. **Test ≥2 different agents across BOTH trigger patterns** (one live-DB-query agent:
   Commentary or CashFlow; one payload-tier agent: Research/Supply/Zoning). A repoint can be
   right in the DB-query path and wrong in the payload path — they read tier completely
   differently (§B).
4. **Evidence = pasted log showing the tier gate's own admit/deny line**, not a downstream
   gate's block. If you can't make the tier gate visibly decide, the test hasn't run.

**If it passes:** A9-Finding-1 is retroactively proven; record it closed. **If it fails:**
real entitlement bug, column already dropped — design the fix around the column's absence
(the repoint readers all use UCB/COALESCE, so a failure points at a missed reader or a
payload-tier path writing the wrong value at deal creation — see §B).

---

## §B — Two tier-check patterns (confirmed this session — the new thread needs both in view)

- **Live-DB-query agents (Commentary, CashFlow):** fire on `research.completed`; tier read via
  `SELECT COALESCE(ucb.subscription_tier,'scout') FROM deals d JOIN users u LEFT JOIN
  user_credit_balances ucb`. This is the authoritative repoint proof path — it reads UCB directly.
- **Payload-tier agents (Research, Supply, Zoning):** read `userTier` **from the event payload**,
  NOT from a live query. Payload is built in `inline-deals.routes.ts` with
  `userTier: row.tier || 'scout'`, where `row.tier` comes from **`deals.tier`** — a real column
  on the deals table, written at deal creation from the request body (`tier || 'basic'`).
  ⚠️ **This is a separate tier surface the repoint did NOT touch.** `deals.tier` is set at
  creation time and can be stale/wrong independently of UCB. The payload-tier agents gate on it.
  **This needs its own trace in the A9 audit** — is `deals.tier` ever reconciled with UCB, or
  is it a third source of truth? (`tier || 'basic'` even defaults to legacy vocab.)

---

## §C — NEW FINDINGS surfaced this session

### C-1: `automation_level` gate (trace target)
Blocks runs before tier becomes decisive. Unknown whether correct for a real operator/principal
user or its own entitlement defect. Trace in the A9 surface audit.

### C-2: `resolve-deal-context` schema fault — CLOSED with agent-run evidence
Verdict was any-deal launch-urgent (parse-time `42703`); fix = Commentary dropped dead
`dp.property_type` from SELECT, Supply+Zoning added `LEFT JOIN properties p ON p.id =
dp.property_id` and re-aliased. **Agent-run proof (post-fix log, 19:57):** Supply + Research sent
full LLM payloads to DeepSeek (past resolve-deal-context); Zoning wrote 3 `ai_usage_log` rows;
`fetch_data_matrix` reached + cache-written; `grep 42703` → 0 hits post-fix. Positive
forward-progress observation, not parse-inference. CLOSED.

**One caveat — Commentary is proven-by-absence, not proven-by-progress.** Its evidence is "no
resolve-deal-context error" in a log path that uses Anthropic (not DeepSeek), so it may not even
capture Commentary's execution. The other three have positive forward evidence; Commentary has
absence-of-failure. Its fix was the lowest-risk of the set (drop a dead unused column), so the
prior is high — but if you want uniform proof, confirm Commentary reached its own LLM/next step
in the Anthropic log path. Not blocking.

**Data note (unchanged, one layer down):** linked `properties` rows have NULL
`property_type`/`lot_size_sqft`. Agents proceed (coalesce to null) but Supply/Zoning may produce
degraded output on null inputs. What pipeline populates these? Triage for property-data area.

---

## §D — Everything else CLOSED this session (unchanged, solid)

| Item | Status |
|---|---|
| Login crash loop + logout | Closed, pushed |
| Pipeline visibility | Closed, pushed |
| A8-F1 (deal_category enum) | Closed. Grep proved `owned` dead in `deal_category` (every hit is the separate lifecycle `status` enum). Fix shipped `['pipeline','portfolio']` — evidence-backed. |
| Migrations (3 failing) | Closed. All baselined (1587/1588/1589). audit_log + deal_capsules are **STALE/retired** — different valid live designs, **NO `deal_id` ADD COLUMN owed** (do not let this drift back). 309 applied / 0 failed. |
| capsule_api_connections | Closed. Misread — real table `recipient_api_connections` exists, column-complete. |
| A9-Finding-1 readers | Repoint complete, sweep clean (all sites UCB/UCB-JOIN/UCB-aliased; `inline-auth:193` was fetch-but-unused, dropped from RETURNING). |
| A9-Finding-1 column DROP | **Applied and PROVEN** — admission test ran for real (automation_level=2 removed the masking gate; both trigger patterns admitted operator, CashFlow denied scout via its own log). See §A. |

Zombie-vocab cosmetic leftovers (fix opportunistically, not blocking): frontend
`authStore.ts`/`testUtils.tsx`/`MapTabsBar.tsx` still declare `'basic'|'pro'|'enterprise'`.

---

## §G — Ecosystem fact: user-facing agent & model selection (NOT a bug — architecture context)
**Users choose which agents run their tasks**, and that selection carries a provider/cost
dimension (DeepSeek = cheaper, Anthropic = premium). DeepSeek is the **intentional** provider for
test runs (cost) AND a live user-selectable option — do NOT "fix" any DeepSeek usage back to
Anthropic; it's part of the product. The session's split (Supply/Research/Zoning on DeepSeek,
Commentary on Anthropic) reflects this, not a misconfig.

Connections the next-session audits must account for:
- **A9 / §E:** agent-selection and model-selection are **entitlement surfaces**. Which agents and
  which providers a user can invoke is likely tier-gated. Audit "which agents/models can this user
  run" as part of the authz surface, alongside roles/`enabled_modules`. Check whether
  `enabled_modules` IS the agent-selection mechanism (may be the same store).
- **Billing/credit:** per-task credit cost depends on chosen agents + provider. Metering must match
  actual provider cost or the 40% margin drifts per user choice. Flag for any future billing audit.
- **Provider 402 (e.g. DeepSeek quota/balance) is now a PRODUCTION failure mode**, not just a
  test-wallet issue — any user who selected a DeepSeek-backed agent hits it. Needs a real error
  path (surface "agent temporarily unavailable", not a silent stuck run). Also: keep test-key
  DeepSeek credit topped up so harness runs don't 402 mid-run and masquerade as code bugs.
- **Commentary proof (§C-2 caveat) explained:** Commentary on Anthropic genuinely won't appear in
  the DeepSeek log path — so check its Anthropic-side `ai_usage_log` rows for positive proof,
  independent of provider.

## §E — Formal A9 RBAC surface audit — COMPLETE (read-only, 5 findings, human gate before any fix)

Audit ran this session. Live DB state at audit time: 13 users (12 investor / 1 admin), all 13 have
non-null `enabled_modules`; 1 UCB row (operator, automation_level=1); deals.tier = basic×16,
enterprise×9, pro×7 (32 tier-bearing deals).

**CRITICAL READ — F2 and F3 mask each other and MUST be fixed together.** F3 keeps every real
user at automation_level=1, so the event-driven tier gate never fires, so F2's legacy-vocab
lockout is currently invisible. Fixing F3 (raising automation_level) turns F2's 16 enterprise/pro
deals into NEW live failures. Triaging them as independent tickets will break 16 deals. They are
one coordinated fix.

### §E-F3 — automation_level never raised on tier upgrade — LAUNCH-BLOCKER (biggest of the five)
`creditService.ts:262` uses `SET automation_level = LEAST(automation_level, $4)` — clamps DOWN on
downgrade, never raises UP on upgrade. `setAutomationLevel()` exists (`:280-311`) but is never
called from the Stripe upgrade path. Result: every real user sits at automation_level=1; all 5
agents gate event-driven runs at `< 2` (research:73, cashflow:107/228, commentary:82, supply:63,
zoning:64). **The entire event-driven Inngest pipeline is dark for the whole user base.** Manual
POST runs unaffected. NOTE: this is why §A's admission proof had to manually set automation_level=2
— the product never grants it. §A is still valid for what it tested (tier gate reads UCB), but the
state it tested can't occur for a real user until F3 is fixed. Bigger than the original
A9-Finding-1: that broke new users; this breaks event-driven agents for ALL users.
→ **PRODUCT QUESTION (gates the fix):** on upgrade to operator+, should automation_level rise
automatically, or is event-driven access a separate opt-in? Auto-raise vs. expose-a-setting.

### §E-F2 — deals.tier client-supplied + legacy-vocab lockout — LAUNCH-BLOCKER (two defects)
**(a) Write-path privilege escalation (SECURITY, most urgent single line):** `inline-deals.routes.ts:453`
`const userTier = tier || 'basic'` takes tier verbatim from `req.body`, inserted at `:494` with no
UCB check. A Scout user can POST `{tier:"institutional"}` and get an institutional-stamped deal;
payload-tier agents (Research/Supply/Zoning, via `:688 row.tier`) then run at that level. No gate,
not a broken gate. Independent fix — do regardless of F3.
**(b) Legacy-vocab lockout (data, coupled to F3):** 16/32 deals carry enterprise(9)/pro(7), neither
in any agent's `ALLOWED_TIERS = ['scout','basic','operator','principal','institutional']`. Dormant
today (blocked at automation_level first); become live denials once F3 raises automation_level. Also
`calculateBillable('enterprise',...)` would crash on a TIER_CONFIG key-miss — no entry for
enterprise/pro (`basic` is a documented scout alias at creditService.ts:42-49).

### §E-F1 — enabled_modules: ghost entitlement column — Medium, BUT see product link
`inline-auth.routes.ts:17,86,128` fetch it; `:53,108,149` place it in session as `modules` (default
`['supply']`). Enforcement sites: **zero** (`grep req.user.*modules|allowedModules|moduleGate` → 0).
Any authenticated user accesses any module. Same shape as the zombie subscription_tier column.
→ **PRODUCT QUESTION (gates whether this is a bug or cleanup):** is `enabled_modules` meant to gate
agent/module selection (ties to §G ecosystem note — then this is MISSING ENFORCEMENT for a live
feature, upgrade to High) or genuinely vestigial (then it's cleanup)? Audit found enforcement
absent; product intent decides severity.

### §E-F4 — capsule attribution branding?.tier always 'free' — Medium, contained
`capsule-sharing.routes.ts:925` reads `branding?.tier ?? 'free'`, but the query at `:873-889` joins
UCB without selecting `ucb.subscription_tier` — no tier key in the result, so always 'free' → never
in `['principal','institutional']` → attribution never suppressed. Principal/institutional users who
disabled attribution still leak it on THIS flow. Lines 465 and 1012 do the alias correctly; only 925
is missing it. One-line fix, safe anytime. (`user_branding_settings` has no tier column — confirmed.)

### §E-F2(a) — CLOSED (write-path privilege escalation fixed)
`inline-deals.routes.ts`: removed `tier` from req.body destructuring (:381); replaced
`const userTier = tier || 'basic'` with `const userTier = balance?.subscriptionTier ?? 'scout'`
(:456-458), reusing `balance` already in scope from the A5-F1 deal-limit check (zero added queries).
Override approach — body value ignored, not validated. **Proof (direct DB observation):** body
`institutional`, UCB `operator`, stored `deals.tier` = `operator` → body ignored. PASS.
**Caveat:** proven at the HANDLER/DB layer via direct insert, NOT end-to-end HTTP — the HTTP test
was blocked by §E-F6 (auth-ordering). The fix is at handler level and the DB proof exercises that
logic, so it's very likely complete; but a one-line re-confirm via a real POST is owed once F6 opens
the HTTP path. Does NOT touch the 16 legacy enterprise/pro deals (that's F2(b), coupled to F3).

### §E-F6 — Auth/authz middleware ordering: `requireWeb` reads req.user before requireAuth populates it — NEW, surfaced during F2(a)
**Discovered incidentally while building the F2(a) HTTP proof.** On the deals route,
`requireSurface('web')`/`requireWeb` evaluates `req.user` and there is **no global JWT parser**, so
`requireWeb` sees `!req.user` and returns **401 for everything** before the route-level `requireAuth`
runs. Authorization gate fires before authentication populates what it authorizes against.
- **Why it matters beyond a broken test:** this ordering shape fails CLOSED here (401 everything),
  but the same flaw on a route where the surface check defaults permissively on missing `req.user`
  would fail OPEN = auth bypass. Other routes with this ordering are unaudited.
- **Open question (unresolved — agent ran out of trace):** is the deals HTTP path actually reachable
  in production by some mounting/middleware detail not yet mapped, or is it broken for real clients
  too? "My test script couldn't auth" is a weak place to leave it — needs a trace of how the deals
  route authenticates successfully (if it does) in the running app.
- Severity: provisionally **Medium-High** pending the open question. If real clients can't reach
  deals via HTTP, it's launch-blocking; if it's test-only and prod works via another path, it's a
  structural cleanup. Trace settles it.

### §E-F5 — Admin authz: inconsistent patterns; non-admin roles enforce nothing — Low
Three admin-check patterns (inline JWT `role!=='admin'` ×5 files; synthetic API-key role
`admin-api-key.routes.ts:42`; duplicated local hybrid in dot-admin + atlanta-url-discovery). No
shared `requireAdmin` middleware; `agent.routes.ts:126` omits the null-guard others have. Real
content: non-admin roles (investor/developer/flipper/broker/landlord/commercial) are stored,
returned, and **checked nowhere** — decorative unless role-based access is ever intended. Login
default `role || 'user'` (`:50`) isn't even a valid role from the type spec.
→ Product question, not a fix: is role meant to gate anything, or does tier do all entitlement work?

### Fix-pass triage — EXECUTED this session (with corrected proof states — READ CAREFULLY)
The coordinated pass ran. States below are corrected from the raw completion summary; two items
are NOT as closed as reported.

- **F3 auto-raise — CLOSED, proven both directions.** `updateTier()` `LEAST(automation_level,$4)`
  → `$4`. Verified: scout→operator 1→2; operator→scout caps at 1. ⚠️ Product consequence now LIVE:
  event-driven runs fire automatically on upgrade → credit burn is real in prod, the opt-in safety
  rail is gone by design. CONFIRM credit headroom per tier is comfortable.
- **F2(a) — CLOSED, now HTTP-proven end-to-end.** POST /deals {tier:"institutional"} + operator
  token → stored `operator`. The owed HTTP re-confirm is done.
- **F2(b) — MIGRATION RAN, PROOF INCOMPLETE. Verify before closing.** Reconciled 16 enterprise/pro
  deals; distribution now basic×16/operator×13/scout×3 — proves vocab is clean, NOT that each deal
  got the CORRECT tier. **This was an irreversible UPDATE on 16 production rows.** Owed proof: for a
  sample of the 16, confirm `deals.tier` == *that deal owner's* `user_credit_balances.subscription_tier`.
  Aggregate distribution looking clean is consistent with a blanket/wrong mapping. DO THIS FIRST next
  session — it's the only irreversible change in the pass whose per-row correctness isn't shown.
- **F1 — CLOSED.** Zombie `users.enabled_modules` dropped, proven absent. Trace confirmed it's
  zombie #3 (after subscription_tier, and this). Real module system is `user_module_settings` +
  `module_definitions` (table-driven, toggle/purchase endpoints). **Enforcement at agent dispatch
  is NOT wired — deferred as a FEATURE gate, not a bug.** NOTE: 3 zombie entitlement columns now
  found this session — a 4th likely exists; sweep for it.
- **F6 — deals route CLOSED & HTTP-proven** (201 where it was 401). **F6(b) — STILL OPEN,
  LAUNCH-BLOCKING.** The chat surface (`/`) has the SAME auth-ordering bug, and chat IS the launch
  surface. Fix is harder: Twilio/Telegram webhooks must stay open, so `requireAuth` goes inside
  MessageRouter's chat routes, not at mount level. Rank this launch-blocking-and-open, not a footnote.
- **F4 — applied, code-level only, NOT execution-proven.** `['enterprise']→['principal',
  'institutional']` at capsule-sharing:925-ish. Lowest risk; a runtime check (principal user with
  attribution off → suppressed) would close it properly.
- **deal_status 'active'→'PROSPECT' — NEW line item, CLOSED & proven.** Surfaced during F6 proof:
  deal INSERT used `status='active'`, not in the `deal_status` enum → every HTTP deal creation 500'd
  at the enum before this. HTTP deal creation was fully broken for everyone; now fixed (response
  shows status:'PROSPECT'). Log as its own launch-path finding, not a side-fix.

### Still gated on remaining product decisions
- **Q1 (enabled_modules): ANSWERED — enforce.** But trace showed the column was the wrong target;
  enforcement belongs on `user_module_settings` at agent dispatch (deferred feature).
- **Q2 (automation_level): ANSWERED — auto-raise.** Shipped (F3). Credit-burn exposure now live.
- **F5 admin roles** — still a product decision: does `role` gate anything, or does tier do all
  entitlement work?

---

## §F — Sequencing for the new thread
The §E fix pass EXECUTED this session. Two items are not fully closed — do these first:
1. **F2(b) per-deal verification (FIRST — only unproven irreversible change):** sample the 16
   reconciled deals, confirm each `deals.tier` == that deal owner's UCB tier. If any mismatch, the
   migration mapped wrong and 16 prod rows need correcting.
2. **F6(b) chat-surface auth-ordering (LAUNCH-BLOCKER, open):** `requireAuth` inside MessageRouter
   chat routes, keeping Twilio/Telegram webhooks open. Chat is the launch surface.
3. **Confirm credit headroom** now that F3 auto-raise makes event-driven runs fire on upgrade (burn
   is live in prod).
4. F4 runtime confirm (principal + attribution-off → suppressed); user_module_settings enforcement
   at agent dispatch (deferred feature, not bug); sweep for zombie entitlement column #4; F5/roles
   product decision; property-data completeness (§C-2 note); cosmetic frontend vocab.

**Honest one-line status:** Big coordinated fix pass landed. PROVEN-CLOSED: F3, F2(a) (now HTTP),
F1, F6-deals, deal_status. NOT FULLY CLOSED: F2(b) (vocab clean but per-deal correctness unproven —
irreversible UPDATE, verify first), F6(b) (chat-surface auth hole, launch-blocking, open), F4
(code-only). 3 zombie columns found total; a 4th likely. Credit-burn now live via F3.
