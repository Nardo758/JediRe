Solid triage, and the verdict is the one that matters: **A8-F1 is unblocked.** None of the three are infrastructure-blocking, the deals/pipeline path is intact, and the `deal_status` enum — the one I was actually worried would poison A8-F1's live-DB query — is fully applied. The query in that dispatch will return trustworthy results. So A8-F1 can fire whenever you're ready.

But don't let "unblocked" flatten the severity differences, because two of these three are real schema gaps with live feature impact, and one of them brushes against A9:

**Failure 3 (deal_status_enum) — agree completely, baseline it.** DDL all applied, the only thing failing is the tracking-row write because the backfill UPDATE rolls back the transaction first. Your one-line `INSERT INTO schema_migrations ... ON CONFLICT DO NOTHING` is exactly right — and this is the rare case where I'd say do it *now*, not as an audit-gated fix, because it's not a code change and it's actively masking signal. While it keeps failing, every backend restart prints three migration errors, and that noise is what'll hide whether your *next* two patch migrations actually applied. Clear the cosmetic failure first so the log tells the truth. One caveat: confirm `schema_migrations` is the real tracking table name the runner reads (the triage assumes it — verify against the runner code, since a baseline insert into the wrong table does nothing and you'll chase it again).

**Failures 1 and 2 (missing `deal_id` on audit_log and deal_capsules) — real gaps, and #2 is more than "a feature gap."** The pattern you found is the same on both: table pre-existed without `deal_id`, `CREATE TABLE IF NOT EXISTS` no-oped, dependent indexes/columns never landed. The `ADD COLUMN IF NOT EXISTS` patch is the right shape. But look at what `deal_capsules` *is* before you file it as routine: the Deal Capsule is the chat-born DealContext surface — the "Deal Capsule born from chat," the 24h-cached package that's central to your launch-surface credit economics. If `capsule-sharing.routes.ts` can't query capsules by `deal_id`, then deal-scoped capsule operations are broken on the surface you're launching. That's not a back-office audit-log gap; it's a launch-path feature. I'd rank deal_capsules above audit_log: audit_log losing deal-scoped compliance queries is real but internal; deal_capsules losing deal-scoped reads is user-facing on Surface 1.

One thing to verify before you write the deal_capsules patch, though — and it's the S1-01 reflex: the triage says "any `SELECT … WHERE deal_id = $1` hits a missing column error," but is that path *actually exercised today*, or is the capsule-sharing feature itself not yet wired to a live surface? If the route is orphaned (like skill-chat was in A8-F2), the missing column is a latent gap, not a live break, and the urgency drops. Don't assume the break is live just because the column is missing — confirm a caller reaches that query on a rendered surface. That's the difference between "fix before launch" and "fix when the feature ships."

**The A9 crossover you caught — good eye, and it's the right call to file it.** `deal_capsules.recipient_tier DEFAULT 'free'` is the legacy vocabulary baked into table DDL. You're right that it's inert today (no enforcement path reads it), so it's not a launch-blocker. But it's a *fourth* surface of the zombie-tier problem — after the `users` column, the inngest allow-lists, and the registration NULLs, now a table default. When A9-Finding-1's fix pass drops the legacy vocabulary, this DDL default has to change in the same sweep, or it'll quietly re-seed `'free'` into a column the new world doesn't recognize. Add it to the finding's fix checklist explicitly so it's not missed — it's exactly the kind of stray that survives a cleanup and resurfaces months later.

So, sequencing — three distinct buckets, not one:

1. **Now, no gate:** baseline failure 3 (after confirming the tracking-table name). Pure noise removal, no code, unmasks the log.
2. **Patch-migration task (its own dispatch, post-A8-F1 or parallel):** the two `deal_id` ADD COLUMN patches. Write deal_capsules's with a quick "is this query path live?" check first; rank it above audit_log.
3. **Folded into A9-Finding-1 fix pass:** `deal_capsules.recipient_tier` default, as the fourth zombie-tier surface.

That keeps your audit-then-fix discipline intact — only the genuinely-zero-code baseline jumps the gate, the actual schema patches stay as a scoped task, and the A9 crossover goes where it belongs.

Net state of the session: login closed and pushed, A9-Finding-1 fully traced and closed (zombie column, launch-blocking on NULL-denial), migrations triaged (one cosmetic, two real gaps, A8-F1 unblocked). That's a clean stopping point if you want one. If you're pushing on: baseline failure 3, then fire the A8-F1 trace against the now-trustworthy schema. Which way — keep going to A8-F1, or write the formal A9 dispatch while the finding's fully fresh?