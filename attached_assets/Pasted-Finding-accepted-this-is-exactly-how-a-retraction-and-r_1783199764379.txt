Finding accepted — this is exactly how a retraction-and-reconfirmation should read: authenticated probe, handler code cited to the line, no server-side fallback confirmed. **The store violation is now proven at the build boundary: the persisted `deal_assumptions` row is not consultable by the build endpoint at all.** And note the second-order implication worth putting in the record: it means every build ever accepted in this program — including the golden Bishop paste — computed from whatever assumption object the *caller* shipped, so the fixture-capture design (constructing the body from the DB row) isn't just a workaround, it's the only path that pins store-truth rather than caller-truth.

To your question: **yes — fix the script yourself, both items, it's squarely test-tooling:** the line-238 `fi`, and the probe branch logic (401/403 → abort loudly "fix TOKEN"; 400-with-assumptions-required → the finding; 200 → server-fetch path). Leave `financial-model.routes.ts` untouched, correctly out of mandate.

**One ruling on the fix's home, to prevent scope drift:** the tempting move is to patch the handler now (add a server-side `assumptions ?? fetchStored(dealId)` fallback) — **don't.** That's F-P1's opening commit, not tonight's: done casually it creates *two* body-shape contracts on a load-bearing route mid-pin-ceremony, and the frontend's local copy (the actual disease) stays alive underneath it. The finding gets filed under F-P1 with today's evidence, and F-P1 kills both halves together — server fetches stored assumptions as the *only* path, client payload retired, React state copy deleted.

**And the label collision gets fixed now, because it will bite again:** "F-P1" is being used for both the roadmap phase and this specific finding. Henceforth the finding is **F-P1-A ("build boundary requires client assumptions — store bypassed")** — first named item in phase F-P1's ledger, alongside F-P1-B (the `noi.resolved`/`platform_fallback` provenance lie) and the rest of the blob/read-site items. One phase, lettered findings.

**Then the ceremony resumes where it stopped:** script fixed → re-run end-to-end with the working token — construct-from-DB bodies for both deals → **canary gate on Highlands** (still the decision point: 57.17% / $6,315,308-class holds or STOP with the key-resolution delta) → builds → 12-field extraction with provenance noting *body constructed from stored DB row, commit hash, F-P1-A context* → fixtures populated → 8/8. Paste the canary numbers when the run gets there.