# CLOSE ORDER — W5 FINAL PASS + DECLARATION (Operator-Approved 2026-07-06)

**Executor:** capture/verification agent. This is the last W5 dispatch. Operator has approved closed-with-residuals contingent on this pass completing P3 items with evidence.
**Repo:** `Nardo758/JediRe.git` · backend :4000 · Standing rules: S1-01 pasted output; verify counts.

## C1 · Runbook Phases 2–3 (the never-run acceptance items — evidence per line)
1. **Smoke shapes, live:** Bishop monthly NOI/occupancy series from his current build — climbs from in-place-era baseline, floor dormant→binding transition month named. Highlands seed surface — steady state, canary values re-confirmed (57.17% / $6,315,308 / 2026-04-01). Paste series/values.
2. **Consumer matrix:** NOI, EGI, loan, IRR, EM fetched via every consumer path (deal-panel `/proforma/:dealId`, F9 surfaces, terminal FinancialsTab, capital-structure route) for both deals — one value per quantity per path, capital-structure serving the built model (modelNotBuilt honest-absence where applicable). Paste the matrix.
3. **D1 behavioral (NINTH session — closes now or gets a named blocker in the residuals table):** backend log + `ai_usage_log` row count before/after; navigate every F9 tab, Deal Details, AssetHub, terminal for both deals; zero LLM provider calls, zero new usage rows from navigation. Paste counts + log window.
4. **T2 forced cache-hit (same age, same rule):** identical large prompt twice through the agent pipeline; second call shows nonzero `cache_read_tokens`; `cost_usd` matches the three-term hand math pasted beside it.

## C2 · Parity list — regenerate as PROVISIONAL
Regenerate `docs/EXCEL_PARITY_ORACLE_REQUEST.md` from currently defensible values only: Highlands seed-path values (pinned, verified) + Bishop's July-5 live-capture five (loan/equity/IRR/EM/DSCR — payload-sourced) marked "subject to F5 re-pin." Fields never payload-extracted are listed as PENDING-RE-PIN, not given numbers. Header: "PROVISIONAL — final list issues after Bishop re-pin (F5 verdict)."

## C3 · Declaration + roadmap edits (commit both)
1. `W5-DISPATCH.md` gains the closing block:
> **W5 CLOSED — 2026-07-06, operator-declared, with named residuals.**
> Closed with evidence: deterministic turn-cohort engine (Findings A–P diagnosed/fixed/doctrine'd); one-NOI-truth across consumer surfaces; loudness system (canonical/alias/orphan, required-only); INV-6 hard invariant + assemble-once + pre-optimization demotion; canonical orchestration (pass-1 → M11 → M14 → equity reconcile → pass-2 → assemble → verdict); golden framework (Highlands seed-path + SyntheticDegenerate pinned/green; K-2; determinism); guard coverage extended to test surfaces with forced-failure proof; fixtures discipline (payload-sourced provenance mandatory).
> **Residuals (owners assigned):** R-1 F5 engine package [external agent] — effective-assumptions hypothesis (lead), Findings Q/R/S, 10 pre-existing production type errors, Bishop re-pin on verdict. R-2 Excel parity [operator] — final list post-re-pin. R-3 CI-run-history verification [parked — needs gh auth]. R-4 D1/T2 if C1 blocked them [named blocker recorded].
2. `docs/POST_D2_PROGRAM_ROADMAP.md`: Phase-0 marked done with the residuals table; **TS-1 becomes the ACTIVE item** (gate satisfied — surfacing renders live outputs; Bishop's fixture status doesn't affect his live build); F-P1 next in chain with its lettered ledger (F-P1-A build-boundary leak, B provenance lie, C owned_import honest-absence, + the read-site/blob items).
3. Commit both; operator pushes if sandbox blocks. Paste `git log --oneline -3`.

## C4 · Handoffs out of this dispatch
- **F5 → external agent** (operator delivers `HANDOFF-FIX5.md` / cross-referenced M-O handoff): lead hypothesis first — populate effectiveAssumptions at the service boundary from ONE live build, run golden against it; expected outcome dissolves the capital-structure delta; only if it fails does the intentional-vs-regression trace run. Accountability header attached (pasted output or it isn't a report; the P-commit needed repairs in four files — delivery quality is part of the record).
- **TS-1 → next session** (`TS1_THIN_SURFACING_PASS.md`, unchanged scope: render-only, frontend-only diff).

**Order: C1 → C2 → C3 → report with pasted evidence → done. The engine arc ends with this report.**
