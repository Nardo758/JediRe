Hold on — I'll take the good news, but **I reject the explanation, and the difference matters.** The bot just retracted three findings by claiming the earlier evidence was "a false schema read from information_schema." That explanation doesn't survive contact with what the earlier evidence actually *was*:

- The D3-W7 migration failure was **PostgreSQL itself** refusing the FK: *"Key columns projection_id and id are of incompatible types: uuid and integer."* That's not a catalog view misreading — that's the engine, at DDL time, asserting `id` was `integer` at that moment.
- Proof (a)'s failure was a **live INSERT rejection**: *"column purchase_price of relation tax_projections does not exist."* The database refused a write against the real table.

`information_schema` is a set of views over `pg_catalog` — the same truth `pg_attribute` reads. It doesn't serve stale schemas. So "the catalog lied" is not what happened. What happened is almost certainly simpler and more important: **the table changed between the failed runs and now.** The evidence was true then; the state is different now. And look at the timestamp: the row was written **2026-07-19 02:20 UTC — after the failures were pasted.** The sequence is: integer-id 9-column table → failures observed → *something rebuilt the table to the 22-column uuid schema* → the writer ran successfully → this row.

And we can even name the likely culprit, because the original audit predicted it: *"no migrations directory entry creates the rich schema — the table exists in minimal form only, likely created by an early Drizzle schema push."* The overwhelmingly probable event: **someone ran `db:push` (Drizzle schema sync) during the D3-W7 work** — possibly the bot itself, reflexively — which dropped/recreated `tax_projections` to match the code's 22-column uuid shape. Then `verify-protocol-runner` (whose `BISHOP_DEAL_ID` the row matches) executed and wrote the row.

**Why this distinction is worth stopping for:** "the evidence was wrong" and "the state changed via an untracked schema push" have opposite implications. The first would mean our audit method is unreliable. The second means **the live schema now has no migration provenance** — a 22-column table exists that no checked-in migration creates, which is the ghost-table disease *inverted*: last time a migration claimed a table that didn't exist; now a table exists that no migration claims. Any fresh environment, any teammate, any redeploy from migrations gets the 9-column skeleton back and the writer breaks again — silently, someday.

**So, before Wave 0 closes on this:**

1. **Establish what changed — one paste each:** shell/agent history for `db:push`/`drizzle-kit` in the last 48h; and `\d tax_projections` (the current DDL, on the record). If the push is found, the retraction becomes "state changed on 07-19 via untracked push" — accurate, and the findings weren't false, they were *timestamped*.
2. **Capture the schema as a real migration:** a checked-in, idempotent `CREATE TABLE IF NOT EXISTS` (or the ALTER set) reflecting the live 22-column uuid shape — so the ledger matches reality and a fresh environment reproduces it. This is non-optional; it's the standing migration rule.
3. **Then D3-W7 proceeds exactly as the bot says** — the FK is now type-compatible, no row migration, **do not truncate** (agreed: the row is valid Bishop data *and* it's the evidence of the change event). Apply the D3-W7 DDL transaction-wrapped, re-run the four reconciliation proofs.
4. **Correct the record:** the three "retractions" get restated as "superseded by schema change on 2026-07-19," not "false reads." The audit was right when it ran. Precision here protects every future audit's credibility.

The genuinely good news stands: the writer works, F40/F41 is alive, the reader will deserialize correctly, and D3-W7's path is clear. Wave 0 closes once the push is confirmed and the provenance migration is checked in — that's one history grep, one `\d` paste, and one migration file. Then the board is fully clean to your two ruling sets, and Wave 1 cuts.