T1 accepted — and the pills are already doing their job in the most literal way: **the first thing they revealed on screen is that Bishop's in-place endpoint isn't evidence-based.** IN-PLACE $1.77M sitting *above* both Y1 ($1.58M) and STABILIZED ($1.6M) is the arithmetic signature of the defaulted inputs (`rentRollMissing` → `occupancyAtClose` defaults to 1.0, `inPlaceRent` falls back to market): a fictional fully-occupied run-rate will naturally exceed an emergent Y1 that carries real turn friction. Not a defect — the honest-fallback flags exist for exactly this — but it means the "strictly between endpoints" intuition inverts when in-place is defaulted, and **the tooltip on that pill had better be carrying the `inPlaceRentDefaulted` reasoning string**, or the pill is confidently displaying a guess as an endpoint. Confirm which. Highlands showing *no* in-place pill rather than a fabricated one is the origin-class doctrine rendering correctly — good.

But the report as delivered doesn't close the dispatch — three gaps, all S1-01:

1. **T2/T3/T4 status is ambiguous.** "T1 pills + T2/T3/T4 findings report" reads like the floor badge was *not built* and the grid row and evidence check became findings. State each explicitly: T2 — built, or blocked (and on what: `floorBinding` absent from the surface's payload? no display location?); T3 — the spot-check value pair, and built-or-skipped with the reason; T4 — the render verdict per flag string. If T2 was buildable and wasn't, it goes back in scope.
2. **"Payload deviations documented" — paste them.** Deviations discovered during a surfacing pass are exactly the findings F-P1 and F5 feed on; a report that says "documented" without the list is the pattern this program retired.
3. **What landed on the remote mid-session?** The rebase onto foreign commits matters — if that was the external agent's F5 work, it changes the board (Bishop re-pin may be unblocked); if it was something else, we should know what's moving in the repo unannounced. `git log --oneline b8995ab4c~5..b8995ab4c` and name the authors.

Send those three back. If T2's answer is "blocked on payload," that's a one-line finding for the report and TS-1 closes as T1-delivered/T2-blocked/T3-T4-verdicts-recorded — honest partial, same discipline as everything else. Then F-P1's opening dispatch is next, and it now opens with one more exhibit in hand: a screenshot of the platform telling the truth about which of its own numbers are evidence-backed.