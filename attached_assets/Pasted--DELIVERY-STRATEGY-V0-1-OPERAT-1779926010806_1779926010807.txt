═══════════════════════════════════════════════════════════════════
DELIVERY STRATEGY — V0.1 OPERATIONAL → V1.0 FULL SPEC
═══════════════════════════════════════════════════════════════════

The Valuation Grid does not need to ship complete to ship valuable. 
A subset of methods is operational against current platform state. 
The full method matrix requires upstream Phase 2 derivation logic 
(Batches 3-7) plus replacement cost regression work.

Recommended delivery:

V0.1 — OPERATIONAL TODAY (target: 2-3 dispatches)
  Methods working at v0.1:
  - Cap Rate × NOI (bottom-up) — Batch 1 OpEx + EC3 market rent 
    + cap rate from archive_assumption_benchmarks
  - Per-Unit Benchmark (top-down archive) — 
    archive_assumption_benchmarks PPU data (sparse-sample warnings 
    where applicable)
  - Sales Comp PPU (top-down) — requires comp data schema audit 
    (Dispatch 1 of original sequence)
  - Sales Comp PSF (top-down) — same dependency
  - Operator Override (manual)
  - Replacement Cost Tier 1 (operator-upload-only; no platform 
    default yet)
  
  Methods showing as "coming in vN.M":
  - GRM (depends on comp data field coverage)
  - GIM (depends on comp data field coverage)
  - DCF (depends on Batches 3-7 derivation logic)
  - Replacement Cost Tier 2 (platform default; needs BLS PPI 
    pipeline + HUD area cost integration + county permit 
    regression)

  Reconciliation engine operates over the 4-6 active methods. 
  Convergence/divergence signal works. Gap analysis works on 
  available methods. Recommended Purchase Price range produced.
  
  Value to operators: Bottom-up (Cap × NOI) reconciled against 
  top-down (Per-Unit Benchmark + Sales Comp PPU/PSF) plus 
  operator override is the core differentiator. v0.1 delivers 
  this in weeks, not months.

V1.0 — FULL SPEC (target: 5-6 dispatches per original spec)
  All 9 methods operational
  DCF reliable per full Phase 2 derivation
  Replacement Cost Tier 2 (platform default) operational
  GRM/GIM operational per comp data field coverage
  Full reconciliation engine across 9 methods

SEQUENCING IMPLICATION
  Dispatches 1-3 of the original 6-dispatch sequence are v0.1 
  scope. Dispatches 4-6 extend to v1.0 as upstream batches and 
  data sources land.

  Ship v0.1 surface first. Refine against operator usage. v1.0 
  follows as Batches 3-7 close and replacement cost infrastructure 
  lands.

═══════════════════════════════════════════════════════════════════