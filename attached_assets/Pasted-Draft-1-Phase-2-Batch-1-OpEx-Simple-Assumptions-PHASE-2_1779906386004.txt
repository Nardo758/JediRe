Draft 1 — Phase 2 Batch 1: OpEx Simple Assumptions
PHASE 2 BATCH 1 — OPEX SIMPLE ASSUMPTIONS DERIVATION LOGIC

CONTEXT
Phase 2 = state-conditional derivation logic per assumption in 
PROFORMA_CALCULATION_TEMPLATE.md. This is Batch 1 — the simpler 
OpEx assumptions where derivation logic can be drafted bot-led 
with operator review.

Batch 1 covers OpEx assumptions that are:
- Less state-conditional than revenue (vacancy/concessions etc.)
- Have clean source priorities
- Don't depend on market rent (EC3) or unresolved data

Subsequent batches:
- Batch 2: OpEx complex (property tax with reassessment, insurance 
  with FL multiplier, management fee operator-specific)
- Batch 3: Capital structure
- Batch 4: Growth assumptions
- Batch 5: Exit cap + terminal value
- Batch 6: Revenue (waits for EC3)
- Batch 7: Purchase Price + reconciliation

This dispatch handles Batch 1 only.

STATE VERIFICATION (per P8 corollary):
Before executing, confirm:
1. PROFORMA_CALCULATION_TEMPLATE.md exists with the 22 
   assumptions and layer tagging
2. No prior Phase 2 derivation dispatch has produced batch 
   work (search docs/operations/ for PHASE_2_BATCH or similar)
3. The Mandate v1.3 lift + Tier 1 RegimeExpand + Tier 2 directional 
   validation are all in place (confirmed COMPLETE in 
   PHASE_1_TASK_MAP.md)

═══════════════════════════════════════════════════════════════════
ASSUMPTIONS IN BATCH 1
═══════════════════════════════════════════════════════════════════

From PROFORMA_CALCULATION_TEMPLATE.md, the Batch 1 OpEx 
assumptions are:

1. Repairs & maintenance ($/unit annual)
2. Turnover costs ($/unit annual)
3. Marketing ($/unit annual)
4. Admin/general ($/unit annual)
5. Contract services ($/unit annual)
6. Utilities — water ($/unit annual)
7. Utilities — gas ($/unit annual)
8. Utilities — electric ($/unit annual)
9. Utilities — trash ($/unit annual)
10. Replacement reserves ($/unit annual)

10 assumptions, all "$/unit annual" granularity, all multifamily-
existing scope. Each has the same shape of derivation logic 
(though specific evidence sources vary).

═══════════════════════════════════════════════════════════════════
DERIVATION LOGIC STRUCTURE PER ASSUMPTION
═══════════════════════════════════════════════════════════════════

For each Batch 1 assumption, produce a structured derivation 
logic block following the principle committed in CLAUDE.md P7 
and the state-conditional epistemology in 
MANDATE_LIFT_DESIGN.md:

ASSUMPTION: [field_name]

DESCRIPTION:
[What this represents in operator language]

SOURCE PRIORITY BY ASSET STATE:

If asset_state == 'Stabilized':
  Primary: Subject's Trailing (T-12 from deal_monthly_actuals)
    → Confidence: HIGH if last 3 months agree within ±15%
    → Caveats: outlier months excluded (one-time spikes)
  Validation: Platform line_item_benchmarks for state×class×vintage
    → Sigma flag if subject > P75 or < P25 of cohort
    → If sigma flag fires: surface to operator, don't auto-reject
  Cross-check: Sales comp implied OpEx (per cap rate triangulation)
    → If divergence >20%: flag and explain in evidence trail

If asset_state == 'Repositioning' (value-add):
  pre_renovation: Subject's Trailing (current degraded state)
    → Source: T-12 (anchored in actuals)
    → Confidence: HIGH (same as Stabilized)
  post_stabilization: Platform line_item_benchmarks at target 
                     asset class
    → Source: cohort distribution for target state
    → Confidence: MEDIUM (projection, not actuals)
    → Tier 2 validation: post should be ≤ pre for OpEx categories 
      that improve with renovation (R&M, turnover); ≥ pre for 
      categories that grow with stabilization (marketing during 
      lease-up)

If asset_state == 'New Construction':
  pre_renovation: N/A
  post_stabilization: Platform line_item_benchmarks at projected 
                     delivery asset class
    → Source: cohort distribution for stabilized comparable
    → Confidence: MEDIUM-LOW (projection, no anchoring)

If asset_state == 'Lease-Up' or 'Distressed':
  Specific logic per state — bot drafts; operator refines

MODULE CONTEXT TO CONSULT:
[Which capsule modules supply context for this assumption]

For OpEx Batch 1:
- M14 Risk: flags OpEx outliers
- M22 Post-Close: what comparable acquisitions actually achieved 
  (validates derivation against realized outcomes)
- M02 Regulatory: only relevant if the assumption is jurisdictional 
  (e.g., trash collection may be municipal)

VALIDATION METHODS:
1. Sigma check (line_item_benchmarks distribution)
2. Comparable assumed-vs-achieved (archive_assumption_benchmarks)
3. Sales comp triangulation (implied OpEx from cap rate × NOI)
4. Tier 2 directional checks (now operational)

OUTPUT SHAPE:
{
  value: <number>,
  source: 'trailing' | 'platform' | 'comp_implied' | 'override',
  confidence: 0.0-1.0,
  asset_state: 'Stabilized' | 'Repositioning' | ...,
  evidence_rows: [<links to underwriting_evidence>],
  validation: {
    sigma_flag: boolean,
    sigma_band: {p25, p50, p75},
    archive_comparison: {assumed_p50, achieved_p50, delta_pct},
    sales_comp_implied: <number>
  }
}

OPERATOR OVERRIDE PATH:
[How operator-provided values flow through]

═══════════════════════════════════════════════════════════════════
SCOPE OF THIS DISPATCH
═══════════════════════════════════════════════════════════════════

A. For each of the 10 Batch 1 assumptions, produce the structured 
   derivation logic block above
B. Maintain the standard structure across all 10 so they read 
   consistently
C. Where assumptions share logic (e.g., all utilities likely 
   have similar derivation), produce a common pattern with 
   per-utility variations noted
D. Flag any assumption where the derivation logic surfaces 
   ambiguity that requires operator review before implementation
E. Identify dependencies on other batches (e.g., if R&M 
   derivation references property tax behavior, note the cross-
   batch link)

═══════════════════════════════════════════════════════════════════
DELIVERABLE
═══════════════════════════════════════════════════════════════════

docs/operations/PHASE_2_BATCH_1_OPEX_SIMPLE.md

Structure:
1. Executive summary (10 assumptions, common patterns observed, 
   cross-batch dependencies)
2. Common derivation patterns (shared logic across assumptions)
3. Per-assumption derivation logic (one section per — 10 sections)
4. Open questions for operator review
5. Cross-batch dependencies surfaced

═══════════════════════════════════════════════════════════════════
DO NOT
═══════════════════════════════════════════════════════════════════

- Produce derivation logic for assumptions NOT in Batch 1
- Implement any code changes (this is documentation only)
- Make operator-judgment calls without flagging — surface them 
  as open questions
- Collapse the per-assumption structure to save space — each 
  needs its own section for review
- "While I'm here" cleanups
Expected close time: ~3-4 hours. Substantial work; first batch of Phase 2 derivation.

Draft 2 — EC3 Investigation: Market Rent Source
EC3 INVESTIGATION — MARKET RENT SOURCE

CONTEXT
EC3 in PHASE_2_ENTRY_CONDITIONS.md is currently UNKNOWN. Phase 2 
revenue derivation depends on a clean market rent source. The 
Validation Grid investigation surfaced this as Open Q1 (market 
rent source dependency). It has not been resolved.

This investigation determines:
- What market rent sources currently exist in the platform
- What gaps remain
- What's the recommended path to EC3 SATISFIED

STATE VERIFICATION (per P8 corollary):
Before executing, confirm:
1. EC3 status in PHASE_2_ENTRY_CONDITIONS.md is still UNKNOWN
2. No prior EC3 investigation document exists (search 
   docs/operations/ for EC3 or MARKET_RENT)
3. The Validation Grid investigation's Open Q1 is still 
   unresolved (sanity check)

═══════════════════════════════════════════════════════════════════
SCOPE
═══════════════════════════════════════════════════════════════════

STEP 1 — INVENTORY CURRENT MARKET RENT SOURCES
A. Search the codebase for market rent references:
   - grep for 'market_rent', 'asking_rent', 'effective_rent', 
     'face_rent', 'in_place_rent'
   - Identify all read sites + write sites + storage tables
B. Document each source:
   - Where it comes from (T-12, rent roll, external API, 
     manual entry, broker package)
   - Granularity (per unit type / building average / submarket 
     average)
   - Coverage (which deals have it, which don't)
   - Freshness (is it updated; how often)
C. Identify the canonical source vs derived/cached sources

STEP 2 — IDENTIFY GAPS
A. For Phase 2 revenue derivation, what does the agent need?
   - Market rent per unit type for subject (often available from 
     rent roll)
   - Market rent for comparable cohort (benchmark; this is the 
     gap question)
   - Rent growth assumption (separate from market rent itself)
B. What's the current gap?
   - Subject market rent: usually available
   - Cohort market rent: ?
   - Is there a market rent benchmark table analogous to 
     line_item_benchmarks?
C. If gap exists, document the shape:
   - Data source needed (RentCast? CoStar? Apartment Locator AI?)
   - Storage schema needed
   - Freshness requirement
   - Validation methods

STEP 3 — RENTCAST AND APARTMENT LOCATOR AI ASSESSMENT
A. RentCast is in the data sources list (Research Agent). Document:
   - What market rent data RentCast provides
   - Coverage (which markets)
   - Cost / API limits
   - Whether currently integrated into market rent flows
B. Apartment Locator AI is the proprietary signal. Document:
   - What demand signal it provides
   - Whether it surfaces market rent directly or implies it
   - How it could feed market rent benchmarks
C. Recommend which is the right primary source

STEP 4 — RECOMMENDATION
A. The investigation produces a clear path:
   - "Market rent is sourced from X for subject, Y for cohort"
   - "Storage schema needs Z modifications"
   - "Validation methods are W"
B. Identify implementation effort to satisfy EC3:
   - GREEN: Can be satisfied with documentation only (sources exist; 
     just need to document the pattern)
   - YELLOW: Small implementation needed (storage schema + read 
     integration)
   - ORANGE: Substantial implementation (new data source ingestion)
C. Estimated dispatch count to reach EC3 SATISFIED

═══════════════════════════════════════════════════════════════════
DELIVERABLE
═══════════════════════════════════════════════════════════════════

docs/operations/EC3_MARKET_RENT_SOURCE.md

Structure:
1. Executive summary (current state, gap, recommended path)
2. Current source inventory (Step 1)
3. Gap analysis (Step 2)
4. RentCast + Apartment Locator AI assessment (Step 3)
5. Recommendation + effort estimate (Step 4)
6. Open questions

═══════════════════════════════════════════════════════════════════
DO NOT
═══════════════════════════════════════════════════════════════════

- Implement any market rent integration
- Modify existing market rent code paths
- Commit to RentCast vs Apartment Locator AI as the primary — 
  surface the analysis and let Leon decide
- Skip Apartment Locator AI consideration — it's the moat
- "While I'm here" cleanups
Expected close time: ~2-3 hours.

Draft 3 — EC1 Production Query
This isn't a dispatch — it's a query you run directly. Here's what to run and what to do with the result:
sql-- EC1 PRODUCTION COUNT QUERY
-- Identifies deals needing investmentStrategy backfill

SELECT
  COUNT(*) AS total_deals,
  COUNT(*) FILTER (WHERE investment_strategy_lv IS NULL) 
    AS missing_strategy,
  COUNT(*) FILTER (WHERE investment_strategy_lv IS NULL 
    AND deal_type IS NOT NULL) 
    AS backfillable_from_deal_type,
  COUNT(*) FILTER (WHERE investment_strategy_lv IS NULL 
    AND deal_type IS NULL) 
    AS orphan_no_strategy_no_deal_type,
  COUNT(*) FILTER (WHERE investment_strategy_lv IS NOT NULL 
    AND deal_type IS NULL) 
    AS strategy_only_no_deal_type,
  COUNT(*) FILTER (WHERE investment_strategy_lv IS NOT NULL 
    AND deal_type IS NOT NULL) 
    AS both_populated
FROM deals;
Decision tree based on result:
ResultActionbackfillable_from_deal_type = 0EC1 SATISFIED. No backfill needed. Mark complete.backfillable_from_deal_type < 50Run backfill manually; ~30 min work. Then EC1 SATISFIED.backfillable_from_deal_type ≥ 50Scope a backfill migration dispatch. ~half day. Then EC1 SATISFIED.orphan_no_strategy_no_deal_type > 0Separate problem; deals with no strategy classification at all. Document and decide whether to backfill via default or flag for operator.strategy_only_no_deal_type > 0Confirms A1 direction is right; deal_type is derived/optional, not authoritative.both_populated should be majoritySanity check the bridge from Task #1233 is working.
Run the query, post the result back here, and I'll help you interpret it. If the count is small enough to run inline, no dispatch needed — just execute the backfill SQL and we're done with EC1.

Sequencing
These three things run in parallel:
TrackActionCloses whenPhase 2 substantive workFire Batch 1 dispatchClosing note + verificationEC3 unblockingFire EC3 investigationClosing note + verificationEC1 closureRun production queryQuery result interpreted
When all three land:

Phase 2 Batch 1 verification produces the Batch 2 (OpEx complex) dispatch
EC3 investigation closure tells us how much work to unlock revenue derivation
EC1 query result either closes EC1 or scopes the backfill

Three substantive moves in flight, all aligned, all P8-disciplined.