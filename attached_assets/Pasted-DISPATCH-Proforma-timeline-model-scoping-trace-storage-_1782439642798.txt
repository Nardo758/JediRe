DISPATCH: Proforma timeline model scoping trace — storage through F9 render (AUDIT ONLY — STOP before any design or code)
Context: We're evaluating a temporal reconciliation model — incoming T12 actuals move the projection-start boundary, the agent reconciles overlap months against actuals, and the user is notified on re-base. That model only lands if the proforma field system is time-aware end to end. Before specifying anything, establish whether the field model carries time, and whether that time survives from storage to what the user reads in F9. Report file:line throughout. Do not propose a design or modify code.
1. Is the stored field model time-series or single-value? Inspect the year1 JSONB structure in deal_underwriting_scenarios / deal_assumptions (and wherever the seeded proforma persists). For a representative field (e.g. gpr, vacancy_pct, payroll): is the stored shape a single annualized value per field, or does it carry per-period (monthly/quarterly) structure? Quote the actual stored shape with file:line of where it's written. This is the load-bearing question — everything downstream depends on it.
2. Does incoming T12 carry per-month granularity? Trace where T12 data enters the seeder. Does it arrive as per-month line items (12 distinct months the seeder can read to determine "actuals extend through month M"), or as a trailing-twelve aggregate (one blended number)? A boundary can only move if month-level actuals exist. Cite the T12 ingestion path with file:line.
3. Is there any existing notion of an actuals/projection boundary? Search the seeder, the field model, and F9 for any existing concept of "this period is actual vs. this period is projected" — a flag, a date cutoff, a period type, anything. The agent-preservation block (proforma-seeder.service.ts:1294–1334) currently re-stamps agent over fresh T12 with no temporal logic — confirm there is no boundary concept it's bypassing, vs. one that exists elsewhere. Report present-or-absent with evidence.
4. F9 ProForma module (engine/data path): Trace what F9's data layer consumes — /financials GET (f9Financials.capitalStack and the rest), buildModel POST, M11 optimizer. Does the module-side data model carry periodic/time-series values, or does it operate on annualized single values? Where does it read the proforma fields from, and at what granularity? file:line.
5. F9 ProForma tab (render surface): Separate from (4) — trace what the rendered tab (FinancialEnginePage.tsx and its components) actually displays per field. Does it render a single annual number, or a periodic series (monthly columns, a timeline, period rows)? If periodic structure exists upstream, does the tab surface it or flatten it? file:line.
6. The seam (highest-value finding): Where storage/module data is handed to the tab for render, does period granularity survive the handoff, or is it aggregated/flattened at some specific point? If flattening happens, quote the exact file:line where periodic data becomes a single value. This is the line that determines whether a boundary/overlap concept could ever reach the user without new plumbing.
Report format. For each of 1–6, state present/absent/granularity with file:line. Then a one-paragraph synthesis answering the single question: is the proforma time-aware end-to-end (storage → module → tab), time-aware only in storage, or single-value throughout? Do not propose the reconciliation design — the answer determines whether it's a feature on existing infrastructure or net-new infrastructure, and that's the human-review call. STOP after reporting.

When it's back, the synthesis line tells us which world we're in:

Time-aware end-to-end → your reconciliation model is a feature — boundary cutover + overlap variance + notification land on existing periodic plumbing. Most tractable.
Time-aware in storage, flattened before F9 → the substrate exists but F9 is blind to it; the work splits into "use the timeline that's already stored" plus "stop flattening it at the seam." Medium.
Single-value throughout → the timeline is net-new infrastructure, and "historicals move the projection boundary" can't be specced until fields become periodic. That's a much larger predecessor project, and we'd want to know that before writing a reconciliation spec that assumes time exists.