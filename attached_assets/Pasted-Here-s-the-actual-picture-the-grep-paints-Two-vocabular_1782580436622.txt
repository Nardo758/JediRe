Here's the actual picture the grep paints:
Two vocabularies that don't agree on what a tier even is.

TIER_CONFIG (the one the app validates against): scout, basic, operator, principal, institutional.
A second vocabulary appears all over the agent layer and validation: operator, professional, enterprise, principal, institutional, basic (commentary/supply/zoning.inngest.ts), operator, professional, enterprise (research.inngest.ts), and most tellingly validation.ts:40: z.enum(['basic', 'professional', 'enterprise']).

So professional isn't an orphaned typo. It's a first-class value in a parallel tier vocabulary that the agent gating and at least one Zod schema accept. That's how users.subscription_tier got set to professional — some write path uses the basic/professional/enterprise vocabulary, not the scout/operator/principal one. Two teams' worth of tier naming were never reconciled. Find the writer with grep -rn "subscription_tier\s*=" backend/src --include=*.ts plus the Stripe webhook handler, but the existence of validation.ts:40 already tells you a request body can legitimately carry professional.
Three read patterns, reading two different tables, with three different fallbacks. From the grep:

user_credit_balances readers (the billing-canonical path): middleware/auth.ts:316 (canAccessSurface), all of settings-ai.routes.ts, creditService.ts, aiService.ts:592. These read the table you fixed → see operator. Fallback 'scout' or 'free'.
users.subscription_tier readers (the stale path): inline-auth.routes.ts (×4), index.legacy.ts, settings-branding.routes.ts, capsule-sharing.routes.ts, the three *.inngest.ts agent SQL queries, cashflow-underwriting.routes.ts. These read the table that says professional. Fallback 'free'.
Reconciled readers (the correct pattern, and proof someone already knew about the split): auth.routes.ts:269 COALESCE(ucb.subscription_tier, 'scout') and email-intake.function.ts:52 COALESCE(ucb.subscription_tier, u.subscription_tier, 'scout'). These deliberately prefer the credit-balance table and fall back to users. This is the pattern that should be everywhere and isn't.

So your empty pipeline: whichever endpoint feeds the pipeline list reads users.subscription_tier directly (the stale path), gets professional, looks it up in TIER_CONFIG, finds nothing — TIER_CONFIG['professional'] is undefined — and the gating logic fails closed to empty. Note one thing the grep doesn't show: no subscription_tier read in an obvious deals/pipeline route file. The pipeline list endpoint may live in inline-deals.routes.ts (which reads balance.subscriptionTier at :389 — the credit-balance object) or it gates somewhere upstream. Before the fix, name the exact pipeline reader: grep -rn "subscriptionTier\|subscription_tier" backend/src/api/rest/inline-deals.routes.ts and whatever serves GET /deals. That's the file:line S1-01 wants.
Now the decision, and it's bigger than this bug:
Canonical column. user_credit_balances is right — tier is a billing fact, Stripe owns it, and two route files already treat it as canonical via COALESCE. Ratify that. Then every users.subscription_tier reader is a bug to convert to either the COALESCE pattern or a shared resolveTier(userId) helper. There is no shared helper today — that's the root cause. Eight-plus files each inline their own tier query against whichever table the author happened to know about.
Canonical vocabulary. This is the launch-gating one. You cannot ship with basic/professional/enterprise and scout/operator/principal/institutional both live. Decide the real five (your TIER_CONFIG set looks intended), then validation.ts:40 and all four *.inngest.ts allow-lists are wrong and must change, and any writer emitting professional must be mapped or killed. Until that's done, professional will keep getting written and you'll be patching rows forever.
What to do, in order:

Dev unblock now (so you can keep testing login, 30 seconds): UPDATE users SET subscription_tier = 'operator' WHERE email = 'm.dixon5030@gmail.com';. Confirm pipeline populates. This only masks it for you locally — it does not fix the bug.
Name the pipeline reader with the targeted greps above. Confirm it's reading users and choking on professional. That's your verified file:line.
Log the A9 finding — this is the headline RBAC finding, and it's three-part: (a) two-table tier divergence with no shared resolver, (b) two incompatible tier vocabularies with a writer emitting the wrong one, (c) ~8 readers split across both tables with inconsistent fallbacks. This is launch-gating because it's an entitlements bug — users could see or be denied surfaces/deals based on which code path checks their tier. That's exactly what A9 exists to find, and you found it by accident.

Don't fix 2 and 3 as a fix pass right now — you're mid-login-verification and your own discipline says audit-then-fix with a gate. Do the dev unblock, confirm login is fully clean (crash gone ✓, logout dev-only, pipeline now showing), push the 4-commit stack, and fold the tier-divergence finding into A9 as its first and most serious entry. The three failed migrations are still queued ahead of A8-F1.
Run the unblock UPDATE and the pipeline-reader grep — does the pipeline populate, and which file reads users?