Worth noting: the resolution is silent. No specific PR closes BUG-11; it just fixed itself somewhere along the way. That's exactly the methodology gap the DQA closes — running on every reseed, findings would have shown both the moment the bug appeared and the moment it disappeared, with timestamps. Right now, you've only got the post-state. The "what fixed it" is unknown.
Ticket A shipping as defensive is the right call. The write-race class isn't disproven by Bishop's GPR being currently correct — it's just dormant on this specific deal at this moment. The fix prevents re-surfacing.
The Task #691 / data-quality-agent.service.ts existence is significant news. The spec I drafted is effectively a description of what should exist rather than a new build — which means the taxonomy task is a delta against the existing service, not a from-scratch rebuild. Cleaner shape.
Three things from this thread that should land in the taxonomy update once you've got the existing taxonomy snapshot:
1. Split SEED_PLUMBING into SEED_PLUMBING_WRITE_RACE and SEED_PLUMBING_STALE_SEED. Same observable symptom (year1.*.om null with source data present), different root causes, different remediation paths. Write-race wants Ticket A's data-layer fix; stale-seed wants the opex back-fill or event-driven reseed. The disambiguator is timestamp comparison — exactly the diagnostic you just ran on Bishop's GPR. The DQA can run that automatically per finding.
2. Make NOT_IN_DOC selectively surfaceable. Spec currently says don't surface it. But Bishop's contract services case showed that "value is null because the document genuinely doesn't have it" is information the underwriter wants to see — distinct from "value is null and we don't know why." Worth a low-severity info-tier surface so users know the column was checked rather than skipped. Suppressed by default but available via a "show all" toggle.
3. Distinguish source-extracted-then-lost from source-never-had-it. This is the Scenario A/B vs Scenario C architectural difference. The taxonomy should make this distinction explicit at the classification level, not bury it in agent_finding JSONB. Different cognitive load on the underwriter, different urgency, different downstream action.
When you have the existing service's current taxonomy snapshot, paste it and I'll draft the taxonomy update task as a delta — what to add, what to split, what to keep, what to retire. Cleaner than reauthoring whole sections.
One process note worth flagging: the spec-vs-code drift risk going forward. The DATA_QUALITY_AGENT_SPEC just became authoritative for what the service should do, and the existing service is what it currently does. Once the taxonomy task lands, re-baseline the spec to match — otherwise the next session that references the spec will be working from a stale document, which is the same shape as the schema-vs-runtime drift the OM extraction schema-lag finding was about. Worth a 5-minute spec-update commit at the end of the taxonomy work.