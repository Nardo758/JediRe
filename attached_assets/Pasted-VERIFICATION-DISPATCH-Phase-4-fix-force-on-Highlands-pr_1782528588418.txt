VERIFICATION DISPATCH: Phase 4 fix — force on Highlands, prove with live rows (paste SELECTs)
The fix is asserted, not proven — the report's own query is "after next re-upload." Force it now on Highlands (the live monitoring deal, real actuals) and on 3f32276f. Code existing is not evidence; this subsystem has produced two dead-path bugs and one false docstring already. Paste every SELECT. STOP at the report.
V-A — Did the Highlands audit (§4.5) run, and is Highlands 2a or 2b? Before anything else: SELECT Highlands' (is_portfolio_asset = TRUE) periodic_seed zone distribution (actual/gap/projection counts) and the min/max period_month of its deal_monthly_actuals. State whether its actuals overlap the projection zone, and by how many months. Report which sub-shape Highlands actually is. If the overlap is multi-month, the build must have a bulk path — confirm it does or flag its absence.
V-B — Migration applied to the live DB, not just the file. SELECT to_regclass('public.deal_reconciliation_log') (non-null), and \d deal_reconciliation_log showing columns + the three indexes. File present ≠ migration run.
V-C — Part 1 capture gate, forced (Path 1). On 3f32276f, simulate a 2nd T12 whose actuals cover previously-projected months, through the production seed path. Then SELECT * FROM deal_reconciliation_log WHERE deal_id='3f32276f' AND trigger_path='T12_REBUILD'. Confirm rows with non-null projected/actual/variance for the overlap months. Confirm periodic_seed still rebuilt (tri-tab reconciles). Paste both.
V-D — Part 3 forward re-derivation, forced on Highlands (the real proving run). Seed Highlands' proforma baseline (the "add the numbers" step), let its actuals reconcile. Paste:

the deal_reconciliation_log row count + sample rows (real per-field-month variance) — if Highlands is 2b, this should be many rows spanning the overlap, not one month;
Highlands periodic_seed zone distribution showing the boundary advanced past the actuals span, tri-tab reconciling;
a forward month's value before and after rebase — changed, not frozen. Frozen-forward = the applyRebase bug is still live.
If only the latest month reconciled and the other ~64 were ignored, or if applyRebase was looped per-month re-deriving forward each time — report it as FAILED, not done.

V-E — Notification store conflation. Confirm what Part 2 writes to deal_lifecycle_events. Paste one such row. Then: does inserting it fire trg_deal_lifecycle_transition? SELECT the trigger definition and confirm whether a reconciliation row (no real status change) trips it or is rejected/mishandled. If reconciliation events are polluting the lifecycle transition log, that's a finding — reconciliation needs its own surface, not the state-machine's log.
V-F — Materiality threshold. Find the >5% check (file:line). Is it a hardcoded literal or a named/configurable constant? Is it applied per-field or aggregate? State exactly what "material" means as built, so the threshold you were supposed to decide is at least visible and tunable.
Report: each = live SELECT or file:line. Close PROVEN / UNPROVEN / FAILED per item. Do not write "done" without a paste. STOP after reporting.

The decisive one is V-A + V-D on Highlands. If Highlands is multi-month-overlap (2b) and the build only did incremental (2a), then seeding its baseline either loses 64 months of calibration signal or loops the rebase wrong — and Highlands is the deal that's supposed to produce your first real deal_reconciliation_log batch and the Phase 1B substrate. Getting it wrong there isn't a generic bug; it's getting it wrong on the one deal that matters most for the calibration loop. Prove that one against real rows before this closes.