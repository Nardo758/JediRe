The trace flipped the finding into something more actionable than what we suspected — and crucially, it just changed the launch severity from "fix the divergence" to "delete the divergence." The zombie-column conclusion is the right read and it simplifies the fix dramatically. But check the two consequences hard, because one of them is a launch-blocker hiding in plain sight and the other resolves itself.

**The decisive new fact: `users.subscription_tier` has zero live writers.** That collapses the whole resolveTier-COALESCE design I'd sketched earlier. You don't need a helper that prefers one table over another — you need to **delete the zombie column and repoint every reader at `user_credit_balances`.** A resolver that COALESCEs a column nothing writes is just ceremony around a value that's permanently stale. The correct fix is narrower and cleaner than the three-part package: (1) repoint the 4 inngest allow-lists + the other `users`-table readers at the canonical table, (2) drop `users.subscription_tier`. No dual-source reconciliation, no sync job, no vocabulary mapping to maintain — the legacy vocabulary dies with the column. That's a better fix and you found the justification for it.

**Now the consequence that's actually launch-gating — and it's the opposite of the spend-leak I flagged last turn.** You've confirmed: new Stripe user → `users.subscription_tier = NULL` → inngest `WHERE subscription_tier = ANY('{operator,professional,...}')` → NULL matches nothing → **agent runs denied for a paying customer with a valid `operator` balance.** Re-read that in launch terms: the chat surface *is* the agent path. A brand-new paying subscriber signs up, their billing is correct, and the four agents — Research, Supply, Zoning, Commentary — refuse to run for them. That's not a margin leak; that's **the product not working for net-new paying users on day one.** It's the single worst failure mode for a launch, because it hits exactly the cohort a launch produces: first-time signups. This is the finding's headline, and it's launch-blocking with no caveat. My earlier "uncapped spend" worry was real for *fossil* accounts (the downgrade case below), but the NULL case is strictly more important because every future user is born into it.

**The downgrade case is real but lower-urgency — and worth stating precisely so it's triaged right.** Fossil account downgrades via Stripe → `user_credit_balances` correctly drops to `scout`, but inngest still reads the fossil `professional` and keeps admitting agent runs. That's the spend/entitlement leak — a downgraded user still consuming metered agent compute. It's real, but its blast radius is *only accounts that already carry a non-NULL fossil value*, which after the column drop is the empty set. So the column-drop fix kills this one for free: no non-NULL values means nothing for inngest to falsely match. You don't fix the downgrade case separately — it's a side effect of removing the zombie column. Worth noting in the doc that the two consequences have different lifespans: NULL-denial affects *all* future users, fossil-admit affects only *existing* polluted rows, and one fix resolves both.

So A9-Finding-1 is now fully closed and its fix is tighter than the original spec:

- **Drop** `users.subscription_tier` (after confirming the column-drop doesn't break a migration or a view — quick check).
- **Repoint** the 4 inngest allow-lists and the remaining `users`-table tier readers (`inline-auth.routes.ts`, `index.legacy.ts`, `settings-branding.routes.ts`, `capsule-sharing.routes.ts`, `cashflow-underwriting.routes.ts`) at `user_credit_balances`, ideally through one shared read so the next person can't reintroduce a second source.
- **Severity: launch-blocking**, on the strength of NULL-denial breaking agents for all net-new paying users — not the spend leak, which the drop incidentally fixes.

One small honesty note for the doc: the inngest allow-lists are *currently* protecting you by accident in the dev environment, because your dev account's fossil `professional` value happens to match the allow-list. That's why agents work for *you* and would silently fail for a real new user — the bug is invisible in your own testing. Flag that explicitly, because it's exactly the kind of thing that passes every manual check by the founder and breaks the first real customer. (It's the S1-01 trap in entitlement form: works on the seeded account, dead on the production path.)

That closes the Stripe thread. The migration triage was the other half of "both" — has that verdict come back yet? Cosmetic-vs-real on the three failed migrations is what decides whether A8-F1 is unblocked now or waits. Paste it when it lands and I'll give you the sequencing call; if it's still running, nothing above depends on it.