Your correction is right and it sharpens the finding — I had the failure mechanism wrong. It wasn't "reader chokes on professional"; it was simpler and the diagnosis you landed on is the correct one: user_credit_balances.subscription_tier = 'scout' → requireSurface('web') denies → empty pipeline. And the two-separate-PG-enum-types detail is materially worse than "two vocabularies in one column," because now the divergence is enforced at the database type level — the tables cannot hold each other's values. Good catch on the UPDATE; I prescribed a cast that the type system rejects. professional is correct for that column.
One thing to nail down before you call A9-finding-1 fully scoped, because it changes the severity tier. You wrote the failure mode of a users-table reader hitting TIER_CONFIG as "depends on how that undefined is handled (silent deny, crash, or fallback)." That parenthetical is the whole risk, and it shouldn't stay a parenthetical — it's the difference between a latent-but-harmless inconsistency and a live entitlements hole. Two of those three outcomes are security-relevant in opposite directions:

If TIER_CONFIG[undefined] → silent deny / crash, a legit paying user gets locked out. Annoying, fail-safe.
If any reader does TIER_CONFIG[tier] ?? SOME_DEFAULT and the default is permissive, a user with an unrecognized users tier gets granted access they shouldn't have. Fail-open. That's the launch-blocker shape.

The *.inngest.ts allow-lists are where I'd look hardest, because they're written as inclusion arrays (['operator','professional','enterprise',...]) rather than TIER_CONFIG lookups — they accept professional as valid-and-entitled. So an agent run gated by that array treats a professional user as authorized, while requireSurface (reading the other table) might say scout/denied. Same user, two subsystems, opposite entitlement answers. That's the concrete exploit-shaped version of the bug, and it's worth one trace to confirm whether those inngest allow-lists actually gate anything access-relevant or are just logging/labeling. If they gate compute or data access, A9-finding-1 is launch-gating. If they're cosmetic, it's fast-follow. Don't resolve it now — log it as the open sub-question on the finding so the fix pass knows to answer it.
Two more things for the written finding, then you're clear to push:
The resolveTier(userId) helper you specced is the right fix, and the spec should name the deny-by-default contract explicitly: unknown tier → lowest privilege (scout), never a permissive fallback, and the helper is the only sanctioned tier read — every inline query becomes a call to it. That converts "8 readers, 2 tables, 3 fallbacks" into one auditable function, which is exactly the shape A9 wants. Worth also deciding in the same fix whether users.subscription_tier gets dropped entirely (collapse to one source) or kept as a mirror the helper COALESCEs behind user_credit_balances — dropping is cleaner but you'd need to confirm nothing writes only to it.
And the write-path question stays open and gets its own line: what writes users.subscription_tier, and does it also write user_credit_balances? If registration/onboarding writes only the legacy column, every new prod user is born desynced — scout/free mismatch on day one. That's the thing that makes this recur in production rather than being a one-environment artifact, so it's not optional for the finding.
Net: dev unblock done, pipeline diagnosis correct, login chapter genuinely closeable. Push the 4-commit stack — it's been unpushed and at-risk on your local machine through this whole detour, and login now verifies (crash gone, logout dev-only-and-acceptable, pipeline populates). Then A9-finding-1 is written with two open sub-questions flagged (inngest allow-list enforcement; legacy write path), the 3 failed migrations are next in queue, and A8-F1 sits behind them.
Go ahead and push. After it lands, want me to draft A9 properly — with this tier-divergence finding as entry 1 and those two sub-questions as its trace targets — or run the migration triage first?