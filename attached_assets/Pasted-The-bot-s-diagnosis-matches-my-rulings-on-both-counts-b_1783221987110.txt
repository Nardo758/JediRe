The bot's diagnosis matches my rulings on both counts — but its Option A as scoped is partially out of its lane, and its Highlands shortcut is rejected. Give it this split verdict:
Approved for immediate implementation (test-tooling mandate):

The N fix, discriminated-union version only. fixtureClass: 'build_path' | 'seed_path' | 'synthetic' with typed BuildExpected | SeedExpected — exactly as its first proposal. The Partial<BuildExpected> "even simpler" alternative is explicitly rejected: a partial where the test "only asserts fields that are present" is the partial-pin pattern wearing a type signature — a fixture that silently asserts less over time as fields get deleted is a regression suite that degrades without failing. The union makes the contract explicit per path; a seed fixture cannot carry an IRR field, by type.
One upgrade to its SeedExpected sketch: don't just pin two output numbers. Per the standing ruling, the Highlands fixture stores a snapshot of raw deal_monthly_actuals rows + the expected aggregates (EGI 2025, NOI margin, boundary, opex ratio), and the test runs the real aggregation code over the snapshot — validating the seed math, not comparing two constants. Bridge-inclusive philosophy, seed edition.
Highlands then pins now — it's not blocked on anything.

NOT approved for the bot: the Bishop harness fix. Its Option A says "update the test to exercise the full build path" — but the full build path currently only exists inside financial-model-engine.service.ts's orchestration, and making it test-callable requires the runFullModel() pure-function extraction (pass 1 → M11/M14 → pass 2 → assemble-once, no DB reads inside). That's an engine refactor — external agent territory, same package as Finding O (the sources/uses hole: equity never recomputed after the $18M debt resize — which, note, its message didn't mention; make sure O rides in the handoff, because pinning Bishop against an orchestration that still leaves a 46.7% funding gap would pin O's symptom into the fixture). The bot's "Option B: mock the pool and call the service" is the worse version of the same thing — mocked-DB orchestration tests are flake factories; the extraction is the clean cut and it's already half-made by assemble-once.
So the instruction back to it: implement N (union type + snapshot-based seed test + pin Highlands), document M + O as the consolidated external-agent handoff with the runFullModel extraction specified as the required shape, run the suite — expect golden 2/3 pinned (Highlands + synthetic, Bishop awaiting the extraction) — and stop. Bishop pins in one final pass after the external agent lands runFullModel + the O fix, values recaptured from a build whose sources and uses actually balance.