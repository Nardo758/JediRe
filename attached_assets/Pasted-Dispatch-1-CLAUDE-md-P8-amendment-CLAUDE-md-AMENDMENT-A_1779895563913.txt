Dispatch 1 — CLAUDE.md P8 amendment
CLAUDE.md AMENDMENT — ADD STANDING PRINCIPLE P8

Add this standing principle to CLAUDE.md alongside existing P1-P7.

═══════════════════════════════════════════════════════════════════
P8 — VERIFY BEFORE QUEUEING
═══════════════════════════════════════════════════════════════════

When a dispatch produces a closing note that informs the scope of 
subsequent work, that closing note must be verified before 
downstream dispatches fire.

Verification steps:
1. Confirm the document exists at the expected path and is 
   complete per its dispatch spec
2. Spot-check 3-5 source citations against the actual codebase
3. Verify cross-fix integration sections are substantive (not 
   placeholder)
4. Classify open questions as BLOCKING / IMPORTANT / INFORMATIONAL
5. Identify gaps — what the investigation should have covered but 
   didn't

Output: append a verification section to the original 
investigation file. Verdict: APPROVED FOR DOWNSTREAM WORK / NEEDS 
AMENDMENT / NEEDS REWORK.

Rationale:
Investigations produce documents that subsequent dispatches treat 
as authoritative. If an investigation has incomplete citations, 
missing cross-fix integration, or unaddressed gaps, downstream 
dispatches inherit those problems. Verification catches issues 
before propagation.

A related failure mode: dispatches that were drafted but never 
fired can be confused with dispatches that landed. The verification 
step catches "the closing note doesn't exist" as a finding.

DO NOT
- Skip verification for "small" investigations — small ones 
  produce small documents but their findings propagate just as 
  much
- Verify your own work as the sole check — the bot verifying its 
  own output is a known confirmation-bias risk. Operator review 
  of the verification is required.
- Treat parallel firing as acceptable for dependency-linked work. 
  Sequential firing is the default for investigation → 
  implementation chains; parallel only when work is genuinely 
  independent.

═══════════════════════════════════════════════════════════════════
INSTRUCTIONS
═══════════════════════════════════════════════════════════════════

Add the P8 entry above to CLAUDE.md in the standing principles 
section, after P7. Preserve existing P1-P7 unchanged.

DO NOT
- Modify existing P1-P7
- Make other changes to CLAUDE.md beyond adding P8
- "While I'm here" cleanups

Dispatch 2 — Verification of STRATEGY_AND_BEFORE_AFTER_INVESTIGATION.md
VERIFICATION PASS — STRATEGY_AND_BEFORE_AFTER_INVESTIGATION.md

CONTEXT
Per CLAUDE.md P8 (verify before queueing), the closing note for 
the Strategy + Before/After investigation must be verified before 
downstream architectural work proceeds. This is read-only 
verification — no code changes, no new investigation. Confirm 
what's documented is accurate and identify any gaps.

═══════════════════════════════════════════════════════════════════
STEP 1 — DOCUMENT INTEGRITY
═══════════════════════════════════════════════════════════════════

A. Confirm docs/operations/STRATEGY_AND_BEFORE_AFTER_INVESTIGATION.md 
   exists and is readable
B. Confirm the document has all sections per the dispatch spec:
   1. Per-strategy field visibility current state (full 7×3 matrix)
   2. Before/after UI existing surfaces inventory
   3. Three architectural findings (investmentStrategy ↔ deal_type,
      regimeDataByField forbidden, missing UI for STR/Flip/Land Hold)
   4. Cross-fix integration with UNIT_MIX_OTHER_INCOME_INVESTIGATION.md
      and VALIDATION_GRID_AND_SALE_COMPS_INVESTIGATION.md
   5. Design recommendations
   6. Implementation phasing recommendation
   7. Open questions classified BLOCKING / IMPORTANT / INFORMATIONAL
C. Flag any section that's missing, truncated, or substantially 
   thinner than expected

═══════════════════════════════════════════════════════════════════
STEP 2 — SOURCE CITATION SPOT-CHECKS
═══════════════════════════════════════════════════════════════════

For 5 specific factual claims in the document, verify the cited 
source actually says what's claimed:

A. Pick a claim about the PATCH /assumptions/strategy endpoint 
   only updating investment_strategy_lv (not deal_type). Open the 
   actual route handler and verify.

B. Pick a claim about the Single-Value Mandate being repeated 9 
   times in line-item-matrix.ts. Open the file and count.

C. Pick a claim about Pattern B routing reading deal_type. Open 
   m09_line_item_patterns.ts and verify.

D. Pick a claim about regimeDataByField never being populated by 
   any code path. Grep the backend for any writer of this field 
   and confirm zero matches.

E. Pick a claim about the 7×6 matrix coverage. Verify a randomly 
   selected cell against the actual codebase behavior.

For each verified claim, document:
- The claim as stated in the closing note
- The actual code/file that confirms or contradicts it
- Verdict: CONFIRMED / PARTIAL / CONTRADICTED / NOT FOUND

═══════════════════════════════════════════════════════════════════
STEP 3 — CROSS-FIX INTEGRATION COVERAGE
═══════════════════════════════════════════════════════════════════

Verify the cross-fix integration section is substantive:

A. Does it surface specific integration points with the Unit Mix 
   investigation (pre/post unit mix split, adoption timeline 
   anchor for value-add)?
B. Does it surface specific integration points with the 
   Validation Grid investigation (current state vs target state 
   column treatment)?
C. For each integration point, is there a recommendation or open 
   question, not just identification?

═══════════════════════════════════════════════════════════════════
STEP 4 — OPEN QUESTIONS ASSESSMENT
═══════════════════════════════════════════════════════════════════

A. List the open questions the document surfaces
B. Classify each:
   - BLOCKING — implementation cannot proceed without this answered
   - IMPORTANT — affects scope or design but doesn't block start
   - INFORMATIONAL — nice to know, can be handled inline
C. Flag any blocking question that depends on data or sources 
   that don't currently exist in the platform

═══════════════════════════════════════════════════════════════════
STEP 5 — POTENTIAL GAPS
═══════════════════════════════════════════════════════════════════

Think about what's MISSING, not what's present:

A. Are there strategy templates the investigation should have 
   covered but didn't?
B. Are there before/after surfaces in the codebase that the 
   investigation didn't reference?
C. Is there a strategy × deal_type matrix combination that was 
   skipped?
D. Are there data dependencies for the architectural fixes that 
   weren't surfaced?

═══════════════════════════════════════════════════════════════════
DELIVERABLE
═══════════════════════════════════════════════════════════════════

Append a verification section to the existing investigation file:

docs/operations/STRATEGY_AND_BEFORE_AFTER_INVESTIGATION.md
Append at the end:

═══════════════════════════════════════════════════════════════════
VERIFICATION PASS — 2026-05-27
═══════════════════════════════════════════════════════════════════

(a) Document integrity check: <result>
(b) Source citations verified (5 spot checks):
    - Claim 1: <verdict + evidence>
    - Claim 2: <verdict + evidence>
    - ...
(c) Cross-fix integration coverage: <assessment>
(d) Open questions classification:
    BLOCKING: <list>
    IMPORTANT: <list>
    INFORMATIONAL: <list>
(e) Identified gaps in the investigation: <list>
(f) Overall verdict: APPROVED FOR DOWNSTREAM WORK / NEEDS 
    AMENDMENT / NEEDS REWORK

═══════════════════════════════════════════════════════════════════
DO NOT
═══════════════════════════════════════════════════════════════════

- Modify the existing investigation content (only append)
- Run new investigation work outside the verification scope
- Implement any code changes
- Skip the source citation spot-checks
- Skip Step 5 (gaps) — the discipline is to think about what's 
  missing
- "While I'm here" cleanups

Dispatch 3 — STR/Flip/Land Hold "not yet supported" notice
STR / FLIP / LAND HOLD — "NOT YET SUPPORTED" NOTICE IN SELECTOR

CONTEXT
Per STRATEGY_AND_BEFORE_AFTER_INVESTIGATION.md, these three 
strategies have zero dedicated F9 UI today:
- str_shortterm (ADR, RevPAR, seasonal occupancy not rendered)
- flip (short-hold exit timing not rendered)
- land_hold (income rows shown for a no-income strategy)

Operators who select these strategies get the multifamily-existing 
layout, which is structurally wrong for their use case. Full UI 
support is deferred. This dispatch adds an explicit "not yet 
supported" treatment so operators get an honest signal rather 
than a broken display.

═══════════════════════════════════════════════════════════════════
SCOPE
═══════════════════════════════════════════════════════════════════

A. Locate the strategy selector(s) in:
   - DealTermsTab.tsx (investmentStrategy dropdown)
   - Deal creation flow (deal_type selector if separate)
   - Any other surface that lets operators pick STR/Flip/Land Hold

B. For each surface, add visual treatment:
   - STR / Flip / Land Hold options shown with badge:
     "Not yet supported — full UI coming in future release"
   - On selection, surface a confirmation modal: "You've selected 
     [strategy]. Full UI support is not yet built — the platform 
     will render a multifamily-existing layout. Continue?"
   - Confirmation persists; modal doesn't re-appear on subsequent 
     edits within the same deal

C. Persist the selection (don't disable it entirely — operators 
   may want to mark a deal as STR even without full UI, for 
   tagging/categorization purposes)

═══════════════════════════════════════════════════════════════════
DELIVERABLE
═══════════════════════════════════════════════════════════════════

Implementation in:
- DealTermsTab.tsx (strategy selector + badge + modal)
- Deal creation flow if applicable
- Documentation note in docs/operations/STRATEGY_SCOPE_NOTICE.md 
  explaining what's deferred and the roadmap

═══════════════════════════════════════════════════════════════════
DO NOT
═══════════════════════════════════════════════════════════════════

- Build any STR/Flip/Land Hold actual UI (deferred work)
- Remove the strategy options from the selector entirely
- Modify the underlying calculation templates
- "While I'm here" cleanups