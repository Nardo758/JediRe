# JEDI RE - Real Estate Intelligence Platform

## Overview

JEDI RE is an AI-powered B2B real estate intelligence platform designed for investors and developers. It provides synthesized, actionable intelligence for deal sourcing, zoning analysis, and market opportunity scoring. The platform's core innovation is a "map-agnostic" architecture that integrates user-provided maps to reduce GIS costs, aiming to combat data overload in real estate investment.

## User Preferences

Preferred communication style: Simple, everyday language.

## System Architecture

### Recent Changes
- **F9 Pro Forma Tier 3 — Refinement (2026-04-29, Task #450):** Implements the spec §10/§11/§14 refinement layer in pure TypeScript modules. **NO database changes** — all calibration constants live in TS. **New backend modules**: `backend/src/services/proforma/layered-growth/position-adjustment.ts` — `PositionMode = 'mean_reverting' | 'constant_gap' | 'widening'`; `evaluatePositionAt(spec, year)` (mean-reverting uses `tau = halfLife / ln(2)`, `position_t = position_0 × exp(−t/tau)`); `derivePositionSeries(spec, horizon)` returns `ProvenancedValue<number>[]` of per-year DELTAs (additive contribution to subject growth per spec §10 formula); `positionContributionForYear(spec, year)` single-year shortcut that drops directly into `RentGrowthInputs.position`; `POSITION_HALF_LIFE_DEFAULTS` per asset class (multifamily 4.5yr premium / 6yr discount per spec §10; office/retail/industrial/STR/flip/land seeded). `backend/src/services/proforma/revenue/revenue-formulas.ts` — `computeSimpleRevenue` (`units × rent_0 × (1+g)^t`), `computeMarkToMarketRevenue` (in-place + turnover decomposition with renewal_rate + escalator), `computeRenewalAwareRevenue` (`(renewing × in_place × (1+g_renewal)) + (new_leases × market_rent_t)` — `marketRent` is documented as year-t / already-trended; new-lease leg does NOT re-multiply by `(1+g_market)` to avoid double-count). `RENEWAL_RATE_BASELINES` per `assetClass × marketType` (multifamily urban 0.50 → tertiary 0.65; retail/office/industrial higher; `lookupRenewalRateBaseline()` falls back to 0.55). **Blueprint extensions** (`backend/src/services/proforma/blueprint/proforma-blueprint.ts`): `STRATEGY_TEMPLATE_TUNING` exhaustive over all 7 templates — BTS (`development_ground_up`) gets `growthTruncationYear: 3` (rent + OPEX growth → 0 past Y3 to prevent stacking on the BTS exit); flip gets `growthTruncationYear: 1`; STR gets a 12-month `seasonalOccupancyFactors` array (sun-belt default, averages ≈ 1.0); helpers `applyTemplateGrowthTuning(templateId, year, growth)` and `resolveSeasonalOccupancyFactor(templateId, month)`. `OPEX_LINE_ITEMS` extended with structured `cycleDriver: { code, label, source, seriesId? }` per line — propertyTax → M26 county millage (`platform_intra`), insurance → reinsurance hardening (`gc_reinsurance_index`, calibration TBD), utilities → BLS CPI Energy Services `CUUR0000SEHF`, repairsMaintenance → BLS PPI `PCU8113`, payroll → BLS ECI Services `CIU2010000000000A`, marketingAdmin/replacementReserves/other → BLS CPI All-Items `CUSR0000SA0`, managementFee → couples to revenue (no external driver). Legacy `growthDriver` string field preserved for back-compat. Composite `PROFORMA_BLUEPRINT` bumped to `2.1.0` with `templateTuning` + `opexCycleDriverCalibration` exports; barrel `layered-growth/index.ts` re-exports `position-adjustment`. **Calibration metadata wrappers** (additive — original constants unchanged): `ASSET_CLASS_SPREAD_CALIBRATION` in `rent-growth.ts`, `REFUSAL_THRESHOLDS_CALIBRATION` in `validators/confidence-bands.ts`, `GORDON_THRESHOLDS_CALIBRATION` in `validators/gordon-growth.ts`, `POSITION_CALIBRATION` + `RENEWAL_BASELINE_CALIBRATION` + `OPEX_CYCLE_DRIVER_CALIBRATION` from the new modules — each with `{asOf, source, calibrationStatus: 'tbd'|'partial'|'calibrated', notes}` shape so dashboards can flag stale calibrations and the §14 backtests can graduate them to `calibrated` later. **Tests**: `backend/src/services/proforma/__tests__/tier3-refinement.test.ts` — 44 vitest tests covering all 3 position modes + half-life resolution + per-year delta semantics + multifamily 4.5yr/6yr defaults; SIMPLE / MARK_TO_MARKET / RENEWAL_AWARE algebra (incl. an explicit Y3 regression test that proves the (1+g_market) double-count bug is gone — varying `marketGrowth` while holding `marketRent` fixed yields identical result); template-tuning registry exhaustiveness over all 7 IDs + BTS Y3 truncation + STR seasonal-multiplier mean ≈ 1.0; calibration-metadata shape on all 5 wrappers; OPEX line items each carry structured `cycleDriver` with expected source codes; composite version bump assertion. Full suite: 138/138 (tier1 44 + tier2 24 + tier3 53 + blueprint-drift 17). Blueprint JSON snapshot regenerated to v2.1.0 (26.4KB). Deal Capsule audit re-ran clean: 33/33 PASS, 0 fail. **Runtime orchestrator** (`backend/src/services/proforma/proforma-projection.service.ts`): pure `projectProforma(inputs)` function that composes all Tier 1+2+3 helpers into a year-by-year projection — calls `computeLayeredRentGrowth`, injects `positionContributionForYear` into the `position` slot, applies `applyTemplateGrowthTuning` to both rent + per-line OPEX growth (so BTS truncates Y4+, flip truncates Y2+), dispatches between `computeSimpleRevenue` / `computeMarkToMarketRevenue` / `computeRenewalAwareRevenue` based on the `revenueFormula` field, surfaces each line's `cycleDriver` verbatim in the result, trends `marketRent_t` year-over-year using the tuned growth, and emits `noiGrowthIdentity` per year as a cross-check. T007 integration tests (9 cases) cover BTS truncation flow-through, flip truncation, formula dispatch, position injection, exhaustive OPEX coverage, mgmt-fee auto-couple, and marketRent_t trending — all asserting actual output values change when Tier 3 inputs are toggled. **How to extend Tier 3**: drop new revenue formulas into `revenue/`; new template tuning knobs go on `StrategyTemplateTuning` interface and the `STRATEGY_TEMPLATE_TUNING` map (registry must stay exhaustive — drift test catches missing keys); new external indicators for OPEX go on `cycleDriver.source` enum + add a calibration entry; new calibration-metadata wrappers follow the `*_CALIBRATION` companion-export pattern (keep original constant unchanged for back-compat). UI surfacing of these new fields (formula picker, position-mode dropdown, seasonal-occupancy editor) is explicitly deferred — `AssumptionsTab.tsx` (~2,219 LOC) untouched.
- **F9 Pro Forma Tier 2 — Data Quality + Audit Substrate (2026-04-29, Task #449):** Implements the persistence/audit substrate from `docs/architecture/f9-proforma-spec.md` §12 + §13. **Migrations** (raw SQL, project convention for the deals domain): `backend/src/database/migrations/20260429_deal_versions.sql` creates `deal_versions(id uuid pk, deal_id uuid → deals.id, version_number int, created_at, created_by uuid, layered_state_snapshot jsonb, model_versions jsonb, override_divergences jsonb, save_trigger text check ('user_save'|'chat_command'|'auto_prompt'), note text, unique(deal_id, version_number))` with indexes on `(deal_id)` and `(deal_id, version_number desc)`. `20260429_correlation_adjustments.sql` adds `correlation_adjustments jsonb default '[]'::jsonb` to `deals` (idempotent `ADD COLUMN IF NOT EXISTS`). Both verified via `information_schema` before apply; UUID FK matches existing `deals.id`. **Backend services**: `backend/src/services/proforma/model-versions.ts` (`MODEL_VERSIONS` registry — rent_growth/opex_growth/gordon_validator/confidence_bands/agent_fill_in/correlation_engine/proforma_generator — plus `stampModelVersion(pv, key)` helper), `backend/src/services/proforma/deal-versions.service.ts` (`saveVersion` with `FOR UPDATE` row lock + monotonic per-deal `version_number`, `listVersions`, `getVersion`, `computeOverrideDivergences` walks both flat & layered ProvenancedValue snapshots and emits `{field, user_value, platform_value, divergence_pct, justification_note}` where user override differs from preceding platform layer), `backend/src/services/proforma/agent-fill-in.ts` (`agentFillIn({deal, template, dataLibrary})` walks `template.sections[].fields[]`, fills missing assumptions via injected resolver with `dataQuality='INFERRED'`, `confidence=0.5`, `fillMethod=<lookup-key>`, stamps `MODEL_VERSIONS.agent_fill_in`; falls back to DEFAULT on miss), `backend/src/services/correlation-adjustments.service.ts` (`persistAdjustments`, `appendAdjustments`, `readAdjustments` — JSONB array of `{cor_id, target_field, delta_pct, signal, confidence, computed_at, model_version}`). **ProvenancedValue extension**: added `dataQuality?: 'ACTUAL'|'INFERRED'|'ESTIMATED'|'DEFAULT'` and `fillMethod?: string` (orthogonal to existing green/yellow/red `qualityFlag`); `deriveDataQuality(origin, source)` maps per spec §12 (t12_extracted/rent_roll/owner_provided→ACTUAL; comp_set/market_agent/tax_intel→INFERRED; opus_inferred/derived→ESTIMATED; platform_default/placeholder→DEFAULT). Existing callers unaffected — fields are optional. **REST endpoints** mounted at `/api/v1/financial-model` (singular, matches the existing frontend path): `POST /build`, `GET /:dealId/latest`, `GET /:dealId/versions`, `POST /:dealId/versions` (body `{snapshot, trigger, note}`), `GET /:dealId/versions/:versionNumber`. **Frontend mirror**: `LayeredValue` (`frontend/src/lib/dealContext.types.ts`) gains optional `dataQuality`/`fillMethod`/`modelVersion` + `deriveLayeredDataQuality` helper; `frontend/src/components/deal/DataQualityBadge.tsx` renders compact pill (ACTUAL→hidden, INFERRED→"library", ESTIMATED→"est", DEFAULT→"default") with `fillMethod` tooltip. **FinancialEnginePage** (`frontend/src/pages/development/FinancialEnginePage.tsx`): `useEffect` GETs `/api/v1/financial-model/:dealId/versions` and maps `DealVersionRow[]` → existing `ModelVersion[]` shape so the picker UI is unchanged; `handleSaveVersion` POSTs `{snapshot:{assumptions,results}, trigger:'user_save', note}` with optimistic local insert; new `lastSavedAt` state renders a "saved HH:MM" indicator next to the Save button (full ISO timestamp in tooltip). **Tests**: `backend/src/services/proforma/__tests__/tier2-data-quality.test.ts` — 17/17 vitest pass covering `deriveDataQuality` mapping (all 4 quality bands + back-compat), `MODEL_VERSIONS`/`stampModelVersion` (idempotent stamping, registry coverage), `agentFillIn` (4 cases — present skip, library hit→INFERRED, library miss→DEFAULT, confidence inflation hook), `computeOverrideDivergences` (flat + layered snapshot shapes, no-divergence case, multi-field). Full backend suite: 195 passed / 20 skipped (no regressions vs Tier-1 baseline of 175). Frontend `tsc` clean (only pre-existing mapbox TS2688 baseline). **How to extend Tier 2**: register a new model in `MODEL_VERSIONS` and stamp at the point of value emission; new save triggers go in the `save_trigger` CHECK list (migration ALTER); to surface a new audit field on the version row, extend `DealVersionRow` + `saveVersion` insert + the snapshot mapper in `FinancialEnginePage.tsx`. The version diff viewer, three full upload-tier UI flows (rent-roll-only / T12-only / OM-only), and M22 attribution feedback consumer are explicit follow-ups.

- **F9 Pro Forma Tier 1 — Protectors (2026-04-29, Task #448):** Implements the Tier 1 mathematical protectors from `docs/architecture/f9-proforma-spec.md` §6-§9. **Backend pure modules** (no external dependencies, fully unit-tested): `backend/src/services/proforma/layered-growth/rent-growth.ts` (5-component layered rent growth: momentum/cycle/anchor/event_deltas/position with mean-reverting weight schedule that hands off from momentum→cycle→anchor between Y1 and Y5+), `backend/src/services/proforma/layered-growth/opex-growth.ts` (per-line OPEX growth with Florida structural overrides — 10% non-homestead cap on property tax, reassess-on-sale Y1 step, hurricane premium on insurance, gated `state==='FL'`; mgmt fees auto-couple to revenue; total OPEX growth = dollar-weighted average + `noiGrowthIdentity(rentG, opexG, noiMargin)` helper), `backend/src/services/proforma/validators/gordon-growth.ts` (`validateGordonGrowth({exitCap, terminalGrowth, requiredReturn})` returns `{valid, flag?, severity?, message?, impliedCap, divergenceBps}` — flags `GORDON_OVER_PROMISE` (high) when divergence < -25bps and `GORDON_CONSERVATIVE` (info) when > +100bps), `backend/src/services/proforma/validators/confidence-bands.ts` (`computeConfidenceBands({forecast, sigmaModel, sigmaSparsity})` returns `{sigmaTotal, p10, p25, p75, p90}` via Z_P25=0.674 / Z_P10=1.282; `classifyOverride(override, bands)` returns `'within' | 'soft_warning' | 'hard_warning'`; `evaluateRefusal({stabilizedComps, historyYears, hasAssetClassRep})` enforces minimum data thresholds with σ_sparsity inflation hook). **Tests**: 41 vitest tests in `backend/src/services/proforma/__tests__/tier1-protectors.test.ts` (all green) + prior 17 blueprint-drift tests still passing (175 total). **Frontend mirror** (`frontend/src/services/proforma/{types.ts, validators.ts, index.ts}`): identical math runs client-side so override warnings render without server roundtrip. **dealStore slices** (`frontend/src/stores/dealStore.ts`): `confidenceBands: Record<string, ConfidenceBands>`, `validationFlags: ValidationFlag[]`, plus `setConfidenceBands`, `setValidationFlags`, `upsertValidationFlag`, `removeValidationFlag`, `dismissValidationFlag(id, justification)`, `runGordonValidation()` (auto-emits/clears `gordon:exit_cap` flag), `classifyFieldOverride(field, value)`, `computeNoiGrowthIdentity()`. **F9 collision view** (`frontend/src/pages/development/financial-engine/F9ProtectorsPanel.tsx`, wired in `AssumptionsTab.tsx` between KeystonePanel and grid): Gordon coupling banner with implied cap, divergence bps, color-coded severity (red over-promise / blue conservative / emerald balanced), expandable (g, cap) mini-chart on the Gordon line via `recharts ComposedChart` showing the user's point against the valid range; live NOI growth identity row showing the algebraic decomposition `(rentG − opexG×opexShare)/noiMargin` with red highlight when negative; active protector flag rail with severity-coded left borders, dismiss + justification note input (stored on the flag). `OverrideBadge` component exported for per-cell soft/hard warning dots. **How to extend Tier 1**: drop new validators into `backend/src/services/proforma/validators/`, add a vitest file under `__tests__/`, mirror the pure function into `frontend/src/services/proforma/validators.ts`, and wire a new `ValidationFlag` source into `dealStore.upsertValidationFlag()`. New protector UIs go into `F9ProtectorsPanel` (panel sits below `KeystonePanel`). For new layered growth lines, extend the line tables in `opex-growth.ts` and update the Florida override block when state-specific economics apply.

- **PropertyCardPage — Replaced Hardcoded Mock with Real Data (2026-04-25):** Fixed bug where every property URL showed "Westshore Commons / 4201 W Boy Scout Blvd · Tampa, FL 33607 · Hillsborough County · Westshore submarket" regardless of the `:id` URL param. Root cause: `frontend/src/pages/PropertyCardPage.tsx` had `const p = PROPERTY` (hardcoded mock) at line 803, completely ignoring `useParams<{id}>()`. Fix: (1) added `useEffect` that detects UUID-format `id` and fetches `GET /api/v1/markets/properties/:id` via `apiClient`, (2) added `mapRealProperty()` that translates `property_records` columns (parcel_id, address, city, state, county, units, year_built, building_sqft, etc.) into the PROPERTY shape, (3) merge logic blanks all identity/location fields (id, name, address, city, state, zip, county, submarket, market, lat, lng, owner) before overlaying real values — empty real fields stay empty so a Georgia parcel never claims a Tampa address, (4) merge applies on **any UUID route** so a 404 also blanks identity (no silent Westshore fallback), (5) PROPERTY mock continues to provide photos/unitMix/walkScore/perf metrics for the rich UI, (6) added LIVE / DEMO DATA / NOT FOUND badges to header, (7) navigation state from F4 (`propertyName`, `submarketName`, `msaName`) is consumed only when DB-derived values are empty, (8) footer "Fulton County, FL · Folio: 123456-7890" replaced with dynamic `{p.county} County, {p.state} · Parcel: {p.parcelId}`. File: `frontend/src/pages/PropertyCardPage.tsx`.

- **Neural Network Gaps 2-6 Verified + Smoke Tested (2026-04-25):** All four session plan tasks confirmed end-to-end live:
  - **Gap 2 — Georgia Sales → Sale Comp Pool**: `POST /georgia/comps/promote` ETL joins `georgia_property_sales + property_info_cache → market_sale_comps` (30 comps promoted from 1 county). `CompSetService.generateCompSet()` queries `market_sale_comps` with full spatial `<@>` radius query. `GET /georgia/analytics/price-trends` returns 18 county×year trend data points with YoY % via `getPriceTrends()`.
  - **Gap 3 — Apt Locator → Competitive Set**: `discoverFromAptLocator()` (comp-set-discovery.service.ts) fetches `apartment_locator_properties` within radius, scores, inserts into both `competitive_sets` (M27 agent) and `deal_comp_sets` (UI). Conflict index on `(deal_id, source, source_id)`. Route: `POST /deals/:dealId/comp-set/discover-rental`. Smoke test: 10 comps inserted for Inman Park deal.
  - **Gap 4 — Market Rent → Projections**: Median asking rent computed from discovered apt_locator comps → upserted into `deal_assumptions.avg_rent_per_unit` via `ON CONFLICT (deal_id)` CASE guard (never overwrites user-entered values). Source tagged `source_type='apt_locator'`, `source_ref='apt_locator:N comps'`.
  - **Gap 5 — Permits/Supply Pipeline**: `apartment-locator-sync.service.ts` syncs from ApartmentLocatorAI API → `apartment_supply_pipeline` (89 projects, 2,576 total units). `GET /georgia/supply/pipeline` returns bySubmarket summary + project list. Full pipeline: `POST /georgia/run-pipeline` sequences county ingest → comps promote → apt locator sync → news.

- **Atlanta MSA Frontend Wiring (2026-04-23, Session 3):** Fixed two data gaps that were preventing ingested data from reaching the UI. (1) **Properties endpoint expanded**: `/api/v1/markets/properties` previously hard-coded `WHERE county = 'Fulton'`; now queries all 4 Atlanta MSA counties (`Fulton, DeKalb, Cobb, Gwinnett`) using `ANY($1::text[])`, with optional `?county=` filter. Filter changed from `units > 0` to `units > 0 OR units IS NULL` to include Cobb/DeKalb records where units column is NULL. Total exposed: **249,417** records (was 4,533). (2) **PropertyDataTab UI**: Added County filter dropdown (Fulton/DeKalb/Cobb/Gwinnett), County column in the property table, and updated the status badge to show all county names / live count. Removed hardcoded `'27% LIVE DATA'` and `'1,028'` fallback text. (3) **New `/api/v1/markets/sale-comps` endpoint**: Serves `market_sale_comps` table with full filtering (marketId, county, minUnits, maxUnits, search) and sort. Returns **216 Atlanta + 9 Charlotte** recorded transactions seeded in Session 2. MSA filter maps `atlanta → 'Atlanta, GA'`, `charlotte → 'Charlotte, NC'`. (4) **DealsTab wired to live comps**: Replaced hardcoded mock "Market Deal Activity" table with live fetch from the new endpoint. Shows real sale data: property name, city/county, sale date, units, asset class, price, $/unit, cap rate, buyer, seller. Falls back gracefully if no data. Files changed: `backend/src/api/rest/market-intelligence.routes.ts`, `frontend/src/pages/MarketIntelligence/tabs/PropertyDataTab.tsx`, `frontend/src/pages/MarketIntelligence/tabs/DealsTab.tsx`.

- **Atlanta MSA Property Ingestion Complete (2026-04-23, Session 2):** Completed full Atlanta MSA property import with batch upsert optimization and comps seeding. (1) **Cobb County** — 1,454 multifamily parcels (HAS_MULTIUNIT='Y' filter) ingested from Cobb ArcGIS FeatureServer in <60s. (2) **DeKalb County** — 243,430 parcels fully ingested in 3 resumable chunks using new `--offset` / `--maxPages` CLI flags; batch `unnest()` upserts replaced per-row loops achieving ~10-50x speed improvement; within-page deduplication added for ArcGIS duplicate-record edge case. (3) **Total property_records**: 249,417 across Fulton (4,532), DeKalb (243,430), Cobb (1,454), Gwinnett (1, server blocked). (4) **Comps seeding** — new `ingest-recorder-transactions.ts` seeds `market_sale_comps` (225 records: 216 Atlanta / 9 Charlotte) and `recorded_transactions` (12 news-sourced deals) from Tavily web search of public deal announcements + Fulton County assessor appraised values as proxy pricing. Key script improvements: `import-county-properties.ts` now supports `[CountyName...] [--offset=N] [--maxPages=N]` CLI arguments for resumable partial ingestion.

- **Atlanta MSA Data Build-Out (2026-04-23):** Completed multi-source data pipeline for Atlanta + Charlotte MSAs. (1) **RSS feed fix**: Removed CNBC (empty body) and MarketWatch (404) from news.service.ts and news.routes.ts event feed; replaced with HousingWire provider (`backend/src/services/news/providers/housingwire.provider.ts`) — registered in both the hourly poller and the `/api/v1/news/events` endpoint. (2) **Census BPS permit ingest**: `ingest-leading-indicators.ts` — replaced hardcoded mock with real FRED calls to `ATLA013BPPRIV` (Atlanta total permits), `ATLA013BP1FHSA` (Atlanta single-family), `HOUST5F` (national 5+ unit starts), and `HOUST` (national total starts) with YoY calculations; national `PERMIT` series as fallback. (3) **Fulton County property import**: `import-county-properties.ts` fetched 2,266 parcels from Fulton ArcGIS FeatureServer (1,238 new + 1,028 updated); DeKalb/Cobb/Gwinnett remain blocked pending `REGRID_API_KEY`. (4) **Census ACS demographics**: New `ingest-acs-demographics.ts` fetches ACS 5-year estimates (B01003, B19013, B25064) for all MSAs in one Census API call; updated `msas.population` and `msas.median_household_income` for both Atlanta (pop 6.09M, HHI $82.6K) and Charlotte (pop 2.67M, HHI $76.2K). (5) **Charlotte as second MSA**: Seeded CBSA 16740 into `msas` table with approximate centroid geometry; BLS CES employment data loaded for 9 supersectors via `ingest-msa-economic-data.ts`. (6) **BLS QCEW flat-file ingest**: New `ingest-bls-qcew.ts` downloads QCEW annual CSVs from `https://data.bls.gov/cew/data/api/{year}/a/area/C{CBSA-4}.csv`, maps to CES supersectors, and back-fills `avg_weekly_wage` and `establishment_count` in `msa_economic_snapshot` — Atlanta avg wage $1,503/wk, 223K estabs; Charlotte $1,465/wk, 98K estabs.

- **Auto-extract on upload (2026-04-22, Task #320):** Documents uploaded via `POST /api/v1/deals/:dealId/files` now trigger background extraction automatically. Migration `20260422_deal_files_extraction_status.sql` adds `extraction_status` (queued/running/done/failed/skipped), `extraction_skill`, `extraction_result`, `extraction_error`, `extraction_started_at`, `extraction_completed_at` columns to `deal_files`. New service `backend/src/services/document-extraction/auto-extract-on-upload.ts` maps file category → skill (`extract_document` for financial/inspections/etc., `review_contract` for legal, `analyze_appraisal` for appraisals, `parse_environmental_report` for environmental), invokes `skillRegistry.execute`, and persists status/result. Image and zip uploads are auto-skipped. New polling endpoint `GET /api/v1/deals/:dealId/files/:fileId/extraction`. Frontend `DocumentsFilesSection` polls in-flight files every 3s; ListView gains an "Extraction" column and GridView a status badge (queued/extracting/extracted/failed/skipped) with tooltips for skill or error.

### Core Platform

JEDI RE employs a lightweight, map-agnostic architecture using Node.js/TypeScript with Express, GraphQL, and WebSockets for the backend, and React with Mapbox for the frontend. PostgreSQL is used for data storage, leveraging both raw SQL and Drizzle ORM. Python is integrated via a TypeScript-to-Python bridge for geospatial processing (GeoPandas, PostGIS) and zoning code processing.

Key features include:
-   **Legacy Agent Workforce Removed (2026-04-22, Task #321):** Deleted the dead frontend "18 agents" abstraction now that the AI Skills system is the sole user-facing entry point. Removed: `frontend/src/config/agentWorkforce.ts`, `frontend/src/components/layout/AgentBar.tsx` (was a re-export shim), `frontend/src/pages/settings/AgentSettingsPage.tsx`, `frontend/src/components/settings/AgentWorkforceSettings.tsx`, `frontend/src/services/agentBus.ts`, `frontend/src/services/agentRegistry.ts`, `frontend/src/services/orchestrator.ts`, `frontend/src/hooks/useAgentBus.ts`, `frontend/src/stores/agentStore.ts`. The `AgentChatDrawer` and `AgentsTabContent` blocks inside `BottomPanel.tsx` were removed (no tab in `BOTTOM_TABS` referenced them — pure dead code). `websocket.service.ts` no longer imports `useAgentStore` and dropped the four `agent:*` socket handlers. `MainLayout.tsx` no longer imports the old frontend `orchestrator` service. The `/settings/agents` route was already a redirect to `/terminal/settings?tab=skills`. **Skills entry points remain unchanged**: `SkillsBar` (in `MainLayout`), `SkillsChatSection` (with @mention routing from #319), and `SkillsSettingsPage`. **Not touched** (and must not be confused with the legacy code): the new backend `backend/src/services/agents/` system (orchestrator, personas, event-dispatcher, scheduled-jobs) merged in alongside the discovery engine — that powers `MorningBriefWidget` and the `/agents` REST routes. Skill execution still flows: frontend → `POST /skills/chat` → backend `skill-chat.service.ts` → `skillRegistry` → each skill's `execute()`.
-   **AI Skills System — Advisor Personas (2026-04-22, Task #318):** Extended the 18-skill capability system (`backend/src/services/skills/skill-registry.ts`) with 16 advisor personas exposed as `consult_<role>` skills. **Backend** (`backend/src/services/skills/skills/personas.ts`): `ADVISOR_PERSONAS` array of 16 entries (CFO, Accountant, Marketing Expert, Developer, Legal Advisor, Lender, Acquisitions, Asset Manager, Property Manager, Leasing Director, Facilities Manager, Investment Analyst, ESG/Sustainability, Compliance Officer, Tax Strategist, Researcher) — each with role-specific system prompt. `runPersonaConsultation()` runs a nested Claude (`claude-sonnet-4-20250514`) tool-use loop with the persona's system prompt and a restricted, read-only subset of capability skills (`READ_ONLY_SKILLS` — query/search/extract/analyze, no mutations). Returns `{persona, personaCode, opinion, toolCallsUsed}` with `displayType: 'markdown'`. New `'advisor'` category added to `SkillDefinition['category']` union. `registerAdvisorPersonas()` invoked from `registerAllSkills()` — total registry now 34 skills (18 capability + 16 advisor). **Frontend** (`frontend/src/components/layout/SkillsBar.tsx`, `frontend/src/pages/settings/SkillsSettingsPage.tsx`): `ADVISORS` constant with 16 entries, advisor color `#FF6FB5`, advisors rendered in the "MORE" panel of SkillsBar and in their own "🧠 Advisor Personas" section in the settings page with explanatory copy. Header now reads "{N} skills (18 capabilities + 16 advisors)". The orchestrator AI calls `consult_<role>` like any other tool; the persona itself can call read-only data skills to ground its opinion.

-   **Agent Platform Phase 4 — Wire Remaining Agents + Coordinator (2026-04-19, Task #226):** Completed full wiring of Zoning, Supply, CashFlow, and Commentary agents into AgentRuntime. **13 new tool files** created: Zoning (`fetch_zoning_code`, `fetch_municode`, `compute_envelope`, `write_zoning_analysis`), Supply (`fetch_permits`, `fetch_costar_pipeline`, `fetch_submarket_deliveries`, `write_supply_analysis`), CashFlow (`fetch_t12`, `fetch_rent_roll`, `fetch_assumptions`, `compute_proforma`, `write_projection`), Commentary (pure generation — no tools). **4 config+seed files** added: `zoning.config.ts`, `supply.config.ts`, `cashflow.config.ts`, `commentary.config.ts` each with an `AgentRuntime` singleton, `OutputSchema`, and idempotent `seed*Prompt()` function. **4 agents refactored** to AgentRuntime adapters: Zoning/Supply/CashFlow delegate all execution to their runtimes; Commentary preserves rich signal/strategy/peer orchestration but routes AI narrative through `commentaryRuntime.run()`. **Coordinator layer** (`backend/src/coordinator/`): `dispatch.ts` — `INTENT_DISPATCH` table mapping 5 Layer 1 agents (research/zoning/supply/cashflow/commentary) + 6 Layer 2 fragment specialists (DEMAND/COMPS/RISK/DEBT/NEWS/STRATEGY) each with `agentId`, `label`, `tier`, `toolCount`, `defaultPersonaId`; `context-fragments.ts` — 6 specialist fragment descriptors with `buildPrompt(deal)` fns; `personas/index.ts` — 16 analyst personas (Marcus/Reyna/Eli/Dasha/Jin/Omar/Priya/Fiona/Kenji/Isaac/Clara/Dev/Nadia/Sofia/Rahul/Lena) each with `voicePrefix`, `emphasizeMetrics[]`, `framingHints[]`. **agent-delegator.ts** rewritten to use `INTENT_DISPATCH` — Layer 1 calls runtime, Layer 2 injects `fragmentPrompt` into analyst prompts via `buildPersonaPrompt()`. **orchestrator.ts** reduced to thin shim with `AGENT_RUNTIME_MAP` (research/zoning/supply/cashflow/commentary → runtime) + `legacyAgents` fallback. **agent-runs.routes.ts** updated: `AGENT_REGISTRY` object with 5 entries (each with `runtime`, `seedFn`, `label`); `POST /:agentId/run` now routes to any of the 5 agents. **DB migration** `20260419_zoning_analyses.sql` applied: `zoning_analyses` table with UNIQUE index on `deal_id`, FK to deals, JSONB `permitted_uses`, entitlement_risk CHECK constraint. Files: `backend/src/coordinator/dispatch.ts`, `context-fragments.ts`, `personas/index.ts`; all 4 config/seed/agent files; `agent-delegator.ts`, `orchestrator.ts`, `agent-runs.routes.ts`.

-   **Agent Platform Phase 3 — Research Agent End-to-End (2026-04-19, Task #225):** Completed full wiring of the Research Agent from deal creation through durable execution to Activity Feed display. **Stub tools** added: `fetch_tax_bill.ts` (annual tax/rate/assessed value from `/deals/:id/tax/summary`), `fetch_comps.ts` (comparable properties from `/deals/:id/comps/summary`), `fetch_ownership.ts` (owner name/type/acquisition from `/properties/:id`). All use `platformClient.as()` for agent identity threading. **Research Agent config** (`research.config.ts`): `RESEARCH_AGENT_CONFIG` wires all 6 tools (fetch_parcel, fetch_costar_metrics, fetch_tax_bill, fetch_comps, fetch_ownership, write_dealcontext) with `ResearchOutputSchema` ({summary, confidence_score, fields_written, completed_at}), `capabilities: ['read:all', 'write:deal_context']`, and `modelName: claude-haiku-4-5-20251001`. Exports `researchRuntime` singleton. **Prompt seeding** (`research.seed.ts`): `seedResearchPrompt()` upserts `research-v2` system prompt into `prompt_versions` on startup (ON CONFLICT DO UPDATE). **Inngest durable execution** (`research.inngest.ts`): `researchOnDealCreated` function triggers on `deal.created`, with tier gate (Principal+/institutional/enterprise), 5 durable steps (tier-gate → seed-prompt → execute-research-agent → write-audit-log → emit-research.completed), concurrency=1 per deal, retries=3. Served at `/api/inngest` via `inngest/express`. **deal.created event emission**: `POST /api/v1/deals` fires `inngest.send({name:'deal.created',...})` via `setImmediate` (non-blocking) after deal INSERT. **New REST endpoints** (`agent-runs.routes.ts`): `POST /api/v1/agents/:agentId/run` (manual trigger), `GET /api/v1/agents/runs/:runId` (run detail), `GET /api/v1/agents/runs/:runId/steps` (step log), `GET /api/v1/deals/:dealId/agent-runs` (all runs for a deal). **ActivityFeedSection** updated: merges activity feed + agent runs into a unified chronological timeline, with dedicated `AgentRunCard` (running/completed/failed/cancelled states, token count, cost, duration) and `AgentRuns` filter tab. Files: `backend/src/agents/tools/fetch_tax_bill.ts`, `fetch_comps.ts`, `fetch_ownership.ts`, `backend/src/agents/research.config.ts`, `backend/src/agents/seeds/research.seed.ts`, `backend/src/lib/inngest.ts`, `backend/src/agents/research.inngest.ts`, `backend/src/api/rest/agent-runs.routes.ts`, `backend/src/api/rest/index.ts`, `backend/src/index.replit.ts`, `backend/src/api/rest/inline-deals.routes.ts`, `frontend/src/components/deal/sections/ActivityFeedSection.tsx`.

-   **Economic Data Framework (2026-04-19):** Full economic context delivery pipeline integrating FRED, BLS QCEW, and DB persistence. **Bug fix**: `FREDApiClient.getLatest()` corrected to use `sort_order=desc` — previously returned 1954 data instead of current values. **Extended FRED series**: Added GDP (GDPC1), CPI (CPIAUCSL), UNRATE, UMCSENT to `FRED_SERIES` constants. **DB migration**: `gdp_growth_pct`, `cpi_yoy_pct`, `unrate`, `consumer_sentiment` columns added to `m28_rate_environment` (today populated: GDP+1.99%, CPI+3.32%, UNRATE 4.3%, sentiment 56.6). New `msa_economic_snapshot` table for BLS QCEW per-MSA labor data (msa_id + snapshot_date + naics_code unique, stores employment/YoY%/avg_weekly_wage/establishments/citation). **Ingest scripts**: `ingest-rate-data.ts` extended to pull macro series + CPI/GDP YoY calculations, uses ON CONFLICT DO UPDATE for all columns. New `ingest-msa-economic-data.ts` uses `msas.cbsa_code` directly as BLS QCEW area FIPS to pull 8 NAICS sectors (531 real estate, 493 warehousing, 721 hotels, 623 nursing/residential, 611 education, 236 construction, 621 health care, 522 lending) per active MSA. **M28 Scheduler**: MSA economic ingest added at 8:30 AM ET daily (after rate ingest at 8:00 AM). **New API endpoint**: `GET /api/v1/economic-context?msaId=&businessType=` (auth required) — assembles FRED macro + BLS MSA labor into structured JSON + formatted text block for AI injection. **Debt advisor enrichment**: `rate-environment.service.ts` now reads `m28_rate_environment` macro columns from DB in parallel with live SOFR fetch, builds `macroBlock` narrative (GDP signal, CPI inflation label, unemployment labor signal, consumer sentiment label), appends to rate environment `narrative` string, and exposes as `macroContext` object in `RateEnvironmentResult`. Files: `backend/src/utils/fred-api.client.ts`, `backend/src/scripts/ingest-rate-data.ts`, `backend/src/scripts/ingest-msa-economic-data.ts`, `backend/src/api/rest/economic-context.routes.ts`, `backend/src/services/m28-scheduler.service.ts`, `backend/src/services/debt-advisor/rate-environment.service.ts`.

-   **Investor & Capital Tracking System (2026-04-21):** Full LP/GP investor management feature integrated into F8 Debt/Cap section. **10 DB tables**: `investors` (full KYC/banking/withholding), `deal_investments` (commitment/ownership/class), `capital_calls` (pro-rata allocation + status flow), `capital_call_items` (per-investor call items), `distributions` (approve/process/reverse flow), `distribution_items` (gross/net + withholding breakdown), `deal_waterfalls` (pref rate/catchup/clawback), `waterfall_tiers` (IRR hurdle bands + LP/GP splits), `capital_account_entries` (ledger entries), `commitment_tranches` (phased funding). **~25 REST endpoints** at `/api/v1/capital`: investors CRUD, deal investments, capital calls with pro-rata allocation, distributions with approve/process flow, waterfall config CRUD, capital account ledger. **Frontend**: `InvestorCapitalModule.tsx` — Bloomberg-style 5-tab module (Investors, Capital Calls, Distributions, Waterfall, Ledger) with KPI cards, add/edit forms, status badges using BT design tokens. **Integration**: wired as "Investor Capital" tab alongside "Exit & Debt Analysis" in `DebtCapitalScreen` (`DealDetailPage.tsx`). Migration saved at `backend/src/db/migrations/20260421_011_investor_capital_tables.sql`.

-   **M11 Debt Advisor Tab (2026-04-15):** Full Bloomberg terminal-style Debt Advisor section replacing the `DebtMarketSection.tsx` placeholder. Added as the first sub-tab under F8 DEBT/CAP in `DebtCapitalScreen`. **4 sub-tabs**: ADVISOR (main plan wired to live API), CONFIGURE (loan builder from real phase data), SENSITIVITY (4 heatmaps: IRR/EM/DSCR/Leverage), EXIT (21-year convergence chart + RSS breakdown + exit strategies). **Hook**: `useDebtAdvisor(dealId)` — fetches `GET /api/v1/deals/:dealId/debt/advisor` via `apiClient`, exposes `recompute(productHint?)`, `accept(phaseIndex)`, and `refresh()`. **Component**: `DebtAdvisorSection.tsx` — state-driven multi-tab layout using M11 design tokens (JetBrains Mono, `#0a0a0c` bg, `#00e5a0` cyan accent). ADVISOR tab renders: strategy origin banner from `summary.headline`+`contextModifications.narrativeNotes`, 4 KPI cards (primary product/loan amount/blended rate/closing costs), interactive `PhaseBar` (clickable phase bands on timeline), expandable `PhaseDetail` (sizing/structure/fee sections + top-3 lender targets with fit scores), `AlternativesPanel` (grid with "Run Alternative" buttons that call recompute), `TriggersPanel` (monitoring triggers with warn/critical color coding), `MarketContextPanel` (rate rows, SOFR forward curve SVG, spread bar chart). Shows `NoStrategyState` when `hasStrategy:false`. Files: `frontend/src/hooks/useDebtAdvisor.ts`, `frontend/src/components/deal/sections/DebtAdvisorSection.tsx`, updated `frontend/src/pages/DealDetailPage.tsx`.

-   **Time Series Data Infrastructure & Explorer (2026-04-02, expanded):** Multi-source data ingestion pipeline and Bloomberg-style data browser. **594,610 data points, 138 metrics, 1,196 geographies, 18 sources.** Sources: zillow (4 metrics, 911 geos), fred/fred_atlfed/fred_bea/fred_bls/fred_census/fred_eia/fred_fhfa/fred_sp (combined 20 metrics), census_acs (3 metrics, 226 geos — all 159 GA counties + 67 FL counties), census_acs5 (16 metrics, Atlanta MSA 2012-2023), DOT_FL (2 metrics, 12 geos), DOT_GA (2 metrics, 4 geos), google_trends (3 metrics: D_SEARCH_VOL, D_SEARCH_VOL_YOY, C_SEARCH_GROWTH_INDEX), derived (9 computed metrics), **costar** (55 metrics, 41 geos — 40 Atlanta submarkets + MSA, quarterly 2000-2031 incl forecasts), **property_ops** (25 weekly operating metrics, 2 properties). **CoStar metrics** (CS_ prefix): vacancy, occupancy, market rent, effective rent, rent growth, cap rate, absorption, inventory, deliveries, construction starts, demolished, demand, sale price, asset value, sales volume, rent by bedroom, NOI index, median cap rate, transaction data, etc. **Property operations** (OP_ prefix): traffic, walk-ins, applications, cancels, denials, net leases, occupancy, move-ins/outs, vacant rented/unrented, on-notice, market rent, effective rent, gross rent PSF, LER (leased effective rent), concession %. **Properties**: Symphony at Suwanee Creek (ssc-suwanee, 200 units, Nov 2021-Mar 2026), Highlands at Sweetwater Creek (hsc-duluth, 290 units, Jul 2021-Mar 2026). **Lease rent ingestion** (Task #141): `lease-rent-ingest.service.ts` parses lease-level transaction files (Excel/CSV with per-unit lease records), aggregates Market Rent at Lease Execution and New LER into weekly buckets aligned with existing OP_ time series, applies LOCF for weeks with no transactions, upserts OP_AVG_MARKET_RENT, OP_LER, OP_CONCESSION_PCT, and recalculates OP_GROSS_MARKET_RENT. Admin endpoint: `POST /api/v1/ingestion/lease-rents`. `uploadService.ts` also updated to support multi-sheet Excel parsing (merges columns from additional sheets by matching date columns). **API**: `GET /api/v1/time-series/metrics|data|summary|geographies`. **Frontend** (`TimeSeriesExplorerPage.tsx` at `/terminal/data`): source filter dropdown, readable source names (SOURCE_DISPLAY map), "All Geographies" default. Files: `frontend/src/pages/TimeSeriesExplorerPage.tsx`, `backend/src/api/rest/time-series.routes.ts`.

-   **Programmable Data Grids (2026-04-02, Task #143):** Exposes all 47 data-backed Strategy Builder catalog metrics in F4 Markets Column Builder with live data rendering, driver insight badges, and grid templates. **Backend**: 3 new endpoints under `/api/v1/columns/`: `GET /catalog` (returns 47 metrics with availability stats — point count, geo count, date range, grouped by 11 categories), `GET /grid-data?metricIds=X,Y&geoIds=Z` (batch latest/previous/yoy for metric×geography combos, returns value, trend, yoyChange), `GET /insights?propertyId=X` (driver analysis correlations per metric — pearsonR, rSquared, lagWeeks, direction). Grid templates CRUD: `GET/POST/PUT/DELETE /api/v1/grid-templates` with `grid_templates` DB table (user_id, name, view_id, columns JSONB, column_config JSONB). **Frontend**: `columnRegistry.ts` extended with `DynamicColumnDef`, `buildDynamicColumn()`, `isDynamicColumn()`, `extractMetricId()`, `formatMetricValue()` — dynamic columns use `metric:` prefix (e.g. `metric:D_SEARCH_MOMENTUM`). Unit-aware formatters for %, $, bps, months, index, ratio, score, rank, units, people. `ColumnPicker.tsx` upgraded with METRICS LIBRARY section — toggleable panel that fetches `/columns/catalog` and displays all 47 metrics grouped by category with geo count, point count, source, and one-click toggle. `F4MarketsView.tsx`: `renderDynamicCell()` renders live metric values with trend arrows (▲/▼) from grid-data API; header insight badges show driver lag/outcome (e.g. "21w→EFFE"); templates dropdown with Save As dialog and delete. `useGridTemplates.ts` hook for CRUD. Files: `backend/src/api/rest/column-catalog.routes.ts`, `backend/src/api/rest/grid-templates.routes.ts`, `backend/src/db/migrations/20260402_002_grid_templates.sql`, `frontend/src/config/columnRegistry.ts`, `frontend/src/components/terminal/ColumnPicker.tsx`, `frontend/src/hooks/useGridTemplates.ts`, `frontend/src/pages/terminal/F4MarketsView.tsx`.

-   **Derived Metrics Computation Engine (2026-04-02):** Service that computes 17 derived/composite metrics from existing DB data and stores them as time series for use in Strategy Builder and Driver Analysis. **Coverage**: 47/58 catalog metrics now have data (81%, up from 58%). **Metrics computed**: D_SEARCH_MOMENTUM (QoQ% from D_SEARCH_VOL), D_DIGITAL_SCORE (normalized 0-100 from search vol), D_DIGITAL_SHARE (digital vs physical traffic ratio), T_EFFECTIVE_ADT (AADT × 0.72 signal adjustment), T_WALKINS (AADT × 0.0085 conversion), T_PHYSICAL_SCORE (normalized AADT 0-100), C_SURGE_INDEX (normalized momentum composite), C_DIGITAL_PHYSICAL_GAP (digital minus physical score), C_TPI (60% digital + 40% physical weighted index), C_TVS (normalized search momentum velocity), C_TRAFFIC_GROWTH_INDEX (normalized YoY growth), S_MONTHS_OF_SUPPLY (vacancy × 12 / absorption from CoStar), M_SUBMARKET_RANK (percentile rank across 40 submarkets using rent growth × 0.4 + occupancy × 0.3 + absorption × 0.3), R_SUPPLY_RISK (pipeline-to-stock × 10, capped 0-100), DEMO_NET_MIGRATION (population change minus 0.4% natural growth), DEMO_POPULATION_DECLINE (binary indicator), DEMO_POPULATION_TREND_3Y (3-year CAGR). **3,226 total data points** computed in ~2 seconds. **Categories now at 100%**: Traffic Physical (5/5), Traffic Composite (5/5), Financial (5/5), Supply (4/4), Demand (5/5), Macro (3/3). **Still missing** (need external data): D_OUT_OF_STATE, M_LEASE_VELOCITY, K_GOOGLE_RATING, K_REVIEW_SENTIMENT, R_CLIMATE_RISK, O_DEBT_MATURITY_MO, O_HOLD_DURATION, SFR_DOM, SFR_INVENTORY, SFR_CASH_BUYER_PCT, DEMO_FOREIGN_BORN_PCT. **Service**: `DerivedMetricsService` (`backend/src/services/derivedMetrics.service.ts`). **REST API**: `GET /api/v1/derived-metrics/status` (coverage report), `POST /api/v1/derived-metrics/compute` (trigger recompute, auth required). Routes registered in `index.replit.ts`. Files: `backend/src/services/derivedMetrics.service.ts`, `backend/src/api/rest/derived-metrics.routes.ts`.

-   **Property Performance Driver Analysis (2026-04-02, Task #142):** Structured dependent/independent variable correlation engine with lag sweep. Tests ALL Strategy Builder catalog metrics (independent variables) against 8 property operational outcome metrics (dependent variables) with 0–26 week lag sweeps. **Dependent variables (8)**: OP_AVG_MARKET_RENT, OP_LER, OP_CONCESSION_PCT, OP_EFFECTIVE_RENT, OP_OCCUPANCY_PCT, OP_TRAFFIC, OP_NET_LEASES, OP_LEASED_PCT. **Independent variables**: All METRICS_CATALOG entries + CS_ CoStar submarket metrics + FRED + Zillow + Census ACS5 + Google Trends. **Engine**: `DriverAnalysisService` dynamically collects all catalog metrics, resolves to DB IDs via `translateMetricId`, fetches time series, aligns by date with nearest-neighbor matching (45-day tolerance), sweeps 0–26 week lags (weekly for 0-12, biweekly for 12-26), computes Pearson R, p-value, R², linear regression (slope/intercept), persists results in batches, calls `applyEmpiricalLeadLag()` to feed discoveries back into catalog. **Database**: `driver_analysis_runs` (run metadata, status, driver/result counts, summary JSON), `driver_analysis_results` (per-relationship: driver_metric_id, outcome_metric_id, optimal_lag_weeks, pearson_r, p_value, r_squared, slope, intercept, sample_size, direction, geography context). **REST API**: `POST /api/v1/driver-analysis/run` (trigger for property), `GET /api/v1/driver-analysis/results/:propertyId` (filtered/sorted results), `GET /api/v1/driver-analysis/summary/:propertyId` (top drivers per outcome), `GET /api/v1/driver-analysis/runs` (run history). **Frontend**: Bloomberg terminal-style results grid at `/terminal/drivers`. Property selector, outcome filter, category filter, min |R| threshold filter. Sortable columns: Driver Metric, Category, Outcome, Pearson R (with visual bar), R², P-value (with significance stars), Optimal Lag, Direction, Sample Size. Expandable rows showing slope, intercept, geography, explanatory power. Run Analysis button triggers engine. Stats bar shows property name, total/filtered results, outcomes tested, categories, last run info. Files: `backend/src/services/driverAnalysis.service.ts`, `backend/src/api/rest/driver-analysis.routes.ts`, `frontend/src/pages/DriverAnalysisPage.tsx`.

-   **Historical Backtesting Engine (2026-04-02, Task #140):** Quantitative backtesting engine that tests how well market indicators predict actual property outcomes. **Flow**: (1) Split property ops data into train/test periods (70/30 default), (2) For each outcome metric, scan 90+ leader metrics across all sources, (3) Find optimal lag (0-26 weeks) via Pearson correlation, (4) Build linear regression model on training data, (5) Generate forecasts for test period, (6) Measure accuracy (RMSE, MAE, MAPE, directional accuracy, R²). **Database**: `backtest_runs` (property, train/test windows, config, summary), `backtest_forecasts` (per-point forecast vs actual), `backtest_accuracy` (per-model accuracy scores). **Outcome metrics**: OP_OCCUPANCY_PCT, OP_EFFECTIVE_RENT, OP_TRAFFIC, OP_NET_LEASES, OP_LEASED_PCT, OP_WALKINS, OP_APPLICATIONS, OP_MOVE_INS, OP_MOVE_OUTS, OP_AVG_MARKET_RENT, OP_TOTAL_VACANT. **Leader sources**: CoStar, FRED, Census, BLS, Google Trends, DOT, Zillow, derived. **Initial results**: SSC — 15 models, best MAPE 0.4% (market rent from CoStar eff rent growth); HSC — 22 models, best MAPE 0.8% (effective rent from CoStar 2BR rent). Rent metrics highly predictable (r>0.98), directional accuracy up to 88% for move-outs. **REST API**: `POST /api/v1/backtest/run` (trigger), `GET /api/v1/backtest/runs` (list), `GET /api/v1/backtest/results/:runId` (detail), `GET /api/v1/backtest/property/:propertyId`. **Traffic Backtesting** (`runTrafficBacktest`): Tests TGI thesis — traffic as leading indicator and as outcome. Uses `runBacktestWithCustomLeaders` to test OP_TRAFFIC/OP_WALKINS as leaders predicting rent/occupancy/leases, plus standard backtest with traffic as outcome. **TGI thesis validated**: Walk-ins lead occupancy by 26 weeks (1.3-2.1% MAPE), traffic leads move-ins by 4-7 weeks (83% directional accuracy), traffic leads net leases directionally (76-83%). No external indicators predict traffic (noise at weekly level). **REST API**: `POST /api/v1/backtest/run/traffic`. Files: `backend/src/services/backtestEngine.service.ts`, `backend/src/api/rest/backtest.routes.ts`.

-   **Cross-Metric Correlation Engine (M08, 2026-04-01, Task #138):** Generalized pairwise Pearson correlation engine scoped to any geography level, extending the existing `CorrelationEngineService`. **New service methods**: `computeMatrix(metricIds, scope, geographyId?)` computes all pairwise correlations for a set of metrics across geographies, `getCorrelationMatrix(metricIds, scope, geographyId?)` retrieves cached correlations (per-geography or aggregated average across geographies), `getStrategyCorrelations(strategyId)` resolves a strategy's conditions → translated metric IDs and returns pairwise correlations split into condition-condition vs condition-outcome types with redundancy detection (r > 0.85) and complementary signal detection (0.3 ≤ r ≤ 0.7), `seedPresetStrategyCorrelations()` seeds correlations for all 8 preset strategies. **Outcome metrics**: `rent_index_yoy`, `home_value_index_yoy`, `home_value_index`, `rent_index`. **REST Endpoints**: `POST /api/v1/correlations/compute` accepts `{metricIds, scope, geographyId?}` to trigger computation (max 20 metrics), `GET /api/v1/correlations/matrix?metricIds=X,Y&scope=Z` returns cached matrix, `GET /api/v1/correlations/strategy/:strategyId` returns strategy-specific correlations with redundancy analysis, `POST /api/v1/correlations/seed-presets` (admin) seeds all preset strategies. **Frontend**: "SIGNAL CORRELATIONS" panel in Strategy Builder Live Preview sidebar — shows pairwise r-values color-coded: green (COMPLEMENTARY, r < 0.5), yellow (MODERATE, 0.5-0.85), red (REDUNDANT, r > 0.85), with warning text for redundant signals. Auto-fetches via `GET /correlations/matrix` when ≥2 conditions are active. Files: `backend/src/services/correlationEngine.service.ts`, `backend/src/api/rest/correlation.routes.ts`, `frontend/src/pages/StrategyBuilderPage.tsx`.

-   **Platform Preset Strategies (M08, 2026-04-01, Task #137):** 8 real investment strategies seeded into `strategy_definitions` table: Core Stabilized, Value-Add Upside, Growth Market Tracker, Supply Constrained, Migration Magnet, Traffic Alpha, Cash Flow Maximizer, Distressed Opportunity. Each uses real metric conditions (cap rate, rent growth, vacancy, TGI, migration, absorption, etc.) with proper operators including `between`. **Backend**: GET `/strategies` now returns full `conditions` JSONB column plus asset_classes/tags. PUT/DELETE guarded against `type='preset'` strategies (403 response). POST `/:id/run` has ownership/preset authorization check. `previewStrategy()` now applies `translateMetricId()` to map preset metric codes (e.g. `C_TRAFFIC_GROWTH_INDEX`, `DEMO_NET_MIGRATION`, `O_DEBT_MATURITY_MO`) to actual `metric_time_series` columns. **Frontend**: Library tab shows "Clone" button for presets (not "Edit"), Run button has loading state and populates preview results inline, Save forces creation (not update) when source is preset. `between` operator renders dual-input min/max fields. `METRIC_COLORS` extended with `sfr`, `demographic`, `macro` categories. Preview call includes `combinator: 'AND'`. Migration: `backend/src/db/migrations/20260401_001_seed_preset_strategies.sql`. Files: `backend/src/api/rest/strategy-definitions.routes.ts`, `backend/src/services/strategyExecution.service.ts`, `frontend/src/pages/StrategyBuilderPage.tsx`.

-   **Search Growth Index (SGI) + Macro Indicators (2026-03-31):** Added 4 new metrics to the catalog and built computation/storage services. **Search Growth Index (`C_SEARCH_GROWTH_INDEX`)**: mirrors TGI formula but for Google Search — `(Current Google Trends Index − 5yr Historical Avg) / Historical Avg × 100`. Computes at submarket/zip/county/MSA level from `D_SEARCH_VOL` time series data. Service: `SearchGrowthIndexService` (`backend/src/services/search-growth-index.service.ts`). **Crude Oil (WTI) (`MACRO_OIL_PRICE`)**: tracks WTI spot price with MoM/YoY deltas. **Official CPI (`MACRO_CPI_OFFICIAL`)**: BLS CPI-U YoY% at national + MSA level. **ShadowStats CPI (`MACRO_CPI_SHADOW`)**: pre-1983 methodology CPI, computed as official CPI + 5.5pp adjustment. Includes `/cpi/comparison` endpoint showing the methodology gap and investment signal. Service: `MacroIndicatorsService` (`backend/src/services/macro-indicators.service.ts`). New `'macro'` category added to metrics catalog. **REST Endpoints** (all under `/api/v1/macro/`): `GET /oil`, `POST /oil`, `GET /cpi`, `GET /cpi/shadow`, `GET /cpi/comparison`, `POST /cpi`, `GET /search-growth/:type/:id`, `POST /search-growth/:type/:id/store`, `POST /search-growth/batch`. Files: `backend/src/services/search-growth-index.service.ts`, `backend/src/services/macro-indicators.service.ts`, `backend/src/api/rest/macro-indicators.routes.ts`, `backend/src/services/metricsCatalog.service.ts`, `backend/src/index.replit.ts`.

-   **Traffic Growth Index (TGI) — Consolidated Metric (2026-03-30):** Consolidated the traffic growth formula into a single named metric: **`C_TRAFFIC_GROWTH_INDEX`** (Traffic Growth Index / TGI). **Formula**: `(Google Realtime ADT − DOT Historical Avg ADT) / DOT Historical Avg ADT × 100`. Takes Google Directions API real-time traffic factor, multiplies it by the DOT historical AADT baseline to get current real-time ADT, then computes the percentage difference vs the historical average. Positive = area gaining traffic; negative = losing traffic vs historical norms. **Service**: `TrafficGrowthIndexService` (`backend/src/services/traffic-growth-index.service.ts`) — `computeForProperty(propertyId)` reads from `property_traffic_context` (DOT AADT + Google factor), returns `{googleRealtimeAdt, dotHistoricalAvgAdt, trafficGrowthIndex, confidence, sources}`. `computeAndStoreForProperty()` persists to `metric_time_series`. `computeBatchForSubmarket()` averages across all properties in a submarket. **REST Endpoints**: `GET /api/v1/traffic-data/growth-index/:propertyId` (single property, `?store=true` to persist), `POST /api/v1/traffic-data/growth-index/batch` (bulk compute up to 100 properties). **Catalog**: Updated in `metricsCatalog.service.ts` — leads rent growth by 6 months (r=0.62), vacancy by 9 months (r=-0.54), absorption by 3 months (r=0.47). **Investment signal**: +15%+ sustained = demand building ahead of rents. Files: `backend/src/services/traffic-growth-index.service.ts`, `backend/src/api/rest/traffic-data.routes.ts`, `backend/src/services/metricsCatalog.service.ts`.

-   **M07 Traffic Engine — Self-Calibrating Backend (2026-04-14, Task #182):** Bayesian calibration stack for traffic prediction coefficients with rent roll ingestion, starting state resolution, and nightly platform calibration job. **DB Migration** (`100_m07_traffic_calibration.sql`): 5 new tables — `traffic_calibration_coefficients` (Bayesian posteriors: coefficient_name, scope_level, msa/submarket/class/vintage_band, cal_window TTM/PYTM/TTM_24, confidence_low/mid/high, match_tier DEAL/PLATFORM/BASELINE), `traffic_calibration_history` (versioned coefficient snapshots), `rent_roll_snapshots` (per-deal upload records with extraction_confidence), `lease_events` (normalized per-unit rows from parsed rent rolls), `traffic_weight_config` (Layer A/C catalog metric weights, updateable); `deal_mode` column on deals (STABILIZED/LEASE_UP/REDEVELOPMENT). **Type System** (`traffic-calibration.types.ts`): `LayeredValue<T>` generic, `TrafficCoefficientFamily` with 6 named coefficients as LayeredValues, `StartingState` discriminated union (3 mode branches), `CalibrationMeta` (match_tier, confidence_band, window, n_peer_properties). **Rent Roll Parser Pipeline** (`backend/src/services/rent-roll/`): 4-service pipeline — `format-detector.service.ts` (Yardi vs generic CSV/XLSX detection), `field-mapper.service.ts` (column normalization), `rent-roll-validator.service.ts` (row confidence scoring), `rent-roll-parser.service.ts` (main parser, writes rent_roll_snapshots + lease_events). **Derivations** (`rent-roll-derivations.service.ts`): 24-month signing velocity histogram, single-snapshot renewal rate proxy, expiration waterfall, per-unit-type metrics, stored as derived JSONB. **Nightly Calibration Job** (`trafficCalibrationJob.ts`): pulls derived snapshots, computes per-property coefficients, rolls up to (msa, submarket, class, vintage) buckets, Bayesian update: `posterior = (prior×n_prior + evidence×n_evidence)/(n_prior+n_evidence)`, TTM/PYTM/TTM_24 windows, writes versioned rows, Kafka `traffic.calibration.updated` event. Absorption benchmarks (median+P25/P75 months-to-stabilization) stored as `coefficient_name='absorption_curve'`. **Starting State** (`starting-state.service.ts`): STABILIZED (occ/renewal/expiration from rent roll), LEASE_UP (absorption benchmark + seasonality overlay), REDEVELOPMENT (phased mini lease-up). Occupancy pulled from `deal_data→market_intelligence→demographics→submarket→avg_occupancy` JSONB path. **Coefficient Resolver** (`coefficient-resolver.service.ts`): 3-layer Bayesian hierarchy Deal→Platform→Baseline for all 6 coefficients. **Catalog Metrics Wiring** (`catalog-metrics-wiring.service.ts`): Layer A weights (search momentum, business formation, wage growth) + Layer C dampers (pipeline%, concession intensity, months of supply) from `traffic_weight_config` DB. **Engine Integration**: `trafficPredictionEngine.ts` extended with `calibration_meta` + `starting_state` on all predictions. **REST API** (`/api/v1/calibration/`, 7 endpoints): `GET /starting-state/:dealId`, `GET /coefficients/:dealId`, `GET /rent-roll/:dealId/snapshots`, `GET /absorption-benchmark/:submarketId`, `POST /rent-roll/upload` (CSV/XLSX, 50MB, writes DB, runs derivations), `POST /job/run` (manual trigger), `PUT /deal/:dealId/mode`. Files: `backend/src/database/migrations/100_m07_traffic_calibration.sql`, `backend/src/types/traffic-calibration.types.ts`, `backend/src/services/rent-roll/*.service.ts`, `backend/src/services/rent-roll-derivations.service.ts`, `backend/src/jobs/trafficCalibrationJob.ts`, `backend/src/services/starting-state.service.ts`, `backend/src/services/coefficient-resolver.service.ts`, `backend/src/services/catalog-metrics-wiring.service.ts`, `backend/src/api/rest/m07-calibration.routes.ts`.

-   **M22 Traffic Analytics — Single-Snapshot Extractors (2026-04-11, Task #156):** Leasing traffic analytics pipeline that derives velocity, seasonality, and trade-out metrics from rent roll + box score data in `deal_lease_transactions`. **8 Extractors** (`backend/src/services/traffic-analytics.service.ts`): (1) Signing velocity — trailing 3/6/12-month new lease + renewal counts with confidence scored by data span; (2) Seasonality curve — 12-month histogram of signings and expirations, normalized to 1.0 factor; (3) Expiration waterfall — 24-month forward projection with month-to-month + MTM exposure, renewal cliff detection (>8% of occupied units expiring in single month); (4) Velocity-vs-seasonality variance — actual vs expected signings per month over trailing 12 months; (5) Lease term distribution — bucketed 1/3/6/9/12/15/18/24-month terms with avg rent; (6) Trade-out analytics — new lease vs market rent spread, renewal rent increases, concession-adjusted rent, overall loss-to-lease; (7) MTM exposure — month-to-month unit count/pct, pre-leased velocity with avg days out; (8) Box Score conversion funnel — channel-level contact→show→apply→approve→lease rates. **Storage** (`deal_traffic_snapshots` table, migration 105): JSONB columns per extractor, upsert by deal+date. **Capsule routing**: deep merges into `deal_data.module_outputs.traffic` with velocity, seasonality summary, expiration exposure, trade-out, conversion metrics. **Data Library attribution**: tags `data_library_assets` with `TRAFFIC_SNAPSHOT` extraction data. **Auto-trigger**: rent roll, box score, and T30 LTO document ingestion automatically computes and persists traffic snapshot via data router hook. **Financial context**: `getDealFinancialContext()` returns `trafficSnapshot` summary (signings/mo, MTM%, LTL%, conversion%, renewal cliffs), included in AI prompt formatting. **REST API**: `POST /api/v1/deals/:dealId/traffic-snapshot` (on-demand compute), `GET /api/v1/deals/:dealId/traffic-snapshot` (fetch latest). Files: `backend/src/services/traffic-analytics.service.ts`, `backend/src/services/deal-financial-context.service.ts`, `backend/src/services/document-extraction/data-router.ts`, `backend/src/api/rest/inline-deals.routes.ts`, `jedire-patches/migrations/105_deal_traffic_snapshots.sql`.

-   **Multi-Document Financial Data Extraction Pipeline v2 (2026-04-12, Tasks #155/#161):** Automated document ingestion pipeline with v2 extraction upgrades. **Migration 110**: deal_properties table (deal-property M:N), deal_assumptions.year1 JSONB+GIN, platform_intel_xdoc_unique index, properties enrichment columns, deals.legal_owner. **V2 Parsers**: t12-parser (description-driven Yardi-aware, ChartFormat detection, actuals vs budget), rent-roll-parser (RRwLC dual-mode, capsuleExtras with floor plan mix/concession/stale lease/MTM), tax-bill-parser (appeal scenario detection, AuthorityLine with taxableAssessment/millage/grossTax/netTax, exemptions). **New Types**: ChartFormat, RentRollLayout, LayeredValue<T>, ProFormaYear1Seed, ExtractionT12Capsule, ExtractionRentRollCapsule, CrossDocVariance. **ProForma Seeder** (proforma-seeder.service.ts): seedProFormaYear1 builds Year 1 from T12+RR capsules into deal_assumptions.year1, applyUserOverride with provenance, buildAssumptionsFromYear1Seed resolves LayeredValue stack. **Cross-Validation** (multi-doc-cross-validation.service.ts): 6 cross-checks (T12-RR revenue, T12-TaxBill taxes, RR-TaxBill units, RR-T12 vacancy, RR-T12 concessions, T12-OIS other income), variance scoring, platform_intel persistence. **Data Router v2**: deal_properties join, v2 capsule shapes, seeder+xval trigger. **Pipeline v2**: documentId threading, real isMultiProperty/hasHistorical flags. **Inline-deals v2**: docId passthrough, POST proforma/year1/seed + PATCH override endpoints, WebSocket notification, financial model auto-build. **Integration test**: Alta Porter multi-doc scenario. Files: backend/src/services/document-extraction/ (types.ts, data-router.ts, extraction-pipeline.ts, parsers/*.ts), backend/src/services/proforma-seeder.service.ts, backend/src/services/multi-doc-cross-validation.service.ts, backend/src/api/rest/inline-deals.routes.ts.

-   **Input-Needed Alerts & Deal Type Input Surfaces (2026-04-11, Task #154):** Bloomberg-style alert pips and deal-type-aware overview surfaces. **AlertPip** (`frontend/src/components/deal/AlertPip.tsx`): inline visual indicator — red `!` for block, amber `▲` for warn, cyan `·` for info, hidden for none. `AlertPipInline` variant with optional label. **AlertCounter** (`frontend/src/components/deal/AlertCounter.tsx`): aggregate F1 scanner — `useAlertScan()` hook traverses all `INPUT_FIELD_REGISTRY` fields applicable to current deal type, counts LayeredValue alertLevels (block/warn/info). `AlertCounter` renders expandable banner showing "N INPUTS NEED ATTENTION" with severity breakdown, click-to-expand sorted field list. `useIdentityGate()` hook checks 5 required identity fields (name, address, city, state, mode) for completeness. `IdentityGateBanner` renders blocking red banner "IDENTITY INCOMPLETE — AGENTS BLOCKED" with missing field list. **OverviewRouter** (`frontend/src/components/deal/OverviewRouter.tsx`): three deal-type variants. *Existing*: read-only broker-stated unit mix table + gap analysis (property vs submarket rent, occupancy, going-in cap, NOI) + override-only assumptions. *Development*: selected development path summary (building type, stories, units, GFA, dev cost, timeline) + editable unit mix builder + zoning hard gates (max density/height/FAR/coverage/parking) + parking as binding constraint + dev assumptions. *Redevelopment*: side-by-side as-is vs as-reconfigured (existing vs proposed units, NOI, occupancy) with scope-of-work deltas + assumptions. Each variant uses `LVRow` displaying source badge and AlertPip per LayeredValue. **BloombergOverviewSection integration**: `IdentityGateBanner` + `AlertCounter` at top, `OverviewRouter` as new row between AI Intelligence Brief and Key Financials grid. Files: `frontend/src/components/deal/AlertPip.tsx`, `frontend/src/components/deal/AlertCounter.tsx`, `frontend/src/components/deal/OverviewRouter.tsx`, `frontend/src/components/deal/sections/BloombergOverviewSection.tsx`.

-   **Assumption Editing & Keystone Cascade (2026-04-11, Task #153):** Complete write-through assumption editing system wired to F9 ProForma Assumptions tab and F7 Strategies sidebar. **Store actions**: `updateAssumption(path, value)` writes LayeredValue user layer with full provenance (backfills existing value into platform/broker fallback layer if missing), appends EditLogEntry, marks downstream sections stale, triggers debounced cascade recompute. `revertAssumption(path)` clears user layer and resolves back to platform/broker fallback. `revertAllAssumptions()` wipes user layer from all 7 financial assumption fields. `_triggerAssumptionCascade()` (debounced 600ms) calls backend `/api/v1/deals/:id/recompute` with `trigger: 'assumption_change'`, atomically updates financial/strategy/scores/risk state. `assumptionCascadeStatus` tracks 'idle'|'pending'|'computing'|'error'. **Sensitivity ranking**: `SENSITIVITY_COEFFICIENTS` maps 7 fields (exitCapRate, rentGrowth, vacancy, holdPeriod, capexPerUnit, managementFee, expenseGrowth) with rank, unit, formatMultiplier. `SENSITIVITY_PATHS` ordered by rank. **AssumptionsPanel** (`frontend/src/components/deal/AssumptionsPanel.tsx`): shared editable component rendering top-5 fields by default with "Show all" toggle. Per-field: inline edit on click, source badge (broker/platform/user), alert dot, strikethrough of platform default when user-overridden, revert button (↩). Global "REVERT ALL" button. Confirmation modal for edits estimated to shift JEDI Score >10 points. Cascade status indicator (RECOMPUTING…/CASCADE ERR). Variance-assumed banner when zoning overrides active. **ZoningContext**: `varianceAssumed: boolean` flag added, auto-set true on zoning field user override, cleared when last zoning override reverted. **F9 wiring**: AssumptionsTab now renders `<AssumptionsPanel />` as top "KEYSTONE ASSUMPTIONS" collapsible section above existing read-only model inputs. **F7 wiring**: Strategies view includes 280px right sidebar with `<AssumptionsPanel compact />`. Both surfaces read/write same dealStore — edits in F7 reflect in F9 and vice versa. **layered() fix**: Now populates `layers` property on creation, ensuring revert always has fallback data. `useAssumptions` convenience hook exported. Files: `frontend/src/stores/dealStore.ts`, `frontend/src/stores/dealContext.types.ts`, `frontend/src/components/deal/AssumptionsPanel.tsx`, `frontend/src/pages/development/financial-engine/AssumptionsTab.tsx`, `frontend/src/pages/TerminalPage.tsx`.

-   **Deal Capsule Schema & LayeredValue Foundation (2026-04-11, Task #152):** Core data model for Deal Capsule system. **LayeredValue<T> enhanced** with `alertLevel: AlertLevel` (none|info|warn|block), `resolvedFrom: 'broker'|'platform'|'user'`, `userReviewed: boolean`. **computeAlertLevel()** implements 4-tier rule set: block (identity missing or confidence<0.4 on high-sensitivity), warn (confidence 0.4–0.7 or divergence>15%), info (not yet reviewed), none (user-edited or high-confidence reviewed). **InputClass taxonomy** (`identity|override|scope`) with `INPUT_FIELD_REGISTRY` mapping 20+ field paths to their metadata (label, sensitivity, appliesTo deal types, category). Helper functions: `getFieldMeta()`, `getFieldsForDealType()`, `getIdentityFields()`, `getHighSensitivityFields()`. **EditLogEntry** audit trail type with path, old/new values, timestamp, actor (user|agent|platform). **RedevelopmentContext** with `RedevelopmentDelta[]` (unit_reconfig, amenity_add, envelope_mod, demo, systems_upgrade), demoScope, existing/projected NOI as LayeredValues, variance tracking. **DealMode expanded** to include 'redevelopment'. **resolveProjectType()** utility (`frontend/src/shared/utils/project-type.ts`) normalizes ~30 raw type aliases to canonical 'existing'|'development'|'redevelopment' with convenience predicates (isExisting, isDevelopment, isRedevelopment, requiresZoningCapacity, supportsUnitMixBuilder, hasExistingBaseline). **dealStore wired**: `updateLayeredValue` appends EditLogEntry, writes layers by `resolvedFrom` (not raw source), sets alertLevel/userReviewed; `INITIAL_CONTEXT` includes `editLog: []`, `redevelopment: null`; hydration uses `resolveProjectType()`. **Backend alignment**: `backend/src/types/dealContext.ts` exports all shared types (LayeredValue, AlertLevel, InputClass, EditLogEntry, RedevelopmentDelta, RedevelopmentContext, InputFieldMeta) plus `computeAlertLevel()` and `resolveProjectType()`. Files: `frontend/src/stores/dealContext.types.ts`, `frontend/src/stores/dealStore.ts`, `frontend/src/shared/utils/project-type.ts`, `backend/src/types/dealContext.ts`.

-   **F4 Markets: Demand Tab (2026-04-10):** Graduated the F3 Demand Tab mockup from the canvas sandbox into the main app as a new tab in the F4 Markets view. **DemandTab component** (`frontend/src/components/terminal/tabs/DemandTab.tsx`): Bloomberg-style demand analysis view showing (1) "Demand by Unit Type" cards — 4 unit types (STU, 1BR, 2BR, 3BR) with demand score (0-100), status badges (OVERSUPPLIED/BALANCED/UNDERSUPPLIED), vacancy %, DOM, concessions, rent with deltas, and sparkline trends; (2) "Supply/Demand Gap" section with horizontal bar chart (supply vs demand share) and gap analysis with directional indicators, percentage point deltas, and AI insights callouts. Uses BT theme system throughout. Integrated into F4MarketsView as the DEMAND tab (between PROPERTIES and COMPARE). Column preferences and metrics legend/suggestions are hidden when DEMAND tab is active since it has its own dedicated layout. Static fallback data provided; props accept `msaName`/`msaCode` for future API wiring. Files: `frontend/src/components/terminal/tabs/DemandTab.tsx`, `frontend/src/pages/terminal/F4MarketsView.tsx`.

-   **F4 Dashboard: Suggested Metrics + Metrics Legend + Ticker Integration (2026-04-02, Task #144):** Consolidated F4 dashboard UX improvements. **Suggested Metrics bar** rendered once in main F4 layout (above tab content), contextual geo IDs per tab (dashboard=tracked, browse=all markets, submarkets/properties=derived MSA IDs). Suggestions exclude metrics already in user's active grid columns. **Metrics Legend** (`renderMetricsLegend()`) with 13 items, toggled via `? LEGEND` button in F4 tab bar. **Ticker tape integration**: Top market movers (6 markets sorted by |Δ30d|) are emitted via `onTopMovers` callback to TerminalPage and displayed in the live ticker bar as `^ CITY·MF JEDI XX (+Y) CYCLE`. **Bottom panels removed**: Recent Alerts and Top Movers bottom panels deleted from F4 dashboard — alert data consolidated into existing JEDI Alerts system, mover data pushed to ticker tape. `MarketMover` interface exported from F4MarketsView. Dead `viewMarketsCorpHealthData` removed from TerminalPage. Files: `frontend/src/pages/terminal/F4MarketsView.tsx`, `frontend/src/pages/TerminalPage.tsx`.

-   **Research Agent Metric Recommendations (2026-03-30, Task #136):** AI-powered metric recommendation engine that analyzes correlation data to suggest which metrics users should add to their F4 Markets dashboard. **Backend**: `generateMetricRecommendations(marketGeoIds, topN)` in `CorrelationEngineService` — scores metrics by |r| strength (40pts), p-value significance (20pts), sample size (10pts), freshness (10pts), leading-indicator lag (5pts), trend momentum (+15% bonus), cross-market consistency bonus. Proper lead/lag direction per metric side (metric_a vs metric_b perspective inversion). Natural-language reason generation with geo name formatting (`city-state-state` → `City, ST`). Caching in `metric_recommendations` table (7-day TTL, batch key by sorted geo IDs). `MetricRecommendationAgent` registered in orchestrator for task-queue execution. Agent delegator wires metric recommendation queries (trigger phrases: "what metrics", "metrics to watch", "leading indicators", etc.) to `MetricRecommendationAgent` via intent question matching. **REST**: `POST /api/v1/correlations/recommendations` accepts `{marketGeoIds, topN}`, returns ranked recommendations with score, reason, correlationR, leadLagMonths, trendDirection, columnId for one-click add. **Frontend**: `useMetricRecommendations` hook in `useCorrelations.ts`. Collapsible "SUGGESTED METRICS" panel in F4 Dashboard with correlation r-value badges (green/amber/red by strength), trend direction arrows, lead/lag display, geo count, and "Add Column" button wired to column preferences. Files: `backend/src/services/correlationEngine.service.ts`, `backend/src/api/rest/correlation.routes.ts`, `backend/src/agents/metric-recommendation.agent.ts`, `backend/src/services/orchestrator/agent-delegator.ts`, `backend/src/db/migrations/20260330_002_metric_recommendations.sql`, `frontend/src/hooks/useCorrelations.ts`, `frontend/src/pages/terminal/F4MarketsView.tsx`.

-   **Background Correlation Engine (2026-03-30, Task #135):** Weekly background correlation sweeps, new query endpoints, F4 column header correlation indicators, and freshness tracking. **Backend**: `CorrelationEngineService` extended with `getTopCorrelations()` (filtered by targetMetric, minAbsR, limit), `getBatchCorrelations()` (multi-geography), `getFreshness()` (staleness per geography with 7-day threshold), `sweepAllGeographies()` (iterates all distinct geographies in metric_time_series). **M28 Scheduler**: Weekly cron (`0 3 * * 0`, Sundays 3AM ET) calls `sweepAllGeographies()` for full correlation refresh. **REST Endpoints**: `GET /api/v1/correlations/top?geoType=X&geoId=Y&targetMetric=Z&minAbsR=0.5&limit=10`, `POST /api/v1/correlations/batch` (accepts queries array, max 50), `GET /api/v1/correlations/freshness` (summary + per-geography staleness). **Frontend**: `useCorrelations.ts` hook — `useTopCorrelations(geoType, geoId, minAbsR)` fetches top correlations, `useColumnCorrelations(geoType, geoId)` maps column IDs to their strongest metric correlation. `COLUMN_TO_METRIC` mapping links F4 columns (rent, vac, popD, etc.) to metric_time_series IDs. F4MarketsView table headers show correlation badges (colored `r±X.X` superscript) when |r|≥0.7, with tooltip showing correlated metric and lag. Files: `backend/src/services/correlationEngine.service.ts`, `backend/src/services/m28-scheduler.service.ts`, `backend/src/api/rest/correlation.routes.ts`, `frontend/src/hooks/useCorrelations.ts`, `frontend/src/pages/terminal/F4MarketsView.tsx`.

-   **Live Metric Computation Pipeline for F4 Markets (2026-03-30, Task #134):** F4 Markets tables (Dashboard, Browse, Submarkets, Properties) now pull live computed metrics from the backend instead of hardcoded mock data. **Backend**: `MarketMetricsAggregator` service (`backend/src/services/market-metrics-aggregator.service.ts`) aggregates data from `available_markets`, `market_vitals`, `metric_time_series` (Zillow rent/home value indices), `rent_comps`, `property_records`, and `user_market_preferences` tables. Computes JEDI scores, cycle phases, vacancy, cap rates, demographic deltas. REST API at `/api/v1/market-metrics/` with 3 endpoints: `GET /markets` (MSA-level, user-aware starred markets), `GET /submarkets?msaId=X`, `GET /properties?msaId=X`. Routes: `backend/src/api/rest/market-metrics.routes.ts`. **M28 Scheduler**: Extended with 6-hour cron job for periodic market metrics refresh (`0 */6 * * *`). **Frontend**: `useMarketMetrics`, `useSubmarketMetrics`, `usePropertyMetrics` hooks (`frontend/src/hooks/useMarketMetrics.ts`) fetch from API. F4MarketsView uses `ALL_MARKETS_RESOLVED`, `SUBMARKET_RESOLVED`, `PROPERTY_RESOLVED` — live data when available, mock fallback when API returns empty (e.g. unauthenticated). LIVE indicator badge, REFRESH button, loading state on Dashboard/Browse filter bars. Files: `backend/src/services/market-metrics-aggregator.service.ts`, `backend/src/api/rest/market-metrics.routes.ts`, `backend/src/services/m28-scheduler.service.ts`, `frontend/src/hooks/useMarketMetrics.ts`, `frontend/src/pages/terminal/F4MarketsView.tsx`.

-   **Customizable Metric Columns for F4 Markets Views (2026-03-30):** Users can add/remove metric columns on all 5 F4 views (Dashboard, Browse, Submarkets, Properties, Compare) using the same global Metrics Catalog that powers the Strategies module. **Backend**: `user_column_preferences` table (UUID user_id, view_id, JSONB columns), REST API at `/api/v1/column-preferences/:viewId` (GET/PUT/DELETE + GET all). Default column sets per view. Migration: `20260330_001_column_preferences.sql`. Routes: `backend/src/api/rest/column-preferences.routes.ts`. **Frontend**: Column Registry (`frontend/src/config/columnRegistry.ts`) — 50+ column definitions across 12 categories (identity, score, rent, occupancy, supply, demand, financial, demographic, traffic, competition, risk, cycle), each with label, width, sortable flag, category, and optional `catalogMetricId` linking to the Metrics Catalog. `useColumnPreferences` hook (`frontend/src/hooks/useColumnPreferences.ts`) — fetches/saves per-view column prefs with API persistence + localStorage fallback, provides `toggleColumn`, `reorderColumns`, `resetToDefaults`. `ColumnPicker` component (`frontend/src/components/terminal/ColumnPicker.tsx`) — slide-out panel with search, category-grouped metric list, checkboxes, drag-to-reorder active columns, reset button, CUSTOM/DEFAULT indicator. Integrated into `F4MarketsView.tsx`: ⚙ COLUMNS gear button on Dashboard, Browse, Submarkets, Properties filter bars; all tables render dynamically from column preferences. Files: `frontend/src/config/columnRegistry.ts`, `frontend/src/hooks/useColumnPreferences.ts`, `frontend/src/components/terminal/ColumnPicker.tsx`, `frontend/src/pages/terminal/F4MarketsView.tsx`, `backend/src/api/rest/column-preferences.routes.ts`, `backend/src/db/migrations/20260330_001_column_preferences.sql`.

-   **F3 Market Intelligence Module Restructure (2026-04-03):** Flattened UnitMixIntelligence inner tabs into F3's parent navigation with deal-type-aware tab sets. **Deal modes**: `development` (OVERVIEW|DEMAND|COMPS|PROGRAM|TRENDS), `redevelopment` (OVERVIEW|POSITIONING|COMPS|REPOSITIONING|TRENDS), `existing` (OVERVIEW|POSITIONING|COMPS|TRENDS). `resolveDealMode()` maps dealType strings to modes. Unit mix state (program, zoning, comps, trends) lifted from UnitMixIntelligence root into MarketIntelligencePage for cross-tab access. **New components**: `ZoningContextBar` (zoning linkage indicator), `ProgramRationale` (AI-synthesized rationale panel — headline, signal cards with impact coding, recommendation narrative), `PositioningScorecard` (existing/redev competitive positioning table with vacancy, DOM, demand scores, gap analysis per unit type), `RepositioningPanel` (current→target conversion matrix with mix delta, rent uplift, EXPAND/REDUCE/HOLD actions, one-click apply). **Enhanced `computeOptimalProgram`**: Now accepts optional `{zoning, demandScores}` — adds leasing velocity scoring (vacancy penalty + DOM penalty weighted 60/40), zoning envelope constraint (caps at maxUnits, adjusts SF when exceeding budget), gap-based rent premium (+5% for undersupplied types), mix floor/ceiling (5-55% per type). Signal weighting rebalanced: demand 35%, rent/SF 25%, gap 20%, velocity 20%. **`buildProgramRationale()`**: Analyzes market data + gaps + comps + zoning → generates headline, signal cards (supply gap, oversupply risk, demand strength, occupancy, economy health, zoning envelope, pipeline pressure), and deal-mode-specific recommendation narrative. **Exports from UnitMixIntelligence.tsx**: All sub-components (DemandMatrix, GapAnalysis, ProgramEditor, ZoningPanel, InventorySnapshot, PropertyDrillDown, TrendDetail, MixMatrix, RentSFScatter, CompTable), utility functions, constants, types, and UMC palette. Files: `frontend/src/pages/development/MarketIntelligencePage.tsx`, `frontend/src/components/deal/sections/UnitMixIntelligence.tsx`.

-   **F8 Financial Engine v3.1 — 9-Tab Rebuild (2026-04-03):** Complete restructure of the Financial Engine (F8) into 9 institutional-quality tabs following Bloomberg Terminal conventions. **Architecture**: `FinancialEnginePage.tsx` is now a thin shell with SubTabBar, version selector dropdown, KPI header bar (IRR/EM/CoC/DSCR), SAVE VERSION dialog, EXPORT XLSX button, and BUILD MODEL trigger. All tab content lives in `frontend/src/pages/development/financial-engine/`. **9 Tabs**: (0) **Overview** — Sources & Uses table, Returns breakdown, 3-Layer comparison model (Broker/Platform/You) with divergence detection, AI insights panel; fetches platform data from strategy analyses API. (1) **Pro Forma Summary** — 12-line condensed P&L (GPR→Vacancy→EGR→OpEx→NOI→DS→CF), hold-period annual columns, Bloomberg heatmap coloring. (2) **Projections** — Full 40-60 row institutional statement with 7 sections (Revenue, Vacancy/Loss, Expenses, Debt, Capital, Waterfall, Returns), timeline selector (3/5/7/10yr), monthly toggle. (3) **Assumptions** — All model inputs organized by section (Acquisition, Revenue, Financing, CapEx, Disposition, Waterfall) with source badges (BROKER blue/PLATFORM teal/USER amber/AGENT violet/CAPSULE green), deal-type variants (existing/development/redevelopment). (4) **Debt** — Multi-loan comparison matrix (up to 3 side-by-side), amortization schedule with IO/amortizing shading, click-to-select active loan. (5) **Waterfall** — LP/GP equity distribution tiers with pref return, promote visualization (SVG stacked bar), tier detail table. (6) **Sensitivity** — Two heatmap grids: IRR×ExitCap×RentGrowth and EM×ExitCap×HoldPeriod, computed via `computeExitReturns` from shared calculations. (7) **Decision** — AI rationale with confidence score, risk flags (severity-coded), recommended actions with impact ratings. (8) **Compare** — Side-by-side version diff with metric deltas, color-coded improvements/deteriorations, multi-version selector. **Version System**: Save/load model versions with name, timestamp, source attribution (user/agent), assumption+results snapshots; API calls to `/api/v1/financial-model/:dealId/versions`. **Excel Export**: Real XLSX workbook generation via SheetJS `xlsx` library with 7 sheets (Summary, Sources & Uses, Assumptions, Cash Flows, Returns, Unit Mix, Waterfall). **Types**: Shared `types.ts` with `ModelAssumptions`, `ModelResults`, `AnnualCashFlowRow`, `DealType`, formatting utils (`fmt$`, `fmtPct`, `fmtX`). Files: `frontend/src/pages/development/FinancialEnginePage.tsx`, `frontend/src/pages/development/financial-engine/` (OverviewTab, ProFormaSummaryTab, ProjectionsTab, AssumptionsTab, DebtTab, WaterfallTab, SensitivityTab, DecisionTab, CompareTab, excel-export.ts, types.ts, index.ts).

-   **Agent Financial Data Stream Wiring (2026-04-02):** All AI agents now receive real deal financial data with provenance metadata. **Central service**: `deal-financial-context.service.ts` — `getDealFinancialContext(dealId)` assembles T12 NOI/revenue/expenses, rent roll summary, balance sheet, CapEx totals, debt summary from all 6 provenance tables. `formatFinancialContextForPrompt()` formats for LLM injection. **agent-chat.service.ts**: `buildSystemPrompt()` injects financial data; agents cite data sources. **agent-delegator.ts**: `delegate()` fetches financial context when dealId present, passes to all specialist+analyst agents (CFO, Developer, Lender, etc.). **CashFlowAgent**: Pulls purchase price from assumptions, rent from rent roll, rates from debt schedule, vacancy from assumptions. Returns T12 actuals, existing debt, DSCR, cap rate. Reports data sources used. **OpusAISection.tsx**: `loadLiveContext()` fetches financial-documents data-sources and assumptions; uses live financial/debt data. **New endpoint**: `GET /:dealId/financial-context`. Files: `backend/src/services/deal-financial-context.service.ts`, `backend/src/services/agent-chat.service.ts`, `backend/src/services/orchestrator/agent-delegator.ts`, `backend/src/agents/cashflow.agent.ts`, `frontend/src/components/deal/sections/OpusAISection.tsx`.

-   **Data Flywheel Wiring — Deal Assumptions → Zoning Modules (2026-04-02):** Connected the `useDealAssumptions` hook to the Zoning Module so cost data flows from the database into all capacity calculations instead of using hardcoded constants. **ZoningModuleSection** now calls `useDealAssumptions(dealId)` on mount and passes `hardCostPsf` to `DevelopmentCapacityTab` via `costPerSf` prop. **DevelopmentCapacityTab** accepts `costPerSf` prop, passes it through `generatePathScenarios()` → `calculateEnvelope()` where it replaces the previously hardcoded `$185/SF`. Includes a dedicated `useEffect` that recomputes path scenarios when `propCostPerSf` changes (avoids stale closure in the main `loadData` callback). Uses `Number.isFinite()` guard for safe fallback. **Data flow**: User enters cost data in any module → persists to `deal_assumptions` table → `GET /api/v1/deals/:dealId/assumptions` → `useDealAssumptions` hook → `assumptions.hard_cost_psf` → flows into Zoning capacity calculations, construction cost estimates, and path comparisons. Falls back to `$185/SF` default when no deal-specific data exists. **Flywheel effect**: More deals with real financial data → more accurate cost estimates across all modules; archived deals retain data for market-level benchmarking. Files: `frontend/src/hooks/useDealAssumptions.ts`, `frontend/src/components/deal/sections/ZoningModuleSection.tsx`, `frontend/src/components/zoning/tabs/DevelopmentCapacityTab.tsx`.

-   **Data Provenance & Financial Document Infrastructure (2026-04-02):** Full data provenance layer and new financial document storage tables for reporting package ingestion. **Provenance columns** added to `deal_monthly_actuals` (`source_document_type`, `source_period_label`), `deal_lease_transactions` (`source_type`, `source_ref`, `source_date` + enhanced fields: `lease_end`, `move_in_date`, `move_out_date`, `tenant_name`, `effective_rent`, `concession_amount`, `lease_status`), `deal_assumptions` (`source_type`, `source_ref`, `source_date`). **New tables**: `deal_balance_sheets` (per-month balance sheet data — assets, liabilities, equity, reserves, escrows with full line-item detail), `deal_capex_items` (CapEx tracking — category, vendor, budgeted/actual/remaining, completion %, invoice ref, status), `deal_debt_schedule` (loan tracking — lender, amount, rate, type, maturity, amortization, DSCR, LTV, debt yield, prepayment terms, covenants). All tables include provenance columns (`source_type`, `source_ref`, `source_date`). **Source types**: `upload`, `manual`, `ai_computed`, `market_default`, `market_comps`, `archived_deal`, `template`, `csv_upload`. **Backend API**: `financial-documents.routes.ts` — CRUD endpoints for balance sheets (`GET/POST /:dealId/balance-sheets`), CapEx items (`GET/POST/PUT /:dealId/capex-items/:itemId`), debt schedule (`GET/POST /:dealId/debt-schedule`), data sources summary (`GET /:dealId/data-sources`). Balance sheets upsert by deal+month. CapEx/debt endpoints include computed summaries (totals, weighted avg rate, nearest maturity). **Upload parsers updated**: `uploadService.ts` and `data-upload.service.ts` now populate `sourceDocumentType` (income_statement/budget) and `sourcePeriodLabel` (e.g. "Mar 2026") on every row inserted. `deal-assumptions.routes.ts` PUT now accepts and stores `sourceType`, `sourceRef`, `sourceDate`. **Frontend**: `DataSourceBadge` component (`frontend/src/components/ui/DataSourceBadge.tsx`) — inline badge with colored dot, label, and source reference. Supports compact/full modes. Source colors: green (upload), amber (manual), purple (AI), gray (market default), cyan (market comps). `DataSourceIndicator` dot-only variant. Wired into `ZoningModuleSection` panel header. `useDataSources` hook (`frontend/src/hooks/useDataSources.ts`) — fetches provenance summary for a deal across all tables. **Document types supported for ingestion**: P&L/Income Statement (existing), Budget (existing via `isBudget` flag), Rent Roll (existing `lease-rent-ingest.service.ts`), Weekly Traffic Reports (existing), Balance Sheet (new table), CapEx Reports (new table), Debt Schedule (new table). **Remaining**: Insurance summary parser, tax assessment parser, budget-vs-actual comparison view. Migration: `20260402_005_data_provenance_tables.sql`. Files: `backend/src/db/schema/dataPipeline.ts`, `backend/src/api/rest/financial-documents.routes.ts`, `backend/src/api/rest/deal-assumptions.routes.ts`, `backend/src/services/uploadService.ts`, `backend/src/services/data-upload.service.ts`, `frontend/src/components/ui/DataSourceBadge.tsx`, `frontend/src/hooks/useDataSources.ts`, `frontend/src/components/deal/sections/ZoningModuleSection.tsx`.

-   **Property Details Page v3.0 (2026-03-29):** Full Bloomberg Terminal-style property intelligence view at `/property-card/:id` and `/terminal/property/:id`. 2190-line component (`PropertyCardPage.tsx`) with 8 F-key tabs: OVERVIEW (photo gallery, property vitals, performance snapshot with sparklines, traffic intelligence score rings, 3-level market position table, unit mix, rent comps), FINANCIALS (5-year income & expense, valuation indicators, rent roll summary, Create Deal CTA), COMPS (rent comps with unit-type drill-down, sale comps), TAX & TITLE (assessment, tax history with bar chart, ownership chain), ZONING (designation, setbacks, parking analysis, density headroom), MARKET (10-year vacancy/rent growth/supply/absorption/employment/demographics/demand score charts with forecast bands), TRAFFIC (M07 engine: AADT history+forecast, hourly distribution, seasonal factors, Layer 1-3 breakdowns, digital signals, quadrant badge), PERFORMANCE (indexed multi-line chart with toggleable series, correlation engine insights, 18-year data table). Features: F1-F8 keyboard navigation, responsive charts via ResizeObserver, JEDI score ring, Create Deal modal, SVG line/bar charts with confidence bands, forecast zone shading. Mock data for Westshore Commons (Tampa, FL). Files: `frontend/src/pages/PropertyCardPage.tsx`.

-   **Layout Migration — MainLayout → Terminal (2026-03-28, Task #132):** Migrated ~30 routes off the old 3-ticker `MainLayout` shell. **Standalone pages** (unique content, no layout chrome): `/news`, `/deals`, `/properties`, `/properties/:id`, `/capsules`, `/capsules/:id`, `/leasing-forecast/:propertyId`, `/assets-owned/:dealId/property`, `/property-card`, `/property-card/:id`, `/market-intelligence/property/:id`. **Deal workflow pages** (own internal nav): `/deals/create`, `/deals/:dealId/detail`, `/deals/:dealId/flywheel`, `/deals/:dealId/design`. **Redirected to terminal**: all `/settings/*` → `/terminal/settings`, `/strategies` → `/terminal/strategies`, `/opportunities` → `/terminal/strategies`, `/map` → `/terminal/dashboard`, `/dashboard/contents` → `/terminal/dashboard`. **MainLayout now only wraps**: `/admin/*`, `/architecture`, `/demo/*`. Unused imports removed from `App.tsx`. Files: `frontend/src/App.tsx`.

-   **Commentary + Strategy Integration — Full Build (2026-03-27, Task #128):** Full-stack commentary and strategy scoring integration across MSA, Submarket, and Property views. **Backend**: `CommentaryAgent` in `backend/src/agents/commentary.agent.ts` — follows ResearchAgent pattern with 24hr cache TTL via `agent_tasks` table, generates MarketNarrative, InvestmentThesis (with pass/warn/fail points + recommendation), SignalCommentary (per signal), RiskOpportunity, PeerContext, SupplyNarrative, plus strategy scores from `strategyArbitrageEngine`. JEDI Score computed with D30/S25/M20/P15/R10 weights. Registered in `orchestrator.ts` as `commentary_generation`. REST endpoints in `index.replit.ts`: `GET /api/v1/commentary/:entityType/:entityId` (msa/submarket/property, with forceRefresh + entityName query params), `POST /api/v1/strategy-scoring/analyze` (dealId + optional signals + envelope for M08 arbitrage). **Frontend Store**: `commentaryStore.ts` (Zustand) — 24hr client-side cache with per-entity loading/error states, `fetchCommentary()`, `getCommentary()`, `isLoading()`, `getError()`, `clearCache()`. **6 Reusable Panel Components** in `frontend/src/components/terminal/commentary/`: `MarketNarrative` (sentiment badge, narrative text), `InvestmentThesis` (bulleted pass/warn/fail items + recommendation badge), `SignalCommentary` (signal-colored left border + sentiment badge), `RiskOpportunity` (severity-coded risks + impact-coded opportunities), `PeerContext` (peer ranking with score bars), `SupplyNarrative` (sentiment-colored supply analysis), `StrategyScoreBadge` (score/100 display with delta badge, sm/md/lg sizes). **MSA Integration**: MSAOverviewTab gets commentary section at bottom (MarketNarrative + InvestmentThesis + JEDI Score card), MSACommentaryTab right sidebar upgraded to use live panel components with fallback to static data. **Submarket Integration**: SubmarketCommentaryTab right sidebar upgraded with live panel components (MarketNarrative, InvestmentThesis, StrategyScoreBadge, RiskOpportunity, PeerContext, SupplyNarrative) with static fallback. **Property Integration**: BloombergPropertyCard gets optional `strategyScore` prop — renders color-coded score badge (green ≥75, amber ≥55, red <55) with ARB flag in header ticker bar. Files: `backend/src/agents/commentary.agent.ts`, `backend/src/agents/orchestrator.ts`, `backend/src/index.replit.ts`, `frontend/src/stores/commentaryStore.ts`, `frontend/src/components/terminal/commentary/*.tsx`, `frontend/src/components/terminal/tabs/msa/MSAOverviewTab.tsx`, `frontend/src/components/terminal/tabs/msa/MSACommentaryTab.tsx`, `frontend/src/components/terminal/tabs/submarket/SubmarketCommentaryTab.tsx`, `frontend/src/components/terminal/BloombergPropertyCard.tsx`.

-   **MSA Terminal Tabs — Unified Bloomberg Design Consistency (2026-03-27, Task #123):** Standardized visual design across all 12 MSA terminal tabs (Overview, Submarkets, Deals, Rankings, Properties, Owners, Supply, Capital, Economics, Trends, News, Compare). **theme.ts updates**: Added missing BT tokens (`bg.card`, `bg.cardHover`, `text.violet`, `text.magenta`), added missing `terminalStyles` keys (`sectionTitle`, `tableHeader`, `tableCell`, `panel`) that tabs were referencing but didn't exist (spreading `undefined`), changed `card.borderRadius` from `'8px'` to `0` for Bloomberg sharp aesthetic. **All 12 tab files**: Replaced all `borderRadius` values (3, 4, 6, 8, 10px) with `0` for sharp corners — only exception is `'50%'` for intentionally circular rank badges. Cards, buttons, badges, filters, progress bars, legend swatches, expanded rows — all now sharp-cornered. Files: `frontend/src/components/terminal/theme.ts`, all files in `frontend/src/components/terminal/tabs/msa/`.

-   **Bloomberg Terminal Shell (2026-03-16):** MainLayout.tsx fully transformed from white SaaS sidebar to Bloomberg Terminal chrome. Sidebar removed entirely. New layout: 24px top status bar (JEDI RE gradient logo, context label PORTFOLIO/deal address, agent count with green pulse dot, email count, Kafka throughput, 24h amber clock), 18px ticker bar (auto-scrolling CSS keyframes from deals API), 28px F-key nav bar (F1-F9 portfolio or F1-F12 deal context, amber active state), 32px deal context bar (amber-tinted, shows deal address + JEDI score + delta + pipeline/strategy badges — appears only inside /deals/), full-bleed content area, 180px collapsible bottom panel (3 tabs: ALERTS with severity-colored borders, NEWS with time/impact badges, AGENTS in 3-column grid — all polled every 30s from /jedi/alerts, /news/feed, /agent-status). CommandPanel restyled: dark bg (T.bg.panel), amber accents, mono font, amber hover states, dark input with amber focus border. All components use T design tokens from terminal-tokens.ts. Files: `frontend/src/components/layout/MainLayout.tsx`, `frontend/src/components/layout/CommandPanel.tsx`.

-   **F5 Strategy Intelligence Expansion — 5-Tab Module (2026-04-04, Task #149):** Expanded F5 Strategy Arbitrage page from 2 tabs to 5 tabs: ARBITRAGE (existing, unchanged), SIGNAL MATRIX, DRIVERS, LEAD-LAG, CUSTOM SCREENS. **SIGNAL MATRIX tab**: Fetches `GET /api/v1/correlations/report?city=X&state=Y` using deal context city/state from `useDealStore`. Renders COR-01–20 grouped by tier (Tier 1 Money Correlations, Tier 2 Supply-Demand Equilibrium, Tier 3 Predictive & Economic, Tier 4 Competitive). Summary KPI strip (bullish/bearish/neutral/insufficient counts), callout panel (rentRunway, affordabilityCeiling, supplyPressure, topOpportunity). Per-row: ID, name, signal (color-coded bullish/bearish/neutral), confidence badge, R value, lead time. Response validation guards. **DRIVERS tab**: Fetches `GET /api/v1/driver-analysis/results/:dealId` sorted by |R|. KPI strip (driver count, top |R|, avg R-sq, significant count). Table: driver metric, outcome metric, Pearson R (color-coded by strength), R-sq, p-value (significance indicator), optimal lag weeks, direction (+/-). Alternating row backgrounds. **LEAD-LAG tab**: API-backed via `GET /api/v1/lead-lag/results` with catalog fallback (8 metrics, 16 chains from METRICS_CATALOG leadsMetrics). KPI strip (chains, unique leaders, unique laggers, avg |R|). Table: leader (source), lagger (target), lag months, R value, category badge. Source indicator (API vs catalog fallback). **CUSTOM SCREENS tab**: Restyled from Tailwind/emojis to full BT2 inline styles. Removed all Tailwind classes, emoji characters, border-radius. Uses BT tokens, MONO font, Bd badges, PASS/FAIL text labels, score coloring, BT panel headers. Strategy cards with expand/collapse condition results. CTA button restyled to BT2. Files: `frontend/src/pages/development/StrategyArbitragePage.tsx`, `frontend/src/components/deal/sections/CustomScreenTab.tsx`.

-   **M08 Strategy Arbitrage Deal UI — Task #81 (2026-03-20):** `StrategySection.tsx` fully wired to live M08 backend. All mock data imports from `enhancedStrategyMockData.ts` and `strategyMockData.ts` removed. Three new M08 live sub-components added: `M08ArbitrageBanner` (amber/gold banner shown when `arbitrage_detected=true`, surfaces winning vs runner-up with F24 delta), `M08ScoreMatrix` (N-column grid with gate badges, sub-score bars, confidence, recommended crown, gated strategies section), `M08SignalHeatmap` (5×N color-coded table with intensity legend), `M08ScoreChart` (Recharts BarChart comparing composite scores, green/red by gate result). `dealStore.ts` extended with `strategyScores: M08StrategyScore[]`, `arbitrageResult: M08ArbitrageResult | null`, `strategyScoresLoading: boolean` state slices + `fetchStrategyScores`, `recalculateStrategyScores`, `fetchArbitrage` actions. Existing `useStrategyArbitrage` hook updated to expose all M08 slices. On mount: fetches scores + arbitrage; auto-recalculates if no scores exist. Signals tab sub-tabs now use BT2_PURPLE `#A78BFA` active indicator (Bloomberg theme preserved). Static data constants replace former mock imports for overview/returns tabs. Bloomberg terminal header (dark `#0A0E17` bg, purple `#A78BFA` top border) unchanged. Files: `frontend/src/stores/dealStore.ts`, `frontend/src/components/deal/sections/StrategySection.tsx`.

-   **Bloomberg Terminal UI (2026-03-16):** `TerminalPage.tsx` graduated from prototype. Full-page component (no MainLayout). Routes: `/terminal` is the new home — `/`, `/dashboard`, and `*` wildcard all redirect to `/terminal`. Architecture: DARK/LIGHT token system, CSS keyframes (blink/glow/ticker/fadeIn/pulse), 36px top bar (logo, clock, agent count, theme toggle), 27px ticker scrolling real estate metrics, 50px KPI bar (live pipeline value from deals API, active deals, alerts), 30px F-key nav (F1–F9), 32px filter toolbar (stage/strategy/map toggle), 190px bottom panel (Alerts/News/Email/Agents/Tasks/Media tabs), 16px status bar. F-key views: F1 Dashboard (20-widget system with add/remove/resize/float/dock/drag-to-reorder, localStorage persistence), F2 Pipeline (live deals from `GET /api/v1/deals` via `mapApiDealToLive()`, click→navigate to `/deals/:id`), F3 Portfolio (static placeholder), F4 Markets (SUBMARKETS table + MARKET_VITALS cards), F5 Email (terminal email UI with folder nav), F6 Compete, F7 Strategies (navigates to `/strategy-builder`), F8 Org Tools (tasks/reports/team), F9 Org Settings (live org member list, invite-by-email, role management, pending invitations — wired to `/api/v1/orgs/*` endpoints). Live data: deals + tasks fetched on mount; alerts/news/agents use static fallbacks. Widget catalog: 21 widgets across DEALS/INTEL/MARKET/OPS/MEDIA categories. **Live TV, News & Social Feed (Task #64):** MEDIA tab in bottom panel with 3 sections — Live TV (4 YouTube Live channels: CNBC, Bloomberg TV, Yahoo Finance, Fox Business), News Feeds (5 RSS sources: CoStar, Globe St, Bisnow, The Real Deal, Housing Wire via backend proxy), Social/X (3 configurable handles/hashtags). Media floating windows are a global overlay (separate from F1 dashboard windows) with full drag/resize/minimize/maximize/z-index management. Windows indicator badge in top status bar with dropdown list + close-all. Backend: `GET /api/media/rss?url=<encoded>` proxy with 5-min cache, hostname allowlist, redirect rejection, content-type validation, and 2MB size cap. Files: `frontend/src/pages/TerminalPage.tsx` (~1900 lines), `frontend/src/App.tsx` (routing), `backend/src/api/rest/media.routes.ts`.

-   **M33 Corporate Health Intelligence Engine (2026-03-16):** Submarket employer health analysis linking corporate financial health to real estate demand. **DB**: Migration 136 — 6 tables (`submarket_employers`, `corporate_financials`, `corporate_stock_prices`, `corporate_health_scores`, `submarket_corporate_health`, `corporate_facility_events`) with INTEGER FKs for `submarket_id`. **Backend Services**: (1) `yahoo-finance.service.ts` — stock price/momentum fetching via yahoo-finance2, (2) `sec-edgar.service.ts` — SEC EDGAR XBRL financial data extraction (revenue, assets, debt, employees, margins), (3) `earnings-transcript-nlp.service.ts` — Claude-powered earnings call NLP for guidance extraction (growth/hiring/expansion/restructuring signals with sentiment/confidence), (4) `employer-concentration.service.ts` — Herfindahl index, top employer share, sector breakdown for submarket employer concentration, (5) `corporate-health.service.ts` — CHS scoring engine (40% financial + 30% stock + 30% guidance), SCHI weighted aggregation, RE_HEALTH from occupancy/cap_rate/rent_growth/absorption, divergence = SCHI - RE_HEALTH with ±15 threshold for bullish/bearish signals. **API**: 8 endpoints under `/api/v1/corporate-health/` — `GET /submarket/:id` (SCHI + employers + trend), `GET /company/:ticker` (CHS + financials + stock), `GET /deal/:dealId` (weighted SCHI across deal submarkets), `GET /sector-rotation`, `GET /alerts` (CHS tier changes + divergence alerts), `POST /refresh/:ticker`, `GET /concentration/:submarketId`, `POST /transcript/:ticker` (NLP analysis). Registered in `index.replit.ts` with `requireAuth`. **Frontend**: Zustand `corporateHealthStore.ts` + `corporateHealthAPI` in api.client.ts. F4 Markets tab system: "MARKET OVERVIEW" (original) + "CORPORATE HEALTH" tab with SCHI/RE_HEALTH/Divergence/Herfindahl metric cards, top employers grid (company/ticker/employees/share/CHS/tier/sector/momentum), sector concentration bars, stress alerts panel. **JEDI Score Integration**: `getCorporateHealthAdjustment()` in jedi-score.service.ts — demand adjustment (SCHI≥70: +2, SCHI<40: -3, divergence>15: ±1.5/−2) and risk adjustment (HHI>0.25: +5, top_share>30: +3, SCHI<40: +4). `getDealInfo` updated to pull `p.submarket_id`. Files: `backend/src/db/migrations/136_corporate_health_tables.sql`, `backend/src/services/corporate-health.service.ts`, `backend/src/services/yahoo-finance.service.ts`, `backend/src/services/sec-edgar.service.ts`, `backend/src/services/earnings-transcript-nlp.service.ts`, `backend/src/services/employer-concentration.service.ts`, `backend/src/api/rest/corporate-health.routes.ts`, `frontend/src/store/corporateHealthStore.ts`, `frontend/src/services/api.client.ts`, `frontend/src/pages/TerminalPage.tsx`, `backend/src/services/jedi-score.service.ts`.
-   **M31 Closing Execution — Remote Online Notarization (2026-03-16):** RON integration for deal closings via Notarize.com provider abstraction. DB: `notarize_sessions`, `notarize_signers`, `notarize_webhooks` tables (migration 134) with indexes on deal_id, status, provider, session_id, email. Provider interface: `NotarizeProvider` abstraction (`provider.interface.ts`) with `createSession`, `getSessionStatus`, `cancelSession`, `downloadCertificate`, `verifyWebhookSignature`. Notarize.com adapter (`notarize-com.adapter.ts`) implements REST calls with HMAC-SHA256 webhook verification. Service: `notarize.service.ts` — `initiateClosing` (validates deal/docs, creates provider session, inserts DB records, logs activity), `getSessionStatus` (live provider refresh with signer sync), `cancelSession`, `getCertificate`, `handleWebhook` (processes signer.verified, signer.completed, session.completed, session.failed events; session.completed auto-advances pipeline_stage to 'post_close'), `getSessionHistory`. API: 6 endpoints — `POST /api/v1/deals/:dealId/notarize` (initiate, requires admin/edit), `GET .../status` (live status with provider refresh), `GET .../history`, `POST .../cancel`, `GET .../certificate`, `POST /api/v1/webhooks/notarize` (HMAC-verified webhook receiver). Activity log: 5 new action types (notarize_initiated, notarize_completed, notarize_cancelled, notarize_failed, pipeline_advanced). Frontend: `NotarizeClosingSection.tsx` — status card (documents/signers/status metrics), signer list with KBA/ID verification badges, initiate modal (document selector from deal files + signer form with add/remove), cancel/download certificate actions. Wired into ExecutionScreen (F11) as "Closing (RON)" tab in DealDetailPage. Files: `backend/src/db/migrations/134_notarize_ron_tables.sql`, `backend/src/services/notarize/provider.interface.ts`, `backend/src/services/notarize/notarize-com.adapter.ts`, `backend/src/services/notarize/notarize.service.ts`, `backend/src/api/rest/notarize.routes.ts`, `frontend/src/components/deal/sections/NotarizeClosingSection.tsx`.
-   **M29 Org Foundation & RBAC (2026-03-16):** Multi-tenant organization layer. DB: `organizations`, `org_members`, `org_invitations` tables + `org_id` FK on `deals` (migration 131). Backfill: auto-creates personal org per existing user, assigns all deals. Roles: Owner/Principal/Analyst/Viewer with hierarchical permissions. RBAC middleware: `requireOrgRole()`, `requireOrgRoleForOrg()`, `requireDealPermission()` in `backend/src/middleware/rbac.ts`. API: `POST /api/v1/orgs` (create), `GET /api/v1/orgs/mine` (list user's orgs), `GET /:orgId/members`, `POST /:orgId/invitations` (invite by email with 7-day token), `POST /invitations/:token/accept`, `PUT /:orgId/members/:id/role`, `DELETE /:orgId/members/:id`. Routes in `backend/src/api/rest/org.routes.ts`, registered at `/api/v1/orgs`. Drizzle schema in `backend/src/db/schema/org.ts`. Frontend: F9 in TerminalPage shows ViewOrgSettings — member list with role badges, invite-by-email form, pending invitations panel, org info card.
-   **M29 Phase 2 — Deal Collaboration Layer (2026-03-16):** Per-deal collaboration with granular permissions. DB: Extended `deal_team_members` with `permission_level` (view/comment/edit/admin), `deal_team_comments` with `module_anchor`, `resolved_at`, `resolved_by`. New `activity_log` table for unified cross-deal/org events (migration 132). API: `GET/POST/PUT/DELETE /api/v1/deals/:dealId/collaborators` (CRUD with permission checks), `GET/POST /api/v1/deals/:dealId/comments` (threaded, module-anchored, filterable by module_anchor, include_resolved), `POST /deals/:dealId/comments/:id/resolve`, `GET /api/v1/deals/:dealId/activity` (paginated), `GET /api/v1/orgs/:orgId/activity` (org-level rollup). Activity log service: `logActivity()` in `backend/src/services/activity-log.service.ts`. Socket.io: `deal:{dealId}:presence` (join/leave/module_change), `deal:{dealId}:updates` (field changes), `deal:{dealId}:comments` (new/resolved). In-memory presence tracking in collaboration.handler.ts. Frontend: `PresenceIndicator` (avatar dots in DealDetailPage header), `DealTeamPanel` (collaborator list with permission dropdowns, add form), `ActivityFeed` (timestamped events with pagination, compact mode for sidebar), `CommentThread` (threaded comments with resolve, module-anchored, real-time via socket). DealDetailPage: PresenceIndicator in header, ActivityFeed in sidebar, new "Collaborate" tab in Overview screen with DealTeamPanel + CommentThread + ActivityFeed. Files: `backend/src/db/migrations/132_deal_collaboration.sql`, `backend/src/api/rest/collaboration.routes.ts`, `backend/src/services/activity-log.service.ts`, `backend/src/api/websocket/handlers/collaboration.handler.ts`, `frontend/src/components/deal/PresenceIndicator.tsx`, `frontend/src/components/deal/DealTeamPanel.tsx`, `frontend/src/components/deal/ActivityFeed.tsx`, `frontend/src/components/deal/CommentThread.tsx`.
-   **DOT AADT Multi-State Historical Data Ingestion (2026-03-16, Task #84):** Automated pipeline to fetch, ingest, and aggregate Annual Average Daily Traffic (AADT) data from state DOT portals into `adt_counts` and `metric_time_series`. **FDOT fetcher**: Pulls from `gis.fdot.gov/arcgis/rest/services/FTO/fto_PROD/MapServer/7` — the real FDOT ArcGIS endpoint with multi-year AADT data (2015–2024). Handles polyline geometry (road segments, extracts midpoint), paginates 1000-record batches, deduplicates by station_id+year. **State configs**: FDOT (verified, working), GDOT/TxDOT/NCDOT (configured, endpoints need verification). **Aggregator**: Spatial bounding box approach maps stations to MSA geographies (12 FL MSAs defined), computes median AADT per MSA per year, writes to `metric_time_series` as `T_AADT` (annual) and `T_AADT_YOY` (year-over-year %). **DB**: `idx_adt_station_year` unique index added on `adt_counts(station_id, measurement_year)` for upsert support. **Admin endpoints**: `POST /api/v1/admin/dot-ingest` (single state+year), `POST /api/v1/admin/dot-ingest/batch` (year range), `POST /api/v1/admin/dot-ingest/aggregate` (re-aggregate), `GET /api/v1/admin/dot-ingest/status`. **AADT history endpoint**: `GET /api/v1/metrics/aadt-history?geoId=X&geoType=msa` — returns full AADT time series + YoY + statistics for a geography. **Backfill script**: `backend/src/scripts/backfill-dot-aadt.ts` — CLI tool with `--states`, `--start-year`, `--end-year`, `--skip-fetch`, `--skip-aggregate` options. **Verified data**: FL 2020–2023 ingested (74,533 records, 18,839 unique stations), all 12 FL MSAs aggregated with AADT + YoY values. Files: `backend/src/services/dot-fetcher.service.ts`, `backend/src/services/dot-aggregator.service.ts`, `backend/src/api/rest/dot-admin.routes.ts`, `backend/src/scripts/backfill-dot-aadt.ts`, `backend/src/api/rest/metrics-catalog.routes.ts`.
-   **JEDI Score Engine:** A 5-signal scoring system (0-100) with demand integration and alerts.
-   **Map Layer System:** Photoshop-like layer architecture supporting five types with drag-and-drop reordering.
-   **Deal Capsule Module:** A 6-group tab navigation for comprehensive deal analysis. DealDetailPage branded as "Deal Capsule" with header badge, sidebar footer, and search placeholder. CapsuleDetailPage redirects to `/deals/:id/detail?tab=overview`. **Module Completeness (2026-03-17):** (1) **Absorption Schedule** sub-tab added to Traffic module (M07/F10) — `AbsorptionScheduleTab.tsx` in `frontend/src/components/deal/sections/traffic/`. Three deal-type variants: Development (lease-up velocity from CO to stabilization), Redevelopment (displacement curve + re-leasing), Existing (occupancy recovery model, auto-shown when occupancy < 92%). SVG occupancy ramp chart, monthly absorption detail table with unit-type breakdown, editable assumptions (velocity, target occupancy, ramp-up months, unit mix). (2) **Concessions section** added to Pro Forma (M09/F6) — `ConcessionsSection` component in `ProFormaTab.tsx`. Inputs: free rent weeks, % of units receiving, concession duration, ongoing toggle. Calculates concession cost per unit, total cost, % of GPR, and shows GPR → Less Concessions → EGI summary. Included in financial model payload. (3) **Environmental & Physical DD section** added to Due Diligence page — `EnvironmentalPhysicalDDSection` component with deal-type-specific checklists (Existing: Phase I ESA + physical inspection + ADA + insurance; Development: Phase I/II ESA + geotech + wetlands + utilities + traffic; Redevelopment: Phase I/II ESA + hazmat + structural + seismic + ADA). Each item has status (pending/in-progress/complete/N/A), assigned party, due date, notes. Progress bar. Add custom DD item button. (4) **Naming cleanup**: "Deal Details" → "Deal Info" in CreateDealModal step label and CreateDealPage form header.
-   **Tiered Comp Analysis + Overview Navigation Fixes (2026-03-11):** (1) **Overview Navigation**: Fixed 7 broken `navigateToTab()` calls in OverviewSection.tsx — `'design'`→`'3d-design'`, `'unit-mix'`→`'unit-mix-intelligence'`, `'dd-checklist'`→`'due-diligence'`, `'risk'`→`'risk-management'`, `'market'`→`'market-intelligence'`, `'capital-structure'`→`'debt'`; also fixed moduleLinks signal navigation. (2) **Tiered Comp Discovery Backend**: `discoverTieredComps()` in `comp-set-discovery.service.ts` returns comps in 3 geographic tiers — trade_area (radius-based + county fallback with state-aware matching), submarket (PostGIS containment), msa (PostGIS containment with spatial filtering). County fallback extracts state from deal address using abbreviation map. `geographic_tier` column added to `deal_comp_sets` (migration 112). Routes: `GET /:dealId/comp-set/discover-tiered` (with radiusMiles bounds 0.5–50, proper 404/422 errors), `POST /:dealId/comp-set/add-to-set` (with geographic_tier validation), `DELETE /:dealId/comp-set/:compId` (with rowCount check). (3) **Frontend Comp Analysis**: `DealCompAnalysisTab.tsx` with 3 collapsible tier sections (trade area/submarket/MSA), color-coded headers with property counts, comp rows showing name, address, units, year_built, stories, class, distance, match score bar, rent, occupancy. "Add to Comp Set" / "In Set" toggle button with optimistic UI + error rollback. Comp set summary bar. "Reset to Defaults" button. Service methods in `competition.service.ts`. Files: `OverviewSection.tsx`, `DealCompAnalysisTab.tsx`, `competition.service.ts`, `comp-set-discovery.service.ts`, `deal-comp-sets.routes.ts`.
-   **Live Financial Data Wiring (2026-03-11):** Eliminated all hardcoded placeholder/mock data from financial components. `FinancialState` interface extended with `purchasePrice`, `totalUnits`, `goingInCapRate`, `exitCapRate`, `stabilizedOccupancy`, `dscr`, `debtService`, `yieldOnCost` for cross-tab data flow. DebtTab NOI fallback removed (was $3M). FinancialDashboard budget/units/rent fallbacks removed (was $15M/200/1800). ProFormaTab fake unit mix (4 rows) replaced with empty scaffold (2 rows); purchase price, loan amount, closing costs all sourced from deal data or zero. OverviewSection `strategyVerdict` and `riskAlert` now derived from live `strategyResults` and `signals` via useEffect instead of permanent mock data. `cashOnCash` and `dscr` array normalization applied consistently.
-   **Deal Overview — Live Data Wiring (2026-03-11, Task #7):** Wired 9 disconnected sections to live backend data, replacing all mock/hardcoded fallbacks with empty states. (1) **Strategy Verdict + Risk Alert**: Removed `mockJediScore`, `mockSignalScores`, `mockStrategyVerdict`, `mockRiskAlert` imports; states init to null with "Run analysis" CTA. (2) **Capital Structure**: ExistingOverview now accepts `capitalStackData` prop from `POST /api/v1/capital-structure/stack`; all 3 tiers (Senior/Mezz/Equity) populated from API layers with fallback to legacy `capitalStructure`. (3) **NOI Market Cap Rate**: `marketCapRate` fetched from `GET /api/v1/deals/:id/geographic-context` (submarket or MSA avgCapRate); computes Implied Value at Market Cap with %-vs-ask comparison. Numeric safety with `typeof` guard. (4) **Zoning Constraints**: Removed hardcoded FAR `'2.0'`, max units `186`; shows '—' when no data. (5) **Entitlement Pipeline**: DevOverview accepts `entitlements` prop from `GET /api/v1/entitlements/deal/:dealId`; shows real pipeline status or empty state with "Start Entitlement Process" CTA. (6) **Entitlement ETA + Timeline**: Fetches from `GET /api/v1/benchmark-timeline/benchmarks?county=X&state=Y`; maps `medianMonths`/`p75Months` to `p50`/`p75`; timeline durations derived from benchmarks + deal assumptions. Empty state when no data. (7) **Unit Mix**: Removed hardcoded 10/40/40/10 split fallback; empty state with "Configure Unit Mix" CTA. (8) **Rent Comps**: Removed 5 fabricated comps (A-E); empty state with "Analyze Rent Comps" CTA. (9) **DD Checklist**: Removed 6-item hardcoded list; shows "Add DD Items" CTA when empty. (10) **Budget Stack**: Removed hardcoded `$28.4M`/`$4.6M`/`$3.3M`; shows '—' for each line when no financial data. Construction duration text derived from deal assumptions. Files: `OverviewSection.tsx`.
-   **Deal Overview Dual-Mode Redesign (2026-03-06):** `OverviewSection.tsx` refactored into dual-mode: EXISTING DEAL / DEVELOPMENT DEAL toggle. **Shared DealHeader**: JEDI Score gauge (SVG arc), 5 Master Signals with progress bars + delta trends (clickable → module navigation), Strategy Verdict with Arbitrage detection + DD progress bar, Risk Alert banner (severity-colored). **ExistingOverview**: Acquisition Metrics 6-col KV grid (Ask Price, PPU, Cap Rate, DSCR, Yield on Cost, Days in Pipeline), Operating Intelligence 2-col (Occupancy & Rent table + NOI Intelligence with Platform Adjustment banner), Capital Structure 3-col (Senior Debt / Mezz / Equity with LTC% and color borders), DD Checklist + Module Access (F-key shortcuts). **DevOverview**: Site+Zoning Constraints 6-col (Land Cost, Max Units, FAR, Entitlement ETA, Target IRR, TDC/Unit), Entitlement Pipeline horizontal stepper (5 steps: Pre-Application→Approved, done=green/active=amber/pending=gray), Dev Path Selector (3 tabs with AI PICK badge + fit score, detail panel with 8-metric grid, pros/cons, path comparison), Unit Mix Program table with proportional color bar, Unit Mix vs Top 5 Rent Comps cross-table (comp rent/SF/delta per unit type), Budget Stack + Gantt Timeline, Returns Comparison (BTS vs Hold as Rental). **Data wiring**: ExistingOverview reads `capitalStructure`, `financial` (NOI, DSCR, TDC), `market` (occupancy, avgRent) from `DealModuleContext`. DevOverview reads `useZoningModuleStore` (entitlements, FAR, max units), `useUnitMixIntelligence` hook (comp properties for rent comps, program data), `financial`/`design3D`/`activeScenario` from context. All sections fall back to placeholder data when real data unavailable. Market Vitals design system (stone cards, mono labels, white backgrounds, border-stone-200).
-   **Market Intelligence Page (2026-03-01):** Redesigned "Market Analysis" tab into comprehensive "Market Intelligence" with 4 sub-tabs: (1) Local Economy — Economic Health Score (0-100, AI-generated), 5 KPI metrics (jobs, wage growth, migration, affordability), Major Employers table with status indicators, Development Pipeline timeline, Industry Composition horizontal bars, Wage-Rent-Traffic Alignment correlation strip. (2) Document Intelligence — OM claim extraction and verification (verified/partial/contradicted/unverifiable), Platform vs Broker verdict, claim-by-claim expandable cards. Upload CTA when no OM exists. (3) Demographics & Demand — Census ACS data (population, income, housing, rent), submarket/MSA stats cards, Renter Demand Quantification funnel (population→renters→income-qualified→age-appropriate→unit-match→demand pool with capture rate), Competitive Supply Context. (4) News Feed — real-time events from `news_events` table with type badges (DEMAND/SUPPLY/INFRASTRUCTURE/ECONOMIC/RISK/REGULATORY). Backend: `deal-market-intelligence.routes.ts` aggregates Census, submarket/MSA, news events, property records, and uses Claude AI to generate economic analysis. Cached in `deal_data.market_intelligence` with 24hr TTL. Frontend: `MarketIntelligencePage.tsx` replaces `MarketAnalysisPage` in DealDetailPage tab `market-intelligence`.
-   **Opus Pro Forma Builder:** AI-powered financial modeling using Anthropic Claude, integrating cross-module context and a Data Library.
-   **Data Library:** Global repository for historical property data, categorized for AI matching. Supports PST email archive import.
-   **Georgia Property Records Data Quality Fix (2026-03-13):** Fixed 4 data quality issues in `rent_scrape_targets` from Fulton County property records sync. Migration 127: (1) Deactivated 17 "0 STREET" invalid sub-parcel entries, (2) Nulled `property_name` where it duplicated `address` (1,003 rows), (3) Replaced unique constraint `(property_name, city)` with `(LOWER(TRIM(address)), LOWER(TRIM(city)))` — deduplicated 17 duplicate groups first, (4) Reset `places_search_done` for targets with bad city data, (5) Updated `sync_property_records_to_targets()` function to skip "0 STREET" addresses, require non-null city, use NULL property_name (for later SERP enrichment), and use new address-based dedup. Schema changes: `property_name` column made nullable, `city` column default `'Atlanta'` removed. Discovery service (`rent-scraper-discovery.service.ts`) updated to handle null `property_name`. Geocoding script (`geocode-ga-runner.js`) using Nominatim to fill city/lat/lng for ~940 GA property records — partial progress (93/1012 geocoded, identifying Sandy Springs, Roswell, Alpharetta, etc.). Admin endpoint: `GET /api/v1/admin/data-coverage/data-quality/georgia` returns overview stats, market distribution, and geocode status. Files: `backend/src/db/migrations/127_georgia_data_quality_fix.sql`, `backend/src/services/rent-scraper-discovery.service.ts`, `backend/src/scripts/geocode-ga-runner.js`, `backend/src/api/rest/admin-data-coverage.routes.ts`.
-   **Atlanta SERP Property Name Discovery (2026-03-13):** Runs SERP/Google Maps discovery on ~946 Atlanta-area `rent_scrape_targets` with null `property_name` to discover real property names, websites, ratings, and phone numbers. Changes: (1) `buildSearchQuery()` appends "apartment" keyword for address-only searches (improves Google Maps match quality), (2) `discoverPropertyWebsite()` extracts `place.title` from Google Maps results and writes it back to `property_name` when null, (3) `markSearchDone()` sets `metadata.name_source = 'google_maps'` when a name is discovered, sets `metadata.needs_manual_review = true` when no place results returned, (4) `discoverById()` now joins `property_records` to fetch `owner_name` for better search queries, (5) Bulk discovery job tracks `namesFound` count alongside `discovered` (websites). Pipeline running via `POST /api/v1/admin/rent-scraper/discovery/run` with `market: 'Atlanta'`. Early results: ~100% name discovery rate, ~87% website discovery rate. Files: `backend/src/services/rent-scraper-discovery.service.ts`, `backend/src/api/rest/rent-scraper-admin.routes.ts`.
-   **Atlanta Property URL Discovery Pipeline (2026-03-13):** Discovers county assessor portal URLs for Atlanta-area properties (Fulton County and DeKalb County) so the browser-based scraper can visit them for richer data. Pipeline: (1) Queries `properties` and `property_records` tables for Atlanta/Fulton/DeKalb properties missing `assessor_url`, (2) For each property, queries Fulton County Tax Parcels 2025 or DeKalb County ArcGIS parcel layer by address to get parcel ID, (3) Constructs qPublic assessor portal URL using parcel ID, (4) Stores URL on property record. DB: Migration 126 adds `assessor_url TEXT` column to both `properties` and `property_records` tables with partial indexes. Admin API: `GET /api/v1/admin/atlanta-url-discovery` triggers full pipeline (admin auth required), `?dry_run=true` for summary only. `GET /api/v1/admin/atlanta-url-discovery/summary` returns coverage stats by county. CLI: `npx tsx backend/src/scripts/discover-atlanta-assessor-urls.ts`. Service: `atlanta-url-discovery.service.ts` with county detection (city-based mapping for Fulton/DeKalb cities), address normalization, ArcGIS parcel lookup with exact/partial/fuzzy matching, 200ms rate limiting. Files: `backend/src/services/atlanta-url-discovery.service.ts`, `backend/src/api/rest/atlanta-url-discovery.routes.ts`, `backend/src/scripts/discover-atlanta-assessor-urls.ts`, `backend/src/db/migrations/126_assessor_url_column.sql`.
-   **Unified Property View Layer (2026-03-13):** Cross-reference layer linking 4 fragmented property tables (`property_records`, `properties`, `rent_scrape_targets`, `comp_properties`) via FK columns. Migration 125 adds `property_record_id UUID FK` to `properties` table, `properties_id UUID FK` to `rent_scrape_targets`. Auto-matching backfill links 1,013 `rent_scrape_targets` → `properties` by name+city, 1 `properties` → `property_records` by address. Materialized view `v_unified_properties` (2,301 rows) joins all sources via UNION ALL with COALESCE for best-available data: name, address, city, state, zip, unit_count, year_built, lat/lng, website_url, google_rating, owner_name, parcel_id, assessed/appraised values, building_class, jedi_score, pipeline_stage, `sources` text array. API: `GET /api/v1/markets/properties/unified` with city/search/source filters, sortBy (8 columns), pagination. `POST /api/v1/markets/properties/unified/refresh` (auth-protected) refreshes materialized view. Files: `backend/src/db/migrations/125_unified_property_view.sql`, `backend/src/api/rest/market-intelligence.routes.ts`.
-   **Rent Scraper Pipeline (2026-03-12):** Automated rent data collection from apartment listing sites using Cloudflare Browser Rendering API. DB tables: `rent_scrape_targets` (106 seeded Atlanta properties across 28 submarkets), `rent_scrape_jobs` (job history with status tracking), `scraped_rents` (unit-level rent data with bedrooms/sqft/rent/specials). Service: `rent-scraper.service.ts` with HTML parsing for pricing grids and table-based layouts. 5 Clawdbot commands: `scrape_property` (single target), `run_scrape_job` (batch by market), `get_rent_changes` (rent delta analysis with configurable lookback), `add_scrape_target`, `list_scrape_targets`. Env vars: `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_BR_TOKEN`. Files: `backend/src/services/rent-scraper.service.ts`, `backend/src/db/migrations/114_rent_scraper_tables.sql`, `backend/src/db/seeds/seed_atlanta_rent_targets.sql`, `backend/src/api/rest/clawdbot-webhooks.routes.ts`.
-   **COMMS Hub — Email Inbox Redesign (2026-03-12):** Redesigned email inbox page (`/dashboard/email`) as a dark-themed intelligence hub branded "COMMS". **Dark Theme**: Custom `T` theme object with navy/slate palette, `FONTS` constants (JetBrains Mono + IBM Plex Sans). **3-Panel Layout**: 420px email list + center detail pane + 300px right sidebar (Agent Actions/Deal/Team/Tasks tabs). **View Tabs**: Inbox, Flagged, Deal-Linked, PST Imports, Unread — each with API-backed filtering (`source=pst`, `deal_linked=true`, `flagged_only=true`, `unread_only=true`). **Classification System**: 6 types (New Opportunity/Deal Event/Market Signal/System Alert/Correspondence/PST Import) with color-coded labels and icons, auto-classified from email metadata. **Signal Tags**: Extracted from subject line keywords (deal room, due diligence, offering memorandum, etc.). **Command Palette**: Cmd+K opens search modal with live query bound to API search endpoint. **PST Backflow**: Migration 119 created "PST Import" virtual email_account and backflowed 230 PST-imported emails into the unified `emails` table (mapped sender/subject/body/date/flags). `backflowPstToEmails()` function in `pst-upload.routes.ts` handles future upload backflow. Backend `deal_linked` filter added to `inline-inbox.routes.ts`. **Connected Accounts Panel (Task #34)**: Left sidebar panel showing all connected Gmail/Outlook accounts with sync status, last-sync time, and per-account manual sync button. "+ Gmail" and "+ Outlook" buttons initiate OAuth flows. Backend: `GET /api/v1/inbox/accounts` unifies `user_email_accounts` (Gmail) and `microsoft_accounts` (Outlook); `POST /api/v1/inbox/accounts/:id/sync` triggers Gmail sync service. OAuth callbacks redirected from `/settings` to `/dashboard/email?connected=gmail|microsoft`. Frontend detects `?connected=` URL param, shows success/error notice, and auto-refreshes account list. Files: `frontend/src/pages/EmailPage.tsx`, `frontend/src/services/inbox.service.ts`, `backend/src/api/rest/inline-inbox.routes.ts`, `backend/src/api/rest/gmail.routes.ts`, `backend/src/api/rest/inline-microsoft.routes.ts`.
-   **PST Email Archive Ingestion (2026-03-12):** Upload Microsoft Outlook `.pst` files to extract real estate intelligence from email history. Pipeline: (1) `pst-extractor` parses all emails (subject, body, sender, recipients, date), (2) AI extraction via LLM identifies property addresses, deal names, unit counts, prices, cap rates, contacts, organizations with confidence scores, (3) results stored in `pst_email_imports` and `pst_extracted_entities` tables linked to `data_uploads`. API: `POST /api/v1/data-upload/pst` (upload + async processing), `GET /api/v1/data-upload/pst/:jobId/status` (poll progress), `GET /api/v1/data-upload/pst/:jobId/emails`, `GET /api/v1/data-upload/pst/:jobId/entities`. Frontend: PST upload section in Data Library with progress bar (parsing/extracting/storing phases), completion summary (emails processed, entities extracted, signal count), and entity results table. DB migration: `backend/src/db/migrations/117_pst_import_tables.sql`. Files: `backend/src/services/pst-ingestion.service.ts`, `backend/src/services/pst-ai-extraction.service.ts`, `backend/src/api/rest/pst-upload.routes.ts`, `frontend/src/services/pstUpload.service.ts`, `frontend/src/pages/DataLibraryPage.tsx`. End-to-end verified: 230 emails from ldixon@myersapartmentgroup.com PST (58MB), processed in 3 seconds, stored with subjects, senders, recipients, dates, body text.
-   **Site Intelligence Module:** Comprehensive site analysis across six scored categories.
-   **Zoning Intelligence Agent:** AI-powered zoning data retrieval and analysis using Anthropic Claude, featuring a verification-first 6-step pipeline, dual-layer knowledge, professional correction, and confidence scoring. This includes deep-linking to Municode citations, dual source links (planning department and Municode), and per-rule source tracing.
-   **Property & Zoning Module:** Embedded within the Deal Detail page, offering a 5-tab progressive workflow for zoning analysis: Boundary & Zoning, Dev Capacity, Highest & Best Use, Regulatory Risk, and Time-to-Shovel. The Confirmation Chain is an internal plumbing/diagnostic system (not a user-facing tab) that validates data flow across the zoning pipeline.
-   **Dev Capacity Redesign (2026-02-28):** Consolidated Conditional Variant, Constraint Set, Controlling Factor, and Calculation Breakdowns into a single "Zoning Constraint Matrix" table. Section order: (1) Market Reality Check, (2) Entitlement Path Comparison, (3) Zoning Constraint Matrix. Market Reality Check expanded with FAR and Lot Coverage comparison bars alongside density, plus enriched project cards showing all metrics.
-   **Entitlement Comparison Always-Visible + Full Path Broadcast (2026-03-04):** Refactored Entitlement Comparison from an IIFE that could return `null` to an always-visible static container shell with conditional body content (skeleton vs real data). Container never disappears once the tab is active; shows "Refreshing paths..." indicator when data exists and is being reloaded. `handleSelectPath` enhanced to capture full column data into `SelectedPathData` (zoning code, density, FAR, binding constraint, risk, timeline, AI insight, all cell data) stored in `zoningModuleStore.selected_path_data`. On selection, also calls `updateActiveScenario()` and `emitEvent('capacity-updated')` via `DealModuleContext` to broadcast to all downstream modules (Pro Forma, Strategy, 3D Design, Debt, Risk). Types: `SelectedPathData` interface added to `zoning.types.ts`.
-   **Benchmark Lot Coverage (2026-02-28):** Added `lot_coverage_achieved` column to `benchmark_projects`. Enrichment service computes `(building_sf / stories) / (land_acres * 43560)` during enrichment. Density-benchmarks endpoint now returns `avgFarAchieved`, `avgLotCoverageAchieved`, `avgBuildingSf`, `avgLotAcres`, `farUtilizationPct`, `lotCoverageUtilizationPct`.
-   **Enriched Building Envelope Service:** Calculates property development potential, returning sources, calculations, and plain-English insights.
-   **Consolidated Rezone Analysis Service:** Unifies rezone analysis, calculating actual uplift (units, GFA, revenue) from target district data.
-   **Zoning Verification Pipeline:** A verification-first architecture providing per-value source citations, insights, and calculation breakdowns.
-   **Parcel-Level Zoning Lookup:** Automatic zoning code detection via ArcGIS, with AI and web search fallbacks.
-   **Municipal Zoning Database:** Integration with 32 ArcGIS REST API connectors across 13 states for zoning data.
-   **M28 Cycle Intelligence (2026-03-06):** Market cycle tracking module with FRED API integration. **Data Ingestion**: 3 scripts — `m28:ingest-rates` (daily FFR/SOFR/10Y/30Y mortgage/M2/DXY from FRED API, stored in `m28_rate_environment`), `m28:ingest-rates:backfill` (historical backfill), `m28:ingest-indicators` (monthly permits/starts/confidence, currently mock data — Census/NAHB APIs TODO). **Cycle Classification Engine**: 8-metric weighted algorithm (rent growth 25%, vacancy 20%, cap rate 15%, txn velocity 15%, PPU 10%, DOM/absorption/concessions 5% each) maps to recovery/expansion/hypersupply/recession. **Service**: `cycle-intelligence.service.ts` — 15 methods covering cycle phases, rate environment, leading indicators, pattern matching, predictions (rent growth, cap rate, value change, full chain FFR→value), phase-optimal strategy, construction cost index, macro risk. **REST API**: 15 endpoints at `/api/v1/cycle-intelligence/*` + 2 no-auth test endpoints. **DB**: 7 tables (`m28_cycle_snapshots`, `m28_rate_environment`, `m28_leading_indicators`, `m28_historical_events`, `m28_pattern_matches`, `m28_market_metrics_history`, `m28_deal_performance_by_phase`), 6 seeded historical events (GFC, COVID, Volcker, etc.). **Routes registered in `index.replit.ts`**. Scripts load dotenv for standalone execution. FRED_API_KEY stored in `backend/.env`. **Automated Scheduler**: `m28-scheduler.service.ts` runs daily rate ingestion (8AM ET), monthly indicators (5th, 9AM ET), monthly cycle classification (1st, 10AM ET) via `node-cron`. **Type Safety Fix (2026-03-06)**: PostgreSQL NUMERIC columns returned as strings — added `safeFloat()` helper and `parseSnapshot()` in service to convert `divergence`, `lag_position`, `lead_position`, `confidence`, rate environment fields, and strategy performance fields. Seed data `similarity_pct` corrected from decimals to integers (column is INT 0-100). **Test Data**: `m28-test-data.sql` seeds 4 markets (tampa/atlanta/orlando/miami), cycle snapshots, deal performance by phase, pattern matches, and market metrics history. All 9 endpoint tests pass.
-   **Competitive Ranking — Program vs. Competition (2026-03-06):** Unit Mix Intelligence Program data feeds into Competition Analysis. New endpoint `GET /api/v1/deals/:dealId/competitive-ranking` joins the deal's saved Program (from `deal_unit_programs`) with competitor data (from `comp_properties`/`comp_unit_types` or `property_records` fallback). Calculates weighted avg rent and SF from unitConfig, ranks all properties by rent, returns subject rank, percentile, rent premium/discount vs market avg. Frontend: `CompetitiveRankingCard` component in `CompetitionSection.tsx` shows ranked table with subject property highlighted (blue row, "YOUR PROGRAM" badge), summary stats (Rank, Percentile, vs Market), and footer with Your Rent, $/SF, Market Avg, Delta. Empty state CTA when no Program saved. Market Vitals design system (stone colors, mono labels). Files: `backend/src/api/rest/competition.routes.ts`, `frontend/src/components/deal/sections/CompetitionSection.tsx`.
-   **Competitive Set Ranked Table — Overview Page (2026-03-06):** Replaced `ProgramRankingPanel` (per-unit-type ranking cards with percentile badges) with a simpler flat comparison table in `OverviewSection.tsx` (Development Deal mode). Section title: "Competitive Set · Ranked by $/SF". All properties (subject + comps) shown in the same row format, sorted by $/SF highest to lowest. Columns: #, PROPERTY, DIST, UNITS, AVG RENT, AVG SF, $/SF, OCC, ANN. TRAFFIC. Subject property row highlighted with blue-50 background, blue left border accent, and "SUBJECT" badge. Subject metrics computed as weighted averages from `unitMix` (rent × units / total). Comp metrics averaged across available unit types from `mix` data. COMP AVERAGE summary row at bottom. Annual traffic estimated via `(units / 290) × 11 × 52`. Purely client-side from existing `unitMix` and `rentComps` data.
-   **Zoning Analysis Fix — Geocoding + Municipality + District Lookup (2026-03-08):** Fixed 3-layer failure in `ZoningService.lookupZoning()`: (1) **Geocoding**: `VITE_MAPBOX_TOKEN` aliased to `MAPBOX_ACCESS_TOKEN`/`MAPBOX_TOKEN` in `start.sh` so Mapbox forward geocoding works as Google Maps fallback. (2) **Municipality detection**: Added Mapbox reverse geocoding fallback and address-string parsing fallback to `getMunicipality()` — was Google Maps API only (which requires `GOOGLE_MAPS_API_KEY` not present). (3) **Zoning district lookup**: Replaced broken PostGIS `ST_Contains(boundary, ...)` query (referenced non-existent geometry column on `zoning_district_boundaries` table with only 3 records) with multi-strategy: ArcGIS municipal API point query (Atlanta GIS etc.) → cross-reference `zoning_districts` table for rules → bbox fallback → municipality-level fallback. Removed reference to non-existent `zoning_rules` table; rules now read from `zoning_districts` columns directly. Test: "1950 Piedmont Circle NE, Atlanta, GA 30324" → MRC-2-C (Multi-Family Residential Commercial), 5 stories max, 85% lot coverage, source=arcgis+db, 197ms. Files: `zoning.service.ts`, `start.sh`.
-   **Performance & Rankings — Real Portfolio Deals (2026-03-08):** Fixed Performance & Rankings tab on Assets Owned page to show real user-owned portfolio deals instead of hardcoded mock data. (1) **Route ordering fix**: Rankings route (`/api/v1/rankings`) was registered after catch-all `app.use('/api/v1', requireAuth, ...)` middleware, causing all unauthenticated requests to be rejected with 401. Moved rankings registration above catch-all middleware with `optionalAuth`. (2) **Backend rewrite**: `GET /api/v1/rankings/owned/:marketId` now queries user's `deals` (portfolio, closed_won) via `deal_properties → deal_monthly_actuals` for real occupancy/NOI data. Matches deals to `property_records` by address for PCS scoring with occupancy bonus. Synthesizes PCS for deals without property_records match. Removed `slice(2,8)` demo hack. (3) **Submarket detection**: Added Gwinnett (Duluth/Satellite/Suwanee), North Fulton, Cobb County, South Atlanta submarket rules. (4) **Frontend fix**: Rankings API call now uses `${API_URL}` prefix instead of bare path. Fixed response unwrapping (`response.data.data.rankedAssets`). Removed mock ranked assets array. (5) **Result**: Highlands at Satellite appears as PCS=74, rank 1/1 in Gwinnett, 290 units. 6 total user deals displayed from live data. Files: `rankings.routes.ts`, `index.replit.ts`, `AssetsOwnedPage.tsx`.
-   **Zoning Data — Suburban GA Cities (2026-03-07):** Added 5 missing GA municipalities (Decatur, Marietta, Sandy Springs, Alpharetta, College Park) with 52 zoning districts to `municipalities` and `zoning_districts` tables. Uses existing schema format (varchar UUID ids, `municipality`/`state` columns). Covers all GA deal cities. Total: 77 municipalities, 1,243 zoning districts across GA and FL.
-   **Apartment Locator AI Integration (2026-03-07):** Connected to external Apartment Locator AI API (`https://apartment-locator-ai-real.replit.app`) for rent/property data sync. Fixed schema mismatches: added missing columns to `apartment_market_snapshots` (`total_listings`, `available_units`, `avg_rent`, `min_rent`, `max_rent`, `studio_rent`, `one_br_rent`, `two_br_rent`, `three_br_rent`, `source`, `updated_at`) and `properties` (`apartment_locator_id`, `last_enriched_at`). Fixed `property_type` enum mismatch (`Multifamily` → `multi_family`). Added NaN-safe parsing for numeric fields from API. Env vars: `APARTMENT_LOCATOR_API_URL` (shared), `APARTMENT_LOCATOR_API_KEY` (secret). Sync script: `npx tsx backend/src/scripts/sync-apartment-locator.ts` (Atlanta default, `--all` for all metros). **11 metros synced**: Atlanta (100), Houston (100), Dallas (92), Austin (84), San Antonio (100), Charlotte (100), Nashville (100), Orlando (100), Tampa (100), Jacksonville (100), Miami (83) — **1,069 total properties** with rent data. Files: `backend/src/services/apartment-locator-sync.service.ts`, `backend/src/scripts/sync-apartment-locator.ts`.
-   **Collision Analysis Module (2026-03-10):** Converted standalone CapsuleDetailPage (`/capsules/:id`) into a `CollisionAnalysisSection` module embedded in DealDetailPage under DUE DILIGENCE stage. **Three-Layer Intelligence**: Layer 1 (Broker Claims) from deal API (`property_data`, `analysis_data`), Layer 2 (Market Reality) from DealModuleContext (`market`, `marketIntelligence`), Layer 3 (Your Model) from DealModuleContext (`financial`). **Collision Scoring**: 5 dimensions computed dynamically — Strategy Arbitrage (rent delta analysis), Portfolio Fit, Broker Validation (rent + occupancy delta), Risk Assessment (supply pipeline), Execution Confidence — weighted to overall 0-100 score. **3 sub-tabs**: Overview (ThreeColumnComparison table + collision highlights + deal summary sidebar), Three Layers (detailed Layer 1/2/3 breakdowns), Collision Details (full dimension cards with insights + recommended actions). **Route Updates**: `/capsules/:id` now redirects to `/deals/:id/detail?tab=collision-analysis`. Dashboard Key Findings links (backend live API, mock findings, frontend fallback) all point to `/deals/:id/detail?tab=collision-analysis`. Finding cards redesigned with capsule-style white cards, stone borders, priority indicators, JEDI Score badges, change indicators, verdict pills. Files: `frontend/src/components/deal/sections/CollisionAnalysisSection.tsx`, `frontend/src/pages/DealDetailPage.tsx`, `frontend/src/pages/CapsuleDetailPage.tsx`, `backend/src/api/rest/dashboard.routes.ts`, `frontend/src/components/dashboard/KeyFindingsSection.tsx`.
-   **Property Data Tab Fix (2026-03-07):** Fixed Property Data tab only showing 5 hardcoded fallback rows instead of 1,028 live properties from the database. Root cause: Express route ordering — catch-all `app.use('/api/v1', requireAuth, ...)` middleware for `buildingEnvelopeRoutes` and `geographicContextRoutes` was mounted before `/api/v1/markets` routes, causing all market API requests to be rejected with 401 even though the markets routes themselves had no auth requirement. Fix: moved `marketIntelligenceRouter` and `createEnhancedMarketIntelligenceRoutes` registrations above the catch-all auth middleware in `backend/src/index.replit.ts`. Also added pagination controls (first/prev/next/last with page indicator) to `PropertyDataTab.tsx` footer, and fixed fallback logic to only show hardcoded rows before first API attempt (not on empty result pages). Filter changes now reset to page 1. Files: `backend/src/index.replit.ts`, `frontend/src/pages/MarketIntelligence/tabs/PropertyDataTab.tsx`.
-   **Property Data Enrichment (2026-03-07):** Script `backend/src/scripts/enrich-deals-fixed.ts` fetches real property data from county ArcGIS tax parcel APIs and stores it in `deals.property_data` (JSONB column). **4 county APIs supported**: Fulton County GA (`services1.arcgis.com` Tax_Parcels_2025), DeKalb County GA (`dcgis.dekalbcountyga.gov` Tax_Parcels), Miami-Dade County FL (`gisweb.miamidade.gov` MD_LandInformation/24), Palm Beach County FL (`services.arcgis.com/B7X7NCOKKXditlwZ` Palm_Beach_County_Parcels). Cobb County GA GIS is offline. **17 of 24 deals enriched** — 7 remaining have addresses that don't exist in county parcel databases (fictional development sites or parcels registered under different addresses). Data includes: units, total_sqft, year_built, lot_size_acres, assessed/appraised values (total/land/improvements), annual_taxes (estimated), parcel_id, owner_name, use_code, class_code. Multi-strategy address search: tries abbreviated + raw + directional variants + core street word matching. Cross-county fallback for Atlanta (Fulton↔DeKalb). Address parsing handles abbreviated/full state names, "United States" suffix. Script skips deals enriched within 30 days.
-   **M22 Post-Close Intelligence Navigation + Real Data (2026-03-07):** Wired M22 Post-Close Intelligence Bloomberg terminal dashboard to real property data from Assets Owned. (1) **Assets Owned Report Buttons**: "Report" action buttons on compset tab table rows and "View Report" on performance tab asset cards, navigate to `/assets-owned/:id/report`. (2) **Backend Endpoint**: `GET /api/v1/grid/owned/:id/report` in `grid.routes.ts` returns property data + 12 months of computed NOI/occupancy/rent metrics with seasonal variation from `property_records`. Grid routes mounted above catch-all requireAuth middleware. (3) **Dynamic Dashboard**: `DealFlywheelDashboard.tsx` reads `propertyId` from URL params, fetches real data from the report endpoint, populates header bar and Performance tab. Falls back to mock data for `/demo/flywheel`. (4) **Deal Sidebar**: `post-close` module in `ASSET_MODULES` (always-enabled bypass), renders dashboard in DealView.tsx. Also fixed: `CommandCenterPage.tsx` wrong `apiClient` import. Files: `grid.routes.ts`, `DealFlywheelDashboard.tsx`, `AssetsOwnedPage.tsx`, `App.tsx`, `DealSidebar.tsx`, `DealView.tsx`, `index.replit.ts`.
-   **Clawdbot Agent Integration (2026-03-07):** Extended Clawdbot webhook API with full access to agent infrastructure. (1) **`run_analysis` command rewritten**: Now actually submits tasks to `agent_tasks` table (was a stub). Supports `analysisType`: `zoning`, `supply`, `cashflow`, `full` (submits all 3). Accepts `inputData` for direct analysis without dealId. Returns real task IDs for polling. (2) **New `get_agent_task` command**: Poll individual task by ID — returns full status, input/output data, execution time, retry info. (3) **New `get_agent_tasks` command**: List recent agent tasks with optional `status`/`taskType` filters, pagination. (4) **New `ai_chat` command**: Conversational AI via Clawdbot webhook — same tool-calling pipeline as `POST /api/v1/chat` (supply, cashflow, zoning, property search). Returns `{ response, conversationId, toolsUsed[] }`. (5) **New `agent_stats` query**: Aggregate agent task metrics — by status, by type (with completion/failure counts, avg execution time), recent tasks, registered agents, AI config status. (6) **`system_stats` enhanced**: Now includes `agentTasks` counts and `agentSystem` info alongside deal/task stats. (7) **`/health` enhanced**: Reports agent system health (orchestrator, registered agents, AI config, API key). (8) **Chat service extracted**: `chat.service.ts` — shared `processChat()` function used by both `chat.routes.ts` and Clawdbot. Exports `getRegisteredAgentTypes()` and `isAiConfigured()` for status reporting. Files: `clawdbot-webhooks.routes.ts`, `chat.service.ts`, `chat.routes.ts`.
-   **AI Chat Endpoint + ChatOverlay Wiring (2026-03-07):** Connected the floating ChatOverlay widget to a real AI backend. (1) **Backend `POST /api/v1/chat`**: New `chat.routes.ts` with Anthropic Claude tool-calling. Defines 4 tools: `analyze_market_supply`, `analyze_cashflow`, `analyze_zoning`, `search_properties`. When Claude calls a tool, endpoint submits to AgentOrchestrator via `agent_tasks` table (poll pattern, 30s timeout), returns result to Claude for summarization. Supports multi-round tool-calling (up to 4 rounds). In-memory conversation history (Map, capped at 100 conversations, 20 messages each). Returns `{ response, conversationId, toolsUsed[], usage }`. Uses `AI_INTEGRATIONS_ANTHROPIC_API_KEY`. Mounted with `optionalAuth` above catch-all middleware. (2) **Frontend ChatOverlay rewrite**: `ChatOverlay.tsx` now calls `POST /api/v1/chat` on send, tracks `conversationId` across messages, shows typing indicator with bouncing dots, displays tool labels (Market Analysis, Financial Analysis, etc.) in agent name, handles errors as system messages, supports conversation reset. Uses `Message` type from `chatStore`. (3) **SupplyAgent type fix**: `year_built` column is `text` not `int` — added `::int` casts with regex guards (`year_built ~ '^[0-9]+$'`). Added `safeFloat()` helper for all numeric parsing from DB (handles text/numeric type mismatches). Files: `chat.routes.ts`, `ChatOverlay.tsx`, `supply.agent.ts`, `index.replit.ts`.
-   **User Agent Platform Integration (2026-03-07):** Connected the Python User Agent (`jedire-agents/user-agent/`) to the Node.js JediRe backend agent system. (1) **`agent_tasks` table**: Created with migration `104_agent_tasks.sql` — stores task queue for orchestrator (status, input/output JSONB, retry logic, execution timing). (2) **API key auth**: Extended `requireApiKey` in `auth.ts` to support multiple API keys with identity mapping. Added `requireAuthOrApiKey` middleware (accepts JWT or `x-api-key`). `JEDIRE_AGENT_API_KEY` env var authenticates as `userId: 'jedire-user-agent'`, `role: 'agent_client'`. Agent routes in `agent.routes.ts` updated to use `requireAuthOrApiKey`. IDOR fix: task status endpoint enforces `user_id` ownership. (3) **Integration files merged** from `jedire-agents-implementation` branch: `integrations/jedire_api.py` (async httpx client with shared connection, `x-api-key` header, submit/poll/wait pattern), `integrations/jedire_tools.py` (3 tool functions: `analyze_property_zoning`, `analyze_market_supply`, `analyze_deal_financials` + `AVAILABLE_TOOLS` registry with JSON Schema), `prompts/system.md` (system prompt with tool usage guidelines). (4) **AI gateway tool-calling**: `ai_gateway.py` rewritten with Anthropic tool-calling support — loads tools from `AVAILABLE_TOOLS`, passes as `tools` param, handles `tool_use` stop_reason, executes tool functions async, loops up to 5 rounds, accumulates tokens across rounds, tracks `tools_used` in response metadata. Multi-strategy import for `integrations` package (relative -> absolute -> sys.path). (5) **Agent routes mounted** above catch-all `requireAuth` middleware in `index.replit.ts`. Env vars: `JEDIRE_AGENT_API_KEY` (shared), `JEDIRE_API_URL=http://localhost:4000` (shared). Files: `auth.ts`, `agent.routes.ts`, `index.replit.ts`, `ai_gateway.py`, `jedire_api.py`, `jedire_tools.py`, `system.md`, `104_agent_tasks.sql`.
-   **Smoke Test Phase 1+2 Fixes (2026-03-11):** Comprehensive smoke testing across 288 endpoints (147 Phase 1, 141 Phase 2) with 0 FAILs. **Phase 1 fixes**: JSON quoting bug in test script; 5 missing tables (`supply_metrics`, `alerts`, `deal_market_intelligence`, `zoning_profiles`, `user_acquisition_preferences`); `stripe_customer_id` column on users; `deal-context.routes.ts` wrong column names; `context-tracker.routes.ts` userId field and activity INSERT column name. **Phase 2 fixes**: `building-envelope.routes.ts` referenced nonexistent `coordinates`/`metrics` columns on `property_boundaries` (uses `parcel_area_sf`/`buildable_area_sf`/`boundary_geojson`); `trafficPrediction.routes.ts` wrong column names (`p.property_name`→`p.name`, `p.address`→`p.address_line1`) and varchar/uuid JOIN mismatch (added `p.id::text` cast); `comp-traffic.service.ts` same uuid/varchar JOIN fix; `calculate_max_units` function referenced nonexistent `metrics` column (rewritten to use `parcel_area_sf`); created missing `supply_pipeline` table, `update_supply_pipeline(UUID)` function, `leasing_traffic_predictions` table. Migration: `111_smoke_test_fixes.sql`. Test scripts: `backend/tests/smoke-test-core.sh`, `backend/tests/smoke-test-phase2.sh`.
-   **Clawdbot Comps/Docs/Notes Commands (2026-03-11):** Added 6 new Clawdbot commands: `search_comps` (rent comps via `v_comp_search` view, supports dealId context, city/state, or lat/lng with Haversine distance), `get_sale_comps` (deal comp set from `deal_comp_sets`), `list_documents` (deal documents from `deal_documents` with optional category filter), `get_document_stats` (document counts by category, total size, missing file suggestions based on deal stage), `get_notes` (deal notes from `deal_notes` with category filter, content truncation, pinned-first sort), `add_note` (creates notes with `author_name='Clawdbot'`, UUID `00000000-0000-0000-0000-000000000001`). TOOLS.md updated with full 21-command reference table. Files: `clawdbot-webhooks.routes.ts`, `TOOLS.md`.
-   **Clawdbot Integration (2026-03-05):** Fixed SQL schema mismatches in `clawdbot-webhooks.routes.ts` (properties query used non-existent `address_line2`, `zip_code` columns) and `admin.routes.ts` (8 references to non-existent `d.city` column on deals table). Added 6 new Clawdbot commands: `get_zoning_profile` (from `deal_zoning_profiles` + `zoning_districts`), `get_market_intelligence` (from `deals.module_outputs`), `get_design` (from `deals.module_outputs`), `get_proforma` (from `deal_financial_models`), `get_capital_structure` (from `deals.module_outputs`), `update_deal` (budget, target_units, timeline, status, description). Admin routes now use `d.address ILIKE` instead of `d.city` for city-based filtering, and parse city from address for benchmark ingestion.
-   **Building Envelope Service:** Universal calculation engine for 6 property types, including Highest & Best Use analysis and Claude-powered optimization.
-   **Zoning Intelligence Module (Constraint Set + Scenarios):** Manages resolved constraint sets and user-defined development programs, including rezone scenarios.
-   **Highest & Best Use (HBU) Analysis:** Three-pillar architecture (Project Data + Municode + Claude AI). Ranks 6 property types by estimated value with permission classification (by-right/conditional/not-permitted from `zoning_districts` use arrays), Claude AI strategist analysis (use scores, mixed-use combinations, caveats, market insight), benchmark project context from `benchmark_projects`, constraint fallback via `resolveConstraints`, and rezone opportunity detection. AI results cached in `zoning_code_interpretations` with `hbu:` prefix key (30-day TTL).
-   **Entitlement Strategy:** Consolidates by-right, variance, and rezone analysis, providing data-driven recommendations. The `compare` method now falls back to `resolveConstraints` (DB → agent service) when the zoning profile has no constraint data, ensuring math fields populate even when the profile row has all-null values.
-   **Unit Mix Intelligence Backend (2026-03-05):** Full backend for the Unit Mix Intelligence module. **DB tables** (migration `080_unit_mix_intelligence.sql`): `comp_properties` (deal comp set, per trade area), `comp_unit_types` (per-property unit type data: mix%, SF, rent, vacancy, DOM, concessions), `unit_type_trends` (12-month aggregated trends per trade area × unit type), `deal_unit_programs` (user-saved unit programs, one per deal, with derived totalNetSf/grossRevPA). **Drizzle schema**: `backend/src/db/schema/unitMix.schema.ts`. **Service layer**: `backend/src/services/unitMixIntelligence.service.ts` — `getCompSet(pool, dealId, tradeAreaId)` (filters by trade_area_id when provided), `getTrends(pool, tradeAreaId)`, `computeDemandScores(compSet)` (vacScore×0.4 + domScore×0.35 + concScore×0.25), `saveProgram(pool, dealId, userId, program)` (upsert with derived fields), `getProgram(pool, dealId)`, `getZoningForUnitMix(pool, dealId)` (reads from `deal_zoning_profiles`). **API routes**: `backend/src/api/rest/unitMix.routes.ts` mounted at `/api/v1/unit-mix` — GET `/:dealId/comps`, GET `/:dealId/trends`, GET/POST `/:dealId/program`, GET `/:dealId/zoning`. **Frontend hook**: `frontend/src/hooks/useUnitMixIntelligence.ts` — parallel fetch of all 4 endpoints, debounced 1.5s auto-save for program changes. **Frontend wiring**: `UnitMixIntelligence.tsx` refactored — all helper functions (`compAvg`, `computeInventory`, `buildScatter`) and sub-components (`DemandMatrix`, `PropertyDrillDown`, `TrendDetail`, `MixMatrix`, `RentSFScatter`, `CompTable`, `InventorySnapshot`, `ProgramEditor`) now accept `comps`/`trendData` as parameters instead of using module-level constants. Falls back to hardcoded seed data (`COMPS`, `TREND_DATA`, `ZONING_SEED`, `PROGRAM_SEED`) when API returns empty/null. Zoning endpoint reads real data from `deal_zoning_profiles`.
-   **Module Wiring System (Hub-and-Spoke, 2026-03-03):** `DealModuleContext` (`frontend/src/contexts/DealModuleContext.tsx`) is the central hub. Each module writes outputs and reads inputs through the context — no direct module-to-module dependencies. **Full pipeline**: Property & Zoning → Market Intelligence → Design 3D → Strategy → Traffic → Pro Forma → Debt/Equity/Exit → Financial Dashboard. **11 state slices**: `design3D`, `financial`, `market`, `zoningProfile`, `activeScenario`, `capitalStructure`, `strategy`, `debtTerms`, `marketIntelligence`, plus `zoningProfile`/`activeScenario` for zoning constraints. **Zoning → Market Intelligence wiring (2026-03-05):** MarketIntelligencePage now reads `activeScenario` and `zoningProfile` from context and subscribes to `capacity-updated` events (via `lastProcessedEventRef` pattern). When zoning path is selected, Market Intelligence re-pushes its data with zoning linkage fields (`linkedZoningCode`, `linkedMaxUnits`, `linkedMaxGba`, `linkedFar`, `linkedMaxStories`, `linkedBindingConstraint`). UI shows indigo "Zoning Linked" banner with constraint pills when a development path is active, or a stone "not set" prompt otherwise. Event payload includes `zoningLinked` and `zoningCode`. **MarketIntelligencePage** pushes `recommendedMix`, `demandPool`, `captureRate`, `targetDemographic`, census data + zoning linkage to `updateMarketIntelligence()`; emits `market-intelligence-updated`. **MarketAnalysisPage** also pushes `recommendedMix` when demand data loads; `handleApplyToDesign` writes to context instead of URL params. **ZoningCapacitySection** pushes district code, FAR, lot area, buildable area to `updateZoningProfile()`; pushes max GBA, max units, parking, stories, binding constraint to `updateActiveScenario()`; emits `capacity-updated`. **Design3DPage** reads `marketIntelligence.recommendedMix` (shows banner with "Apply" button), reads `activeScenario` for zoning envelope constraints (max stories, GBA, FAR); pushes metrics to `updateDesign3D()` and emits `design-updated`. **ProFormaTab** reads `design3D` to auto-populate totalUnits, netRentableSF, unitMix (with "Source: 3D Design" badge); listens to `design-updated` event. **TrafficModule** pushes occupancy, rentGrowth, supplyPipeline, demandScore to `updateMarket()`; emits `market-updated`; listens to `strategy-selected`. **ProFormaTab** also pushes NOI, IRR, equityMultiple, costs to `updateFinancial()`; emits `financial-updated`; listens to `strategy-selected` + `market-updated`. **FinancialDashboard** reads all context slices, patches local model, includes all context in JEDI AI chat system prompt (including market intel recommended mix), uses `moduleStatus` for 8 module pills (Strategy, Traffic, Pro Forma, Debt, Exit, Market Intel, Zoning, Design 3D). **moduleStatus** (`ModuleStatus` interface): computed from `lastUpdated` timestamps — `{ strategy, traffic, proforma, debt, exit, marketIntelligence, zoning, design3d }` each `'live'|'none'`.
-   **Capital Structure Engine:** Full capital stack design with a 9-tab frontend component (unified Debt, Equity & Exit), backend service, and 13 REST endpoints.
-   **Combined Debt, Equity & Exit Module (2026-03-03):** Merged standalone Exit Strategy into DebtTab as a unified 9-tab module. Tab order: Exit & Capital (convergence overview) → Capital Stack → Equity Waterfall → Debt Products (+ DebtProductsChart scatter/bubble) → Rate Strategy (+ DebtCycleChart 10yr cycle) → Key Metrics → Exit Windows (RSS timeline) → Sensitivity (IRR heat map) → Monitor (post-acquisition). **Three-Factor Exit Formula**: Optimal Exit = Low Rates (35%) + Low Supply (25%) + High Rent Growth (40%). **ExitDrivesCapital.tsx**: Hero convergence SVG chart (14 quarters, rent growth/rates/supply lines, composite exit score, optimal window band, scenario markers), three factor cards with scores, scenario selector (Early/Optimal/Late with quarter+IRR+multiple), causal chain flow, compact mismatch warnings banner, collapsible "Why This Structure" section. **DebtCycleChart.tsx**: 10-year SVG rate cycle (Fed Funds/10yr Treasury/SOFR/Cap Rate lines), phase shading (Tightening/Peak/Easing/Trough), "YOU ARE HERE" marker, optimal exit window, MiniStat cards. **DebtProductsChart.tsx**: Scatter/bubble chart (term vs rate, bubble=LTV), color-coded by product type, target rate line, toggleable bar chart view. **ExitStrategyTabs.tsx**: ExitWindowsTab (10yr quarterly RSS timeline, gauge, year selector), SensitivityTab (IRR heat map), MonitorTab (drift detection). All Market Vitals style (white cards, #f8fafc bg, #e2e8f0 borders, Lucide icons). Standalone Exit Strategy tab removed from DealDetailPage; ExitSection.tsx deleted. Exit data surfaced in FinancialDashboard (Exit module pill, convergence score in CompView, optimal exit window banner in DecView, cross-link in DebtView). **Redundancy cleanup**: Capital stack summary removed from exit-overview (handled by Capital Stack tab), monitoring triggers removed from exit-overview (handled by Monitor tab), detailed scenario financial grids removed from exit-overview (handled by Exit Windows tab), mismatch warnings converted from expandable to inline alert, DecView three-factor bullet breakdown removed (kept optimal exit window banner only).
-   **Design 3D Controls Overhaul + AI Chat (2026-03-03):** Streamlined the Design3DPage controls and added a JEDI AI Design Chat panel. **Header consolidation**: Merged header bar + market recommendation banner + zoning constraints banner into a single compact h-12 command bar. Market mix and zoning shown as clickable pills instead of full-width banners. Auto-save toggle removed (always-on, shows status passively). **Toolbar consolidation**: Merged the bottom floating toolbar (7 buttons) and top-left View Settings panel (5 checkboxes) into a single vertical icon strip on the left edge of the 3D viewport. Removed duplicate floating MetricsPanel overlay. **Editable metrics sidebar**: Converted read-only metrics display to editable inputs (unit mix, stories, efficiency, rentable SF, parking spaces/type, amenity SF). Added Quick Config presets (Garden 3-Story, Mid-Rise 5, High-Rise 20+). Sidebar narrowed from w-96 to w-72. Market mix "Apply" action moved inside Unit Mix section. **AI Design Chat**: New `DesignAIChat.tsx` component in the sidebar (tab toggle between Metrics and AI Chat). Same pattern as FinancialDashboard JEDI chat: system prompt includes current design state + zoning constraints + market intelligence, quick prompts ("Optimize unit mix for market", "Add 50 more units", etc.), AI returns `design_update` JSON blocks that are auto-applied to the 3D model. Backend: `POST /api/v1/design/:dealId/chat` route in `design.routes.ts`, calls Anthropic Claude. **Phase 2 placeholders removed**: `window.prompt()` and `alert()` calls in Building3DEditor replaced — AI Design button now opens the chat panel. Files: `Design3DPage.updated.tsx`, `Building3DEditor.tsx`, `DesignAIChat.tsx` (new), `design.routes.ts` (new).
-   **Assets Owned — Portfolio Deal Routing Fix (2026-03-08):** Fixed missing navigation path from Assets Owned to portfolio deals. (1) Grid endpoint (`/api/v1/grid/owned`) now LEFT JOINs `deals` table (scoped by authenticated user) to return `deal_id` alongside `property_record_id` — enables row click to navigate to the actual deal detail page. (2) Added "+ Add Asset" button to Assets Owned header bar, navigates to `/deals/create` with `location.state.dealCategory: 'portfolio'` pre-selected. (3) `CreateDealPage` reads `location.state.dealCategory` to initialize portfolio selection. (4) `handleRowClick` uses `deal_id` when available for deal navigation, falls back to `property_record_id`. Report links continue using `property_record_id`. Files: `grid.routes.ts`, `AssetsOwnedPage.tsx`, `CreateDealPage.tsx`.
-   **Highlands at Satellite — Portfolio Deal Import (2026-03-08):** Created "Highlands at Satellite" (290 units, 2789 Satellite Blvd, Duluth GA 30096) as a portfolio/owned deal with full data import from Yardi financials and Excel weekly reports. **Data imported**: (1) Deal record (portfolio category, POST_CLOSE state) + Properties record (Class A, 2020 vintage, Bell Partners operator, Gwinnett County, Duluth/Suwanee submarket). (2) 13 months of Yardi P&L (Dec 2021 – Dec 2022) into `deal_monthly_actuals` — GPR, vacancy loss, concessions, credit loss, all expense line items (payroll, property tax $1.07M/yr, insurance, utilities, mgmt fees, turnover, marketing, admin), NOI ($3.6M annual), capex, debt service, cash flow. Data source: `BPI_Financial_Package_p2122_Accrual_p21221222.csv` (latin1 encoded Yardi/YSL.Net export). (3) 243 weeks of traffic data re-linked from `highlands-2789-satellite` to deal UUID. (4) 1,429 lease transactions (900 new + 529 renewals, Nov 2020 – Mar 2026) into new `deal_lease_transactions` table — unit-level rent, market rent, prior rent, loss-to-lease, rent PSF, change amounts. (5) 18 unit types into `deal_unit_programs` with SF, bedroom count, avg/min/max rent. **New table**: `deal_lease_transactions` (id, deal_id, unit_number, unit_type, sqft, lease_type, lease_start, market_rent, prior_rent, new_rent, rent_change_dollar, rent_change_pct, loss_to_lease, loss_to_lease_pct, rent_psf). Schema in `dataPipeline.ts`, created via SQL. Import script: `backend/src/scripts/importHighlandsDeal.ts`. **Key metrics**: Peak rent Q4 2021 ($2,116 avg new lease), current Q1 2026 ($1,563, -$553 from peak), loss-to-lease widening from 0% (2022) to -10.4% (2026), renewal bumps collapsed from +$370/mo to +$13/mo, turnover declining 59%→41%, retention improving 44%→55%.
-   **Traffic Prediction Engine (2026-03-01, recalibrated 2026-03-08):** Rebuilt as a prediction-first engine — shows 10-year projections immediately without any upload. Three data source modes: `predicted` (baseline from property data), `uploaded` (from weekly reports), `blended` (actuals + predictions). Baseline prediction logic: `(units/290) × 11` weekly traffic, 99% tour conversion. **Recalibrated from 243 weeks of Highlands actuals (Jul 2021 – Mar 2026):** (1) Seasonality multipliers updated — June is actual peak (1.50x, not May), September (0.59x) and December (0.49x) are deep troughs; Jan/Feb stronger than assumed. (2) Occupancy multiplier replaced binary thresholds with 8-point gradient interpolation (80%→1.60x, 88%→1.25x, 94%→1.00x, 98%→0.78x). (3) Static 20.7% closing ratio replaced with dynamic `calculateClosingRatio(occupancy, month)` — base 20.4% adjusted by occupancy factor (1.30x at <88% occ, 1.00x at >96%) and seasonal factor (Jun 1.50x, Oct 0.85x). (4) Holiday week adjustments added — Labor Day (0.20x), Christmas (0.15x), New Year's (0.30x), July 4th (0.50x), Thanksgiving (0.40x). Backtest results: Traffic MAE improved 9.4% (5.37→4.87), Lease MAE improved 2.6% (1.89→1.84). Biggest gains: Dec (32%), Feb (23%), 2023 (18%), 2025 (22%). Data imported into `weekly_traffic_snapshots` table (deal_id: `highlands-2789-satellite`). Migration 087 added rent columns (avg_market_rent, gross_market_rent, gross_rent_psf, effective_rent, effective_rent_psf) to weekly_traffic_snapshots. 3-layer projection model: (1) Base Traffic — baseline from property data OR EMA from actuals, optionally calibrated with submarket data, (2) Market Adjustments — demand/supply/digital/seasonal factors, (3) Adjusted Output. **Submarket Calibration System** (`traffic-calibration.service.ts`): learns patterns from uploaded weekly reports across deals — avg traffic/unit, closing ratio, tour conversion, seasonal factors, website split. Stored in `traffic_submarket_calibration` table (migration 073). Auto-recalculates after each upload. Cascading lookup: submarket → city/state. Frontend: Always shows predictions (no empty state), data source badge (PREDICTED amber / BLENDED blue / LIVE DATA green), KPI cards with sparklines from predictions or actuals, Leasing Funnel with predicted typical week values, Market Intelligence panel, 10-Year Projection Table with Week/Month/Year toggle, Vacancy & Availability (when actuals exist), Data Library Calibration panel (shows calibrated vs default values, upload CTA). Backend: `weekly-report-parser.service.ts` (parser + baseline + projection engine), `traffic-calibration.service.ts` (calibration service), routes on `leasing-traffic.routes.ts` (POST upload, GET history, GET projection, GET calibration, PUT snapshot/overrides). DB: `weekly_traffic_snapshots` (072), `traffic_projection_overrides`, `traffic_submarket_calibration` (073).
-   **Trade Area Database Wiring (2026-03-01):** All `trade-areas.routes.ts` and `geographic-context.routes.ts` endpoints now use real PostgreSQL with PostGIS geometry. `POST /trade-areas` inserts into `trade_areas` table (UUID IDs, GeoJSON→PostGIS boundary). `POST /deals/:id/geographic-context` sets `deals.trade_area_id` FK. `GET /deals/:id/geographic-context` reads real trade area with geometry, plus now auto-lookups submarket/MSA via `ST_Contains` on deal boundary centroid and returns `submarket.stats` (avg_occupancy, avg_rent) and `msa.stats` from real DB tables. Submarket/MSA lookup queries use `geometry` column with `ST_Contains`. Frontend `tradeAreaId` type updated from `number` to `string` (UUID). `preview-stats` counts properties within geometry via spatial query. **DealDetailPage** (the main deal page at `/deals/:id/detail`): merged two header bars (deal info + geographic scope) into single compact row — deal name + address below on left, compact pill-style GeographicScopeTabs inline on right. `GeographicScopeTabs` now supports `compact` prop for inline pill mode with active-scope stats shown inline. Address moved from right side to below deal name. Saves ~60px vertical space. Same pattern on DealView page (uses full-width non-compact mode). **Census API Integration (2026-03-01):** `census.service.ts` connects to U.S. Census Bureau ACS 5-Year API for real demographic data. Uses Census geocoder to resolve lat/lng → state/county FIPS, then fetches tract-level data (population, median income, housing units, median rent) for the county. Estimates trade area stats by proportional area scaling. Wired into `preview-stats` endpoint as primary population/rent source; falls back to area-based density estimate if Census unavailable. Frontend shows Population, Median Income, Existing Units, Pipeline Units, Avg Rent, Housing Units with "Source: U.S. Census ACS 2022" attribution. **All 4 Trade Area Methods Fixed (2026-03-01):** (1) Radius — generates circle via `@turf/circle`, works end-to-end. (2) Drive-Time — Mapbox Isochrone API via `isochrone.routes.ts`, removed null stats from response so store falls through to Census `loadPreviewStats`. (3) Traffic-Informed AI — `traffic-ai.routes.ts` generates 6 isochrones (2 profiles × 3 time variants), unions them using `@turf/union` v7 `featureCollection()` API (NOT v6 two-argument API), extracts largest Polygon from MultiPolygon result. (4) Custom Draw — dual-mode: `CreateDealPage` passes `onCustomDraw`/`onCustomDrawCancel` callbacks → MapboxDraw added to the existing main map; `DealDetailPage` (no main map) → inline `TradeAreaDrawMap.tsx` renders inside the panel's Custom Draw tab. All 4 methods shown everywhere. Single-polygon enforced (new polygon deletes prior); `draw.delete` clears `draftGeometry`. `handleSubmit(tradeAreaIdOverride?)` fixes stale React state on save. TradeAreaDefinitionPanel: method switching calls `clearDraft()` + `onCustomDrawCancel()`, `generatedMethod` state tracks which method produced current boundary, `lastMethodRef` prevents auto-regeneration when switching back to radius. Store: `tradeAreaStore.ts` checks `stats.population !== null` before using inline stats (prevents null stats from overriding Census data).
-   **Three-Layer Traffic Fusion (2026-03-01):** Upgraded M07 to a full Three-Layer fusion model. 8-step pipeline: PULL→ADJUST→DISTRIBUTE→SEASON→CONVERT→AGGREGATE→SCORE→PROJECT. **Layer 1** (FDOT AADT): `effective_base_adt = AADT × distance_decay × road_class_weight × frontage_factor`, multi-segment aggregation for primary+secondary roads. Distance decay: `max(0.5, 1 - dist_mi × 0.15)`. Road weights: Interstate 0.3 → Local 1.0. Frontage: corner 1.4, main 1.0, side_street 0.7, interior 0.4. **Layer 2** (FDOT Temporal): `dot-temporal-profiles.service.ts` with 36 seeded profiles (6 road classes × 4 profile types: hourly/seasonal/DOW/directional). K-factor hourly curves, monthly seasonal multipliers (FL Jan=1.12 snowbird, Aug=0.91), DOW factors (Fri=1.12, Sun=0.79), D-factor directional split (AM inbound=0.62). DB: `dot_temporal_profiles` table (migration 076). `mapToFdotRoadClass()` maps normalized classes to FDOT profile keys. **Layer 3** (SpyFu $89/mo): Trend momentum via `getDomainTrend()` (QoQ comparison), competitor digital share via `getCompetitorDigitalShare()` (SEO+PPC competitors from SpyFu API). DB: `property_digital_competitors` table (migration 076). **Conversion Chain**: 3-stage `visibility_capture_rate × apartment_seeker_pct × stop_probability` → hourly walk-in potential across leasing hours → Mon-Sun daily breakdown. **T-02 Score**: Submarket percentile ranking (60% effective_base_adt percentile + 40% weekly_walkins percentile). **T-07 Trajectory**: `digital_momentum × 0.5 + yoy_aadt_growth × 0.3 + seasonal_deviation × 0.2`. **5 Cross-Layer Patterns** (`trend-pattern-detector.ts`): DEMAND_SURGE, MOMENTUM_CONFIRMED, DIGITAL_DIVERGENCE, MARKET_EXHAUSTION, SEASONAL_NOISE. **Downstream Wiring** (`p2-service-adapters.ts`): M09 seasonal vacancy, M25 momentum score, M08 acquire signals, M14 risk alerts, M05 digital share. **SpyFu Domain Pipeline**: `properties.website` column (migration 077) stores domain; `POST /property-analytics/connect` accepts `dealId` or `propertyId` + `domain`, resolves deal→property via `deal_properties`, saves website on property, auto-connects to `property_ga_connections`, and triggers immediate SpyFu fetch. `loadDataSourceSignals` auto-connects properties with `website` column to SpyFu on first load. Total cost: $89/mo flat (saves $7,668/yr vs Google polling for 10 properties).
-   **Traffic Data Source Infrastructure (2026-03-01):** Connected four real data feeds into the Traffic Prediction Engine. DB migration `075_traffic_data_sources.sql` created 9 tables: `property_visibility` (7-factor visibility assessment), `adt_counts` (DOT ADT stations), `property_traffic_context` (property↔ADT linkage + Google real-time factor), `property_ga_connections` (GA4 connection mapping), `property_website_analytics` (GA4 web traffic per property, supports `is_comp_proxy` for new-dev fallback), `property_transit_access`, `property_demographics`, `property_competition`, `traffic_comp_snapshots` (periodic comp grid snapshots). **Four Data Sources:** (1) DOT ADT + Google Maps Traffic — `traffic-data-sources.service.ts` ingests DOT CSV/Excel, links properties to nearest ADT station, fetches Google real-time traffic factor; (2) Apartment Locator AI — existing integration enhanced; (3) SpyFu Domain Analytics — `property-analytics.service.ts` connects property to website domain, calls SpyFu `getLatestDomainStats` for monthly organic/paid clicks, keyword counts, SEO value, domain strength; score blends volume (60%) + domain strength (40%); comp proxy fallback for new-dev using trade area averages; (4) Location Visibility — `visibility-scoring.service.ts` 7-component weighted scoring (positional, sightline, setback, signage, transparency, entrance, obstruction penalty) → capture rate. **Public API:** `trafficPredictionEngine.loadDataSourceSignals(propertyId)` returns all 4 source signals + data_quality score. **New Endpoints:** `GET /api/v1/leasing-traffic/data-sources/:dealId` (signals + trade area + warnings), `GET /api/v1/traffic-comps/:dealId` (comp grid), `GET /api/v1/traffic-comps/:dealId/averages`, `POST /api/v1/traffic-comps/:dealId/snapshot`, `POST /api/v1/visibility/assess`, `GET /api/v1/visibility/assessment/:propertyId`, `PUT /api/v1/visibility/update/:propertyId`, `POST /api/v1/visibility/preview`. **Frontend 6-Tab Redesign:** TrafficModule.tsx restructured into Predictions / Data Sources / Comps / Visibility Assessment / Market Adjustments / Calibration tabs. New components: `TrafficDataSourcesTab.tsx` (4-panel source grid with connection status), `TrafficCompsTab.tsx` (sortable/filterable comp traffic grid with CSV export), `VisibilityAssessmentTab.tsx` (full field survey form with live score preview). **M07 Wired:** Traffic Intelligence module wired into orchestrator, feeds M05 (Pro Forma), M08, M09 (JEDI Score), M14, M25 (Strategy). `traffic-module-wiring.ts` publishes TrafficIntelligenceV2 to DataFlowRouter.
-   **Financial Module Pipeline (2026-02-28):** Restructured Deal Capsule financial tabs from combined "Pro Forma + Traffic" and "Financial Model" into a clear 5-stage pipeline: Strategy → Traffic Module → Pro Forma → Debt → Financial Dashboard. New components:
    - `TrafficModule.tsx`: Rebuilt (2026-03-01) — see Traffic Engine above
    - `ProFormaTab.tsx`: Dual-column assumptions (Data Recommendation vs Your Override), fetches from Strategy + Traffic + Pro Forma APIs, 10-year income statement (GPR→Vacancy→EGI→OpEx→NOI→Debt Service→BTCF), NOI trend chart
    - `DebtTab.tsx`: Renamed to "Debt, Equity & Exit" — comprehensive capital markets workbench. 9 sub-tabs (Exit & Capital overview, Capital Stack (sources=uses + "Send to Pro Forma" pipeline), Equity Waterfall (fully editable multi-scenario with add/delete/rename), Debt Products (strategy-aware), Rate Strategy (live NY Fed + Treasury rates with LIVE badge, 2yr daily SVG historical chart with SOFR/EFFR/Treasury 10Y/Prime, period selector 6M/1Y/2Y, Claude AI optimal debt strategy with Cost of Capital Score, rate sheet upload with Claude parser), Key Metrics (LTV/LTC/DSCR/debt yield/WACC). Backend services: `rate-index.service.ts` (fetchLiveRates 15-min cache, fetchRateHistory 1-hr cache from NY Fed search API, getOptimalStrategy via Claude), `rate-sheet-parser.service.ts` (xlsx/csv → Claude → structured products → DB). DB: `deal_rate_sheets` table (migration 071). Routes: `/rates/live`, `/rates/history`, `/rate-sheet/upload`, `/rate-sheet/:dealId/latest`, `/optimal-strategy`. Pipeline: DealModuleContext.debtTerms flows financing terms from Debt & Equity → Pro Forma (source badge, user override support, re-syncs on each "Send to Pro Forma" click)
    - `FinancialDashboard.tsx`: JEDI RE Financial Module Architecture v3.1 — split-panel UI with 380px dark JEDI AI chat sidebar (#0c0d10) + light model panel (#f0f2f5). 7 tabs: Model Comparison, Assumptions, Debt, Projections, Sensitivity, Waterfall, Decision. Chat sidebar sends to Claude with live model as system prompt; Claude can respond with `model_update` JSON to modify assumptions in real-time. Module status pills (Strategy/Traffic/Pro Forma/Debt) with live/none status + expandable drawer. Backend: `financial-dashboard.routes.ts` with 4 endpoints: GET summary (aggregates deals, strategy_analyses, apartment_market_snapshots, traffic_projections, deal_financial_models), POST auto-assumptions, POST analyze (Claude investment recommendation cached in deal_financial_model_analyses), POST chat. Data wiring: deals table (budget, target_units, address, deal_data JSONB for city), strategy_analyses (strategy_slug + assumptions JSONB), apartment_market_snapshots (normalized to avg_rent/occupancy_rate/rent_growth), capital options extracted from model financing. Decision tab: Claude returns {recommendation, conviction, rationale[], riskFlags[], actionItems[]}
-   **Financial Model Engine (2026-02-28):** Claude-powered financial model builder. ProFormaTab redesigned as a comprehensive input control grid with 10 sections (Deal Info, Unit Mix, Acquisition/Development Costs, Disposition, Revenue, Other Income, Operating Expenses, Financing, CapEx, Waterfall). Supports "Existing Asset" and "Development" model types. User clicks "Build Model" to send all assumptions to Claude AI (`claude-sonnet-4-6`) which builds a complete financial forecast (3/5/7/10-year). Results include annual cash flows, sensitivity analysis (Exit Cap × Hold Period, Rent Growth × Hold Period), waterfall distributions, sources & uses, and return metrics (IRR, EM, CoC, DSCR). Excel export generates multi-sheet professional workbooks matching prototypical model structure. Backend: `financial-model-engine.service.ts` (Claude prompt + DB caching in `deal_financial_models` table), `excel-export.service.ts` (xlsx workbook generation), `financial-model.routes.ts` (POST /build, GET /:dealId/latest, GET /:dealId/export/excel). Database: `deal_financial_models` table (id, deal_id, model_type, assumptions jsonb, results jsonb, status, excel_path).
-   **M26 Tax Intelligence & M27 Sale Comps (2026-03-05):** Frontend modules wired into deal capsule. TaxModule.tsx (amber accent) added to Deal Design stage with tabs: Tax Summary, Yearly Projections, Assessment History, Millage Rates. CompsModule.tsx (green accent) added to Market Research stage with tabs: Comps Grid, Map View, Trend Analysis, Adjustment Engine. Both registered in DealDetailPage.tsx tab arrays, DealView.tsx renderModule switch, and sections/index.ts exports. Backend routes already registered in inline-deals.routes.ts.
-   **Unit Mix Intelligence (2026-03-05):** New `UnitMixIntelligence.tsx` component added to Market Research stage between Market Intelligence and Competition Analysis tabs. Self-contained module with 5 sub-tabs: Demand (demand matrix + supply/demand gap analysis), Program (zoning constraints + unit program editor), Inventory (submarket unit snapshot + property drill-down), Trends (12-month vacancy/DOM/rent/concessions charts), Comps (mix matrix + rent×SF scatter + comp detail table). Features demand scoring (undersupplied/balanced/oversupplied), interactive program editor with live zoning constraint checks, and comp average overlays. Modules referenced: M02 (zoning), M05 (inventory), M07 (demand signals), M15 (property drill-down). Wired into DealDetailPage.tsx, DealView.tsx, and sections/index.ts.
-   **Data Pipeline Foundation:** Core data infrastructure including granular P&L tracking, data uploads, proforma templates, and a Comp Query Engine.
-   **Benchmark Ingestion Pipelines:** Automated ingestion of rezoning and permit data from public ArcGIS REST APIs for Atlanta and various Florida counties.
-   **Rezoning Precedent & Evidence:** Integrates rezoning history and evidence into zoning details, development capacity, and comparator tools.
-   **Time-to-Shovel Tab (Fully Powered Up):** All mock data removed. Frontend (`TimeToShovelTab.tsx`) fetches real data from 4 API endpoints: (1) Monte Carlo simulation (`POST /benchmark-timeline/simulate`) with percentiles, Gantt phases, histogram, and financial impact; (2) Municipal benchmarks (`GET /benchmark-timeline/benchmarks`) with per-entitlement-type stats; (3) Detailed steps (`GET /benchmark-timeline/detailed-steps`) with P25/P50/P75/P90 durations; (4) Jurisdiction comparison (`GET /benchmark-timeline/jurisdiction-comparison`). Plus AI Timeline Intelligence (`GET /deals/:dealId/timeline-intelligence`) that sends deal context, benchmark data, and constraints to Claude for deal-specific risk factors, path insights, milestone estimates, recommendations, and benchmark comparison. AI results cached with `tts:{dealId}:{district_code}:{path}` prefix (30-day TTL, deal-specific to avoid cross-deal cache poisoning). Sections: DealContextBar (real deal/path/units), MonteCarloSection (histogram + Gantt + financial impact), TimelineIntelligenceSection (AI-powered), MunicipalBenchmarkSection (real benchmark stats), JurisdictionComparisonSection (cross-market comparison).
-   **3D Building Design Editor:** Interactive Three.js/React Three Fiber viewport for building massing design, displaying zoning envelopes and scenario visualizations.
-   **Design Reference Library:** Manages design reference images with upload, AI analysis, and integration into the 3D viewport.
-   **Zoning Triangulation & Confirmation Chain:** Three-source zoning data reconciliation (County Parcel Records, County Zoning Categories, Municode Ordinance) with a 10-link confirmation chain that transforms raw zoning into investment decisions. Includes parcel ingestion service, jurisdiction calibration feedback loop, and a Confirmation Chain Plumbing UI for diagnostics.
-   **Zoning Recommendation Orchestrator:** Unified intelligence layer with two data sources: (1) nearby parcel records from `county_parcels` (within 500m radius) for code distribution analysis, and (2) `benchmark_projects` for municipality-wide entitlement pattern analysis across ALL entitlement types (rezone, CUP, variance, by-right, site plan). The `scanEntitlementActivity` method queries benchmark records to produce approval rates, avg timelines, avg units, common rezone transitions, and recent precedent projects per entitlement type. Generates a `recommendedPath` (rezone vs CUP vs variance) based on approval rate, timeline, and precedent count. Scores candidates using density uplift (40%), proximity/entitlement evidence (30%), and approval precedent (30%). Produces a `strategyInsight` explaining the best entitlement path. Surfaces consistent recommendations on both the Boundary & Zoning tab (Section 3: Entitlement Intelligence with path comparison cards, common transitions, recent projects) and the Development Capacity tab (Entitlement Strategy Intelligence card with type breakdown, recent projects, recommended path badge). Results cached in `zoning_recommendations` table with 24-hour TTL.
-   **Nearby Entitlement Activity Endpoint:** Dedicated `GET /api/v1/deals/:dealId/nearby-entitlements` endpoint in `zoning-triangulation.routes.ts` providing tiered entitlement search (municipality first, county-wide fallback if <5 records). Returns per-type summary stats (count, approval rate, avg days, avg units), auto-generated insights (approval rate trends, CUP corridor patterns, industrial conversion speed, dominant rezone targets, fastest/slowest paths, unit count tiers), common rezone transitions with ordinance URLs, and up to 50 project records. Replaces the old "Common Rezone Targets" and "Rezoning Precedent" sections on the Boundary & Zoning tab with a rich "Nearby Entitlement Activity" section featuring colored insight cards, type summary strip, transitions table, and collapsible project list. Handles three data tiers gracefully: rich (10+ records), sparse (1-9), and empty (0).
-   **Benchmark Enrichment Service:** Extensible county assessor enrichment pipeline (`backend/src/services/benchmark-enrichment.service.ts`) that queries county ArcGIS property assessor APIs to backfill lot sizes, building SF, assessed/appraised values, tax districts, and sale history on `benchmark_projects`. Three county connectors configured: Fulton County GA (Tax Parcels FeatureServer), Hillsborough County FL (HC_Parcels), Miami-Dade FL (Property Boundaries with geometry-derived lot acres). Adding a new county is just adding a config object. Admin endpoint: `POST /api/v1/admin/benchmark-enrichment/run?county=X`. Single property enrichment: `POST /api/v1/properties/:id/enrich`. All fields nullable — works universally whether data exists or not.
-   **Density Benchmarks Endpoint:** `GET /api/v1/deals/:dealId/density-benchmarks` computes real-world "density achieved" (units/acre) from benchmark projects matching a deal's zoning code. Tiered search: municipality → county → state prefix. Now includes geography-based nearby projects (same municipality/county, any zoning code) alongside code-matched results. Returns `projects` (code-matched), `nearbyProjects` (geography-matched), `codeMatchCount`, `nearbyMatchCount`, benchmarks (per-code stats grouped from both sets), utilization percentage, and rezone-from data. Each project tagged with `matchType: 'code' | 'nearby'`. Data availability computed from combined project pool. Three data tiers: rich (3+), sparse (1-2), none (0). All fields nullable.
-   **Dev Capacity Market Reality Check:** Similarity-ranked comparable projects on Development Capacity tab. Backend computes a similarity score (0-100) for each benchmark project based on: lot size proximity (40%), unit count proximity (20%), same municipality (15%), same project type (15%), same zoning code (10%). Projects sorted by similarity score descending. Returns top 10 code-matched + top 10 nearby projects, and a `bestComparable` field with the highest-scored project. `cleanProjectMapper` now includes `projectName`, `stories`, `sourceUrl`, `similarityScore` alongside existing `docketNumber` and `ordinanceUrl`. Frontend redesigned from grouped-card layout to: (1) compact summary header row with density/FAR/lot coverage averages vs zoned max with utilization badges, (2) highlighted "Best Comp" card showing the most similar project with all metrics and document links, (3) concise ranked table of top 5 projects (expandable to all) with columns: rank, project name/address, lot size, units, density, entitlement path, similarity score, and document links (PDF icon for ordinance URL, link icon for source URL, docket badge). "Projects That Left [code]" section preserved at bottom.
-   **Entitlement Comparison (AI-Driven Dynamic Table):** Replaced fixed 3-column table with AI-driven dynamic comparison powered by `EntitlementComparisonEngine` (`backend/src/services/entitlement-comparison-engine.service.ts`). The engine takes any deal and: (1) resolves constraints for each zoning code (DB first, then Claude via `ZoningAgentService` if missing), (2) runs `BuildingEnvelopeService` math for each path, (3) detects and computes overlay district impacts, (4) calls Claude for per-path AI insights and an overall recommendation. **Dynamic columns**: By-Right (always), Variance (always), Overlay(s) (multiple supported — one column per overlay, up to 3, with deduplication across profile overlays, `zoning_districts.overlay_districts`, and `zoning_overlays` table; first overlay uses key `overlay`, subsequent use `overlay_1`, `overlay_2`), Rezone (4-tier fallback: user-selected → orchestrator → rezone-analysis → multiplier). Column count adapts (2-7 columns). **Dynamic rows**: Standard metrics (Zoning Code, Density, FAR, Max Units, GBA, Stories, Parking, Binding Constraint) plus AI-suggested extra rows when codes have special rules (step-backs, conditional uses, buffers). **AI Insights**: Per-column insight row with practical developer-focused commentary, plus overall summary card below the table. **No dollar cost estimates** — removed from the table. Frontend renders whatever structure the API returns. Avg Unit Size control (default 900 SF, range 400-2000) spans all columns in a row above the per-path Controls row; changing it triggers a debounced recalculation of Max Units, GBA, and Parking across all paths. The `BuildingEnvelopeService` accepts `avgUnitSizeOverride` in its inputs, which overrides the config default when provided. Multifamily and mixed-use defaults changed from 850 to 900 SF. Variance % input and rezone code dropdown preserved in the Controls row. Backend `GET /deals/:dealId/scenarios/recommendations` returns both legacy `recommendations` array (backward-compat) and new `comparison` object with `columns`, `rows`, `cells`, `aiSummary`. Route handler reduced from ~325 lines to ~15 lines.
-   **FAR-Derived Floor Calculation (2026-02-28):** `calculateMaxFloors` in `BuildingEnvelopeService` now derives floors from `Math.ceil(appliedFAR / lotCoverageFraction)` when no height/stories data exists, instead of defaulting to 1 floor. Ensures FAR-based zones show true multi-story capacity. Density back-calculated from `maxUnits / lotAcres` for FAR-derived zones.
-   **Path Selection to 3D Design (2026-02-28):** "Select Path" row at bottom of Entitlement Comparison table. Clicking a column builds a `BuildingEnvelope` (units, GBA, stories, footprint, parking, construction type) and writes it to `zoningModuleStore.selected_envelope` + `development_path`. Downstream modules (3D Design, Strategy, ProForma, Risk) read from the store. Summary bar shows selected path with Clear option.
-   **MRC Conditional Variants (2026-02-28):** Backfilled `zoning_districts` for MRC-1-C (35ft/3stories), MRC-2-C (52ft/5stories/FAR4.0/residential_far=1.49/nonresidential_far=2.50), MRC-3-C (225ft/20stories/FAR8.0/residential_far=3.20/nonresidential_far=4.00) with density_method=far_derived.
-   **Entitlement Comparison Audit Fixes (2026-02-28):** Six root-cause fixes:
    1. **Residential FAR selection**: By-Right and Variance now use `residentialFAR` (1.49 for MRC-2-C) instead of combined `maxFAR` (4.00) for residential deals. `extractBaseConstraints` rewritten with `parseOrNull`/`intOrNull` helpers to correctly handle `0` values (fixes `0 || null` JavaScript falsy bug).
    2. **Rezone re-computation**: Orchestrator/rezone-analysis fallback paths now re-compute the envelope via `resolveConstraints` + `computeEnvelope` instead of using pre-computed metrics. This ensures FAR-derived density is used and `appliedFar` is populated (no more "--").
    3. **Parking exclusion for FAR-derived zones**: `byParking` surface-parking constraint removed from `constraintEntries` when `densityMethod=far_derived`, preventing surface parking from artificially capping units on high-density urban sites.
    4. **Variance label transparency**: Label changed to `Variance (+X% Density / +Y% FAR)` to reflect the 0.75x FAR dampening. Achieved FAR shown when height is binding: e.g., "4.25 (of 4.60)".
    5. **BeltLine Overlay parking exemption**: Added `BELTLINE` overlay to `zoning_overlays` table and test deal profile with `min_parking_per_unit = 0`. Parking exemption propagated to rezone constraints via base-constraint override.
    6. **Lot coverage normalization**: `resolveConstraints` now auto-normalizes `maxLotCoverage` values stored as fractions (≤1) to percentages (*100), fixing a double-division bug where 0.85 → 0.0085 → tiny footprint.
-   **Regulatory Risk Analysis (AI-Powered Tab 4):** Three-pillar architecture (Project Data + Data Library + Claude AI). Backend endpoint `GET /api/v1/deals/:dealId/regulatory-risk-analysis` gathers deal context, resolved zoning constraints, benchmark project data (15 recent projects, permit timelines), Data Library costs (impact fees, construction costs, rent comps), and existing regulatory alerts — sends everything to Claude for structured risk assessment. Returns 8 risk categories (Zoning Stability, Permit Timeline, Impact Fees, Inclusionary Housing, Rent Control, STR Regulation, Environmental, Historic Preservation) each with score (0-100), level, trend, impact description, and cost impact. Also returns strategy matrix (4 strategies: Build-to-Sell, Flip/Value-Add, Long-Term Rental, Short-Term Rental), active alerts with severity/probability/timeframe, and mitigation strategies. Cached in `zoning_code_interpretations` with `regrisk:{district_code}` prefix (30-day TTL). Frontend `RegulatoryRiskTab.tsx` fetches real API data — no mock data.
-   **DataLibraryContextService:** Shared service (`backend/src/services/data-library-context.service.ts`) that any module can use to pull real cost/comp data from the Data Library. Method: `getContextForDeal({dealId, municipality, state, propertyType, zoningCode, lotAreaSf})` returns structured context with sections: `impactFees`, `constructionCosts`, `rentComps`, `permitTimelines`, `recentProjects`, and a `costSummary` text block. Pulls from `data_library_files` (parsed spreadsheets tagged by category) and `benchmark_projects` (real permit/project costs and timelines). Gracefully returns empty sections when no data available.
-   **Fulton County Enrichment Run (2026-02-28):** Ran enrichment for 17 Fulton County benchmark records via `run-enrichment.ts Fulton`. All 17 enriched successfully with land_acres, assessed/appraised values, and density_achieved. Building SF not available from Fulton County ArcGIS API (no building footprint field), so FAR and lot_coverage remain null for Fulton records.
-   **Properties Table Assessor Fields:** Extended `properties` table with `assessed_value`, `assessed_land`, `assessed_improvements`, `appraised_value`, `appraised_land`, `appraised_improvements`, `tax_district`, `annual_taxes`, `millage_rate`, `building_sf`, `parcel_id`, `last_sale_amount`, `last_sale_date`, `enrichment_source`, `enriched_at`. All nullable.
-   **Deal-Property Auto-Linker:** `DealPropertyLinkerService` (`backend/src/services/deal-property-linker.service.ts`) auto-links deals to properties via address matching. Creates property records from deal data when no match exists. Handles duplicate addresses gracefully. API endpoints: `GET /api/v1/deals/:dealId/properties` (linked properties with all assessor data), `POST /deals/:dealId/properties/:propertyId/link` (manual link), `DELETE /deals/:dealId/properties/:propertyId` (unlink), `POST /admin/deal-property-autolink` (batch). All 22 deals linked to 18 properties.
-   **Property Data Tab:** Renamed Market Intelligence "Market Data" tab to "Property Data" (`PropertyDataTab.tsx`). Added "Property Assessment" section to property flyout showing parcel ID, lot size, building SF, assessed land/improvements, appraised value, and tax district — all fields shown only when non-null with graceful empty state. Includes enrichment source badge and "Enrich from County" button that calls `POST /api/v1/markets/property-records/:id/enrich` for supported counties (Fulton GA). Property records table extended with `enrichment_source` and `enriched_at` columns.
-   **Zoning Interpretation Cache:** Two-table caching layer (`zoning_code_interpretations` + `zoning_ai_analysis_cache`) that stores fully-resolved zoning constraints and Claude AI analysis results in the database. Migration: `069_zoning_interpretation_cache.sql`. Service: `ZoningInterpretationCache` (`backend/src/services/zoning-interpretation-cache.service.ts`). Integrated into `EntitlementComparisonEngine` as a 6th constructor arg. Cache-first lookup in `resolveConstraints` (30-day TTL, keyed by UPPER code+municipality+state), `computeSingleOverlay` (same), and `generateAIAnalysis` (7-day TTL, keyed by sorted code set joined by `|`). Write-through: after resolving from DB or agent, result is persisted to cache. Second load of same deal skips all constraint resolution and Claude calls, making repeat loads near-instant. Logs `[Cache] HIT/MISS` for observability.

-   **Settings & Billing Overhaul (2026-03-09):** Complete redesign of the Settings page (`/settings`) with 10 functional tabs. **Profile tab**: reads real user data from `GET /api/v1/auth/me`, editable first/last name and phone, real subscription tier badge (color-coded per tier), saves via `PUT /api/v1/auth/profile`. **Subscription tab**: billing dashboard showing current plan card (tier, status, renewal date), credit usage progress bar (green/yellow/red zones), 30-day usage breakdown by operation type from `ai_usage_log`, "Manage Billing" button opens Stripe Customer Portal, "Change Plan" links to PricingPage. **AI Model tab**: 4 selectable model preference cards (Auto/Fast/Balanced/Powerful) with speed/quality indicators and credit cost notes. Tier-gated: Opus locked for scout/operator with upgrade prompt. Preference persists in `user_credit_balances.llm_preference` column and overrides the default MODEL_ROUTING in `aiService.ts`. **Notifications tab**: SMS toggle with phone input, 5 email alert categories (deal updates, market alerts, credit warnings, weekly digest, agent results), saved to `users.notification_preferences` JSONB column. **PricingPage → Stripe Checkout**: CTA buttons now call `POST /api/v1/billing/create-checkout-session` with tier mapping (Starter->scout, Flipper->operator, Investor->principal). Creates Stripe Customer before checkout if none exists, sets `client_reference_id` for webhook resolution. **Backend**: `billing.routes.ts` (4 endpoints: create-checkout-session, create-portal-session, GET subscription, GET usage), `settings-ai.routes.ts` (GET/PUT ai-preferences with tier validation). **Migrations**: 088 (llm_preference), 106 (notification_preferences). Files: `billing.routes.ts`, `settings-ai.routes.ts`, `SettingsPage.tsx`, `SubscriptionSettings.tsx`, `AIModelSettings.tsx`, `NotificationSettings.tsx`, `PricingPage.tsx`, `auth.routes.ts`.
-   **Admin API Panel (2026-03-04):** Comprehensive admin REST API at `/api/v1/admin` (`backend/src/api/rest/admin.routes.ts`), registered in `index.replit.ts`. All endpoints require `requireAuth` + `requireAdmin` (role check). **7 endpoint groups**: (1) Data Ingestion — POST zoning-districts, atlanta-benchmarks, florida-benchmarks, map-properties-to-zoning; GET ingest/status. In-memory job tracker (`Map<string, IngestionJob>`). (2) System Monitoring — GET system/health (pg_stat_activity, table sizes, memory), system/stats (counts for users/deals/properties/scenarios/benchmarks/municipalities), system/logs (error_logs + deal_activity). (3) Data Management — GET data/municipalities, data/zoning-coverage, data/benchmark-stats; POST data/refresh-cache (clears stale zoning interpretations/AI cache). (4) User Management — GET users (paginated+search), GET users/:id, POST users/:id/suspend, DELETE users/:id. (5) Deal Management — GET deals (filtered), GET deals/:id/audit, POST deals/:id/fix (remove empty scenarios, fix multi-active), DELETE deals/:id/force (transactional cascade delete across 20+ related tables). (6) Data Quality — GET quality/missing-zoning, quality/invalid-boundaries, quality/orphaned-data; POST quality/auto-fix. (7) Integration Status — GET integrations/apartment-locator-ai, integrations/apis; POST integrations/test. (8) Background Jobs — GET jobs, POST jobs/:id/cancel, GET jobs/:id/logs. Destructive operations use `transaction()` wrapper and `logger.info` audit trail.
-   **Admin Panel Frontend (2026-03-04):** Full admin panel UI at `/admin` (`frontend/src/pages/admin/AdminDashboard.tsx`). Redesigned from static card grid to tabbed layout with left sidebar nav + content area. 5 sections: (1) **System Health** (`SystemHealthSection.tsx`) — DB status, connections, size, server version/uptime, heap memory bar, platform totals grid (7 metrics), scenarios by type, integration test results (database/apartment-locator/census). (2) **User Management** (`UserManagementSection.tsx`) — paginated user table with search, email/name/role/status/deals/login columns, expand for detail+activity, suspend/reactivate toggle, delete with confirmation. (3) **Deal Oversight** (`DealOversightSection.tsx`) — paginated deal table with status badges, expandable audit trail (scenarios/state history/activity), fix deal action, force delete with confirmation, collapsible Data Quality panel (missing zoning/invalid boundaries/orphaned data counts + auto-fix button). (4) **Data Coverage** (`DataCoverageSection.tsx`) — zoning coverage % bar, benchmark totals by state/entitlement, municipality table (collapsible), state district bars, Apartment Locator AI status card, cache refresh button, links to Data Tracker and Property Coverage tools. (5) **Background Jobs** (`BackgroundJobsSection.tsx`) — job trigger buttons (4 ingestion types), job table with status badges/progress bars/log viewer, cancel running jobs, auto-refresh every 10s while jobs running. Top header shows live quick stats (users/deals/properties/scenarios). All sections use `apiClient` with auth tokens, graceful error states, and action feedback. Files: `frontend/src/pages/admin/sections/`.
-   **Apartment Locator AI Integration (2026-03-02 multi-metro):** Live data pipeline from `https://apartment-locator-ai-real.replit.app`. **Zip-Based Scraping System**: 928 zip codes across 17 metros (up from 109 city-name searches), 30 listings per zip for complete neighborhood coverage, ~140 actor runs per scrape (~$2.80/run on Apify Starter $29/mo). **Coverage**: Atlanta (91 zips), Houston (120), South Florida (129), DFW (92), Tampa Bay (78), San Antonio (64), Austin (53), Gulf Coast FL (53), Jacksonville (44), Charlotte (43), Orlando (42), Raleigh-Durham (38), Nashville (33), Charleston (17), Savannah (11), Tallahassee (10), Gainesville (10). **Sync Schedule**: Apartment Locator AI scrapes every Friday 9 AM ET → JediRE syncs all 17 metros every Saturday 9 AM EST via `apartment-sync-scheduler.ts` (sequential per-metro sync with error isolation). Market snapshots use `ON CONFLICT (city, state, snapshot_date)` upsert. **New Admin Endpoints**: `GET /api/v1/apartment-sync/zip-stats` (full coverage breakdown with sync status per metro), `POST /api/v1/apartment-sync/zip-batch` (manually sync specific metros), `POST /api/v1/apartment-sync/sync-all-metros` (trigger full 17-metro sync). Existing endpoints: pull, status, properties, market-snapshots, rent-comps, supply-pipeline, trends, submarkets, user-analytics, demand-signals.
-   **M07 Traffic Routes Mounted (2026-03-01):** `traffic-data.routes.ts` and `traffic-comps.routes.ts` now mounted in `backend/src/api/rest/index.ts` at `/api/v1/traffic-data` and `/api/v1/traffic-comps`. Traffic-data routes: ADT upload (CSV/XLSX), station queries, nearest-ADT lookup, property traffic context, property-ADT linking, real-time traffic factor, bulk-link. Traffic-comps routes: per-deal comp grid, averages, snapshots, proxy candidates. Services: `TrafficDataSourcesService` and `CompTrafficService`.
-   **Correlation Engine (COR-01 through COR-20):** `correlationEngine.service.ts` computes 20 market correlation metrics from real synced apartment data + MSA data. Currently 10 metrics computable from available data (COR-01,03-06,09,13-16), 10 pending external data sources (FDOT AADT, Census BFS, IRS SOI, BLS QCEW, SpyFu, NLP review analysis). REST endpoints at `/api/v1/correlations` with `/report` (full 20-metric report), `/summary` (signals only), `/metric/:metricId` (single metric), `/property/:propertyId` (property-scoped). Key live signals: COR-01 bullish (search surge +24.8%), COR-04 neutral (affordability 30.9%), COR-13 bearish (rent-to-income 33.3% exceeds ceiling).
-   **DealsTab (Acquisition Intelligence Focus):** Lean, action-oriented tab per UI Enhancement doc (S1, S2, Items 2/3). Shows: PCS Rank + movement, T-04 Quadrant Badge with QuadrantFilterChips, Target Score (TS) with tooltip, Owner entity, Triple Trigger badge, Traffic Qualification badge, TAR, Lifecycle Phase badge, T-07 Trajectory, T-02/T-03 scores, PCS component breakdown, Surge Index. AI insight box focuses on acquisition intelligence (PCS gap, management underperformance, triple trigger, traffic qualification). Correlation Intelligence and Comp Intelligence sections removed — those belong in Trends tab and Deal Page respectively per architecture doc. Market Deal Activity table includes Owner column and Target Score. Strategy Arbitrage Leaderboard retained.
-   **TrendsTab (Correlation Intelligence Home):** Per UI Enhancement doc (S4, Item 11), Trends tab is the home for correlation analysis. Fetches live data from `/api/v1/correlations/report` via `apiClient` on mount. Renders all 10 computed COR metrics (COR-01,03,04,05,06,09,13,14,15,16) as individual rows with signal badges, confidence, X/Y values, and actionable insights. 10 pending metrics in collapsible section with missing data sources. Shows "LIVE DATA"/"SAMPLE DATA" badge. Also includes: Pearson r-value chart (8-quarter rent-traffic-wage overlay), Rent-Wage Divergence Alert, Affordability Ceiling Gauge (with live COR-04/COR-13 data), and Rent Runway Indicator (wage vs rent gap visualization with live COR-04 data). Falls back gracefully if API unavailable.
-   **F40 Performance Score Engine:** 4-dimension scoring system in `backend/src/services/f40-performance-score.service.ts`: Rent Position (30%), Occupancy Strength (30%), Pricing Power (20%), Vintage Physical (20%). API routes at `/api/v1/f40` with market, rankings, and calculate endpoints.
-   **Opportunity Engine:** Value-add opportunity detection in `backend/src/services/opportunity-engine.service.ts`. Core formula: High Market Score + Low Property Score = opportunity. Combines F40 scores with trend/demand data to rank submarkets by opportunity score, estimated upside (% and $), and recommended strategy (renovate/rebrand/reposition/acquire). API at `/api/v1/opportunities` with `/detect` and `/rankings` endpoints. Frontend `OpportunityEngineSection.tsx` with ranked cards, signal indicators, strategy filters, and sort controls.
-   **Full Demand Intelligence Ingestion (2026-03-02):** Extended ingestion pipeline to fully consume all new JediRE API fields from `/api/jedi/demand-signals` and `/api/jedi/user-preferences-aggregate`. **TypeScript interfaces** added to `apartmentLocatorIntegration.ts`: `DemandSignalsResponse` (budget, bedroom_demand, commute_preferences, location_demand, top_amenities, deal_breakers, apartment_features, move_in_timeline) and `UserPreferencesAggregateResponse` (preferred_cities, price_preferences, bedroom_preferences, alert_preferences, poi_interests, top_amenities, deal_breakers, lifestyle_priorities, apartment_features, setup_stats) with typed client methods `getDemandSignals()` and `getUserPreferencesAggregate()`. **Enriched demand consumption** in `opportunity-engine.service.ts`: `getDemandData()` now returns full `EnrichedDemandData` with all fields parsed from stored JSON blobs (handles both camelCase and snake_case API field names). `demandSignalScore()` factors in moveInTimeline urgency, bedroomDemand volume, and budget presence. **New REST endpoint** `GET /api/v1/demand-intelligence?city=` (`demand-intelligence.routes.ts`) exposes parsed demand + preferences data. **JEDI Score wiring** in `jedi-score.service.ts`: `fetchDemandIntelligence()` feeds new signals into all 5 sub-scores — apartment_features boost demand, bedroom_demand enriches supply gap analysis, move_in_timeline boosts momentum, commute_preferences boost position, deal_breakers increase risk. **Module wiring** in `data-flow-router.ts`: 4 new data flow connections (M06→M14 deal_breakers, M06→M08 budget/bedroom, M06→M07 commute, M06→M25 full demand intel). **Strategy arbitrage** in `strategy-arbitrage-engine.ts`: budget distribution and bedroom demand factor into strategy scoring (higher avg budget boosts rental/STR, family-size demand boosts build-to-sell).
-   **F40 Comp Set Builder:** `getCompSetForSubmarket` method on F40 service returns dual-tab comp sets: trade area comps (all submarkets in the city, ranked by F40 score) and like-kind comps (ranked by similarity score using unit count 40%, property count 30%, F40 score proximity 30%). API at `GET /api/v1/f40/comp-set?city=&submarket=`. Competition page (`CompetitionPage.tsx`) shows F40 quartile badges on competitor cards and a dedicated "F40 Performance" tab with rankings table and dimension breakdowns.
-   **LIVE/SAMPLE Data Badges:** OverviewSection, StrategySection, DealStatusSection, and RiskIntelligence now show visible "LIVE DATA" (green) or "SAMPLE DATA" (amber) badges based on whether real API data or mock fallback is being displayed. No more silent mock data.
-   **Deal Capsule Module Wiring (22 modules wired to real APIs):** All Deal Capsule sections now fetch from real backend APIs with mock data fallback. Each module shows a "LIVE DATA" badge when using real API data. Wired modules include:
    - **OverviewSection**: JEDI Score from `/api/v1/jedi/score/:dealId`
    - **MarketIntelligence**: Apartment sync submarkets + trends data
    - **SupplyIntelligence**: Real supply pressure computed from apartment trends + submarkets
    - **DealStatusSection**: Deal lifecycle from `/api/v1/deals/:dealId/state`
    - **RiskIntelligence**: Comprehensive risk from `/api/v1/risk/comprehensive/:dealId`
    - **StrategySection**: Strategy analyses from `/api/v1/strategy-analyses/:dealId`
    - **CapitalStructureSection**: Capital stack, debt products, rate analysis from `/api/v1/capital-structure/*`
    - **ExitSection**: Exit strategy data loaded/saved via deal state API
    - **FinancialModelingSection**: Built from placeholder to functional component with scenario builder, sensitivity analysis, auto-save to `/api/v1/financial-models`
    - **TrafficEngineV2Section**: Leasing traffic from `/api/v1/leasing-traffic/*` with clear DEMO/LIVE indicators
    - **ProFormaWithTrafficSection**: Pro forma from `/api/v1/proforma/:dealId/*` with initialize flow
    - **FilesSection**: Real file upload/download/delete via `/api/v1/deals/:dealId/files`
    - **TimelineSection**: Timeline data from deal state API with save functionality
    - **ProjectManagementSection**: Project tasks from deal state API with save functionality
    - **AIRecommendationsSection**: Recommendations from deal state or JEDI score API
    - **OpusAISection**: Enriched with live market/strategy/risk context for AI analysis

### Agent Dashboard

Provides CRM functionalities for client, deal, lead, and activity management using a Drizzle ORM schema.

### Traveloure

AI-driven travel booking platform for itinerary generation and booking orchestration.

## External Dependencies

-   **PostgreSQL:** Primary database.
-   **Stripe:** Payment processing.
-   **Anthropic/Claude API:** AI capabilities.
-   **Mapbox:** Frontend map rendering.
-   **CoStar:** Historical real estate market data.
-   **Census API:** Demographics data.
-   **Google Trends:** Search demand data.
-   **Microsoft Graph API:** Contact import (Outlook).
-   **Google People API:** Contact import (Google accounts).
-   **Booking.com, Viator, GetYourGuide, OpenTable, Resy, Skyscanner:** Affiliate partners (for Traveloure).
-   **Python Libraries:** GeoPandas, NumPy, SciPy, pandas, openpyxl.

-   **Competitive Intelligence & Opportunity Engine (2026-03-01):** 4-system CI engine integrated into sidebar Intelligence section. Each system has its own standalone page following the Market Vitals design pattern (dark header with "THE DECISION THIS PAGE DRIVES", 5 KPI cards with sparklines, insight summary bar, detailed content sections). Systems and routes:
    - S1 Performance Rankings (`/competitive-intelligence/performance`, `PerformanceRankingsPage.tsx`) — emerald theme, PCS component breakdown, rankings table, vantage group + trajectory outputs
    - S2 Acquisition Intel (`/competitive-intelligence/acquisition`, `AcquisitionIntelPage.tsx`) — blue theme, underperformer detection algorithm, ownership/debt intelligence, vantage group targeting, acquisition targets table with 10 properties (performance gap, owner intel, debt signals, urgency, value-add)
    - S3 Comp Analysis (`/competitive-intelligence/comps`, `CompAnalysisPage.tsx`) — violet theme, **market-level competitive overview** (not deal-specific): 5 KPI cards (Tracked Properties, Avg PCS Score, Rent Ceiling, PCS Spread, Hidden Gems Detected), Submarket Comp Landscape table (top 20 properties ranked by PCS, sortable by PCS/Rent/Occupancy, "Analyze" action per row), 4 Vantage Group Clusters (avg PCS, rent range, count, top performer), 4 Competitive Pattern cards (Rent Ceiling Compression, Amenity Arms Race, Digital Share Shift, Vintage Cascade). Deal-specific comp analysis lives in Deal Capsule (see below)
    - S6 Opportunity Alerts (`/competitive-intelligence/alerts`, `OpportunityAlertsPage.tsx`) — cyan theme, 5 alert types with examples, 4 delivery channels
    - Removed standalone pages: Data Flywheel (Deal Archive → future Assets Owned tabs; Property Records → Market Intelligence tabs), Pattern Engine (detection feed → Dashboard intelligence feed; Correlation Lab → Trends tab advanced mode; individual patterns → Market Intelligence tabs), Rank-Me Tool (embeds as section/drawer on Deal Page + Assets Owned, not standalone)
    - `/competitive-intelligence` redirects to `/competitive-intelligence/performance`
    - All pages in `frontend/src/pages/CompetitiveIntelligence/`. Original `CompetitiveIntelligencePage.tsx` retained as data reference.
-   **CI Engine Frontend Integration Phase 1 (2026-03-01):** Embedded CI intelligence into 4 existing pages with 8 changes (P0+P1 priority). All use mock data — backend services not yet built.
    - **Market Intelligence — Power Rankings Tab** (`tabs/PowerRankingsTab.tsx`): **Wired to live data (2026-03-01).** Backend `GET /api/v1/rankings/:marketId` (`rankings.routes.ts`) queries `property_records` table for multifamily properties (units > 50), computes PCS score from assessed_value/unit, year_built, units, building_sqft/unit, and owner portfolio. Derives property class from class_code (C3/C4/C5 mapping) and submarket from address/neighborhood_code. Returns top 30 ranked properties. Frontend fetches from API with loading state, error fallback to mock data, and LIVE DATA / SAMPLE DATA badge. Sortable table with PCS score, rank, movement arrows, sparkline trends, VantageGroupFilter (Class/Vintage/Size), PCSBreakdownDrawer (5-component score), owner display. Dynamic AI insight summary based on actual top risers/fallers.
    - **Market Intelligence — Trends Tab Enhancement** (`tabs/TrendsTab.tsx`): Added Rent-Traffic-Wage Correlation chart (3-line SVG with Pearson r-values), Divergence Alert Banner (rent outpacing wages), Affordability Ceiling Gauge (rent-to-income ratio with 30% threshold). 8 quarters mock data.
    - **Market Intelligence — Deals Tab Enhancement** (`tabs/DealsTab.tsx`): Added PCS Rank column (movement badges), T-04 Quadrant badges (Hidden Gem/Validated Winner/Hype Risk/Dead Weight), Target Score column, QuadrantFilterChips for filtering. **Enriched (2026-03-01):** Featured card now shows T-02/T-03 Physical/Digital scores, Lifecycle Phase badge (Emergence/Acceleration/Maturation/Contraction), T-07 Trajectory with label, Traffic Surge Index (COR-01), TAR (Traffic Arbitrage Ratio) with underpricing %, Traffic Qualification badge (Qualified/Marginal/Disqualified) next to strategy, collapsible PCS Component Breakdown (5 mini-bars: Revenue/Traffic/Occupancy/Ops/Asset + performance gap), Triple Trigger alert (underperformance + hold stress + debt maturity), management company with PCS percentile, Correlation Intelligence section (Rent-Traffic Gap COR-01/03, Wage Runway COR-04/13, Sentiment-to-Value COR-14/15), Comp Intelligence section (Like-Kind Benchmark + Amenity Gap Summary), updated AI Insight box referencing all new signals. Compact cards enhanced with lifecycle badge, trajectory, TAR, traffic qualification, and traffic share.
    - **Deal Page — Traffic Intelligence Section** (`sections/TrafficIntelligenceSection.tsx`): New collapsible section with T-02/T-03 score gauges, T-04 quadrant badge, walk-in prediction chart (hourly/daily toggle), T-07 trajectory, Google Reviews sentiment (5-category NLP), review trend chart, submarket rank position bar.
    - **Deal Page — Competitive Position Section** (`sections/CompetitivePositionSection.tsx`): Dual-tab comp analysis (Trade Area + Like-Kind), amenity gap matrix with rent lift estimates, rent position scatter plot, 4 pattern alert cards (Amenity Arms Race, Vintage Cascade, Occupancy Divergence, Traffic Concentration Risk).
    - **Deal Capsule — Competition Module (2026-03-01):** Added **Comp Analysis** as the first tab inside CompetitionPage (`development/CompetitionPage.tsx`), before existing tabs (Competitive Set Map, Unit Comparison, F40 Performance, Advantage Matrix, Aging Competition, Waitlist Intelligence). Comp Analysis tab renders `DealCompAnalysisTab.tsx` — Submarket Comp Landscape table (same design as sidebar CompAnalysisPage): 20 ranked properties with columns for Rank, Property (name+class+submarket), Units, Avg SF, Avg Rent, Occupancy, PCS Score (with colored bar), Rent vs Ceiling, Trend arrow, Analyze action. Sortable by PCS/Rent/Occupancy. Market Vitals header with 5 KPI cards and insight bar. CompetitionPage reads `subtab=comp-analysis` URL param to default to Comp Analysis when navigated from external links.
    - **Assets Owned — Comp Set Performance Tab (2026-03-02):** Real comp set management on AssetsOwnedPage (violet theme). DB: `deal_comp_sets` table (migration 078) with match_score, match_factors JSONB, distance_miles, year_built, stories, units, class_code, google_rating, google_review_count. Discovery: `comp-set-discovery.service.ts` weighted scoring (proximity 35%, vintage 20%, size 20%, class 15%, stories 10%) queries `property_records` joined with `properties` for coordinates, falls back to metro-wide matching. API: `deal-comp-sets.routes.ts` with GET/POST/DELETE/PATCH + `/discover`. Auto-discovery fires async on deal creation. Frontend: expandable rows per owned asset showing real comps from API, inline add comp form, remove comp, re-discover button. No mock data — all real API-driven.
    - **Navigation Bridges (2026-03-01):** "Run Comp Analysis" button on Deals Tab featured deal navigates to deal page Competition tab with `subtab=comp-analysis`. "Comps" button on Acquisition Intel targets navigates to `/competitive-intelligence/comps`.
    - **Deal Page — Strategy Traffic Gates** (`sections/StrategySection.tsx`): TrafficGateBadge per strategy (Qualified/Marginal/Disqualified), T04QuadrantInfluencer badge showing weight shifts. Integrated into both strategy score matrix and strategy cards.
    - **Pipeline Grid — Intelligence Columns** (`DealsPage.tsx`): 3 new columns (PCS Rank, T-04 Quadrant, Target Score) with colored badges. QuickFilterRow with quadrant and rank tier filter chips. Mock data injected into 20 deals.
    - **Assets Owned — Performance & Rankings Tab** (`AssetsOwnedPage.tsx`): New first tab with PortfolioAggregateScore header, AssetActionPriority card, OwnedAssetRankingTable (6 assets with PCS, rank, movement, trajectory, 12-month SVG sparkline with target line, Rank-Me button linking to `/competitive-intelligence/projection`).

-   **Full Ingestion Script & Endpoint (2026-03-04):** Consolidated ingestion script `backend/src/scripts/run-full-ingestion.ts` handles all data ingestion with connection retry logic (5 retries with exponential backoff), per-step error isolation, and progress reporting. 6 steps: (1) Seed API Municipalities, (2) Fetch Zoning Districts, (3) Florida Benchmarks, (4) Benchmark Enrichment, (5) Map Properties to Zoning, (6) Data Quality Cleanup. Run directly: `npx tsx backend/src/scripts/run-full-ingestion.ts` or single step: `--step=municipalities`. Also added `POST /api/v1/admin/ingest/full` endpoint that runs 5-step ingestion as a background job with status tracking. Frontend: "Full Ingestion (All Steps)" primary button in Background Jobs section.
-   **Admin Panel Frontend (2026-03-04):** 5-section tabbed admin panel at `/admin` with left sidebar nav. Sections: SystemHealthSection (DB metrics, server info, table sizes, integration test), UserManagementSection (paginated+searchable user table, suspend/delete), DealOversightSection (deal table, audit trail, data quality), DataCoverageSection (zoning coverage, benchmarks, municipality table), BackgroundJobsSection (job trigger buttons, progress bars, log viewer, auto-refresh). All wired to real backend APIs via `apiClient`.

-   **Portfolio Property Detail Page + Real Yardi Actuals (2026-03-08):** Full portfolio deal integration for Assets Owned. (1) **Portfolio API**: `backend/src/api/rest/portfolio.routes.ts` — 4 endpoints: `GET /api/v1/portfolio/:dealId/summary` (deal info + latest financials via `deal_properties`→`deal_monthly_actuals` join + unit programs + lease/traffic stats), `/financials` (all months for charting), `/leasing` (monthly stats, retention by quarter, recent transactions from `deal_lease_transactions`), `/traffic` (weekly data from `weekly_traffic_snapshots`). Uses `deal_properties` junction table to link deals→properties→actuals (since `deal_monthly_actuals` uses `property_id`, not `deal_id`). (2) **Portfolio Property Page**: `frontend/src/pages/PortfolioPropertyPage.tsx` — dedicated detail page at `/assets-owned/:dealId/property`. Header with OWNED/Class badges, unit count, vintage, traffic weeks. KPI cards: Annual NOI, Occupancy (×100 from decimal), Avg Eff Rent, Monthly Cash Flow, DSCR, Loss-to-Lease. 4 tabs: Financial Overview (NOI trend bar chart, occupancy trend line, revenue vs expenses, expense breakdown, P&L table), Leasing (new vs renewal rent trends, retention rate, loss-to-lease, transactions table), Unit Mix (unit configs from `deal_unit_programs`), Traffic (weekly traffic/closing ratio/move-in-out charts from `weekly_traffic_snapshots`). (3) **Report Endpoint Wired to Real Data**: `GET /api/v1/grid/owned/:id/report` now checks if ID is a deal first; if so, joins `deal_properties`→`deal_monthly_actuals` for real monthly data instead of synthetic seasonal-factor estimates. (4) **Grid Merges Portfolio Deals**: `GET /api/v1/grid/owned` prepends portfolio deals (deal_category='portfolio', status='closed_won') with real actuals via LATERAL join through `deal_properties`, deduplicates by address (deal rows replace property_record rows), adds `source` field ('deal'|'property_record'). (5) **Row Click Navigation**: AssetsOwnedPage routes deal-sourced rows to `/assets-owned/:dealId/property`, others to existing deal page. **Highlands at Satellite** (290 units, Duluth GA): $3.5M annual NOI, 97.6% occupancy, $1,798 avg rent, 13 months P&L, 1,429 lease transactions, 18 unit types, 243 traffic weeks — all real Yardi data. Files: `portfolio.routes.ts`, `PortfolioPropertyPage.tsx`, `grid.routes.ts`, `App.tsx`, `AssetsOwnedPage.tsx`.

-   **5th-Brain Architecture Integration (2026-03-08):** Wired 14 new files (AI Coordinator, Credit System, Chat Pipeline, Stripe Webhooks) into the running backend. (1) **Migration 105**: `105_platform_architecture.sql` creates 5 tables: `ai_usage_log`, `user_credit_balances`, `chat_sessions`, `deal_contexts`, `proactive_alerts_log` + `phone` column on `users`. (2) **MessageRouter mounted**: `services/chat/messageRouter.ts` creates routes at `/webhooks/twilio` (WhatsApp/iMessage/SMS via Twilio Conversations), `/webhooks/telegram` (Telegram Bot API), `/api/v1/chat/message` (REST API for web app — returns welcome message with quick replies for new sessions). Coexists with legacy `chat.routes.ts` at `/api/v1/chat`. (3) **Stripe webhooks mounted**: `services/stripe/webhooks.ts` at `/webhooks/stripe` — handles subscription events. (4) **API key fix**: `aiService.ts` updated to use `AI_INTEGRATIONS_ANTHROPIC_API_KEY || ANTHROPIC_API_KEY` (matching existing `chat.service.ts` pattern). (5) **SessionStore schema fixes**: Removed non-existent `is_active` column from INSERT, fixed `role` enum value from `'user'` (invalid) to `'investor'` (valid `user_role` enum). (6) **Vite proxy**: Added `/webhooks` proxy rule in `frontend/vite.config.ts` so webhook endpoints are accessible through dev domain. New services: `JediAIService` (coordinator.ts), `CreditService`, `ResearchAgent`, `ProactiveAlerts`, `SessionStore`. Files: `index.replit.ts`, `aiService.ts`, `sessionStore.ts`, `vite.config.ts`.

-   **Stripe + Twilio Replit Integrations (2026-03-08):** Wired both services using Replit's managed connector system (OAuth-based credentials, automatic token refresh). (1) **Stripe**: `stripe-replit-sync` package handles schema creation (29 tables in `stripe` schema), managed webhook auto-registration at `/api/stripe/webhook`, and data backfill on startup. `stripeClient.ts` fetches credentials from Replit connector API (`getUncachableStripeClient()`, `getStripeSync()`). Webhook route registered BEFORE `express.json()` with `express.raw()` for proper signature verification. `initStripe()` runs on server startup: migrations → webhook config → background backfill. Old manual `/webhooks/stripe` route removed. (2) **Twilio**: `twilioClient.ts` fetches credentials from Replit connector API (`getTwilioClient()`, `getTwilioFromPhoneNumber()`). Uses API key auth (account_sid + api_key + api_key_secret). MessageRouter updated to use Replit connector client instead of manual `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN` env vars, with graceful fallback if connector not configured. Files: `backend/src/services/stripe/stripeClient.ts`, `backend/src/services/stripe/webhookHandlers.ts`, `backend/src/services/twilio/twilioClient.ts`, `backend/src/services/chat/messageRouter.ts`, `backend/src/index.replit.ts`.

-   **PropertyDetailsPage — Full Data Wiring (2026-03-10):** Completed Bloomberg Terminal-style PropertyDetailsPage data pipeline. (1) **buildPropertyFromRow enhanced**: `parseCurrency()` helper parses `$28.5M`, `$158K/unit` strings to numbers. Maps all PropertyDataTab row fields: `purchaseDate`→`acquisitionDate`, `purchasePrice`→`acquisitionPrice`/`lastSalePrice`, `assessedLand`+`assessedImprovements`→`assessedValue`, `zoningCapacity`→`maxDensity` (regex-parsed), `concessions`→formatted string, `marketRent`→`avgMarketRent`, `rentPerSF` computed from rent/avgUnitSF. (2) **Default amenities by class**: A/A+ gets premium (Pool, Fitness, Business Center, Package Lockers, Dog Park, EV Charging, Rooftop, Controlled Access), B gets standard (Pool, Fitness, Laundry, Covered Parking), C gets basic (Laundry, Parking, Management). (3) **Rent comps from siblings**: PropertyDataTab passes `siblingRows` (all sorted rows) in navigation state; `buildRentCompsFromSiblings()` filters same-submarket properties as comps (top 5). (4) **Google Places photos**: `getPropertyPhotos()` called on mount with 3-tier fallback (scraped→Google Places API→styled placeholders). `photoUrls` state feeds PhotoGallery. (5) **PowerRankingsTab navigation**: Added `useNavigate` + "VIEW PROPERTY →" button in expanded PCS breakdown, navigates to `/market-intelligence/property/:id` with row data. (6) **Cleanup**: Removed unused duplicate `MarketIntelligence/PropertyDetailsPage.tsx` (498 lines). Files: `PropertyDetailsPage.tsx`, `PropertyDataTab.tsx`, `PowerRankingsTab.tsx`.

-   **Deal Assumptions & Returns Engine (2026-03-11):** Added `deal_assumptions` table (all underwriting inputs: land, construction, building, unit mix, revenue, opex, financing, exit, computed returns) and `deal_market_data` table (submarket, rent comps, occupancy, pipeline, demographics). Extended `properties` table with zoning fields (max_far, max_stories, max_units, parking_required, setbacks, buildable_area, zoning_source/confidence). Added `v_deal_summary` view joining deals→deal_properties→properties→assumptions→market_data. API: `GET/PUT /deals/:id/assumptions` (upsert), `POST /deals/:id/compute-returns` (calculates TDC, NOI, YOC, IRR, equity multiple, DSCR, etc.), `PUT /deals/:id/site-data` (zoning/parcel updates), `GET /deals/:id/full-context` (combined view). All routes use `requireAuth` JWT auth. PostgreSQL NUMERIC columns parsed with `parseFloat()` helper to prevent string concatenation. Division-by-zero guard on compute-returns when no units set. Files: `110_deal_assumptions_schema.sql`, `deal-assumptions.routes.ts`, `deal-assumptions.types.ts`.
-   **Property Essentials Save Fix (2026-03-11):** PropertyDetailsForm was saving via `/api/v1/clawdbot/command` (webhook endpoint requiring special auth token), causing 401 errors. Added new `PATCH /api/v1/deals/:id/property` endpoint on `inline-deals.routes.ts` using standard JWT `requireAuth`. Updates linked property via `deal_properties` join table, syncs `lot_size_acres→deals.acres` and `land_cost→deals.budget`. Creates property record if none linked. Frontend updated to use new endpoint.
-   **Metrics Expansion + 5-Year Projection Engine (2026-03-16):** Task #83. Extended MetricDefinition interface with `leadsMetrics`/`laggedBy` arrays for leading/lagging indicator metadata. Added 4 new catalog metrics: DEMO_POPULATION_DECLINE, DEMO_POPULATION_TREND_3Y, L_JOBS_PER_UNIT, C_TRAFFIC_GROWTH_INDEX (total now 54). Fixed `/catalog` endpoint to include `categories: getCategoriesWithCounts()`. Created `metric_projections` table (migration 135) with unique index on `(metric_id, geography_type, geography_id, horizon_months)` and 30-day cache. Built `metric-correlation-engine.service.ts`: Pearson r sweep −24 to +24 months in 3-month steps, upsert to `metric_correlations`, seed function for core pairs across metro geographies (111 correlations seeded). Built `metric-projection.service.ts`: Method A (OLS + seasonal dummies for ≥60mo data, linear OLS for ≥24mo), Method B (anchor-correlation for sparse metrics via configured anchor pairs — C_TRAFFIC_GROWTH_INDEX→rent_index_yoy, T_AADT_YOY→home_value_index_yoy, etc.), 30-day projection cache. New endpoints: GET `/correlations`, POST `/correlations/compute`, POST `/correlations/seed`, GET `/correlations/sweep`, GET `/:metricId/projection`. Anchor configs use `metro` geography type matching Zillow data. Files: `metricsCatalog.service.ts`, `metric-correlation-engine.service.ts` (new), `metric-projection.service.ts` (new), `metrics-catalog.routes.ts`, `135_metric_projections.sql` (new).

-   **Lot Size Data Pipeline Fix (2026-03-11):** `inline-deals.routes.ts` GET `/:id` now uses `COALESCE(d.acres, ST_Area(...))` so stored acres (4.81) takes priority over PostGIS boundary computation (30.83). Response now includes `property_data` and `zoningProfile` fields for canonical data layer. `canonicalDealData.ts` prioritizes `property.lot_size_acres` over computed zoning area.

-   **Full API Smoke Test — Comprehensive All-Route Coverage (2026-03-18):** `backend/tests/smoke-test-full.sh` — comprehensive script using `extract-routes.js` to extract routes from all route files and `run-smoke-test.js` to execute them with JWT auth. Run: `bash backend/tests/smoke-test-full.sh`. **Results (2026-03-18):** ~1,176 endpoints tested, 0 FAIL (zero 500-level errors). Infrastructure: `extract-routes.js` parses route files to build URL list with correct mount prefixes; `run-smoke-test.js` issues requests with JWT auth, captures first error line from non-2xx responses, reports PASS/WARN/FAIL per route file. Smoke test runner fixes: capsule.routes mount prefix corrected to `/api/v1/capsules`; duplicate `pipeline` key removed; error body capture added. **500-error fixes (2026-03-18):** zoning-verification UUID parse errors → 404; context-tracker NOT NULL defaults (content/impact/likelihood); email-extractions column fixes (source_name, raw_data removed); property-boundary confirmed_at defaults; zoning-triangulation recommendations 404; benchmark-timeline fire-and-forget 202; development-scenarios AI timeout wrapper (hbu/regulatory-risk/timeline-intelligence/recommendations). **Security fixes (clawdbot-webhooks.routes.ts):** auth is now fail-closed (returns 401 when secrets unset); SQL injection in hours interval parameterized.

-   **Full Platform Smoke Test — Final Report (2026-03-20):** Expanded smoke test suite to 1,268 endpoints across 5 phases, all reaching 0 FAIL. `backend/tests/smoke-test-misc.sh` (Phase 5 — 389 endpoints, 124 PASS, 0 FAIL) covers: full module-wiring matrix (55 routes), capital structure (13), financial models (7), strategy analyses (5), DD checklists (5), modules (4), **module libraries (7)**, strategy definitions (4), property metrics/scoring/opus/data-library/market-research/benchmark-timeline, **maps (9)**, **layers (7)**, **mapAnnotations (6)**, **m22-archive (11)**, **audit (11)**, **extractions (6)**, **asset news (5)**, **asset notes (7)**, **note categories (6 incl. /stats/usage)**, **note replies (5)**, **task completion (4)**, **team-management (12)**, **microsoft integration (15 routes + 3 inline + 1 security negative-test)**, **visibility (5)**. **Bugs fixed:** (1) `log_team_member_activity()` trigger wrong column names — migration `113_fix_team_member_activity_trigger.sql`. (2) `wireCapitalStructurePipeline` null-guard. (3) `deal_tasks` INSERT schema fix. (4) `microsoftRouter` unmounted — now mounted. (5) `link-property` wrong column names. (6) `check_optional` SKIP=4xx FAIL=5xx. (7) **Microsoft OAuth security:** `state` param is now HMAC-SHA256 signed + time-limited (10 min); `verifyOAuthState`/`verifyState` verify signature with `timingSafeEqual` and TTL before any DB write — prevents cross-account token linkage via state tampering; applied to both `microsoft.routes.ts` and `inline-microsoft.routes.ts`; forged-state negative test added. (8) **`task-completion.routes.ts` mounted** at `/api/v1/task-completion`; SQL fixed (`body_preview` not `body`, `from_address` not `sender_email`, `mockEmails`/`mockTasks` → `emails`/`tasks`). (9) **`map-annotations.example.ts` excluded** (Prisma prototype, never mounted, superseded by `mapAnnotations.routes.ts`). (10) Mount summary corrected: module-wiring 22→55 endpoints. Phase totals: P1=278 (PASS=130), P2=196 (PASS=110), P3=186 (PASS=64), P4=219 (PASS=155), P5=389 (PASS=124). Grand total: **1,268 endpoints, PASS=583, SKIP=685, FAIL=0, 0 server errors (5xx)**. (11) **Gmail coverage completed:** added 4 missing routes (`GET /callback`, `PATCH /accounts/:id`, `GET /emails`, `GET /sync-logs`) — all 12 gmail.routes.ts routes now tested. (12) **noteReplies paths corrected:** smoke test updated from `/api/v1/notes/:id/replies` to `/api/v1/assets/:aId/notes/:nId/replies` matching actual route file structure (`/:assetId/notes/:noteId/replies`). Consolidated report: `backend/tests/SMOKE-TEST-REPORT.md`.

-   **Comprehensive Smoke Test & 500-Error Fixes — 5 Phases (2026-03-11):** Full smoke testing across 600+ endpoint tests in 5 phases. All phases reached 0 FAILs. **Migration 111** (`111_smoke_test_fixes.sql`) consolidated all DB fixes: added `deal_id` to `properties`, `property_id`/`stats_snapshot` to `trade_areas`, `strategy` to `deals`, `updated_at` to `scenario_assumptions`; created tables `scenario_assumptions`, `scenario_templates` (with bull/base/bear/stress seed data), `deal_scenarios`, `scenario_events`, `risk_escalations`, `adjustment_formulas`, plus M27 tables and `property_tax_records`. **Code fixes:** `scenario-generation.service.ts` — 5+ SQL queries rewritten (demand events join chain via `demand_event_types.category`, supply pipeline query for aggregate table, risk escalations for actual columns, null-guard on `assumptionNarrativeTemplate`); `proforma-adjustment.service.ts` — `active`→`is_active` column; `jedi-score.service.ts` — `p.deal_id`→`deal_properties` junction (5 occurrences); `capital-structure-adapter.ts` — null-guard on `params.layers`; `excel-export.service.ts` — `waterfallDistributions` iterable fix; `comp-query.service.ts` — SQL param alignment; `context-tracker.routes.ts` — `created_at`→`uploaded_at`. **Phase results:** P1: 55 tests 0 FAIL, P2: 205 tests 0 FAIL, P3: 124 tests 0 FAIL, P4: 143 tests 0 FAIL, P5: 79 tests 0 FAIL. **Total: 606 tests, 0 FAIL.** Test scripts: `backend/tests/smoke-test-phase{1-5}.sh`. **Post-review fixes (2026-03-11):** (1) Removed destructive `DROP TABLE IF EXISTS tax_comp_analyses CASCADE` from migration 111 — replaced with safe additive `CREATE TABLE IF NOT EXISTS`. (2) Fixed demand query in `scenario-generation.service.ts` to validate trade area existence before returning demand events (prevents cross-tenant data leakage). (3) Fixed baseline key mapping mismatch in `getBaselineAssumptions` — SQL aliases now use camelCase (`"rentGrowth"`, `"opexGrowth"`, `"exitCap"`) matching downstream reads, plus explicit `parseFloat`/`parseInt` with fallback defaults.

-   **Dead Code Cleanup (2026-04-12, Task #160):** Removed 13 orphaned backend route/service files and cleaned barrel imports. **Backend deleted (13 files)**: `inline-tasks.routes.ts` (replaced by `tasks.routes.ts`), `marketIntel.routes.ts` (replaced by `market-intelligence.routes.ts`), `apartmentMarket.routes.ts` (replaced by `apartment-locator.routes.ts`), `dealState.routes.ts` (replaced by `inline-deals.routes.ts`), `events.routes.ts` (replaced by `kafka-events.routes.ts`), `extractions.routes.ts` (replaced by `email-extractions.routes.ts`), `files.routes.ts` (replaced by `documentsFiles.routes.ts`), `errors.routes.ts` (replaced by middleware `errorWebhook.ts`), `financial-assumptions.routes.ts` (replaced by `deal-assumptions.routes.ts`), `analysis.routes.ts` (replaced by `driver-analysis` + `tax-comp-analysis`), `map-annotations.example.ts` (example file, never mounted), `DesignToFinancialService.ts` (placeholder, never used), `FinancialAssumptionsAPI.ts` (orphaned, only imported by deleted `financial-assumptions.routes.ts`). **Barrel cleanup**: Removed 4 stale imports and 4 stale `app.use()` mounts from `backend/src/api/rest/index.ts` (`extractionsRoutes`, `analysisRoutes`, `filesRoutes`, `errorsRoutes`). None of the deleted files were imported in `index.replit.ts` (the running entry point), so no runtime behavior changed. **Note**: Some frontend services (`eventTrackingService.ts`, `errorLogging.ts`, `analysisApi.ts`, `FileUploader.tsx`) reference endpoints from the deleted files, but these endpoints were never mounted in `index.replit.ts` — they were only in the unused `setupRESTRoutes` barrel. These frontend callers already fail silently and are candidates for future migration.

-   **Events + Admin Data Coverage Wiring (2026-04-12, Task #159):** Mounted `kafka-events.routes.ts` at `/api/v1/events` with `requireAuth` and `admin-data-coverage.routes.ts` at `/api/v1/admin/data-coverage` (before generic admin catch-all). **Events endpoints**: `GET /log` (paginated event log with topic/type/deal/tradeArea/publisher/date filters), `GET /trace/:eventId` (cascade tree visualization), `GET /status` (consumer health), `POST /replay/:eventId` (manual retry), `GET /analytics` (event stats by period). **Admin data-coverage endpoints**: `GET /` (coverage summary + recent activity), `GET /activity/recent`, `GET /data-quality/georgia`, `GET /:county/:state` (detailed county coverage), `POST /:county/:state/scrape` (trigger bulk scrape), `POST /refresh-stale` (refresh stale properties), `POST /:county/:state/test-api` (API health check), `PUT /:county/:state` (update county config). **Bug fix**: quoted `"offset"` column in events log SQL (reserved word). Frontend consumers: `EventLog.tsx` (`/api/v1/events/log`), `EventCascadeViewer.tsx` (`/api/v1/events/trace/:eventId`), `DataCoverageSection.tsx` (`/api/v1/admin/data-coverage`).

-   **Scenario Module Backend Wiring (2026-04-12, Task #158):** Mounted `scenarios.routes.ts` in `index.replit.ts` at `/api/v1/scenarios` with `requireAuth`. **10 endpoints** now active: `GET /templates`, `POST /custom`, `POST /generate/:dealId`, `GET /:dealId`, `GET /:dealId/details`, `GET /:dealId/comparison`, `GET /:dealId/events`, `PUT /:scenarioId/recalculate`, `PUT /:scenarioId`, `DELETE /:scenarioId`. **Route ordering fix**: `/templates` and `POST /custom` moved before `/:dealId` catch-all to prevent Express parameter conflicts. **Events endpoint SQL fix**: queries updated from non-existent table schemas (`demand_projections`, `supply_pipeline`, `risk_escalations`) to actual tables (`demand_driver_events`, `supply_pipeline_projects`, `risk_events`). **Migration 107** (`107_custom_scenario_configs.sql`) creates `custom_scenario_configs` table and adds `is_active` column to `deal_scenarios`. **Security**: all 3 mutating endpoints (`generate`, `recalculate`, `custom`) now derive userId from auth context, not request body. Frontend consumers: `ScenarioComparison.tsx`, `ScenarioBuilder.tsx`.

-   **Risk Module Backend Wiring (2026-04-12, Task #157):** Mounted the complete risk scoring router (`risk.routes.ts`) in `index.replit.ts` at `/api/v1/risk`. **14 endpoints** now active: `GET /categories`, `GET /events`, `GET /trade-area/:id` (composite), `GET /trade-area/:id/supply`, `GET /trade-area/:id/demand`, `GET /trade-area/:id/regulatory`, `GET /trade-area/:id/market`, `GET /trade-area/:id/execution`, `GET /trade-area/:id/climate`, `GET /deal/:id`, `GET /history/:tradeAreaId`, `GET /comprehensive/:dealId`, `POST /calculate/:tradeAreaId`, `POST /narrative/:dealId` (AI streaming). **Migration 106** (`jedire-patches/migrations/106_risk_module_tables.sql`) creates 20 tables: `risk_categories` (6 seeded), `risk_events`, `risk_score_history`, `risk_alert_thresholds`, `supply_pipeline_projects`, `supply_absorption_tracking`, `employer_concentration`, `demand_driver_events`, `regulatory_risk_events`, `zoning_changes`, `tax_policy_changes`, `market_risk_indicators`, `interest_rate_scenarios`, `execution_risk_factors`, `construction_cost_tracking`, `climate_risk_assessments`, `natural_disaster_events`, `risk_escalations`, `risk_scores`, `composite_risk_profiles`. **Bug fix**: `p.unit_count` → `p.units` in supply risk calculation. Frontend consumers: `RiskDashboard`, `RiskDDPage`, `RiskTimeline`, `RiskBreakdown`, `MarketRiskPanel`, `ExecutionRiskPanel`, `ClimateRiskPanel`, `RegulatoryRiskPanel`, `OpusAISection`, `RiskIntelligence`.

-   **M35 Event Impact Engine — Phases 4-7 (2026-04-14):** Complete build of the remaining Event Impact Engine phases. **Phase 4 (Forecast Generator):** Created `event_forecasts` + `forecast_actuals_tracking` DB tables. Built `m35-forecast.service.ts` with a 5-step pipeline: playbook lookup → magnitude scaling → submarket adjustment (vacancy/supply/traffic factors) → regime adjustment (rent trend) → CI construction. Forecast versioning: re-generations supersede prior active forecasts (status: active/superseded/invalidated). Auto-generation fires on event status transitions to `announced`/`in_progress`; auto-invalidation on `cancelled`/`reversed`. Nightly divergence tracking job compares actuals vs forecasts. `m35-forecasts.routes.ts` with 6 REST endpoints. **Phase 5 (Backtesting):** Created `backtest_results` + `regime_shift_alerts` DB tables. Monthly backtest job using DiD method for each event at T+12/24/36. Bayesian confidence refinement: `new_conf = prior×(1−λ) + hit_rate×λ` (λ=0.25). CI widening when coverage <70%. Regime-shift detection when ≥5 consecutive same-direction errors. `m35-backtest.routes.ts` with 6 endpoints. **Phase 6 (Metric Key Mapping):** Built `m35-metric-mapping.ts` — full bidirectional mapping between M35 shorthand keys (`rent_growth_yoy`, `cap_rate`, `search_growth`) and canonical DB `metric_id` values (`CS_EFF_RENT_GROWTH`, `CS_CAP_RATE`, `C_SEARCH_GROWTH_INDEX`). Module-specific alias maps for M06 Demand Signal, M08 Strategies, M09 Market Intelligence. Integration helpers: `toM06DemandOverlay`, `toM08OutcomeMetrics`, `toM09ReportSection`. Metric display labels, bull/bear direction, unit formatting. Wired into backtest DiD queries for correct canonical metric resolution. **Phase 7 (UI Surfaces):** Built `EventForecastPanel.tsx` — compact/detail forecast panel with per-metric × window projections, CI display, actuals tracking status, derivation summary, and on-demand regeneration. Built `PlaybookAccuracyDashboard.tsx` — admin dashboard with backtest accuracy table (coverage rate, RMSE, bias direction, confidence bars), regime-shift alert management, and one-click backtest runner. Wired CAUSALITY/FORECAST tab toggle into `MSAEventsTab.tsx` right-panel. Wired `PlaybookAccuracyDashboard` into `M35ConnectorAdminPage.tsx`. Files: `backend/src/services/m35-forecast.service.ts`, `backend/src/routes/m35-forecasts.routes.ts`, `backend/src/services/m35-backtest.service.ts`, `backend/src/routes/m35-backtest.routes.ts`, `backend/src/services/m35-metric-mapping.ts`, `frontend/src/components/terminal/tabs/msa/EventForecastPanel.tsx`, `frontend/src/components/terminal/tabs/msa/PlaybookAccuracyDashboard.tsx`.

-   **Task #327 — Inngest Cron Activation (2026-04-22):** Wired the autonomous agent and discovery cron jobs into the Inngest `serve()` handler at `POST/PUT/GET /api/inngest`. Spread `scheduledAgentFunctions` (5 functions: daily morning briefing, daily compliance, weekly portfolio review, weekly market intelligence, hourly threshold monitor) and `scheduledDiscoveryFunctions` (7 functions: hourly market discovery, daily news, daily deal news, daily economic, weekly market scan, on-demand news, on-demand web search) into `backend/src/index.replit.ts`. Migrated all 12 cron/event function definitions in `backend/src/services/agents/scheduled-jobs.ts` and `backend/src/services/discovery/scheduled-discovery.ts` from the Inngest v2 three-arg signature `createFunction(opts, trigger, handler)` to the v4 signature `createFunction({...opts, triggers: [trigger]}, handler)`. Added admin-only `POST /api/v1/agents/_debug/trigger?fn=<name>` endpoint in `backend/src/api/rest/agent.routes.ts` (the singular router actually mounted at `/api/v1/agents`) for manual on-demand firing. The endpoint uses `requireAuthOrApiKey` and additionally enforces `req.user.role === 'admin'` (returns 403 otherwise). Supported `fn` values: `dailyMorningBriefing`, `dailyComplianceCheck`, `weeklyPortfolioReview`, `hourlyMarketDiscovery`, `dailyNewsDiscovery`, `dailyEconomicDiscovery`. `dailyComplianceCheck` dispatches the real `task_due` trigger event; `dailyEconomicDiscovery` refreshes interest rates only (the full cron also iterates per-MSA employment). Added new `Inngest Dev Server` workflow running `npx inngest-cli@latest dev -u http://localhost:4000/api/inngest -p 8099` so durable cron execution works locally. Updated `backend/src/lib/inngest.ts` to set `isDev: true` and `baseUrl: http://localhost:8099` whenever `INNGEST_EVENT_KEY` is unset, eliminating "cloud mode" misconfiguration warnings in dev. **Production env vars required at deploy time:** `INNGEST_EVENT_KEY` (events written by the SDK) and `INNGEST_SIGNING_KEY` (verifies dev-server-signed POSTs). When both are set, the SDK switches off dev mode automatically and registers with Inngest Cloud instead of the local dev server.

## Atlanta MSA Data Enrichment — API Exploration Log (2026-04-23)

Focus: enrich 1,998 Fulton County properties (100+ units) with sale history, unit mix, 3-mile census demographics, construction pipeline (building permits + CO dates), and recorded mortgage/loan records.

### Confirmed Working APIs

#### 1. Fulton County Assessor — Tax_Parcels_2025 (ArcGIS FeatureServer) ✅
- **URL:** `https://services1.arcgis.com/AQDHTHDrZzfsFsB5/ArcGIS/rest/services/Tax_Parcels_2025/FeatureServer/0`
- **Filter:** `LivUnits > 5` (multifamily); `LivUnits > 100` for 100+ unit properties
- **34 fields confirmed:** `ParcelID`, `Address`, `Owner`, `OwnerAddr1/2`, `TotAssess`, `LandAssess`, `ImprAssess`, `TotAppr`, `LandAppr`, `ImprAppr`, `LUCode`, `ClassCode`, `LivUnits` (unit count!), `LandAcres`, `NbrHood`, `TaxDist`, `Subdiv*`, `ExCode`
- **Missing from this layer:** year_built, building sqft, sale date/price, grantor/grantee — only one layer (no sub-layers/tables)
- **Script:** `backend/src/scripts/import-county-properties.ts`

#### 2. Fulton County AGOL — Structures FeatureServer ✅ MASSIVE FIND
- **URL:** `https://services1.arcgis.com/AQDHTHDrZzfsFsB5/arcgis/rest/services/Structures/FeatureServer/0`
- **Total records:** 383,483 building footprints; 312,049 have `YearBuilt` populated; **6,720 with 100+ LiveUnits**
- **25 fields:** `FeatureID` (format: LST0004081), `FeatType`, `StructForm`, `Stories` (integer), `YearBuilt` (string — e.g. "1971"), `LiveUnits` (integer), `BaseElev`, `RoofElev`, `AreaSqFt`, `Comments`, `LUC` (Land Use Code), `LUCDesc` (e.g. "Apt Garden Class C **"), `LUCShtDesc`, `LUESRICode`, `LUESRIDesc`, `GEO_OID`, `STRUCT_ID`
- **LUC codes for multifamily:** `2C1` (Apt Garden Class C), `2B1`, `2A1`, etc.; filter `LUC LIKE '2%' AND LiveUnits > 0` → 9,534 multifamily structures
- **Key limitation:** `FeatureID` in Structures = "LST0004081" — does NOT directly match Tax_Parcels `ParcelID`. **Must spatial join** by geometry intersection (building footprint intersects parcel polygon) to link YearBuilt → ParcelID
- **Sample data:** LST0035938 | YearBuilt=1970 | Stories=1 | Units=120 | LUC=2C1
- **Solves:** year_built gap for all pre-2023 stock (older than Building_Permit_latest coverage)

#### 3. Fulton County AGOL — Tyler_TaxParcels FeatureServer ✅ CONFIRMED
- **URL:** `https://services1.arcgis.com/AQDHTHDrZzfsFsB5/arcgis/rest/services/Tyler_TaxParcels/FeatureServer/0`
- **Total records:** 364,797 | TaxYear: 2023
- **34 fields** including `TotAssess`, `LandAssess`, `ImprAssess`, `TotAppr` (total appraised value), `LandAppr` (land appraisal), `ImprAppr` (building improvement appraised value), `LivUnits`, `LUCode`, `ClassCode`, `NbrHood`, `Owner`, `Address`, full subdivision fields
- **ParcelID format:** "22 482512690060" (matches Tax_Parcels_2025 format — same districtXXXXXX scheme)
- **Sample:** MF COTTON HOUSE | ParcelID=22 482512690060 | TotAppr=$24.4M | ImprAppr=$24.2M | LivUnits=119
- **Join:** ParcelID → Tax_Parcels_2025.ParcelID (direct match)

#### 4. Fulton County AGOL — Tyler_YearlySales FeatureServer ✅ MAJOR FIND
- **URL:** `https://services1.arcgis.com/AQDHTHDrZzfsFsB5/arcgis/rest/services/Tyler_YearlySales/FeatureServer`
- **Layers:** 6 years of data (Layer 2=2018, 3=2019, 4=2020, 5=2021, 6=2022) + Layer 7=Parcels reference
- **Record counts:** 2018: 22,951 | 2019: 26,347 | 2020: 32,010 | 2021: 43,627 | 2022: 12,875 | **Total: 137,810 sale records**
- **Fields (per layer):** `ParID` (parcel ID — matches Tax_Parcels_2025 ParcelID format!), `TaxYear`, `Price` (integer sale price), `Cur` (Y/N = current year flag)
- **ParID format:** "07 140001181899" → matches Fulton Tax_Parcels_2025 district prefixes (07/09/14/17/22 etc.)
- **High-value examples:** 2018: ParID=22 524008310370 @ $26.3M; 2018: ParID=07 170001524780 @ $21.8M; 2020: ParID=09F250301270530 @ $14.3M
- **Join:** `ParID` → Tax_Parcels_2025.`ParcelID` (direct string match)
- **Limitation:** Data covers 2018–2022 only. No 2023/2024 layers found. No grantor/grantee fields.

#### 5. Atlanta Building Permits — Building_Permit_latest (ArcGIS FeatureServer) ✅
- **URL:** `https://services5.arcgis.com/5RxyIIJ9boPdptdo/arcgis/rest/services/Building_Permit_latest/FeatureServer/0`
- **Discovered via:** ArcGIS Online item `cecbda6378be4c289910f7dc145e8cdd` (Atlanta Building Permit Tracker ExB app config)
- **Total records:** 36,115 permits
- **22 fields:** `RecordID` (Accela permit#), `Name`, `OrigOpened` (date), `Opend`, `TypeCombo`, `Use_`, `Subtype`, `Group_`, `Address`, `Status_1`, `StatusDate`, `R_StatDate`, `JOB_VALUE`, `PARCEL` (links to Tax_Parcels_2025 ParcelID!), `QUADRANT`, `ACA_Link` (Accela deeplink), `StreetView`, `statusP`, `DisplayX/Y`, `ObjectId`
- **Key permit types for multifamily:**
  - `TypeCombo = 'Multi Family New'` + `Status_1 = 'CO Issued'` → **CO date = year built proxy** (StatusDate field)
  - `TypeCombo = 'Multi Family New'` + `Status_1 = 'Issued'` → **active construction pipeline**
  - `TypeCombo = 'Multi Family Alteration'` + `Status_1 = 'CO Issued'` → renovation completions
- **1,620 total CO Issued permits** across all types; data covers ~2023–2024 ("latest" dataset)
- **Coverage caveat:** Recent permits only — buildings built before ~2023 won't have a CO record here
- **Parcel linkage:** `PARCEL` field matches `ParcelID` in Tax_Parcels_2025 (e.g., `17 02520017012`)
- **Accela agency code confirmed:** `ATLANTA_GA` (not `ATLANTAGA`) — visible in ACA_Link URLs

#### 6. Census ACS 5-Year — Multiple Table Series ✅
- **Base URL:** `https://api.census.gov/data/2022/acs/acs5`
- **Geography:** `for=tract:*&in=state:13+county:121` → 327 Fulton County tracts
- **Confirmed table series:**
  - `B25024` — Units in structure (001=total, 007=10-19 units, 008=20-49, 009=50+)
  - `B25041` — Bedrooms per unit (001-006: no bedroom through 5+)
  - `B25036` — Year structure built (10 vintage buckets: pre-1950 through 2014+); 309/327 tracts have 100+ units built 2014+
  - `B08303` — Travel time to work (transit proxy)
  - `B25070` — Gross rent as % of income (affordability)
- **Year Built proxy:** B25036 gives tract-level vintage distribution — can assign neighborhood vintage year to each property

#### 7. APD Crime Online (ArcGIS FeatureServer) ✅
- **URL:** `https://services3.arcgis.com/Et5Qfajgiyosiw4d/ArcGIS/rest/services/Crime_Online/FeatureServer/15`
- **Layer:** 15 — CrimeOnline
- **23 fields:** `IncidentNumber`, `ReportDate`, `OccurredFromDate`, `OccurredToDate`, `Day_of_the_week`, `Part` (I/II), `Crime_Against` (Person/Property/Society), `NibrsUcrCode`, `NIBRS_Offense`, `StreetAddress`, `LocationType`, `Longitude`, `Latitude`, `FireArmInvolved`, `IsBiasMotivationInvolved`, `event_watch`, `Zone`, `ChargeId_First`
- **Use:** Safety scoring by lat/lon proximity to parcels; can aggregate crime counts within 0.5/1/3 mile rings

#### 8. MARTA GTFS Feed ✅
- **URL:** `https://www.itsmarta.com/google_transit_feed/google_transit.zip` (20MB ZIP)
- **Files:** trips.txt, agency.txt, calendar.txt, calendar_dates.txt, routes.txt, shapes.txt, stop_times.txt, **stops.txt**
- **Stops:** 7,052 total stops — fields: `stop_id`, `stop_code`, `stop_name`, `stop_desc`, `stop_lat`, `stop_lon`, `zone_id`, `stop_url`, `location_type`, `parent_station`, `stop_timezone`, `wheelchair_boarding`
- **Sample:** `907933, HAMILTON E HOLMES STATION, lat=33.754553, lon=-84.469302`
- **Use:** Transit access scoring — count MARTA stops within 0.25/0.5 mile of each parcel; rail vs bus distinction via `location_type`
- **Extraction:** Use Python `zipfile` module (not `unzip` CLI — fails silently); download to `/tmp/marta2.zip`

#### 9. Fulton County AGOL — CE_AddressPoints ✅ VALUABLE
- **URL (via gismaps.fultoncountyga.gov):** `https://gismaps.fultoncountyga.gov/arcgispub/rest/services/PublicSafety/CE_AddressPoints/MapServer/0`
- **Total records:** 13,577 apartment address points (with `PtClass LIKE '%Apartment%'`)
- **Key fields:** `ParcelID` (links to Tax_Parcels_2025), `CommonName` (building/complex name!), `Label` (full address), `City`, `Zip`, `PtType`, `PtClass`, `UnitLow`, `UnitHigh`
- **Building name examples:** "Lexington Apartment Homes" → ParcelID=14 019000051087; "Oakley Shoals Apartments" → 09F230100840659; "Diplomat Townhouse Apartments" → 14 0224 LL0556
- **Use:** Match apartment names to ParcelIDs — enriches property records with official marketing names

#### 10. Fulton County AGOL — Zoning FeatureServer ✅
- **URL:** `https://services1.arcgis.com/AQDHTHDrZzfsFsB5/arcgis/rest/services/Zoning/FeatureServer/0`
- **Fields:** `CaseID`, `ZClass` (zoning class code), `ZClassDesc` (full description), `EffDate` (effective date)
- **Use:** Spatial join to assign zoning class to parcels; verify multifamily-permissive zoning

#### 11. Fulton County Geocoder — Composite Address Locator ✅
- **URL:** `https://gismaps.fultoncountyga.gov/arcgispub/rest/services/GeocodeService/Composite_Address_Locator/GeocodeServer/findAddressCandidates`
- **Result:** Score=100 for "100 Peachtree St" with state plane coordinates (X=2229132, Y=1366657)
- **Use:** Match raw street addresses → Fulton parcel coordinates for spatial joins

#### 12. FCDemographics — Commission District Level ✅ (too coarse for property scoring)
- **URL:** `https://gismaps.fultoncountyga.gov/arcgispub/rest/services/Demographic/FCDemographics/MapServer/0`
- **42 records** at Commission District level (Year=2020)
- **Rich fields:** TotPop, MedHHIncom, Renter%, HousingUnits, Vacant%, Owner%, race breakdown (White/Black/Asian/Hispanic/MultiRace), age cohorts (A_0_4 through A_Ab_85), education (NoDiploma/HighGED/SomeCollege/Associates/Bachelors/GradProf), income brackets (I_10_14999 through I_200_More), MedianAge
- **Limitation:** Only 7 districts — too coarse for 3-mile property-level demographics (use Census ACS tracts instead)

#### 13. DeKalb County Tax Parcels — dcgis.dekalbcountyga.gov ✅
- **URL:** `https://dcgis.dekalbcountyga.gov/mapping/rest/services/TaxParcels/MapServer/0`
- **Total records:** 244,945 parcels, TaxYear=2023
- **51 fields** including `PARCELID`, `BUILDING`, `UNIT`, `CLASSCD`, `CLASSDSCRP`, `SITEADDRESS`, `OWNERNME1`, `OWNERNME2`, `CNTASSDVAL` (current assessed value), `TOTAPR1` (total appraised value), `LANDUSE`, `ZONING`, `OVLDISTRICT`, `ACREAGE`, `HISTDESC`, `CONDITION`
- **Note:** No `YearBuilt` in parcel layer (building-level table is not exposed in this service)
- **Access:** JSON responses returned but record body occasionally empty — retry works

### Blocked / Inaccessible from Replit Server IPs

| Source | Reason | Notes |
|---|---|---|
| qPublic / schneidercorp.com | Cloudflare Managed Challenge (bot protect) | Blocks even CF Browser Rendering headless |
| fultonassessor.org | Hosted on sgpreview.com (SchneiderCorp) — same Cloudflare block | |
| iasworld.fultoncountyga.gov | DNS does not resolve | Domain may not exist / internal only |
| permits.atlantaga.gov | AWS CloudFront 403 — IP blocked | CF Browser Rendering also blocked |
| aca3.accela.com / aca-prod.accela.com | Cloudflare "Attention Required" WAF | Accela developer API needs `x-accela-appid` key |
| Atlanta open data (data.atlantaga.gov) | Socrata API returns empty | Replit IP likely rate-limited/blocked |
| GSCCCA (gsccca.org) | Web-form only, returns 75 bytes | No public API; mortgage/deed records inaccessible |
| HUD FMR API | Returns `{"error":"Unauthenticated"}` | Needs HUD-specific token (not BLS key) |
| Atlanta GIS ArcGIS Server | Behind auth, no public REST catalog | ExB apps use internal ArcGIS Enterprise |
| HMDA bulk download | S3 returns 403, CFPB API serves HTML | No machine-readable endpoint accessible |
| HUD LIHTC | Page moved, link broken | No current accessible endpoint |
| Tyler Tool (TSC_PB_TASKLIST_EVW) | M_SALEPRICE NULL for all sampled records | Table is parcel boundary task log, not sale history |

### Cloudflare Browser Rendering (CLOUDFLARE_BR_TOKEN + CLOUDFLARE_ACCOUNT_ID) — Status
- **Available:** Both secrets set and API responds
- **Endpoint format:** `POST https://api.cloudflare.com/client/v4/accounts/{CLOUDFLARE_ACCOUNT_ID}/browser-rendering/content`
- **Snapshot endpoint:** returns `{ result: { screenshot: base64, content: html } }`
- **Max timeout:** 60,000ms (`gotoOptions.timeout`)
- **What it CAN bypass:** Standard server-IP blocks (CloudFront, nginx IP filtering)
- **What it CANNOT bypass:** Cloudflare's own WAF/Managed Challenge (qPublic, Accela, fultonassessor.org all use Cloudflare WAF against automated access)
- **Key win:** Used snapshot endpoint to crack open the Atlanta Building Permit Tracker ExB app config and extract the live `Building_Permit_latest` FeatureServer URL

### Data Gaps — Remaining Unsolved

| Data Point | Status | Best Available Proxy / Plan |
|---|---|---|
| Year built (individual property) | ✅ SOLVED — Structures FeatureServer | `Structures` layer has YearBuilt for 312K/383K buildings; link via spatial join (building footprint ∩ parcel polygon) |
| Building sqft | ⚠️ Partial — AreaSqFt in Structures (many nulls) | Use AreaSqFt where populated; fall back to Shape__Area × Stories estimate |
| Sale history (date + price) | ✅ SOLVED — Tyler_YearlySales 2018-2022 | 137,810 Fulton County sale records; join ParID → Tax_Parcels_2025.ParcelID |
| Grantor / Grantee names | ❌ Not in Tyler_YearlySales | Only Price + TaxYear available; no buyer/seller in public layer |
| Recorded mortgage / loan amount | ❌ GSCCCA web-only | No free API available |
| Historical CO dates (pre-2023) | ❌ | Accela historical data requires App ID registration |
| Appraised value (enriched) | ✅ — Tyler_TaxParcels | TotAppr, ImprAppr, LandAppr for all 364,797 Fulton parcels (TaxYear 2023) |
| Zoning classification | ✅ — Zoning FeatureServer | Spatial join ZClass/ZClassDesc to parcels |
| Property building name | ✅ — CE_AddressPoints | 17 unique apartment ParcelIDs with CommonName (partial) |
| DeKalb year built | ❌ | dcgis parcel layer has no YearBuilt; building table not exposed |
| Cobb / Gwinnett year built | ❌ | GIS servers blocked or no field available |
| Tyler_YearlySales 2023/2024 | ❌ | Only layers 2-7 exist; no 2023/2024 data found |

### Critical Linkage Architecture (for ingestion pipeline)

```
Fulton Tax_Parcels_2025 (ParcelID)
  ├─→ Tyler_TaxParcels (ParcelID) — assessed + appraised values
  ├─→ Tyler_YearlySales 2018-2022 (ParID) — sale prices per year
  ├─→ Building_Permit_latest (PARCEL) — CO dates for 2023+ builds
  ├─→ CE_AddressPoints (ParcelID) — building common names
  └─→ [SPATIAL JOIN] → Structures (FeatureID=LSTxxxxxx) — YearBuilt, Stories
```

### Other FeatureServer URLs Found in Atlanta Planning AGOL Account (5RxyIIJ9boPdptdo)
- `Building_Permit_latest/FeatureServer/0` — ✅ confirmed working (36,115 permits)
- `test_building_permit/FeatureServer/0` — test/staging version
- `OfficialCityDesignAreas/FeatureServer/0` — Atlanta design district overlays
- `Atlanta_Main_Street_Districts/FeatureServer/0` — Main Street Districts

### Session 5 Discoveries (2026-04-24) — Cobb GIS Server, Gwinnett Property_and_Tax FeatureServer

#### 14. Cobb County — gis.cobbcounty.gov (22-folder GIS server) ✅ JACKPOT

**Server:** `https://gis.cobbcounty.gov/gisserver/rest/services`
- 22 folders: `tax`, `accela`, `cobbpublic`, `comdev`, `openData`, `core`, `parks`, `pubsafety`, `Fire`, etc.

**Key service: `tax/taxassessorsdaily` MapServer** (updated daily by Tax Assessor)
- 4 geometric layers + 6 standalone tables
- **Layer 0: CobbParcels** — 278,920 total parcels; **12,559 HAS_MULTIUNIT='Y'**
  - Fields: `PIN` (11-digit parcel group ID), `PARID` (individual unit/building ID), `SITUS_ADDR`, `OWNER_NAM1/2`, `OWNER_ADDR/CITY/STAT/ZIP`, `FMV_LAND`, `FMV_BLDG`, `FMV_TOTAL`, `ASV_LAND`, `ASV_BLDG`, `ASV_TOTAL`, `CLASS`, `TAXDIST`, `HAS_MULTIUNIT`, `NBHOOD_NUM`, `ACRES`
  - Sample: PIN=17087909000 | PARID=17087900390 | SUNSHINE CP FLATS LLC | 925 BATTERY AVE SE | FMV_Total=$68.5M
- **Table 5: YearBuilt** — **264,696 records**
  - Fields: `PIN` (= PARID from CobbParcels!), `TAXYR`, `CARD` (building card for multi-building parcels), `YRBLT`, `SQFT`
  - Sample: PIN=17087900390 | YRBLT=2017 | SQFT=134,415 (and 4 more CARD rows for same parcel)
- **Table 9: ParcelSales** — **927,722 records** (full historical back to 2002)
  - Fields: `PIN` (= PARID from CobbParcels), `SALEDT` (date), `PRICE`, `SALETYPE` (1=arm's length, 2=related party, etc.), `SALEVAL`, `STEB`, `ASMT` (assessed at sale), `INSTRTYP` (instrument type), `APRTOT` (appraised at sale), `ASR` (assessment/sale ratio), `USER4/5/6`, `NBHD`
  - High-value confirmed: PIN=17098400050 | 2022-07-13 | $143.5M; PIN=16080000010 | 2022-05-05 | $140.9M; PIN=16057300020 | 2021-02-02 | $130.4M
  - Note: $6B record (2002) is a known data-entry error — filter by SALETYPE and sanity-check >$500M

**Key service: `comdev/Accela_PermitsDashboard` FeatureServer** — 13 layers
- **Layer 3: MultiUnit Permits** — 71 records (current), fields: `PermitNo`, `PermitType`, `PermitSubType`, `DateIssued`, `ParcelNo` (PIN format, links to CobbParcels), `STATUS`, `VALUE`, `FMV_LAND`, `FMV_BLDG`, `CLASS`
- **Layer 4: Residential Permits** — broader coverage
- Also: `comdev/Accela_Permits_PrevYr` FeatureServer — prev year permits with `G6_COMPL_DD` (completion date!), `DateIssued`, `PermitSubType`, `SQFT`, `VALUE`

**Other tax services:**
- `tax/tax_assessors_map_mercator_rel` — 35 layers + 6 tables (same YearBuilt/ParcelSales tables + historical parcel layers 2010-2023!)

**CRITICAL COBB LINKAGE ARCHITECTURE — Verified End-to-End:**
```
AGOL CobbParcelsCopy.PIN (e.g. "16054309050")
  → taxassessorsdaily.CobbParcels [WHERE CobbParcels.PIN = AGOL.PIN]
    → CobbParcels.PARID (e.g. "16054301270") — individual unit/building ID
      → YearBuilt.PIN = CobbParcels.PARID → YRBLT + SQFT (CONFIRMED: PIN=16054301270 → YRBLT=1986, SQFT=4,208)
      → ParcelSales.PIN = CobbParcels.PARID → SALEDT + PRICE history
```
- The `PIN` in YearBuilt/ParcelSales tables is actually the PARID (individual parcel), not the group PIN
- For apartment complexes: master PIN ends in "09000"; individual records use the PARID prefix (e.g., "17087900390")
- Multiple YearBuilt rows per PARID (CARD=1,2,3...) for multi-building parcels — aggregate by SUM(SQFT) or take most recent TAXYR

#### 15. Gwinnett County — Property_and_Tax FeatureServer ✅ MAJOR FIND

**URL:** `https://services3.arcgis.com/RfpmnkSAQleRbndX/arcgis/rest/services/Property_and_Tax/FeatureServer`
- **5 layers + 4 tables**
  - Layer 0: Parcels | Layer 1: Zoning | Layer 2: Subdivisions | Layer 5: County-owned Property | Layer 6: LandLots
  - **Table 3: Tax Master Table** | **Table 4: Tax Owner Address Table** | **Table 8: Property Improvements Table** | **Table 10: Land Value Table**

**Table 3: Tax Master Table — Assessment + Owner + Sale References**
- Key fields: `PIN` (format "6197 112", no "R" prefix), `LRSN` (local record sequence number — primary join key!), `RPIN`, `LOCADDR`, `LOCCITY`, `LOCZIP`, `OWNER1`, `OWNER2`, `MAILADDR/CITY/STAT/ZIP`, `DWLGVAL1` (dwelling value), `LANDVAL1`, `TOTVAL1` (total assessed value), `TAXTOT1` (total tax), `PROPCLAS` (property class code), `PCDESC` (property class description), `ZONING`, `ZONEDESC`, `LEGAL1`, `DISTNUM`, `DOC1REF/DOC2REF/DOC3REF` (deed book+page refs), **`GRANTOR1/GRANTOR2/GRANTOR3`** (seller names!), `PUBLIC_NEINUM`
- Multifamily property class filter: `PCDESC LIKE '%Apart%'` → "Apartments (3sty/under)", "Apartments high rise"
- **Confirmed multifamily:** PIN=6197 112 | PRCP-ATLANTA CANOPY LLC | 1635 PIRKLE RD | TotVal=$98,023,000; PIN=6199 016 | MP WILLOW TRAIL LLC | 1500 WILLOW TRAIL PKW | TotVal=$26,951,800

**Table 8: Property Improvements Table — Year Built + Building Details**
- Key fields: `PIN` (format "R5026 227", has "R" prefix — different from Tax Master PIN!), `LRSN` (join key to all tables), `IMPRTYPE`, `USECODE`, `USEDESC`, `BLDGTYPE`, **`YRBUILT`**, **`STORIES`**, **`FINSIZE`** (finished sqft), `FOUNDAT`, `CONSTFR`, `EXTCOVC1/2`, `CONDCODE`, `CONDDESC`, `ROOFTYPE`, `NUMRMS`, `NUMBDRMS`, `NUM2BATHS`-`NUM5BATHS`, `HEATFUEL`, `CENTRLAC`, `BSMTAREA`, `LASTUPDD`, `LRSN`
- **Multifamily use code: `USECODE='APART'`** → confirmed apartment records:
  - R5026 227 | LRSN=336025 | YrBlt=2022 | Stories=4 | FinSqFt=235,017 + 64,336 (two buildings!)
  - R1001 032 | LRSN=232734 | YrBlt=2021 | Stories=4 | FinSqFt=116,332
  - R5083 009 | LRSN=433730 | YrBlt=2021 | Stories=3 | FinSqFt=107,010
- Other multifamily use codes: `MULTRESA` (Elderly Assist. Multi. Res.), likely also `CONDO`, `TOWNHOME`

**Table 10: Land Value Table — Sale History + Additional Grantor Data**
- Key fields: `PIN`, `LRSN`, **`SALE1D/SALE2D/SALE3D`** (up to 3 sale dates!), **`SALE1AMT/SALE2AMT/SALE3AMT`** (sale amounts!), **`GRANTOR1/GRANTOR2/GRANTOR3`** (seller names at each sale), `DOC1REF/DOC2REF/DOC3REF`, `DOC1TYPE/2/3` (deed type), `DOC1NUM/2/3`, **`NUMDWLG`** (number of dwellings — unit count proxy!), `SQFT` (land sqft), `ACRES`, `PERMIT1/2/3` + `PERMIT1D/2/3D` + `PERMIT1T/2/3T` (permit data!), `MAPBOOK`, `PUBLIC_CENSUSTR`, `CONTRACT`

**Layer 0: Parcels — Geometry + LRSN**
- Fields: `PIN`, `TAXPIN`, **`LRSN`** (join to all tables), `ADDRESS`, `PARCELTYPE`, `EXEMPTION_TYPE`, `DEEDEDACREAGE`, geometry

**GWINNETT JOIN ARCHITECTURE:**
```
Gwinnett Parcels.LRSN  ← primary join key across ALL tables
  → Tax Master Table.LRSN → OWNER1, TOTVAL1, PROPCLAS/PCDESC, GRANTOR1/2/3, DOC1REF, ZONING
  → Property Improvements.LRSN → YRBUILT, STORIES, FINSIZE, USECODE, CONDCODE
  → Land Value.LRSN → SALE1D, SALE1AMT, SALE2D, SALE2AMT, GRANTOR1/2/3, NUMDWLG, PERMIT data
```
- Tax Master PIN format: "6197 112" (no "R" prefix)
- Property Improvements PIN format: "R5026 227" (has "R" prefix)
- Join ALL tables via LRSN, not PIN (PIN format differs between tables)

#### 16. DeKalb iasWorldParcels — Confirmed Dead End ❌

- **URL:** `https://dcgis.dekalbcountyga.gov/mapping/rest/services/iasWorldParcels/MapServer/0`
- **Status:** Field schema shows 90 fields including `RESYRBLT`, `FLOORCOUNT`, `BLDGAREA`, `CNTASSDVAL`, `TOTAPR1` — BUT all field values return NULL for every record including with `WHERE 1=1`
- **Reason:** Layer exists as a secured/view layer; underlying CAMA data not exposed via public REST endpoint
- **Alternative for DeKalb year built:** `Building_Permit_Applications/FeatureServer/0` — 86,814 records with `cooIssuedDateTime` (CO date), `squareFootage`, `WorkTypeDescription` — match to parcels by address

### Updated Data Gaps Table (as of Session 5)

| Data Point | County | Status | Source |
|---|---|---|---|
| Year built | Fulton | ✅ SOLVED | Structures FeatureServer — 312K/383K buildings with YearBuilt |
| Year built | Cobb | ✅ SOLVED | taxassessorsdaily Table 5 (YearBuilt) — 264,696 records; join via PARID |
| Year built | Gwinnett | ✅ SOLVED | Property_and_Tax Table 8 (Property Improvements) — YRBUILT field; USECODE='APART' |
| Year built | DeKalb | ⚠️ PROXY | Building Permit Applications cooIssuedDateTime — address match to parcels |
| Sale history | Fulton | ✅ SOLVED | Tyler_YearlySales 2018-2022 — 137,810 records |
| Sale history | Cobb | ✅ SOLVED | taxassessorsdaily Table 9 (ParcelSales) — 927,722 records back to 2002! |
| Sale history | Gwinnett | ✅ SOLVED | Property_and_Tax Table 10 (Land Value) — SALE1D/SALE1AMT/SALE2D/SALE2AMT (3 sales per parcel) |
| Sale history | DeKalb | ❌ | No public sale history API found |
| Building sqft | Fulton | ⚠️ Partial | Structures FeatureServer AreaSqFt (many nulls); fallback Shape__Area × Stories |
| Building sqft | Cobb | ✅ SOLVED | YearBuilt Table SQFT field |
| Building sqft | Gwinnett | ✅ SOLVED | Property Improvements FINSIZE field |
| Stories / floors | Fulton | ✅ SOLVED | Structures FeatureServer Stories field |
| Stories / floors | Cobb | ❌ | Not in YearBuilt table; not in CobbParcels |
| Stories / floors | Gwinnett | ✅ SOLVED | Property Improvements STORIES field |
| Grantor / Grantee | Fulton | ❌ | Not in Tyler_YearlySales |
| Grantor / Grantee | Cobb | ❌ | Not in ParcelSales (USER4/5/6 fields are not grantor) |
| Grantor / Grantee | Gwinnett | ✅ SOLVED | Tax Master GRANTOR1/2/3 + Land Value GRANTOR1/2/3 |
| Assessed value | All counties | ✅ | Fulton Tyler_TaxParcels; Cobb CobbParcels FMV/ASV; Gwinnett Tax Master TOTVAL1 |
| Unit count proxy | Gwinnett | ✅ SOLVED | Land Value NUMDWLG field |
| Permit/CO data | Cobb | ✅ SOLVED | Accela_PermitsDashboard MultiUnit Permits (71 records, current) + Accela_Permits_PrevYr |
| Recorded mortgage | All | ❌ | GSCCCA web-only; no public API |

### Updated Critical Linkage Architecture

```
FULTON:
  Tax_Parcels_2025 (ParcelID)
    ├─→ Tyler_TaxParcels (ParcelID) — appraised + assessed values
    ├─→ Tyler_YearlySales 2018-2022 (ParID) — sale prices
    ├─→ Building_Permit_latest (PARCEL) — CO dates for 2023+ builds
    ├─→ CE_AddressPoints (ParcelID) — building common names
    └─→ [SPATIAL JOIN] → Structures (FeatureID) — YearBuilt, Stories

COBB:
  AGOL CobbParcelsCopy (PIN, e.g. "16054309050")
    → taxassessorsdaily CobbParcels (PIN = same) → PARID (e.g. "16054301270")
      ├─→ YearBuilt Table (PIN = PARID) — YRBLT + SQFT
      └─→ ParcelSales Table (PIN = PARID) — SALEDT + PRICE + APRTOT

GWINNETT:
  Parcels Layer (LRSN = primary key)
    ├─→ Tax Master Table (LRSN) — OWNER1, TOTVAL1, PCDESC, ZONING, GRANTOR1/2/3, DOC1REF
    ├─→ Property Improvements Table (LRSN) — YRBUILT, STORIES, FINSIZE, USECODE
    └─→ Land Value Table (LRSN) — SALE1D, SALE1AMT, SALE2D, SALE2AMT, GRANTOR, NUMDWLG
    Note: Tax Master PIN = "6197 112" (no "R"); Improvements PIN = "R5026 227" (with "R") — join on LRSN not PIN

DEKALB:
  TaxParcels MapServer (PARCELID) — 244,945 parcels
    └─→ Building_Permit_Applications FeatureServer — cooIssuedDateTime (CO date proxy) — address match only
```

### Next Steps Queued
1. **Build ingestion pipeline** for Tyler_YearlySales 2018-2022 → `market_sale_comps` table (by ParID match)
2. **Build Structures spatial join** script → pull YearBuilt/Stories from building footprints by parcel intersection
3. **Enrich Tyler_TaxParcels** values (TotAppr/ImprAppr) → `property_records` table
4. Ingest `Building_Permit_latest` → `building_permits` table (CO dates + pipeline)
5. Load MARTA GTFS stops → `transit_stops` table (lat/lon + type)
6. Pull Census B25036 vintage + B25024 unit structure + B25041 bedrooms → `property_demographics`
7. Pull APD crime by parcel proximity → safety score per property
8. **Cobb ingestion:** Pull `taxassessorsdaily` YearBuilt + ParcelSales via PARID join to existing Cobb parcels
9. **Gwinnett ingestion:** Pull `Property_and_Tax` FeatureServer — Property Improvements + Tax Master + Land Value via LRSN join
10. **DeKalb year built:** Match `Building_Permit_Applications` cooIssuedDateTime to parcels by address

---

## Georgia MSA Multifamily Ingestion Pipeline (2026-04-24)

**Architecture:** Four county ingestion services (Cobb, Gwinnett, DeKalb, Fulton) pull live ArcGIS Feature Layer data for multifamily parcels and persist to PostgreSQL. All routes are authenticated and registered at `/api/v1/georgia/...`.

**DB Tables Created (migration `20260424_georgia_ingestion.sql`):**
- `georgia_property_sales` — sale history per parcel, UNIQUE on `(parcel_id, county, state, sale_date, sale_price)`. Separate from the existing `property_sales` table (which has a FK to `property_records` and incompatible schema — not touched).
- `georgia_ingestion_jobs` — job tracking per run (county, status, counts, errors, timestamps).
- Seeded 4 providers in `property_data_providers`: `cobb_ga`, `gwinnett_ga`, `dekalb_ga`, `fulton_ga`.

**DB Write Pattern (all 4 county services):**
- `saveProperty()` → upserts into `property_info_cache` ON CONFLICT `(parcel_id, county, state)` DO UPDATE (COALESCE to preserve existing non-null values).
- `saveSales()` → inserts into `georgia_property_sales` ON CONFLICT `(parcel_id, county, state, sale_date, sale_price)` DO NOTHING.
- DB import: `import { query as dbQuery } from '../../../database/connection'`

**County-specific notes:**
- **Cobb:** `saveSales(CobbParcelSale[])` — raw ArcGIS fields `SALEDT` (unix ms), `PRICE`, `SALETYPE`, `SALEVAL` (`'Q'`=qualified), `INSTRTYP`. Converts via `new Date(sale.SALEDT)`.
- **Gwinnett:** sales via Land Value table `SALE1D`/`SALE1AMT` and `SALE2D`/`SALE2AMT` fields per parcel record.
- **DeKalb:** no sales data available from ArcGIS — `saveProperty()` only.
- **Fulton:** `Tyler_YearlySales` table 2018-2022 per parcel.

**11 API endpoints** all mounted in `georgia-ingestion.routes.ts`, registered in `index.replit.ts` after apartment-locator block. See `backend/docs/GEORGIA_INGESTION_HANDOFF.md` for full endpoint reference.

---

## Known Bug Fixes (2026-02-28)

-   **Token Key Inconsistency (FIXED):** Many frontend components used `localStorage.getItem('token')` while the auth system stores tokens under `auth_token`. This caused widespread 401 Unauthorized errors across dashboard findings, market intelligence, JEDI score, alerts, credibility, notifications, and other components. All 14 affected files corrected to use `auth_token`.
-   **Operator Precedence Bug (FIXED):** `Dashboard.tsx` line 138 had `\`Bearer ${localStorage.getItem('token')}\` || ''` where `||` applied to the full template literal (always truthy) rather than the getItem call. Fixed to `\`Bearer ${localStorage.getItem('auth_token') || ''}\``.
-   **Double-Prefixed API Paths (FIXED):** `leadAPI` and `commissionAPI` in `frontend/src/services/api.ts` used paths like `/api/agent/leads` which combined with the `baseURL: '/api/v1'` produced `/api/v1/api/agent/leads`. Fixed to use relative paths like `/agent/leads`.
-   **Unauthenticated Onboarding Check (FIXED):** `MainLayout.tsx` made an API call to check onboarding status even when no auth token existed, causing unnecessary 401 errors on the login page. Added a token check before making the API call.
## Bug Fixes (2026-04-28)

-   **Frontend assumptions JSON.parse crash (FIXED):** `frontend/src/hooks/useDealAssumptions.ts` called `JSON.parse(unit_mix)` on a JSONB column that PostgreSQL already returns as a parsed array/object. Caused "Error fetching assumptions" toast on the deal screen. Now normalizes `unit_mix` defensively, accepting array, object, or string shapes.
-   **`GET /api/v1/properties/:id` returned "Cannot GET" HTML 404 (FIXED):** The full `propertyRoutes` router (defined in `backend/src/api/rest/property.routes.ts`) was never mounted from `backend/src/index.replit.ts` — only the `unified-properties` router was registered, which exposes only `/unified` and `/unified/refresh`. `setupRESTRoutes()` in `backend/src/api/rest/index.ts` is dead code; the real entry point registers each router individually. Mounted `propertyRoutes` at `/api/v1/properties` AFTER the unified router so `/unified` keeps priority and `/:id` falls through to the new mount. Also wrapped the `properties_with_zoning` view query in a narrow `try/catch` (Postgres error code `42P01` only) so a missing view falls back to the base `properties` table instead of 500-ing.
-   **`GET /api/v1/supply/deals/:id/supply` returned "Cannot GET" HTML 404 (FIXED):** Same root cause — `supplyRoutes` was already imported once and mounted at `/api/v1` (without the `/supply/` prefix), but the agent tools (`fetch_costar_metrics`, etc.) call paths under `/api/v1/supply/*` via `PlatformClient`. Mounted the existing `supplyRoutes` at `/api/v1/supply` with `requireAuth`, placed BEFORE `dataRouter` so `inline-data.routes.ts`'s `GET /supply/:market` doesn't shadow single-segment routes like `/supply/events`.
