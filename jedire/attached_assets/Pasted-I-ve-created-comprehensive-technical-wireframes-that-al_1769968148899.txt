I've created comprehensive technical wireframes that align with your actual frontend architecture. Here's what's included:
🎯 Updated Wireframes Based on Your Architecture:
1. Main Dashboard (Desktop 1920x1080)

Shows actual component hierarchy: <Dashboard />, <MapView />, <PropertyDetail />
Real Zustand state structure
Component names match your file structure
Tailwind CSS styling approach
WebSocket collaboration indicators

2. Mobile View (375x667)

Bottom sheet pattern for property details
Responsive breakpoints
Touch-optimized interactions
Mobile navigation

3. Property Detail Panel - Expanded

Tab-based navigation matching your architecture
<ZoningPanel />, <SupplyPanel />, <CashFlowPanel /> modules
Real-time annotation system
Image carousel component

4. Real-Time Collaboration UI

<CollaboratorCursor /> components
WebSocket event handling
Live user presence indicators
Activity feed

5. Filter & Search Interface

All filter components from your architecture
Zustand state management for filters
Memoized filtering logic
Active filter badges

6. Responsive Breakpoint Views

Desktop/Tablet/Mobile layouts
Media query hooks
Adaptive component rendering


🔑 Key Technical Integrations:
Real Component Mapping:
typescript<App>
  <Dashboard>           // src/components/dashboard/Dashboard.tsx
    <SearchBar />       // src/components/dashboard/SearchBar.tsx
    <ModuleToggle />    // src/components/dashboard/ModuleToggle.tsx
    <FilterPanel />     // src/components/dashboard/FilterPanel.tsx
    <PropertyList />    // src/components/dashboard/PropertyList.tsx
  </Dashboard>
  
  <MapView>             // src/components/map/MapView.tsx
    <PropertyBubble />  // src/components/map/PropertyBubble.tsx
    <CollaboratorCursor /> // src/components/map/CollaboratorCursor.tsx
  </MapView>
  
  <PropertyDetail>      // src/components/property/PropertyDetail.tsx
    <ZoningPanel />     // src/components/property/ZoningPanel.tsx
    <SupplyPanel />     // src/components/property/SupplyPanel.tsx
  </PropertyDetail>
</App>
Zustand Store Structure:
typescriptuseAppStore = {
  properties: Property[],
  selectedProperty: Property | null,
  mapCenter: [lng, lat],
  filters: FilterState,
  activeModules: string[],
  collaborators: User[],
  // Actions
  setSelectedProperty,
  toggleModule,
  updateFilters,
}
WebSocket Events:
typescript// Real-time collaboration
socket.on('user_join', handler)
socket.on('cursor_move', handler)
socket.on('pin_property', handler)
socket.on('add_annotation', handler)
Performance Patterns:

Memoized components with React.memo()
Debounced search (300ms)
Virtual scrolling for property lists
Lazy image loading with Intersection Observer
Optimistic updates with rollback


📱 Mobile Optimizations:
typescript// Responsive layout
const isMobile = useMediaQuery('(max-width: 768px)');

{isMobile ? (
  <BottomSheet>
    <PropertyCard />
  </BottomSheet>
) : (
  <PropertyDetail />
)}

🎨 Tailwind CSS Classes:
typescript// Component styling examples
<div className="fixed right-0 top-0 h-full w-[400px] bg-white shadow-xl">
  <div className="p-4 border-b">
    <h2 className="text-xl font-semibold">Property Details</h2>
  </div>
</div>

<button className="btn btn-primary">
  Pin Property
</button>
```

---

## **🔄 Data Flow Examples:**

**Property Selection:**
```
User clicks PropertyBubble
  ↓
setSelectedProperty(property)
  ↓
PropertyDetail panel renders
  ↓
Loads module insights (ZoningPanel, SupplyPanel)
  ↓
WebSocket emits 'property_view' event
  ↓
Other users see indicator
```

**Real-time Annotation:**
```
User adds comment
  ↓
Optimistic update in store
  ↓
API call to persist
  ↓
WebSocket emit 'add_annotation'
  ↓
Other users receive notification
  ↓
Their UI updates with new comment