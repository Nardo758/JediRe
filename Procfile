# Railway/Heroku Procfile
# Defines process types for deployment

# Release command - runs before deployment
release: cd backend && npm run migrate

# Web process - main application server
web: npm run start:prod
