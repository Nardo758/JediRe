# JEDIRE POST-D2 PROGRAM — Consolidated Roadmap
**Date:** 2026-07-03 · **Status:** Phase 0 CLOSED 2026-07-06 (W5 closed with named residuals, operator-declared, commit 7b21f5cab). Active item: TS-1. Next: F-P1.
**Governing specs:** `F9_UNDERWRITER_MODEL_SPEC.md` (ratified), `PROFORMA_COMPOSITION_MODEL_SPEC.md` + §4b factory (ratification pending probe), `DEAL_SHAPING_ADDENDUM.md` (pending numbering/vintage fixes), `LIFECYCLE_ORIGIN_VINTAGE_ADDENDUM.md` (ratified), `F9_PROFORMA_BLUEPRINT.md` (rehoming map)

## PHASE 0 — CLOSED 2026-07-06 (W5 closed with named residuals, operator-declared, see `W5-DISPATCH.md`)

W5 (deterministic turn-cohort engine re-acceptance) is operator-declared **CLOSED WITH RESIDUALS** as of 2026-07-06. Gate satisfied: surfacing renders live outputs across all consumer surfaces (deal-panel, F9, terminal FinancialsTab, capital-structure) for both reference deals; D1 zero-LLM-navigation behavioral confirmed PASS live; golden fixture suite green (Highlands + SyntheticDegenerate pinned, 4/5 passing, Bishop intentionally unpinned); guard coverage extended to test/fixture surfaces with forced-failure proof. Bishop's fixture-pin status does **not** block TS-1 — his live build already renders correctly, only the golden-harness pin is blocked (Finding M/O, structural harness gap, external-agent territory).

**Residuals table (owners assigned, tracked in `W5-DISPATCH.md`):**

| ID | Item | Owner | Status |
|---|---|---|---|
| R-1 | F5 engine package: effective-assumptions hypothesis (lead), Findings Q/R/S, Finding U (capital-structure DSCR ~100x bug, new 2026-07-06), 10 pre-existing production type errors, Bishop re-pin on verdict | External agent | Open — `HANDOFF-M-O-DISPATCH.md`, `HANDOFF-FIX5.md` |
| R-2 | Excel parity — final list issued post-Bishop-re-pin | Operator | Open — provisional list in `docs/EXCEL_PARITY_ORACLE_REQUEST.md` |
| R-3 | CI-run-history verification | Parked | Needs `gh` auth |
| R-4 | T2 live evidence / DeepSeek top-up | Operator | D1 CLOSED PASS. T2 named blocker: DeepSeek account `402 Insufficient Balance` — billing/funding issue, not a code defect |

## ACTIVE — TS-1 Thin Surfacing Pass

**Gate satisfied 2026-07-06:** surfacing renders live outputs across all consumer paths for both reference deals; Bishop's golden-fixture pin status is orthogonal to his live build correctness. TS-1 scope: render-only, frontend-only diff (see `TS1_THIN_SURFACING_PASS.md`, owner: next session).

**Gate G1 (operator, on probe results):** engine unification ruling (default: A merges into runner); bad-debt base ruling (EGI vs GPR — recommendation: GPR); composition-spec ratification with blueprint as rehoming map.

## THE CHAIN (each step audit-then-fix gated as usual)

**D2b — Engine unification + formula hygiene.** Engine A absorbed per G1; bad-debt drift fixed at one home; frontend math retired (client debt-service `ProFormaSummaryTab:711-727`, `noiAfterReserves`, PlausibilityPanel math → server). GoalSeekWidget survives until S2 gives it a real endpoint. Acceptance: overlap set from the probe now agrees to the cent; tri-tab identity holds across ALL surfaces incl. terminal. **Includes D2b-G, the golden-deal regression harness:** Bishop + Highlands as permanent golden fixtures with hand-verified expected outputs (operator's Excel as oracle — Excel-parity line-by-line on one real deal); identity invariants (yearly = Σ monthly, GPR→EGI→NOI chain, debt-schedule ties, sale-proceeds reconciliation) as property tests over randomized assumption sets; runs on every engine change. Discipline: the suite is a drift alarm between dispatches, never an acceptance substitute — S1-01 live proof still closes dispatches (forceReseed rule).

**F-P1 — Assumption store consolidation. Queued after TS-1.** 4 stores → canonical `deal_assumptions` + sparse overlays (migrate `deal_underwriting_scenarios` into overlay records; retire `deal_financial_models` as assumption store; kill the `FinancialEnginePage:579` local-state copy). Value-identity blocker on both reference deals. Edit-semantics rule (§3 composition spec) ships here. **Multi-user attribution (operator ruling 2026-07-04):** every user-layer write records `edited_by` (user_id) + timestamp — badges show WHO, not just "user"; last-write-wins within org with visible attribution (no locking v1); append-only per-field edit history (version-inputs makes writes events already — expose as query). Approval workflows = Institutional-tier later (reuses D3's propose-under-override machinery). **Includes the trending ruling (operator, 2026-07-04): ALL growth rates — market rent (done), other income, per-category expense growth (insurance/payroll/utilities each their own rate; no flat default across categories), inter-event assessed-value growth — become LayeredValue assumptions with four-door sourcing (CPI/FRED · CE outputs · source-material extrapolation · owned-asset performance), user override wins, agent-authored values arrive via D3. TAXES get a different method: a deterministic TAX ENGINE computes reassessment steps fired by triggers — sale (reassess to purchase basis), CO (development completion), jurisdiction cycle — per jurisdiction ruleset files (millage, ratios, caps; FL 10% non-homestead cap machinery partially exists in runner — verify and complete); BETWEEN events the four-source trend applies, clamped by jurisdiction cap where present (`min(trend, cap)`, clamp surfaced as provenance when binding, floorBinding pattern). Trigger dates are platform-known (close date, dev timeline, county cadence) → reassessment steps render as F-P2c action-layer markers. Trending prior-owner taxes forward is prohibited output.

**D3 — Agent assumption seam + validation gates.** Agent writes through resolve()/FIELD_PRIORITIES with full provenance contract (rationale, evidence_refs w/ referential integrity, input_snapshot_hash); deterministic plausibility bounds with escalate-don't-reject; `update_assumption` skill re-routed through this seam (its current raw write to deal_assumptions dies). **Includes tax verification (operator ruling 2026-07-04), two layers per taxonomy: (a) deterministic reconciliation — tax engine's current-state output vs actual county tax bill (ATTOM/county GIS) runs as an automatic validation check inside the engine, divergence flags without LLM involvement (Ground Rule #8); (b) CashFlow Agent judgment layer — investigates flags (stale millage ruleset? unmodeled special assessments/exemptions?) and checks OM-claimed taxes against the engine's POST-reassessment projection (the standard broker understatement: quoting prior-owner's bill), producing either a proposed ruleset/assumption correction through the seam (provenance + county-record evidence refs) or a broker_claims divergence flag on the resolution view. Agent never re-performs or edits tax arithmetic.

**CU — Chat Capability Unification** (lands with/immediately after D3). One brain, surface-biased: skill registry becomes THE coordinator tool registry; agent-chat prompt map retires into it; dedup per inventory (one web_search; one persona system — 16 consult_* onto coordinator personas; run_return/refi/hold_sell become free deterministic-runner wrappers); metering tag fixed (surface per skill, not 'agent'); billing classes per capability (engine-wrappers free · data queries cheap · persona/structuring = billable judgment); M16 skills tab retires; skills surface via chat command palette globally + context-filtered per surface; F9 panel = same brain, CashFlow-forward bias (hard dedication available as config, not code).

**F-P2 — Chassis migration.** 11 tabs → CHART · PRO FORMA · ASSUMPTIONS · RETURNS + dynamic, per blueprint rehoming map (every feature has a named destination; RETIRE list operator-approved). **CATCH-ALL CLAUSE (operator ruling 2026-07-04, binding): (a) the blueprint feature census becomes the REHOMING LEDGER — every row carries exactly one disposition: REHOMED (destination named) / DEFERRED (wake ticket) / RETIRED (operator-approved by name; agent never retires unilaterally); F-P2 acceptance includes zero-undispositioned-rows, and no legacy surface deletes until every feature it hosts is accounted; (b) F-P2 Phase 1 re-runs the census fresh and diffs against the pre-D2 blueprint (INPUTS panel, TS-1 additions, and anything newer enter the ledger); (c) SURVIVAL PERIOD — legacy surfaces demote behind a Legacy toggle rather than die at cutover, for ≥30 days AND per-surface operator kill order, whichever later; anything the operator reaches for and can't find is a ledger gap fixed by rehoming; (d) orphaned-consumer check — post-F-P1, any legacy read of a store field nothing in the new chassis reads is detected by query (_orphanedOpexKeys pattern applied to UI).** OVERVIEW decomposes into headers; Opus panel promoted to persistent underwriting console with free-vs-billable command signaling; PRO FORMA gets dual view (RESOLUTION columns — the existing Broker/T-12/Platform/Resolved surface, preserved intact — and BRIDGE view); terminal FinancialsTab + DealDetailPage F9 embed become references to the same rendered model; SCENARIOS tab + M10 superseded by scenario records (SCENARIOS UI → diff-first comparison view). Includes:

> **F-P2c — CHART business-plan action layer (new requirement, 2026-07-03).** The ribbon gains a third annotation class showing the ACTIONS that move the cash flows: the value-add execution window rendered as a labeled band (strategy start → end), with action markers (reno start, reno complete per tranche, stabilization reached, planned refi/exit nodes). DERIVED, never hand-drawn: the layer is computed from the active scenario's overlay assumptions (reno capex schedule, pace, `months_to_stabilization`, debt clocks) — so when the agent or user changes the plan, the band moves with it, and the NOI inflections on the curve visually align with the actions that cause them. Layer classes on the chart are then: zone bands (data state) · M35 pins (market context) · action layer (the deal's own thesis) · boundary. For owned assets, v2 extends the actuals side with realized capex/intervention events (needs the interventions data source flagged NOT-YET in the modal probe — stays gated on that schema). Mark-don't-model discipline applies: markers annotate; the engine's assumptions are what actually move the curve.

**F-P3 — Factory dynamics.** Situation-flag taxonomy (S1's cashflow_distress group + shaping §3.2) lands in DealContext; module registry wired to rendering (flag ⇒ module ⇒ inline lines or exhibit tab per ruleset); SHAPE tab; working-set cap; strategy overlays BUILT FOR THE FIRST TIME (FLIP/STR/LAND HOLD row-set definitions become overlay field sets; "Not Yet Supported" dies with its cause); ROADMAP's input-gating generalized.

**D4 — CE feed + context cache.** COR signals as typed underwriting-context section; DealContext/input-signature-hash cache (same hash serves provenance + vintage pinning).

**D5 — Market artifact cadence.** Monthly per-(geography, period) Commentary + Supply artifacts; `market_commentary_snapshots` time series (sentiment-over-time, CE substrate); generate-if-absent guardrail; per-deal strategy readout → deterministic template; navigation-trigger kill already done (D1).

**D6 — Billing wiring.** Underwriting pass billable w/ tier markup; shaping pass as its own (heavier) billable event; CU's skill billing classes wired; compute/navigation free forever; meters the NEW event shapes.

**S-chain (shaping, interleaved):** S2 goal-seek inversion `solve_for: purchase_price` (post-D2b; retires GoalSeekWidget math) → S3 source-tier tagging + deterministic document-request generator (consumes D2's typed input contract) → S4 DealShapeProposal authorship + SHAPE/chat surfacing (post-D3+S2; accepted proposal pins into underwriting vintage).

**EXIT-BASIS ruling (operator, 2026-07-06 — lands in F-P1 assumption schema + engine disposition):** `exit_valuation_basis: 'forward_12' | 'trailing_12'` as LayeredValue assumption, platform default forward_12; turn-cohort monthly series supplies both bases free (T12 = Σ months m−11..m; forward = Σ m+1..m+12); exit cap gets four-door sourcing (CE cap-rate signals natural source) + optional entry-to-exit spread assumption (aging convention); RETURNS displays BOTH sale prices side by side with basis labeled — the spread between conventions is itself underwriting intelligence; evidence block states which basis drove the pinned exit value.

**DEBT-EVENT machinery (named capability, slots with F-P3 structure overlays; feeds two consumers):** the engine currently models one static M11-sized loan at close. Needed: a debt-event timeline supporting (a) LOAN ASSUMPTION scenarios — in-place below-market debt as an asset; assumable-NPV registry module is this math, currently metadata-only; consumes `debt_positions` + S1 flags; (b) MID-HOLD REFI events — ribbon debt clocks already contemplate the nodes; (c) RESTRUCTURE/recast scenarios for distressed entries (pairs with NOTE PURCHASE overlay); (d) EXIT PAYOFF mechanics — prepayment penalties, yield maintenance/defeasance, assumption-transfer — required by both restructure deals and lifecycle Phase F/G sell decisions (exit-timing dashboard + M30-class modules are the surface). One machinery, structure-overlay shaped, wake: F-P3 or first restructure deal on platform.

## PARKED (register, with wake conditions)
**SHARE-1 — capsule sharing to non-platform users (operator ruling 2026-07-04):** tiered — (1) export (see EXPORT-1); (2) read-only tokenized share link, no account required: rendered deal room (ribbon, PRO FORMA with provenance badges, RETURNS), revocable + expiring + watermarked + view-logged (recipient analytics as sourcing intel); (3) comment-capable guest (conversion funnel, later). Lane boundary binding: share renders the user's OWN capsule (their uploads, assumptions, platform-computed outputs) — never platform-global corpus beyond what the deal legitimately embeds; licensed-source raw data follows its own redistribution rules. Wake: first user needing to send a deal to a lender — candidate for promotion to active chain near F-P2 (demo-adjacent, acquisition channel).
**EXPORT-1 — institutional-quality Excel export (operator ruling 2026-07-04: aesthetics must be institutional grade):** upgrade existing `excel-export.service.ts` from data-dump to banker-grade workbook. Conventions: tab structure mirrors the chassis (Summary · Pro Forma · Annual CF · Monthly CF · Debt Schedule · Returns · Assumptions + summoned exhibits); standard IB formatting (blue inputs / black formulas convention, proper number formats, borders, header blocks, print-ready layouts); provenance PRESERVED in export — source-tier footnotes, floor-binding note, broker-claim flags (a proforma that shows its evidence discipline is itself marketing). Key design decision to rule at wake: values-only vs live-formula export — recommended middle path: values + live formulas for aggregations/ratios + assumption cells wired so recipients can run simple sensitivities without access to the engine's proprietary derivations (LayeredValue resolution logic never exported). Known defect already logged: `:415` "Year 1 Stabilized" mislabel (three-quantity naming applies to exports too). Wake: with SHARE-1, or first institutional user request — whichever first. Build uses xlsx tooling; operator reviews a sample workbook before the format is declared standard.
**3D design module as assumption door (DEV-1):** Design3DPage/design3d.store + DesignToFinancialService exist; massing schemes become structure overlays for development deals (units, mix, GFA, hard cost → provenance `design_3d` through the resolution chain, replacing the service's hardcoded direct import); closes the Zoning→DevCap→Strategy chain (ZM-2/P0-3). **Candidate substrate: pascalorg/editor (MIT, npm: @pascal-app/viewer|editor|core|mcp; R3F+WebGPU+Zustand — stack-aligned). Incorporation shape per Feb ruling: viewer-first — JediRE's parametric massing generator emits Pascal scene nodes, viewer renders the preview, MCP server = the agent's design door; full editor mode later if demanded. Pascal is canvas only — parcel/zoning/FAR/packing/optimization intelligence remains JediRE-built (per Mar TestFit-replication scoping). Pre-adoption checks: WebGPU fallback story, scene-JSON persistence into deal capsule, v0.x API stability.** Wake: modernization of the runner's development branch (fenced out of turn-cohort work) or first ground-up deal on platform — whichever first. · Bridge decomposition Δ_market/platform/operator/capex (wake: shaping arc needs quantified diagnosis) · M28 macro cycle (10% fallback already dies in D2; scenario bands interim) · Lifecycle origin/vintage implementation chain (schema → pin triggers → Phase H → cards; wake: post-F-P2) · Asset Hub 3-tab rebuild (origin-class chassis variant likely subsumes it — re-scope at F-P2) · F9 cache-term fix (wake: only if the old build path survives D2, which it didn't — verify then close) · Highlands geography backfill · 2026 shell-row deletion · org_id historical backfill · distress-supply census (wake: S1 shipped) · distillation research (CoStar gate) · cross-deal overlay templates (Lane ruling needed).

## STANDING DISCIPLINE (unchanged)
S1-01 evidence per claim · two-phase gated dispatches with STOPs · operator rules on product decisions · Highlands value-identity as the universal regression canary · forced-failure/forced-cache-hit proofs where a formula term or guard can't fire naturally.
